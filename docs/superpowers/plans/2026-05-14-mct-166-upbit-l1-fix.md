# MCT-166 upbit L1 forward-only loss fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** MCT-164 §10 진단 결과 (channel mismatch 단일 확정 — `collector.py:82 exchange == "bithumb"`) 의 fix. 선결 게이트 (upbit WS adapter `orderbook_delta` 지원 여부) 결과 의존 primary/alternative 분기. forward-only loss 누적 차단 + WAL freeze 해제 + MCT-165 health framework V2 잔존 NO 의무.

**Architecture:** codeforge consumer 1 Story = 2 PR pair + Phase 2 선결 게이트 분기 (D7=C). Phase 1 hub PR (docs only — spec/plan/Story §1-§7 + counters + MCT-173 reservation). Phase 2 mctrader-data PR (선결 검증 → fix → verify → freeze 해제). Phase 2 hub PR (결과 박제 + channel matrix 결함→정상 + RETRO).

**Tech Stack:** Python 3.13, pytest, Docker compose, mctrader_data CLI (`health-check`), gh CLI.

**Spec reference:** [docs/superpowers/specs/2026-05-14-MCT-166-upbit-l1-fix-design.md](../specs/2026-05-14-MCT-166-upbit-l1-fix-design.md)

**Trigger:** MCT-164 §10 (INV-5 진단→fix 인과 chain), Issue #295.

---

## File Structure

### mctrader-hub (governance)

- **Create**: `docs/stories/MCT-166.md` — Story §1-§12
- **Create**: `docs/superpowers/specs/2026-05-14-MCT-166-upbit-l1-fix-design.md` (이미 박제)
- **Create**: `docs/superpowers/plans/2026-05-14-mct-166-upbit-l1-fix.md` (본 file)
- **Modify**: `.codeforge/counters.json` — MCT-166 title 확장 + MCT-173 reservation (next 173→174)
- **Modify**: `docs/domain-knowledge/domain/data-health/exchange-channel-matrix.md` — upbit orderbookdepth 결함→정상 전이 (INV-5, Phase 2 hub PR)
- **Modify**: `CLAUDE.md` — §upbit L1 status + §WAL freeze flags + §pending stories
- **Create**: `docs/retros/RETRO-MCT-166.md` — PMOAgent retro

### mctrader-data (구현)

- **Create**: `scripts/upbit_ws_capability_probe.py` — 선결 검증 (D1=B, AC-1)
- **Create**: `docs/audit/MCT-166-precondition-upbit-ws-capability.md` — 선결 결과 박제
- **Modify**: `src/mctrader_data/collector.py` — L82 조건 제거 또는 channel matrix dispatch
- **Create OR Modify**: `src/mctrader_data/ingesters/upbit_orderbookdepth.py` (primary) OR `src/mctrader_data/compactor/l1.py` (alternative) — 선결 결과 의존
- **Create**: `src/mctrader_data/allowlist.py` — unsupported channel/exchange ValueError (collector level fail-fast)
- **Modify**: `src/mctrader_data/metrics.py` — `collector_unsupported_channel_total` + `compactor_unsupported_source_total` Counter
- **Create**: `scripts/verify_upbit_l1_fix.py` — WAL freeze 해제 + partition 존재 verify
- **Create**: `tests/unit/test_collector_l1_dispatch.py` — 단위 (channel matrix dispatch + fail-fast)
- **Create**: `tests/integration/test_upbit_l1_partition.py` — 통합 (30 분 collector → partition 생성)
- **Create**: `tests/integration/test_compactor_fail_fast.py` — 통합 (unsupported source ValueError + Prometheus)
- **Modify**: `CLAUDE.md` — upbit L1 status + channel matrix dispatch

---

## Task 1: Phase 1 사전 — counters + branch

**Files:**
- Modify: `mctrader-hub/.codeforge/counters.json`

- [ ] **Step 1: counters MCT-166 title 확장 + MCT-173 reservation**

```bash
python -c "
import json, pathlib
p = pathlib.Path('.codeforge/counters.json')
d = json.loads(p.read_text(encoding='utf-8'))
d['counters']['mctrader-hub']['next'] = 174
d['reservations']['MCT-166']['title'] = 'upbit L1 forward-only loss fix — channel mismatch 해소 (collector.py L82 fix or compactor 분기 활성화) + fail-fast + verify + WAL freeze 해제'
d['reservations']['MCT-166']['retitle_history'] = [{
    'date': '2026-05-14',
    'from': 'upbit L1 forward-only loss fix (MCT-164 진단 결과 기반 — scope 확정 후 brainstorm) placeholder',
    'rationale': 'MCT-164 §10 채택 후 brainstorm 진입 — channel mismatch root cause 단일 확정 인용. D7=C 1 Story + 선결 게이트 분기, D3=B backfill 별 MCT-173.'
}]
d['reservations']['MCT-166']['rationale'] = 'MCT-164 INV-5 진단→fix 인과 chain. D1=B 선결 검증 / D2=C 자율 / D3=B backfill 별 / D4=C 양쪽 fail-fast / D5=A matrix 전이 / D6=B 당일 / D7=C 선결 게이트 / D8=A fix LAND verify 후 freeze 해제 / D9=C health framework + verify 스크립트.'
d['reservations']['MCT-166']['phase_pair'] = 'phase1_phase2'
d['reservations']['MCT-173'] = {
    'title': 'upbit L1 backfill — frozen orderbooksnapshot WAL → orderbooksnapshot L1 historical compaction (MCT-166 D3=B 별 Story)',
    'reserved_at': '2026-05-14',
    'epic': 'EPIC-data-accumulation-umbrella',
    'repo': 'mctrader-data',
    'phase_pair': 'phase1_phase2',
    'rationale': 'MCT-166 D3=B 결정 — backfill scope 통제 별 Story 분리. MCT-164 D4=C WAL 복구 verdict 부분 가능 (snapshot WAL → snapshot L1 직접 compaction). MCT-166 LAND 후 brainstorm 진입.'
}
p.write_text(json.dumps(d, indent=2, ensure_ascii=False) + '\n', encoding='utf-8')
"
```

- [ ] **Step 2: branch 생성**

```bash
git checkout -b mct-166-phase-1
```

---

## Task 2: Story file + spec + plan commit + Phase 1 PR

**Files:**
- Create: `docs/stories/MCT-166.md` (§1-§7)

- [ ] **Step 1: Story file 작성** — spec §1-§9 + Story 표준 §1-§7. §8-§12 = Phase 2 박제. MCT-164/165 패턴 답습. frontmatter:

```yaml
story_key: MCT-166
story_scope: cross-repo
story_issues:
  - repo: mclayer/mctrader-hub
    number: 295
status: IN_PROGRESS
epic_key: EPIC-data-accumulation-umbrella
parent_dependency: null
depends_on: [MCT-164, MCT-165]
related_adrs:
  - "ADR-017 Amendment 2 (compactor source 규약, INV-2 정합)"
  - "ADR-027 Amendment 2 (silent-skip 차단, INV-1 정합)"
  - "ADR-009 §D12 (forward-only invariant, INV-3 정합)"
created_at: 2026-05-14
retro_file: docs/retros/RETRO-MCT-166.md
delegates: [mctrader-data]
```

- [ ] **Step 2: commit + PR**

```bash
git add .codeforge/counters.json \
        docs/stories/MCT-166.md \
        docs/superpowers/specs/2026-05-14-MCT-166-upbit-l1-fix-design.md \
        docs/superpowers/plans/2026-05-14-mct-166-upbit-l1-fix.md

git commit -m "docs(MCT-166): Phase 1 — spec + plan + Story §1-§7 + counters title 확장 + MCT-173 reservation"

git push -u origin mct-166-phase-1

gh pr create --repo mclayer/mctrader-hub --base main --head mct-166-phase-1 \
  --title "[MCT-166] Phase 1 — upbit L1 forward-only loss fix spec + 선결 게이트" \
  --body "Closes part of #295.

## Trigger
MCT-164 §10 진단 결과 (INV-5): channel mismatch 단일 확정 = collector.py:82 exchange == 'bithumb' 조건.

## 9 D 결정 (Codex 합성 + Sonnet)
D1=B Phase 2 entry 선결 / D2=C 자율 / D3=B backfill 별 MCT-173 / D4=C 양쪽 fail-fast / D5=A matrix 전이 / D6=B 당일 / D7=C 선결 게이트 / D8=A fix LAND verify 후 freeze 해제 / D9=C framework + 스크립트

## Phase 2 다음
mctrader-data: 선결 (upbit WS orderbook_delta 지원) → primary OR alternative path → fail-fast → verify → WAL freeze 해제. hub: 결과 박제 + channel matrix 결함→정상."
gh pr edit <PR#> --add-label "type:story" --add-label "phase:설계"
```

- [ ] **Step 3: DesignReviewPLAgent dispatch + CI green + admin merge**

---

## Task 3: Phase 2 mctrader-data — 선결 검증 (D1=B, AC-1)

> **CRITICAL**: 본 Task = Phase 2 PR open 첫 commit. 선결 결과가 D2 = primary or alternative 결정. Story §3/§8/§8.5 amendment 의무 발생 가능.

**Files (mctrader-data):**
- Create: `scripts/upbit_ws_capability_probe.py`
- Create: `docs/audit/MCT-166-precondition-upbit-ws-capability.md`

- [ ] **Step 1: upbit WS adapter 코드 read** — `src/mctrader_data/adapters/upbit.py` (또는 유사 path) read. `orderbook_delta` event 지원 여부 확인. WS subscribe message + on_message handler 추적.

- [ ] **Step 2: upbit 외부 공식 spec 확인** — upbit WebSocket API docs (https://docs.upbit.com/) `orderbook` channel 의 event type 확인. delta event vs snapshot only 결정.

- [ ] **Step 3: 결과 박제** — `docs/audit/MCT-166-precondition-upbit-ws-capability.md`:

```markdown
# MCT-166 Precondition Audit — upbit WS orderbook_delta capability

## Verdict (D1=B 선결 결과)

[지원 | 미지원 | 부분지원]

## 근거

1. **Adapter 코드 read**: src/mctrader_data/adapters/upbit.py ...
2. **외부 spec**: upbit WebSocket API docs ...
3. **시도 결과**: ...

## D2 결정 (선결 결과 의존)

- 지원 → **primary path 선택** (collector.py upbit orderbookdepth ingester 추가)
- 미지원 → **alternative path 선택** (L1 compactor orderbooksnapshot source 분기 활성화)
- 부분 지원 → ... (Story §3 amendment 의무)
```

- [ ] **Step 4: Story §3 amendment** (선결 결과에 따라) — `docs/stories/MCT-166.md` §3 (Scope) update.

---

## Task 4: Phase 2 mctrader-data — fix (primary OR alternative, D2=C)

### Path A: primary (upbit WS orderbook_delta 지원)

**Files:**
- Modify: `src/mctrader_data/collector.py` — L82 조건 제거 또는 channel matrix dispatch
- Create: `src/mctrader_data/ingesters/upbit_orderbookdepth.py` — upbit 전용 orderbookdepth ingester
- Create: `tests/unit/test_collector_l1_dispatch.py` — 단위 (collector dispatch)
- Create: `tests/integration/test_upbit_l1_partition.py` — 통합 (30 분 → partition)

- [ ] **Step 1**: failing test 작성 — upbit orderbookdepth WAL emit 검증
- [ ] **Step 2**: collector.py L82 fix — `if self._include_orderbook and self._exchange in {"bithumb", "upbit"}:` 또는 channel matrix dispatch
- [ ] **Step 3**: upbit_orderbookdepth.py 신규 (필요 시) — bithumb pattern 답습
- [ ] **Step 4**: test 실행 + commit

### Path B: alternative (upbit WS 미지원)

**Files:**
- Modify: `src/mctrader_data/compactor/l1.py` — orderbooksnapshot source 분기 활성화 (`_ob_snapshot_dicts_to_arrow()` 재사용)
- Modify: `src/mctrader_data/compactor/runner.py` — channel matrix dispatch (ADR-017 Amendment 2)

- [ ] **Step 1**: failing test — L1 source = orderbooksnapshot 처리 검증
- [ ] **Step 2**: l1.py 의 channel allowlist 확장 + `_ob_snapshot_dicts_to_arrow()` 호출 분기
- [ ] **Step 3**: ADR-017 Amendment 2 channel matrix 정합 — runner.py 의 source dispatch logic
- [ ] **Step 4**: test 실행 + commit

---

## Task 5: Phase 2 mctrader-data — fail-fast (D4=C, AC-4/5)

**Files:**
- Create: `src/mctrader_data/allowlist.py` — unsupported channel/exchange ValueError
- Modify: `src/mctrader_data/metrics.py` — Counter 신규
- Modify: `src/mctrader_data/collector.py` — allowlist 호출
- Modify: `src/mctrader_data/compactor/l1.py` — source channel discovery fail-fast
- Create: `tests/integration/test_compactor_fail_fast.py`

- [ ] **Step 1**: collector level fail-fast (AC-4)
  - allowlist.py 신규 — `validate_channel_exchange(channel, exchange)` 함수
  - unsupported 조합 → `ValueError(f"unsupported {channel} for {exchange} — see exchange-channel-matrix")`
  - Prometheus Counter `collector_unsupported_channel_total{exchange,channel}` +1

- [ ] **Step 2**: compactor level fail-fast (AC-5)
  - l1.py source discovery 시 channel matrix lookup
  - unsupported source → `ValueError(f"compactor: unsupported source {channel} for {exchange}/L1 — see exchange-channel-matrix")`
  - Prometheus Counter `compactor_unsupported_source_total{tier,exchange,channel}` +1

- [ ] **Step 3**: 통합 test — 양쪽 fail-fast + Prometheus emit 검증

- [ ] **Step 4**: commit

---

## Task 6: Phase 2 mctrader-data — verify + WAL freeze 해제 (D8=A + D9=C, AC-2/3/6)

**Files:**
- Create: `scripts/verify_upbit_l1_fix.py`

- [ ] **Step 1: collector 30 분 실행 (AC-2)**

```bash
docker compose -f compose.yml restart collector
# 30 분 대기
ls /var/lib/mctrader/data/market/orderbookdepth/.../tier=L1/exchange=upbit/symbol=*/date=2026-05-14/
# ≥ 1 partition 생성 확인
```

- [ ] **Step 2: MCT-165 health framework 재실행 (AC-3, D9=C)**

```bash
docker exec mctrader-collector python -m mctrader_data.cli health-check \
  --target collector --window 1d --start-date 2026-05-14 \
  --symbols <upbit-50-sym-list> --output json
# V2 (upbit L1 partition 0) verdict = 0 건 확인
```

- [ ] **Step 3: verify 스크립트 (D9=C 별 스크립트)**

`scripts/verify_upbit_l1_fix.py`:
- AC-2 partition 존재 검증
- AC-3 health framework V2 = 0 확인
- AC-6 WAL freeze flag 자동 제거 — `data/.wal-freeze/upbit-L1` rm
- AC-7 channel matrix update trigger (hub 측 update 자동 안 함, 별 Phase 2 hub PR)

- [ ] **Step 4: mctrader-data Phase 2 PR commit + PR open + admin merge**

---

## Task 7: Phase 2 mctrader-hub — 박제 + channel matrix update + RETRO

**Files (mctrader-hub):**
- Modify: `docs/stories/MCT-166.md` — §8-§12 추가
- Modify: `docs/domain-knowledge/domain/data-health/exchange-channel-matrix.md` — upbit orderbookdepth 결함→정상 (AC-7, INV-5)
- Modify: `CLAUDE.md` — §upbit L1 status + §WAL freeze flags + §pending stories
- Create: `docs/retros/RETRO-MCT-166.md`

- [ ] **Step 1: Story §8-§12 박제**:
  - §8 Test Contract (단위 + 통합 test 박제)
  - §9 Operational Risk
  - §10 FIX Ledger + 선결 결과 (D2 결정 박제) + fix path 결정 근거
  - §11 Invariant cross-ref
  - §12 PMOAgent retro

- [ ] **Step 2: channel matrix update (AC-7, INV-5)**:

```markdown
| upbit | orderbookdepth | orderbookdepth (primary) OR orderbooksnapshot→orderbookdepth 변환 (alternative) | ... | **정상 (MCT-166 LAND 2026-05-14)** |
```

status: confirmed → 정상.

- [ ] **Step 3: CLAUDE.md 갱신** — §upbit L1 status + §WAL freeze flags + §pending stories (MCT-173).

- [ ] **Step 4: RETRO-MCT-166.md** — PMOAgent dispatch (memory feedback_pmo_retro_mandatory).

- [ ] **Step 5: hub Phase 2 PR commit + PR open + admin merge**.

---

## Task 8: 종료 처리

- [ ] **Step 1**: Issue #295 close (gate:retro-complete + 선결 결과 + fix path + verify summary comment)
- [ ] **Step 2**: MCT-173 backfill Issue 발의 — MCT-164 D4=C WAL 복구 verdict 인용 + MCT-166 D3=B 결정 인용 + counters reservation 인용
- [ ] **Step 3**: Story file frontmatter status: COMPLETED + completed_at: 2026-05-14

---

## Self-Review

- [x] Spec §1-§10 coverage: 각 결정 D1~D9 → Task 매핑
- [x] 선결 게이트 분기 명시 (Task 3 → Task 4 Path A/B)
- [x] forward-only loss 긴급도 = D2=C 자율 결정 + D8=A fix LAND verify 후 즉시 freeze 해제
- [x] INV-5 진단→fix 인과 chain: §10 §10 FIX Ledger 에 MCT-164 §10 인용 의무

---

## Execution Handoff

Plan complete. Execution = Subagent-Driven (memory feedback_subagent_execution).
