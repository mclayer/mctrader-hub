---
story_key: MCT-124
status: 완료 (Task 8 pending — 사용자 수동 XHR 탐색)
component: signal-collector (신규 리포)
type: story
parent_epic: null
related_adrs: []
pr:
  - mclayer/mctrader-signal-collector (신규 리포 — main 직접 push)
---

# MCT-124: mctrader-signal-collector — 외부 시그널 수집 서비스

- **Status**: 완료 (Task 8 PENDING — Upbit/Bithumb XHR 엔드포인트 사용자 수동 탐색 필요)
- **Trigger**: mctrader 엔진이 OHLCV/tick 데이터만 보고 결정 — 외부 컨텍스트(공포탐욕지수, 한국 리테일 수급, 글로벌 파생 압력, 거래소 공지) 부재
- **Repos**:
  - `mctrader-signal-collector` (신규) — 외부 시그널 5종 수집, Redis Stream publish
  - `mctrader-hub` — compose.yml signal-collector 5 서비스 추가, prometheus.yml 5 scrape jobs 추가

## 1. 배경

mctrader는 현재 Bithumb + Upbit OHLCV / WebSocket tick만 수집. 거래 판단에 필요한 외부 컨텍스트가 없으면 엔진이 가격 패턴만 보고 결정.

본 Story는 Tier 1 외부 시그널 5종을 `mctrader-signal-collector` 신규 독립 서비스로 수집해 Redis Stream(`signals:{kind}`)에 publish. Engine은 Redis Stream을 subscribe해 포지션·리스크 결정에 반영.

**채택 접근법 — 별도 `mctrader-signal-collector` 서비스 (접근법 B)**
- 소스별 독립 컨테이너 → 장애 격리
- Engine과 완전 decoupled (Redis Stream 인터페이스만 공유)
- stale 데이터 시 `stale=true` neutral 메시지 publish → engine fallback

## 2. 대상 변경

### 2.1 mctrader-signal-collector (신규 리포)

| 파일 | 역할 |
|------|------|
| `src/signal_collector/core/publisher.py` | Redis Stream XADD + stale fallback 공통 |
| `src/signal_collector/core/dedup.py` | Redis Set 기반 announcement dedup |
| `src/signal_collector/core/health.py` | Prometheus Counter/Gauge + HTTP `/metrics` 노출 (instance-level CollectorRegistry) |
| `src/signal_collector/workers/base_worker.py` | BaseWorker ABC — `poll_once()` / `_run_cycle()` / `run()` |
| `src/signal_collector/workers/fear_greed.py` | Alternative.me F&G, 5분 폴링 |
| `src/signal_collector/workers/ecos.py` | 한국은행 ECOS KRW/USD, 6시간 폴링 |
| `src/signal_collector/workers/kimchi_premium.py` | CryptoQuant Korea Premium, 1분 폴링 |
| `src/signal_collector/workers/announcement.py` | Upbit + Bithumb 공지, 15초 폴링 + dedup |
| `src/signal_collector/workers/coinglass.py` | CoinGlass 청산·OI·펀딩비, 1분 폴링 |
| `Dockerfile` | 2-stage (uv==0.5.11 deps + python:3.12-slim runner, uid 1001) |
| `compose.yml` | 로컬 개발용 — redis + 5 worker 서비스 |
| `tests/` | 31 pytest (fakeredis, MagicMock health, fixture JSON) |

### 2.2 mctrader-hub

| 파일 | 내용 |
|------|------|
| `compose.yml` | signal-collector 5 서비스 추가 (commit `9207561`) |
| `monitoring/prometheus.yml` | signal-worker 5 scrape jobs 추가 (port 9200) |
| `.env.example` | ECOS_API_KEY / CRYPTOQUANT_API_KEY / COINGLASS_API_KEY / UPBIT_NOTICE_API_URL / BITHUMB_NOTICE_API_URL |

## 3. Redis Stream 스키마

| kind | 소스 | TTL (maxlen) |
|------|------|-------------|
| `fear_greed` | Alternative.me | 10,000 |
| `ecos_fx` | 한국은행 ECOS | 1,000 |
| `kimchi_premium` | CryptoQuant | 10,000 |
| `announcement` | Upbit + Bithumb | 50,000 |
| `coinglass` | CoinGlass | 10,000 |

## 4. 핵심 설계 결정

- **독립 리포**: 장애 격리 + Engine 완전 decoupled (Redis Stream 인터페이스만 공유)
- **stale fallback 의무화**: upstream 실패 시 `stale=true` neutral publish — engine이 stale=true 시 시그널 무시
- **instance-level CollectorRegistry**: 테스트 다중 인스턴스 시 전역 registry 충돌 방지
- **_CLASSIFY_MAP specific-first 원칙**: "상장폐지" → "상장" 순서 — substring 충돌 방지
- **React SPA fallback**: Upbit/Bithumb 공지 페이지는 React SPA — env var `UPBIT_NOTICE_API_URL`, `BITHUMB_NOTICE_API_URL`로 실제 XHR 엔드포인트 주입 구조

## 5. AC

- [x] fear_greed / ecos / kimchi_premium / announcement / coinglass 5 workers 구현
- [x] BaseWorker ABC (poll_once / _run_cycle / run)
- [x] Publisher (Redis Stream XADD, stale fallback)
- [x] DedupStore (Redis Set, announcement 중복 방지)
- [x] HealthExporter (Prometheus /metrics, instance-level CollectorRegistry)
- [x] 31 pytest GREEN (fakeredis 기반, 외부 API 의존 0)
- [x] Docker 빌드 성공 (2-stage, uid 1001)
- [x] hub compose signal-collector 5 서비스 추가
- [x] prometheus.yml signal-worker 5 scrape jobs 추가
- [x] GitHub repo mclayer/mctrader-signal-collector 생성 + main push
- [ ] Task 8: Upbit/Bithumb XHR 엔드포인트 수동 탐색 (사용자 수동 액션)

## 11. 회고

> 본 Story 회고: `docs/retros/RETRO-MCT-124-signal-collector.md` 참조

**핵심 결과**:
- 신규 독립 리포 `mctrader-signal-collector` — 5 worker, 31 pytest, Docker 빌드 완료
- hub compose + prometheus.yml signal-collector 통합
- stale fallback 의무화 패턴 확립 — upstream 장애 시 engine 무음 실패 방지
- Upbit/Bithumb React SPA 문제 → env var 주입 구조로 대비, 실제 엔드포인트 탐색은 사용자 follow-up

**PENDING**:
- Task 8: 사용자가 브라우저 DevTools로 XHR 엔드포인트 확인 후 `UPBIT_NOTICE_API_URL`, `BITHUMB_NOTICE_API_URL` 설정 + `_fetch_upbit()` / `_fetch_bithumb()` 파싱 로직 실제 구조에 맞게 조정
