---
story_key: MCT-180
story_scope: cross-repo
story_issues:
  - repo: mclayer/mctrader-hub
    number: ~     # Phase 1 PR (docs only) — TBD
  - repo: mclayer/mctrader-data
    number: ~     # Phase 2 PR1 data code — TBD (land_order 1)
  - repo: mclayer/mctrader-engine
    number: ~     # Phase 2 PR1 engine code — TBD (land_order 2)
  - repo: mclayer/mctrader-hub
    number: ~     # Phase 2 PR1 hub code — TBD (land_order 3)
  - repo: mclayer/mctrader-hub
    number: ~     # Phase 2 PR2 (박제) — TBD
status: IN_PROGRESS
started_at: "2026-05-15"
completed_at: ~
epic_key: EPIC-mctrader-docker-stack
sequential_phase: 6
parent_dependency: ~
depends_on: [MCT-179]
decisions_implemented: [D4, D11, D18]
carry_over_from: MCT-179
carry_over_items:
  - "Grafana 5 [MCT-180 TODO] panel metric emit 신규 (collector ticks_total + active_symbols + engine universe_size + reader_cache hit_ratio + p99)"
  - "${IMAGE_TAG} prod pin (D12 — MCT-181 owner)"
  - "R2 CRITICAL production 측정 별 PR (EPIC-tier-promotion-single-source prod-2, production deploy 후)"
related_adrs:
  - "ADR-030 §D4 amendment box (MCT-180 SIGTERM 회귀 검증 박제)"
  - "ADR-030 §D11 amendment box (MCT-180 publish)"
  - "ADR-030 §D18 amendment box (MCT-180 publish)"
retro_file: docs/retros/RETRO-MCT-180.md  # PMOAgent sub-dispatch, Phase 2 PR2 LAND 시 생성
---

# MCT-180 — integration smoke + testcontainers + resource limits + 5 TODO panel metric emit

## §1 동기

**EPIC-mctrader-docker-stack Story-6 (sequential_phase 6)** — MCT-179 LAND (hub#339 + data#66 +
hub#340 + Phase 2 PR2) 로 observability (Prometheus + Grafana + alert) + WAL 30G synthetic baseline
측정이 완성됨. 본 Story 는 전체 stack 의 **integration smoke CI gate (D11)** + **resource limits
명시 (D18)** + **SIGTERM graceful 회귀 검증 (D4)** + **MCT-179 carry over 5 panel metric emit** 을
박제한다.

4 구현 목표:

1. **D11 integration smoke (2 layer gate)** — `.github/workflows/integration-smoke.yml` 신규:
   compose up full stack (postgres+redis+minio+collector+paper-engine) → collector ingest 60s →
   compactor promotion 1회 → paper-engine health 200 → SIGTERM graceful 검증 (D4 회귀). testcontainers
   Python = repo-level boundary test (collector→NAS mock + paper-engine→Redis). 2 layer: compose CI
   smoke (stack 정합) + testcontainers (component boundary).

2. **D18 resource limits** — compose.yml 각 service `deploy.resources.limits` (mem_limit + cpus)
   명시. collector 512M / paper-engine 512M / backtest-runner 1G / postgres 1G / redis 256M (hard cap) /
   prometheus+grafana 512M. Prometheus `container_memory_usage_bytes` >80% limit alert (cadvisor
   MCT-123 LAND 기반).

3. **D4 SIGTERM graceful 회귀 검증** — integration smoke SIGTERM 단계 (collector + paper-engine
   `docker compose stop` → exit 0 within stop_grace_period). MCT-176/177 LAND graceful 검증 회귀 0.

4. **MCT-179 carry over: 5 [MCT-180 TODO] panel metric emit 신규** — collector
   `mctrader_collector_ticks_total` + `mctrader_collector_active_symbols` (data) / engine
   `mctrader_engine_universe_size` (engine) / reader_cache `nas_reader_cache_hit_ratio` +
   `nas_reader_p99_ms` (engine reader_cache MCT-170). 신규 Prometheus metric emit → docker-stack.json
   panel TODO 해제.

## §2 Epic context

- **Epic**: EPIC-mctrader-docker-stack (7 Story sequential)
- **sequential_phase**: 6 (MCT-179 COMPLETED → MCT-180 IN_PROGRESS → MCT-181 PLANNED)
- **ADR**: ADR-030 §D4/§D11/§D18 amendment box (본 Story Phase 1 박제)
- **spec**: `docs/superpowers/specs/2026-05-15-EPIC-mctrader-docker-stack-design.md`
- **plan**: `docs/superpowers/plans/2026-05-15-mct-180-integration-smoke.md`
- **scope_manifest**: `scope_manifests/EPIC-mctrader-docker-stack.yaml`

## §3 Risk acceptance

아래 risk 는 MCT-175 Phase 1 에서 사용자 explicit accept 완료. 본 Story 진입 시 재확인 불요.

| Risk | Severity | 상태 |
|------|----------|------|
| R1 NAS HTTP-only 평문 | HIGH | **user_acknowledged_at: 2026-05-15** by mclayer8865@gmail.com |
| R4 host disk 손실 → WAL 영구 손실 | MEDIUM | **user_acknowledged_at: 2026-05-15** by mclayer8865@gmail.com |

R2 (WAL 30G 미측정 CRITICAL) = MCT-179 owner (synthetic baseline 완료, production 별 PR carry over).
본 Story 에서 R2 해소 의무 없음 — EPIC-tier-promotion-single-source prod-2 production 별 PR 유지.

### Op-risk (D18 resource limits)

- OOM kill 위험: limits 산정 보수적 (INV-4 DualWriter +buffer). cadvisor alert >80% 선제 경보.
- testcontainers: CI 격리 환경 (MinIO mock — 실 NAS 미접근). 신규 trust boundary 없음.

ADR-030 §Consequences R1/R4 acceptance carrier 박제 참조:
`docs/adr/ADR-030-docker-stack-governance.md` §Consequences

## §4 Acceptance Criteria

### AC-1 integration-smoke.yml compose up 전체 stack PASS (D11)

```bash
# GitHub Actions PR push 시 자동 실행 (paths: compose.yml / integration-smoke.yml):
# compose up (postgres+redis+minio+mc+collector+paper-engine) → 60s ingest → paper-engine health 200
# 기대: integration-smoke.yml job exit 0 (10분 budget 이내)
# Phase 2 PR2 §12 측정에 CI 결과 박제
```

### AC-2 testcontainers boundary test PASS (D11)

```bash
# mctrader-data testcontainers test:
cd mctrader-data
python -m pytest tests/integration/test_collector_nas_boundary.py -v
# 기대: collector → MinIO testcontainer put_streaming verify PASS

# mctrader-engine testcontainers test:
cd mctrader-engine
python -m pytest tests/test_paper_redis_boundary.py -v
# 기대: paper-engine → Redis testcontainer engine: prefix key verify PASS
```

### AC-3 compose.yml deploy.resources.limits 전 service 명시 + cadvisor alert PASS (D18)

```bash
# compose.yml service limits 확인:
docker compose config | grep -A5 "resources:"
# 기대: collector (512M/1.0 cpu) / paper-engine (512M/1.0 cpu) / backtest-runner (1G/1.0 cpu)
#        postgres (1G/1.0 cpu) / redis (256M/0.5 cpu) / prometheus+grafana (512M/0.5 cpu)

# prometheus-alerts.yml ContainerMemoryHigh rule 존재 확인:
grep -l "ContainerMemoryHigh" monitoring/prometheus-alerts.yml
# 기대: alert rule 존재
```

### AC-4 integration smoke SIGTERM graceful 회귀 0 (D4)

```bash
# integration-smoke.yml SIGTERM step:
docker compose --profile dev stop collector paper-engine --timeout 60
docker compose --profile dev ps --status exited | grep -E "collector|paper-engine"
# 기대: 두 service exit 0 within 60s stop_grace_period
# MCT-176 (collector) + MCT-177 (paper-engine asyncio shutdown.py) 회귀 검증
```

### AC-5 5 [MCT-180 TODO] panel metric emit 신규 + docker-stack.json TODO 해제 (MCT-179 carry over)

```bash
# metric emit verify (각 service 측):
# collector: mctrader_collector_ticks_total Counter + mctrader_collector_active_symbols Gauge
curl -s http://localhost:8080/metrics | grep -E "mctrader_collector_ticks_total|mctrader_collector_active_symbols"

# engine: mctrader_engine_universe_size Gauge + nas_reader_cache_hit_ratio Gauge + nas_reader_p99_ms Gauge
curl -s http://localhost:8080/metrics | grep -E "mctrader_engine_universe_size|nas_reader_cache_hit_ratio|nas_reader_p99_ms"

# 기대: 5 metric 존재 + docker-stack.json panel id=3,4,6,7,8 title [MCT-180 TODO] 해제
```

## §5 Invariants

본 Story 구현 중 아래 invariant 위반 시 즉시 중단:

| Invariant | 출처 | 검증 방법 |
|-----------|------|----------|
| forward-only (WAL 객체 삭제 금지) | ADR-009 §D12 | integration smoke = ephemeral compose up/down (WAL 파일 수정 없음) |
| startup scan warn+continue | ADR-030 §D17 | MCT-179 LAND 정합 — integration smoke startup log 확인 |
| Prometheus metric additive | D8 policy | 신규 5 metric emit only (기존 metric 제거/rename 없음) |
| SIGTERM graceful exit 0 within 60s | ADR-030 §D4 | MCT-176/177 LAND 재사용 — integration smoke 회귀 검증 |
| resource limits OOM kill 방지 | D18 | limits 보수적 산정 + cadvisor >80% 선제 경보 |

## §6 Risk 상세

### R-MCT-180-1 integration smoke 10분 budget 초과 (MEDIUM)

- **위협**: compose up full stack + 60s ingest + compactor promotion + health check 합산 > 10분
  → CI timeout (GitHub Actions `timeout-minutes: 12`)
- **mitigation**:
  - infra (postgres+redis+minio+mc) `--wait --wait-timeout 180` 선행
  - collector + paper-engine `--wait --wait-timeout 240` 별도 단계
  - SIGTERM step 이전 health 200 확인 (불필요한 대기 차단)
  - 실패 시 teardown `if: always()` 보장 (CI cost 낭비 방지)

### R-MCT-180-2 testcontainers CI 환경 호환성 (LOW)

- **위협**: ubuntu-latest GitHub Actions 환경에서 testcontainers-python Docker socket 접근 미허용
  → CI FAIL
- **mitigation**:
  - `testcontainers-python` = GitHub Actions ubuntu-latest Docker socket 기본 허용
  - MinIO testcontainer = `minio/minio:latest` (공개 image, ghcr.io 인증 불필요)
  - Redis testcontainer = `redis:7-alpine` (공개 image)

### R-MCT-180-3 resource limits OOM kill (MEDIUM)

- **위협**: 실 production 부하 시 reader cache 256MB + DualWriter buffer 초과 → OOM kill
- **mitigation**:
  - paper-engine limits 512M: reader_cache 256MB (MCT-170 max_bytes default) + asyncio runtime + 여유 버퍼
  - collector limits 512M: INV-4 DualWriter peak RSS+TM delta ≤ 50MB (MCT-163 실측) + 여유
  - cadvisor ContainerMemoryHigh (>80% = 410M) → 선제 alert + D11 amendment 발의 경로 보유

## §6.5 Change Plan §7/§11 N/A 박제

### §7 Security (N/A — docs only Phase 1, 참고 박제)

Phase 1 PR 은 실 코드 변경 없음. Phase 2 구현 시 해당:

- integration smoke = CI 격리 환경 (실 NAS 미접근, hub MinIO mock). 신규 trust boundary 없음.
- testcontainers = Docker network isolation (host network 미사용). MinIO/Redis testcontainer
  = ephemeral (test 완료 후 즉시 제거).
- resource limits = OOM kill 위험 → limits 보수적 산정 (D18 정합).

### §7.4 Op-risk (N/A — docs only Phase 1, 참고 박제)

- resource limits OOM kill: limits 산정 보수적 (INV-4 DualWriter + reader cache 256MB + buffer).
  cadvisor alert >80% 선제 경보 (R-MCT-180-3 mitigation).
- integration smoke CI budget: 10분 budget (timeout-minutes: 12). R-MCT-180-1 mitigation 적용.

### §11 Data-migration (N/A — docs only Phase 1, 참고 박제)

- schema 변경 없음. Prometheus metric 5건 신규 emit (additive — 기존 metric 제거/rename 없음).
- integration smoke = ephemeral compose up/down (데이터 지속 없음, idempotent).
- resource limits = compose 선언형 추가 (기존 서비스 동작 영향 없음).

### §11.6 Idempotency (N/A — docs only Phase 1, 참고 박제)

- integration smoke = ephemeral compose up/down (동일 상태 재실행 = 동일 결과). idempotent.
- metric emit = read-only observe (tick count incr / symbol count gauge). stateful incr 는
  Prometheus Counter 표준 (restart 시 reset = 정상).
- resource limits = 선언형 YAML (재적용 멱등). idempotent.

## §7 Dependencies

| 의존 | 상태 | 내용 |
|------|------|------|
| MCT-179 LAND | COMPLETED 2026-05-15 | observability + WAL 30G synthetic baseline. Phase 2 PR2 포함 전체 LAND. |
| MCT-176 collector SIGTERM | COMPLETED 2026-05-15 | `_SHUTDOWN_REQUESTED` + collect loop wiring — MCT-180 = SIGTERM 회귀 검증만 |
| MCT-177 paper-engine asyncio SSOT | COMPLETED 2026-05-15 | `shutdown.py` asyncio graceful drain — MCT-180 = SIGTERM 회귀 검증만 |
| MCT-123 cadvisor | COMPLETED (기존 LAND) | `container_memory_usage_bytes` + `container_spec_memory_limit_bytes` Prometheus metric emit. D18 alert 기반 |
| MCT-170 reader_cache.py | COMPLETED 2026-05-14 | `hit_ratio()` 메서드 존재 (engine `io/reader_cache.py:280`). nas_reader_cache_hit_ratio Gauge expose = MCT-180 신규 |
| ADR-030 §D4/§D11/§D18 | 신규 (본 Story Phase 1) | 본 Story Phase 1 박제 의무 |

### MCT-179 carry over 흡수 표

| carry over 항목 | 흡수 | MCT-180 처리 |
|----------------|------|-------------|
| 5 [MCT-180 TODO] panel metric emit | MCT-180 owner | data (collector metric 2종) + engine (universe_size + reader_cache 2종) + hub (docker-stack.json 5 panel TODO 해제) |
| ${IMAGE_TAG} prod pin (D12) | MCT-181 owner | MCT-180 외 영역 (dev=latest 현행 유지) |
| R2 CRITICAL production 측정 | 별 PR carry over | production deploy + peak 09:00 KST 1h burst — EPIC-tier-promotion prod-2 |

## §8 Test contract

### D11 integration smoke (Phase 2 PR1, mctrader-hub)

`.github/workflows/integration-smoke.yml` + compose up 검증:

- `integration-smoke.yml` trigger: `pull_request` (paths: compose.yml / integration-smoke.yml) + `workflow_dispatch`
- compose up infra → collector + paper-engine → 60s ingest → paper-engine health 200 → SIGTERM step

### D11 testcontainers boundary (Phase 2 PR1, mctrader-data)

`tests/integration/test_collector_nas_boundary.py` 신규:

- `test_collector_nas_put_streaming`: collector → MinIO testcontainer `put_streaming()` verify (sha256 정합)
- `test_collector_nas_connection_error`: NAS unreachable 시 retry_queue fallback verify

### D11 testcontainers boundary (Phase 2 PR1, mctrader-engine)

`tests/test_paper_redis_boundary.py` 신규:

- `test_paper_engine_redis_prefix`: paper-engine → Redis testcontainer `engine:*` prefix key verify
- `test_paper_engine_redis_connection`: Redis unreachable 시 graceful fail verify

### D18 resource limits (Phase 2 PR1, mctrader-hub)

`compose.yml` deploy.resources.limits 존재 확인:

- 전 service (collector / paper-engine / backtest-runner / postgres / redis / prometheus / grafana)
  `deploy.resources.limits.memory` + `deploy.resources.limits.cpus` 명시
- `monitoring/prometheus-alerts.yml` `ContainerMemoryHigh` alert rule 존재

### AC-5 metric emit (Phase 2 PR1, mctrader-data + mctrader-engine)

- `mctrader_collector_ticks_total` Counter + `mctrader_collector_active_symbols` Gauge
  (data `nas_metrics/prometheus_exporters.py` 패턴 정합, MCT-171 SSOT)
- `mctrader_engine_universe_size` Gauge (engine `metrics.py` MCT-170 패턴)
- `nas_reader_cache_hit_ratio` Gauge + `nas_reader_p99_ms` Gauge
  (engine `io/reader_cache.py` MCT-170 `hit_ratio()` 메서드 → Prometheus expose)

## §8.5 Impl Manifest

> Phase 2 PR1 LAND 시 DeveloperPLAgent 가 git diff 분석 후 박제 (CFP-39).
> Phase 2 PR2 (박제 PR) 시 ArchitectPLAgent confirm.

### mctrader-data Phase 2 PR1 (data code, land_order 1)

| 파일 | 변경 유형 | owner | Change Plan §5 매핑 |
|------|-----------|-------|---------------------|
| `src/mctrader_data/nas_metrics/prometheus_exporters.py` or `collector.py` | MODIFY | DevAgent | AC-5 collector ticks_total Counter + active_symbols Gauge emit |
| `tests/integration/test_collector_nas_boundary.py` | CREATE | DevAgent | D11 testcontainers collector→NAS boundary |

### mctrader-engine Phase 2 PR1 (engine code, land_order 2)

| 파일 | 변경 유형 | owner | Change Plan §5 매핑 |
|------|-----------|-------|---------------------|
| `src/mctrader_engine/metrics.py` or `io/reader_cache.py` | MODIFY | DevAgent | AC-5 universe_size Gauge + hit_ratio/p99 Gauge expose |
| `tests/test_paper_redis_boundary.py` | CREATE | DevAgent | D11 testcontainers paper→Redis boundary |

### mctrader-hub Phase 2 PR1 (hub code, land_order 3)

| 파일 | 변경 유형 | owner | Change Plan §5 매핑 |
|------|-----------|-------|---------------------|
| `.github/workflows/integration-smoke.yml` | CREATE | InfraEng | D11 compose up full stack smoke + SIGTERM D4 회귀 |
| `compose.yml` | MODIFY | InfraEng | D18 deploy.resources.limits 전 service |
| `monitoring/prometheus-alerts.yml` | MODIFY | InfraEng | D18 ContainerMemoryHigh >80% alert |
| `monitoring/grafana/provisioning/dashboards/docker-stack.json` | MODIFY | InfraEng | AC-5 5 TODO panel 해제 |

## §9 Plan reference

- **plan**: `docs/superpowers/plans/2026-05-15-mct-180-integration-smoke.md`
- **spec**: `docs/superpowers/specs/2026-05-15-EPIC-mctrader-docker-stack-design.md`
- **scope_manifest**: `scope_manifests/EPIC-mctrader-docker-stack.yaml` §MCT-180

## §10 FIX Ledger

> Orchestrator 독점 append (fix-event-v1 contract). 본 에이전트 직접 기록 금지.

| iter | lane | category | mechanical_category | file | suggestion | resolution |
|------|------|----------|---------------------|------|------------|------------|
| — | — | — | — | — | — | — |

## §10.5 Git Ops Log

> GitOpsAgent 단독 append-only. 삭제/수정 금지.

| Event | 시각 | Actor | Detail | Outcome |
|-------|------|-------|--------|---------|
| WORKTREE_CREATE | 2026-05-15 | GitOpsAgent | Phase 1 worktree 생성 — hub-mct180-phase1 from main ef07394 feat/MCT-180-integration-smoke-phase1 | — |

## §11 Retro

> Phase 2 PR2 LAND 시 PMOAgent 자동 dispatch 후 박제 완료.

- **retro_file**: `docs/retros/RETRO-MCT-180.md` (PMOAgent sub-dispatch, Phase 2 PR2 신규)

## §12 측정 (Phase 2 PR1 LAND 후 박제 — Phase 2 PR2 confirm)

> Phase 2 PR1 LAND 후 DeveloperPLAgent 박제.

| 항목 | 측정값 | gate | verdict |
|------|--------|------|---------|
| integration smoke CI (AC-1) | TBD | exit 0 (10분 budget) | TBD |
| testcontainers boundary (AC-2) | TBD | PASS | TBD |
| resource limits 전 service (AC-3) | TBD | 7 service 명시 | TBD |
| ContainerMemoryHigh alert (AC-3) | TBD | alert rule 존재 | TBD |
| SIGTERM graceful 회귀 (AC-4) | TBD | exit 0 within 60s | TBD |
| 5 metric emit + panel 해제 (AC-5) | TBD | 5 metric Prometheus 존재 | TBD |
| FIX 루프 | TBD | ≤ 3 iter | TBD |
| cross-repo LAND | TBD | data → engine → hub sequential gate | TBD |
