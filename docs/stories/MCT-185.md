---
story_key: MCT-185
story_scope: cross-repo
status: COMPLETED
phase: COMPLETED  # 요구사항 ✓ → 설계 ✓ → 설계-리뷰 PASS CONDITIONAL (P0=0, P1×2 mechanical inline 정정, P2×6 carry, FIX 0회) → 구현 → 구현-리뷰 PASS (FIX 0회) → Phase 2 PR2 박제 COMPLETED
started_at: "2026-05-17"
completed_at: "2026-05-17"
epic_key: EPIC-data-domain-decoupling
sequential_phase: 4
parent_dependency: ~
depends_on: [MCT-184]
decisions_implemented: [D2, D3]
repo: "mctrader-hub + mctrader-data + mctrader-engine"
story_issues:
  - repo: mclayer/mctrader-hub
    number: 366   # Phase 1 PR docs — Story §0-§11 + ADR-029/031 amendment box draft + ADR-030 cross-ref + scope_manifest + CLAUDE.md
  - repo: mclayer/mctrader-data
    number: 76    # Phase 2 PR1 data (land_order 1) — realtime_stream.py + historical/orderbook 2 endpoint + CodeQL CWE-22 fix
  - repo: mclayer/mctrader-engine
    number: 59    # Phase 2 PR1 engine (land_order 2) — data_client/ 신설 + cold-read 8곳 + reverse-write 3곳 cutover + AC-3 grep0
  - repo: mclayer/mctrader-hub
    number: ~     # Phase 2 PR2 박제 (본 PR) — §8.5 Impl Manifest + ADR-029 §D2 + ADR-031 §D2/§D3 VERIFIED confirm + scope_manifest 4/7 + CLAUDE.md COMPLETED + RETRO
related_adrs:
  - "ADR-029-tier-promotion-single-source (Status: POLICY_FINALIZED. 본 Story = §D2 VERIFIED amendment confirm — engine NAS 직독 폐기 실 cutover LAND. io reader 6 module = mctrader-data Layer2 + cold-read 8곳 = data REST indirection 실 적용 박제. presigned-NAS-handoff 기각 효력 실증)"
  - "ADR-031-data-domain-decoupling (Status: Accepted — MCT-182 §D1 VERIFIED + MCT-183 §D2 partial + MCT-184 §D3 partial. 본 Story = §D2 cold-read cutover 절반 + §D3 realtime stream 절반 동시 진행 → §D2 VERIFIED + §D3 VERIFIED 박제. Status 전이는 MCT-188 POLICY_FINALIZED 시점)"
  - "ADR-030-docker-stack-governance (Status: POLICY_FINALIZED. MCT-184 amendment box (data api service compose topology 예고) + MCT-185 본 Story = Redis Stream loopback 정합 재명시 (data 기존 redis[hiredis] 의존 — 신규 service 추가 0). 실 compose wiring + engine NAS cred drop = MCT-186 owner)"
  - "ADR-032 (PMO 발의 — VERIFIED badge evidence triad). MCT-184 = dead-in-data 박제 + AC-6 consumer=MCT-185 명시. 본 Story = dead-in-data → production wiring 전환 → evidence triad 갱신 (file:line + caller grep ≥1 + integration test result). 선제 reapply 효력 검증 시점"
retro_file: docs/retros/RETRO-MCT-185.md  # Phase 2 PR2 PMOAgent sub-dispatch 시 생성
---

# MCT-185 — data realtime stream + engine thin client + cold-read cutover

> **요구사항 lane 작성 범위**: §0 Phase 0 Verify Gate + §1·§2·§4·§5·§6 (self-write).
> **§3·§7·§11 = 설계 lane (ArchitectPLAgent Change Plan) 영역 — "설계 lane TBD" placeholder**
> (Change Plan = `docs/change-plans/MCT-185-change-plan.md`).
>
> **본 Story 위치**: EPIC-data-domain-decoupling 7 Story sequential strangler-fig 4단계
> (가장 복잡 Story). MCT-184(REST 신설 dead-in-data) 의 **production wiring 전환점** +
> realtime stream(Redis Stream) 신설 + engine cold-read 8곳 실 cutover + engine
> data_client/ 신규 = D2 cold-read cutover + D3 realtime stream **동시 충족**.

## §0 Phase 0 Verify Gate (R1 — 가설 금지, brainstorm deep-verify 선이행 + 요구사항 lane 재검증)

brainstorm 단계에서 codeforge-brainstorm Phase 0 deep-verify agent 가 spec §2 사실 5
(engine `io/` NAS reader 6 module = engine `src/` 호출자 0, dead-in-prod) 와 사실 7 (data
adapters.py 다중거래소 팩토리, mctrader-market-upbit v0.1.0) 을 file:line 근거로 실증했다.
본 Story 요구사항 lane 정형화 시점(2026-05-17, MCT-184 LAND + post-merge fix LAND + MCT-189
LAND 후 HEAD)에 §4 cross-repo delta 정확성에 결정적인 V1~V8 을 **요구사항 lane PL 이 실제
코드 git fetch/grep/Read/ls 로 재검증**했다. 결과 박제(verified-via `git fetch origin`/
`Grep`/`Read`/`Test-Path`, 2026-05-17, engine `origin/main` HEAD `18275737` (MCT-183 io
removal LAND) + data `origin/main` HEAD `a1a8ccf` (MCT-189 Phase 2 PR2 LAND, MCT-184 fix
`e612296` 포함) + hub `origin/main` HEAD `330c124` (F-1 정정: 박제 시점 `bcbae579` → MCT-189 PMO-AUDIT LAND 후 `330c124`) (MCT-184 post-merge amendment LAND)):

| V | plan 항목 | brainstorm/Orchestrator 가설 | 요구사항 lane 재검증 결과 | 판정 |
|---|-----------|------------------------------|-----------------------------|------|
| V1 | engine/data/hub `git fetch origin` (working tree stale 차단) + MCT-184 LAND + MCT-184 post-merge fix LAND prerequisite | MCT-184 LAND + post-merge fix LAND | `git fetch origin` 3 repo 실행 → **engine origin/main = `182757373a2c996cddc279eaba8343fbe61f8484` `feat(MCT-183): Layer2 io/ removal — 6 module + 7 test deleted (ADR-031 §D2, land_order 2)`** + **data origin/main = `a1a8ccfa84bd65e813a06ca36cadb6dd8e3fb696` `feat(MCT-189): Phase 2 PR2 — legacy 130GB retroactive cleanup` (MCT-184 fix `e612296` `fix(MCT-184): post-merge FIX iter1 — F-1/F-2/F-4 data correctness` 직전 commit 포함 LAND ✅)** + **hub origin/main = `330c124` (F-1 정정: 박제 시점 `bcbae579` → MCT-189 PMO-AUDIT LAND 후 `330c124`) `docs(MCT-184): post-merge FIX iter1 hub amendment` (F-3 hub#360 amendment + F-3 TBD 정정 LAND ✅)**. **PMO-AUDIT-MCT-184 §6.1 carry over #2 (F-1/F-2/F-4 data 측 post-merge fix PR LAND prerequisite gate) 충족 ✅** (silent data corruption + INV-3 mismatch + bytes-level 정밀도 차단 LAND). **코드 작업(data#N + engine#N) 착수 전 V1 `git fetch origin` 3 repo 재실행 + 본 §0 수치 HEAD 재대조 의무** (working tree stale ≠ origin HEAD 차단 — MCT-170/183/184 lesson reapply, 불일치 시 ArchitectPL escalate) | ✅ 정합 (MCT-184 LAND + post-merge fix LAND 전수 prerequisite 충족) |
| V2 | engine cold-read 실 호출부 = **8 위치 4파일** (Orchestrator 가설 — `cli.py:279-280` + `executor/tick_replay.py:26,559` + `wfo/evaluator/data_loader.py:43-44` + `wfo/search/data_loader.py:81-82`). `from mctrader_data.{storage,path,orderbook_replay}` 직독 | 8곳 4파일 cold-read | `Grep -P "from mctrader_data\.(storage\|path\|orderbook_replay)" -- src/` (engine repo HEAD `18275737`) = **8 위치 4파일 전수 정합** — (1) `cli.py:279 from mctrader_data.path import resolve_data_root` (2) `cli.py:280 from mctrader_data.storage import scan_candles` (3) `executor/tick_replay.py:26 from mctrader_data.orderbook_replay import (...)` (top-level) (4) `executor/tick_replay.py:559 from mctrader_data.orderbook_replay import (...)` (function-local re-import) (5) `wfo/evaluator/data_loader.py:43 from mctrader_data.path import resolve_data_root` (6) `wfo/evaluator/data_loader.py:44 from mctrader_data.storage import scan_candles` (7) `wfo/search/data_loader.py:81 from mctrader_data.path import resolve_data_root` (8) `wfo/search/data_loader.py:82 from mctrader_data.storage import scan_candles`. **본 Story cutover scope = D2 cold-read 8곳 전수 정확** | ✅ 정합 (8곳 4파일 — Orchestrator 가설 ↔ HEAD 실상 byte 1:1 정합. MCT-180 reader_cache producer-path 오류 동형 사전 차단) |
| V3 | **engine 전체 `from mctrader_data` 직접 import 잔존 전수 grep (Phase 0 lazy import 의무 reapply, PMO-AUDIT-MCT-183 §5-5 / PMO-AUDIT-MCT-184 #4 신규 의무)** — V2 cold-read 8곳 + 부수 import (reverse-write reverse-direction caller + MCT-182 shim 잔존 등) 전수 식별 | 8곳만 가정 | `Grep -P "from mctrader_data\|import mctrader_data" -- src/` = **14 위치 7파일** (cold-read 8 + 부수 6) — (a) cold-read 8 = V2 박제 (b) 부수 6 = `backtest/nas_sync.py:36 from mctrader_data.nas_storage.nas_uploader import NASUploader` (lazy/type-ignore, reverse-write nas_sync carrier) + `executor/tick_replay.py:31 from mctrader_data.orderbook_storage import OrderbookEventRecord` (MCT-182 shim re-export 잔존) + `executor/tick_replay.py:32 from mctrader_data.tick_storage import TickRecord` (MCT-182 shim re-export 잔존) + `hot/state_machine.py:33 from mctrader_data.aggregation import (...)` (MCT-182 shim re-export 잔존) + `runtime/paper_runner.py:290 from mctrader_data.paper_lineage import PaperLineage, canonical_jsonl_hash` (MCT-182 shim re-export 잔존) + `runtime/paper_runner.py:291 from mctrader_data.paper_storage import write_paper_candles` (reverse-write paper-candles carrier) + `strategy/templates/tick_scalping.py:76 from mctrader_data.tick_storage import TickRecord` (MCT-182 shim re-export 잔존, function-local lazy). **본 Story cutover scope 정정** = D2 cold-read 8곳 (V2) + D3 reverse-write client carrier 3곳 (paper_runner:290/291 paper_lineage+write_paper_candles → data_client POST /v1/reverse-write/paper-candles 경유 + backtest/nas_sync.py:36 nas_uploader → data_client POST /v1/reverse-write/backtest-artifact 경유). **MCT-182 shim 잔존 5곳** (tick_storage/orderbook_storage/aggregation 등 re-export 동작 정상, MCT-188 grep0 quad gate final cleanup owner — 본 Story 비대상). **strategy/templates/tick_scalping.py:76 = TickRecord shim** (MCT-188 final cleanup) | ⚠️ **Phase 0 lazy import grep 정정 발견** — Orchestrator session prompt "8곳 4파일" 가설은 D2 cold-read 한정으로 정확. 그러나 본 Story 가 D3 owner 도 겸함 → **reverse-write carrier 3곳 (paper_runner:290/291 + backtest/nas_sync.py:36) cutover 포함 의무** (paper-candles + backtest-artifact reverse-write client). **MCT-182 shim 잔존 5곳 = MCT-188 owner 분리 SSOT 박제** (본 Story 비대상 명시 — MCT-189 wiring drift 동형 차단 사전) |
| V4 | data `src/mctrader_data/api/realtime_stream.py` 부재 (신규 배치 안전) + redis[hiredis] 기존 가용 | 부재 + dep 가용 | `Test-Path src/mctrader_data/api/realtime_stream.py` = **False** (ABSENT — 신규 배치 충돌 0). data api/ 기존 5 파일 = `__init__.py`/`app.py`/`arrow_ipc.py`/`deps.py`/`routes_v1.py`/`schemas.py` (MCT-184 LAND, 6 파일). `pyproject.toml:20 "redis[hiredis]>=5"` + `:23 "fastapi>=0.110"` + `:24 "uvicorn[standard]>=0.27"` (MCT-184 LAND 후 정합). data api/ redis import = **0건** (신규 도입 — `realtime_stream.py` 가 첫 redis client). **신규 redis 의존 도입 0 (기존 redis[hiredis] 재사용, ADR-030 single-host loopback 정합)** | ✅ 정합 (realtime_stream.py 부재 + redis 가용 — 신규 도입 안전) |
| V5 | engine `src/mctrader_engine/data_client/` 부재 (신규 배치 안전) + engine 측 HTTP client 의존 가용성 | 부재 + dep 가용 | engine `src/mctrader_engine/data_client/` = **ABSENT** (신규 서브패키지 배치 충돌 0). engine `pyproject.toml` = `mctrader-data @ git+...` 의존 **잔존** (MCT-188 D7 final 제거 owner — 본 Story = import 0건 grep 목표이나 pyproject 의존 line 유지). HTTP client 의존 (`httpx`/`requests`/`urllib3`) = 설계 lane 확정 (Change Plan §3 — `httpx>=0.27` 신규 의존 추가 예상, OpenAPI generated client 또는 hand-written 결정). **redis-py client 의존 = 본 Story engine 측 신규 도입 0** (D3 realtime stream consumer = MCT-186 owner, 본 Story = data 측 publisher 만) | ✅ 정합 (data_client/ 부재 + HTTP client 신규 의존 설계 lane 확정 인계. redis-py consumer = MCT-186) |
| V6 | MCT-184 routes_v1.py 307 lines (LAND + post-merge fix `e612296` 적용 후) — 본 Story engine data_client 가 wrap 대상. data api OpenAPI emit SSOT 활용 | routes_v1.py LAND | `routes_v1.py` line count = **307 lines** (MCT-184 LAND 21 passed + 2 skipped test + F-1/F-2/F-4 post-merge fix 적용). MCT-184 §8.5.1 Impl Manifest 매핑 = `/v1/historical/candles` (TierReader wrap Arrow IPC) + `/v1/historical/candles/l1` (L1Reader wrap) + `/v1/reverse-write/paper-candles` (idempotent sha256, INV-3) + `/v1/reverse-write/backtest-artifact` (dead-in-data → 본 Story = production wiring 활성화). **MCT-184 dead-in-data routes_v1.py 4 endpoint = 본 Story engine data_client wrap 대상 SSOT** (OpenAPI emit + hub `.codeforge/contracts/data-api-v1.openapi.json` snapshot 재사용 — 설계 lane Change Plan §3 SSOT) | ✅ 정합 (MCT-184 routes_v1.py 307 lines LAND — engine data_client wrap 대상 SSOT 확정) |
| V7 | ADR-031 amendment box 박제 정합 (MCT-182 §D1 VERIFIED + MCT-183 §D2 partial + MCT-184 §D3 partial) — 본 Story = §D2 cutover + §D3 realtime stream 두 절반 동시 완결 → §D2 VERIFIED + §D3 VERIFIED | 박제 정합 | hub `docs/adr/ADR-031-data-domain-decoupling.md` 존재 + Status `Accepted` (MCT-182 LAND, ADR-031 §D1 VERIFIED amendment box 박제). MCT-183 LAND = §D2 io-relocate 절반 진행 (amendment box). MCT-184 LAND = §D3 historical+reverse-write 절반 진행 (amendment box, dead-in-data). **본 Story = §D2 cold-read cutover 완결 + §D3 realtime stream 신설 + dead-in-data → production wiring 전환 → §D2 VERIFIED + §D3 VERIFIED amendment box 박제** (Status 전이는 MCT-188 POLICY_FINALIZED 시점, 본 Story = D2+D3 VERIFIED 진전). ADR-029 = POLICY_FINALIZED — `§D2 amendment box (engine NAS 직독 폐기 LAND confirm + io reader 6 module = mctrader-data Layer2 + cold-read 8곳 = data REST indirection 실 적용)` 박제 (본문 11 D 정책 무변경, MCT-181 LAND Status Amendment box 패턴 정합) | ✅ 정합 (ADR-031 §D2+§D3 VERIFIED + ADR-029 §D2 amendment confirm 박제 대상 정합) |
| V8 | ADR-030 cross-ref (Redis Stream loopback 정합) + plugin-codeforge-design#44 OPEN (§3.6.1 gate v2 cross-Story reapply 의무) + ADR-032 selectee reapply (dead-in-data → production wiring 전환 evidence triad 갱신) + PMO-AUDIT-MCT-184 §codeforge upstream ADR escalation 후보 2 (박제 PR 자체 완결도 mechanical gate) | gate v2 + ADR-032 reapply + 박제 PR self-discipline | ADR-030 = POLICY_FINALIZED + 19 D 박제. **Redis Stream = ADR-030 single-host loopback 정합** (MCT-184 amendment box 박제 data api service compose topology 예고 line 정합, 본 Story = data 기존 redis[hiredis] 의존 재사용 — 신규 service 추가 0 + ADR-030 본문 정책 무변경). **plugin-codeforge#795 (OPEN — cross-document SSOT mechanical gate)** + **plugin-codeforge#804 (OPEN — 박제 PR 자체 완결도 mechanical gate, MCT-184 §4.3 발의)** + **plugin-codeforge#805 (OPEN — post-merge audit lane, MCT-184 §4.4 발의)** 모두 **미가용** → §3.6.1 gate v2 cross-Story reapply + 박제 PR 5 체크리스트 + Codex post-LAND audit 4 axis = self-discipline 의무 (PMO-AUDIT-MCT-184 §4.5 (e) + §6.2 reapply 8 항목) | ✅ 정합 (ADR-030 Redis loopback 정합 + plugin 3 issue 미가용 → 3중 self-discipline 의무) |

**Phase 0 결론**: V1~V2/V4~V8 = brainstorm/Orchestrator 가설 ↔ HEAD 실상 **정합**. **V3 =
정정 발견** — Orchestrator session prompt "8곳 4파일" 가설은 D2 cold-read 한정으로 정확하나,
본 Story 가 D3(reverse-write client) owner 도 겸함 → **engine 전체 `from mctrader_data`
import 14곳 7파일 전수 식별 후 본 Story cutover scope = (a) D2 cold-read 8곳 (V2) + (b)
D3 reverse-write carrier 3곳 (paper_runner:290/291 paper_lineage+write_paper_candles +
backtest/nas_sync.py:36 nas_uploader, dead-in-data routes_v1.py reverse-write endpoint
production wiring 활성화)** = **11곳 5파일** 정확 (Orchestrator 가설 정정). **MCT-182 shim
잔존 5곳** (tick_storage/orderbook_storage/aggregation re-export 동작 정상) = **MCT-188 owner
final cleanup 분리 SSOT 박제** (본 Story 비대상 명시 — MCT-189 wiring drift 동형 차단 사전).
realtime_stream.py 부재 (V4) + data_client/ 부재 (V5) + MCT-184 routes_v1.py LAND (V6) +
ADR amendment box scope (V7) + plugin gate 미가용 (V8) 의 신규 도입 안전 조건 전수 HEAD
실증. **§0 박제 수치는 HEAD 실증치 — 가설로 수용 금지. 단 코드 작업(data#N → engine#N)
착수 전 V1 `git fetch origin` 3 repo 재실행 + V2(cold-read 8곳 grep)/V3(전체 grep
14곳)/V4(realtime_stream 부재)/V5(data_client 부재) HEAD 재대조 의무** (working tree stale
≠ origin HEAD 차단 — MCT-170/183/184 lesson reapply, 불일치 시 ArchitectPL escalate).

**PMO-AUDIT-MCT-184 §6.2 reapply 8항목 명문 (MCT-182+183+184 lesson 누적 — 필수 reapply)**:

| # | 항목 | 출처 | 본 Story 적용 |
|---|------|------|--------------|
| 1 | **R1 가드 + §0 Phase 0 Verify Gate (V-체크)** | MCT-182~184 lesson | engine `data_client/` 신규 namespace 부재(V5) + Redis Stream publisher 신규 의존 영향 deep-verify (HEAD 재검증 V1-V8) + **engine 전체 `from mctrader_data` grep 14곳 7파일 정확 식별 (V3 Phase 0 lazy import grep 의무 reapply — Orchestrator session prompt 가설 정정 1회 실증)** |
| 2 | **D-row 1:1 reconcile** (scope_manifest D2/D3 ↔ ADR-031 amendment ↔ ADR-029 amendment box ↔ Change Plan) | MCT-179 lesson | D2 cold-read cutover (실 amend confirm = ADR-029 §D2 VERIFIED) + D3 realtime stream (partial VERIFIED → VERIFIED 진전) = **Epic ADR amendment 누적 4 layer 동반 reconcile 의무** (scope_manifest `§design_decisions.D2/D3` + `§story_decision_matrix.MCT-185` + `§planned_adrs.amendments` ADR-029 ↔ ADR-029 amendment box ↔ ADR-031 §D2/§D3 amendment box ↔ §2/§4 DELTA 전수 1:1) |
| 3 | **cross-document SSOT forcing function self-discipline** (mechanical gate plugin 가용 전 까지) | PMO-AUDIT-MCT-183 §4.3 + PMO-AUDIT-MCT-184 §4.5 (e) | **§3.6.1 gate v2 패턴 cross-Story reapply** (glob-scope `docs/adr/ADR-0*.md docs/stories/MCT-18*.md docs/change-plans/MCT-18*.md scope_manifests/EPIC-data-domain-decoupling.yaml .codeforge/contracts/*.json` + 변형포괄 pattern + self-verify TEST1/TEST2 + repo-wide post-LAND grep 0줄 evidence) — 설계 lane Change Plan §3.6.1 박제 인계. plugin-codeforge#795 OPEN (mechanical gate 미가용) |
| 4 | **Phase 0 lazy/conditional import grep** | MCT-183 8-C 신규 발견 + PMO-AUDIT-MCT-184 §6.2 #4 (engine data_client/ 신규 시 lazy/conditional grep 의무 reapply) | engine 전체 `from mctrader_data\|import mctrader_data` grep 14곳 7파일 전수 식별 (V3) + 본 Story cutover scope 11곳 5파일 정확 식별 (D2 cold-read 8 + D3 reverse-write 3). MCT-182 shim 잔존 5곳 = MCT-188 owner 분리 SSOT 박제 (본 Story 비대상). engine data_client/ 신규 시 동일 lazy import grep 의무 (코드 lane Phase 0 재실증 의무 V3 carry) |
| 5 | **Codex pre-LAND audit + post-LAND audit 의무** | MCT-182+183+184 lesson (3회 연속 효과) | data realtime_stream.py + engine data_client/ + cold-read 11곳 cutover 신규 시 **pre-LAND 4 axis (lint/typing/test/security) + post-LAND 4 axis (production correctness + bytes-level 정밀도 + SSOT 재검증 + security) 의무 운용** (plugin-codeforge#805 post-merge audit lane 미가용 전 self-discipline) |
| 6 | **MCT-189 wiring drift 동형 차단 (ADR-032 evidence triad reapply)** | MCT-184 AC-6 패턴 (1회 실증) | **dead-in-data → live cutover 전환 시 production caller grep evidence triad 갱신 의무 (test/runtime/code 3종)** — MCT-184 routes_v1.py dead-in-data 박제가 **본 Story = production wiring 활성화 시점** (engine data_client REST 경유 cold-read cutover + reverse-write client cutover) → ADR-032 evidence triad 박제 (caller grep ≥1 + integration test result + file:line evidence). MCT-184 AC-6 의 의도된 dead-in-data SSOT → **본 Story AC-6 = 의도된 production wiring SSOT 박제** (선제 reapply 효력 실증) |
| 7 | **박제 PR 자체 완결도 self-discipline (mechanical gate plugin-codeforge#804 가용 전)** | PMO-AUDIT-MCT-184 §4.3 신규 발의 | **박제 PR LAND 전 5 체크리스트 (RETRO 존재 + EPIC-RESULTS §Story-N + frontmatter status=COMPLETED + completed_at + CLAUDE.md hub#TBD 잔존 0줄 + ADR amendment confirm)** self-discipline 의무. MCT-184 hub#359 부분 박제 (≈58% carry) + hub#360 amendment 동형 재발 사전 차단. Phase 2 PR2 박제 시 5 체크 전수 PASS 의무 |
| 8 | **post-merge audit lane self-discipline (mechanical lane plugin-codeforge#805 가용 전)** | PMO-AUDIT-MCT-184 §4.4 신규 발의 | **LAND 후 Codex post-LAND audit 4 axis 의무 운용** (production correctness + bytes-level 정밀도 + SSOT 재검증 + security) + finding 시 별 post-merge fix PR carry over template. MCT-184 F-1/F-2/F-4 post-LAND 4건 동형 재발 사전 차단 (cutover lane 의 production correctness 영역은 sentinel sentry 의무 강화 — silent data corruption + INV-3 mismatch + bytes-level 정밀도 영역 특히 강화) |

**R1 가드 cross-document SSOT 정합 self-discipline 명문 (PMO-AUDIT-MCT-184 §4 + §6.2 reapply
의무 — plugin-codeforge#795/#804/#805 mechanical gate 미가용)**: 본 Story 의 모든 SSOT
산출물 — Story §본문(특히 §2 목표 + §4 DELTA + §5 AC-1~AC-6) ↔ scope_manifest
`§story_decision_matrix.MCT-185` + `§design_decisions.D2/D3` + `§planned_adrs.amendments`
ADR-029 ↔ ADR-029 §D2 amendment box ↔ ADR-031 §D2/§D3 amendment box ↔ ADR-030 Redis loopback
cross-ref ↔ (설계 lane) Change Plan §3 — 은 **D2+D3 row 기준 1:1 전수 reconcile** 의무다
(MCT-179 lesson reapply — Out-of-scope/D-row stale 누적 사전 차단, MCT-182~184 D-row reconcile
패턴 계승). 1차 FIX 발생 시 정정 산출물 list ↔ 전 산출물 동반 reconcile 체크리스트 의무
(MCT-183 iter1→3 5회 누적 cross-document desync 동형 재발 사전 차단). **설계 lane 인계 —
§3.6.1 gate v2 cross-Story reapply 의무** (glob-scope + 변형포괄 pattern + self-verify
TEST1/TEST2 + repo-wide post-LAND grep 0줄 evidence — ArchitectPL Change Plan §3.6.1 박제
의무. plugin-codeforge#795 OPEN — mechanical gate plugin 가용 전 까지 mctrader-hub
self-discipline 유지). **박제 PR self-discipline (plugin-codeforge#804 OPEN) + post-LAND
audit self-discipline (plugin-codeforge#805 OPEN)** = 3중 self-discipline 의무.

## §1 동기 (배경/문제)

사용자 원문: *"data 영역은 mctrader와 아예 분리하고 싶다. 표준 mctrader에는 호출용 interface,
data에는 그를 처리하는 REST API가 있는게 좋겠다. 프로젝트 구조는 mctrader-hub에서도 구현할 수
있고 mctrader-data 단독으로도 필요사항을 반영할 수 있어야겠다."*

**EPIC-data-domain-decoupling Story-4 (sequential_phase 4, milestone target 4/7) — 가장
복잡 Story**. Epic 종착점 = mctrader-engine 을 (1) data-free (`mctrader_data` python 의존
0) + (2) exchange-agnostic pure consumer (`mctrader_market_bithumb/upbit/*` 어댑터 직접
의존 0) 로 전환. 사용자 첫 요구 "표준 mctrader에는 호출용 interface, data에는 그를 처리하는
REST API" 의 **호출 관계 실 cutover 완결 Story** — 본 Story 가 engine→data python 결합의
실 끊김 시점이다. MCT-184(REST 신설 dead-in-data) COMPLETED 2026-05-16 + MCT-184 post-merge
fix `e612296` LAND (F-1/F-2/F-4 data correctness) + MCT-189 Phase 2 PR2 LAND →
production-readiness 확정 → 본 Story 진입 prerequisite 충족.

### 문제 (Phase 0 deep-verify 실증)

MCT-184 LAND 결과 — data 측 `src/mctrader_data/api/` FastAPI `/v1` REST API 신설 완료
(routes_v1.py 307 lines, §0 V6: historical Arrow IPC + reverse-write POST + OpenAPI emit
+ hub `.codeforge/contracts/data-api-v1.openapi.json` snapshot). 그러나 MCT-184 §2 비목표
명시 — **REST API = dead-in-data 신설 (engine cutover = MCT-185 owner)**. 현 상태:

1. **engine cold-read 11곳 5파일 = `mctrader_data.*` 직접 python import 잔존** (§0 V3 정확
   식별):
   - **D2 cold-read 8곳 4파일** (Orchestrator 가설 정합, §0 V2): `cli.py:279-280`
     (`resolve_data_root` + `scan_candles`) + `executor/tick_replay.py:26,559`
     (`orderbook_replay` top-level + function-local) + `wfo/evaluator/data_loader.py:43-44`
     (`resolve_data_root` + `scan_candles`) + `wfo/search/data_loader.py:81-82`
     (`resolve_data_root` + `scan_candles`)
   - **D3 reverse-write carrier 3곳 2파일** (§0 V3 정정 발견 — 본 Story 가 D3 owner 도
     겸함): `runtime/paper_runner.py:290-291` (`paper_lineage.PaperLineage + canonical_jsonl_hash`
     + `paper_storage.write_paper_candles`) + `backtest/nas_sync.py:36`
     (`nas_storage.nas_uploader.NASUploader` lazy import)

2. **MCT-184 dead-in-data REST endpoint = production caller 0건** (§0 V6 routes_v1.py
   307 lines LAND 후 production wiring 0건 — MCT-184 AC-6 의도된 dead-in-data SSOT 박제).
   "VERIFIED 박제 ↔ 실 caller 0건" wiring drift = MCT-189 동형 위험 (PMO-AUDIT-MCT-184
   §3 패턴 #3 — 본 Story = MCT-184 AC-6 의 production wiring 전환 시점, evidence triad
   갱신 의무).

3. **data 측 realtime 정규화 stream 부재** (§0 V4 — `src/mctrader_data/api/realtime_stream.py`
   ABSENT). Milestone-2 tick.v1.1 패턴 (MCT-135~146 LAND, hub#137) 의 정규화 schema 는
   존재하나 stream publisher 신설 0. ADR-030 §D15 Redis prefix (signal:/market:/engine:)
   + redis[hiredis] 기존 의존 (§0 V4) 만 가용 — Redis Stream publisher 신규 신설 의무.

4. **engine data_client/ 부재** (§0 V5 — `src/mctrader_engine/data_client/` ABSENT). MCT-184
   OpenAPI SSOT (data 단방향) ↔ engine generated client = MCT-185 owner (MCT-184 비목표
   table 명시) → 본 Story 가 engine thin/generated client 신설 owner.

문제의 본질: **engine 이 data 를 표준 계약 표면(REST + realtime stream)이 아니라 python
import 로 호출**한다 (cold-read 8곳 + reverse-write 3곳 = 11곳 5파일). 사용자 핵심 요구
"표준 mctrader에는 호출용 interface" 가 미충족 상태 (data 측 REST endpoint 박제 ↔ engine
production wiring 0건 — MCT-184 AC-6 의도된 dead-in-data). engine pure consumer 전환
(Layer2') 의 **production 실 연결 핵심 시점** = 본 Story.

본 Story(MCT-185) = strangler-fig **4단계 (가장 복잡)**:
1. data `src/mctrader_data/api/realtime_stream.py` 신규 (Redis Stream 정규화 publisher,
   tick.v1.1 패턴 — D3 절반 충족)
2. engine `src/mctrader_engine/data_client/` 신규 (thin HTTP client, MCT-184 OpenAPI SSOT
   소비 — historical fetch + reverse-write client. realtime consumer = MCT-186 owner)
3. engine cold-read 8곳 4파일 (§0 V2) → `data_client/` REST 경유 cutover (D2 cold-read
   cutover 완결)
4. engine reverse-write carrier 3곳 2파일 (§0 V3) → `data_client/` REST 경유 cutover
   (D3 reverse-write client wiring 완결)
5. MCT-184 dead-in-data → production wiring 전환 → ADR-032 evidence triad 갱신 (AC-6
   selectee reapply — MCT-184 AC-6 의도된 dead-in-data SSOT 박제가 본 Story = 의도된
   production wiring SSOT 박제로 전환)
6. ADR-029 §D2 VERIFIED amendment confirm (engine NAS 직독 폐기 LAND + io reader Layer2
   + cold-read 8곳 REST indirection 실 적용) + ADR-031 §D2/§D3 VERIFIED amendment box 박제

**engine `pyproject.toml` mctrader-data 의존 잔존 = MCT-188 D7 final 제거 owner** (본
Story = import 0건 grep 목표이나 pyproject 의존 line 유지 — `data_client/` 가 mctrader-data
import 안 함, HTTP REST client only). **engine `mctrader_market_bithumb/upbit` 어댑터
직접 import ~5곳 제거 (D4) + 다중거래소 invariant (D5) + grep0 quad gate (D7) = MCT-186/187/188
owner** (본 Story 비대상).

### MCT-184 dead-in-data → production wiring 전환 패턴 (ADR-032 evidence triad 선제 reapply 효력 실증)

MCT-184 AC-6 (`PMO-AUDIT-MCT-184 §3 패턴 #3` 박제) = "REST endpoint 박제 ↔ 실 production
caller 0건 + 명시적 consumer=MCT-185 박제 = drift 아닌 의도된 dead-in-data evidence (ADR-032
evidence triad: caller grep 0건 + 명시적 consumer 박제)" 패턴. **본 Story = consumer=MCT-185
실 cutover 시점 → evidence triad 갱신 의무**:

- MCT-184 evidence triad (의도된 dead-in-data) → **본 Story evidence triad (의도된 production
  wiring)** = file:line (engine `data_client/historical.py` + `cli.py:279-280` cutover
  카운터) + caller grep ≥1 (engine `from mctrader_engine.data_client` import grep ≥11곳 =
  cold-read 8 + reverse-write 3) + integration test result (engine→data REST round-trip
  smoke test PASS)
- MCT-189 wiring drift 동형 (`promote_l1()` VERIFIED 박제 ↔ 실 caller 0건 → 130GB legacy
  누적 HIGH severity) 차단 의무 = MCT-184 AC-6 선제 reapply + **본 Story AC-6 production
  wiring 전환 evidence triad 갱신** = 선제 reapply 효력 1회 실증 시점

### 추출된 동기 (spec §1 확정, 4종 + 본 Story 핵심 위치 명시)

| why | 내용 |
|---|---|
| 코드 결합도 감소 | **본 Story = engine→data python 결합 실 끊김 시점**. engine cold-read 8곳 + reverse-write 3곳 = 11곳 5파일 python import 잔존 → data REST 경유 cutover. MCT-184 routes_v1.py wrap 대상 (historical Arrow IPC + reverse-write POST) 실 소비 시작 |
| 독립 배포·버전 관리 | engine data_client/ thin client = MCT-184 OpenAPI SSOT 단방향 소비. engine data_client 변경 시 data 측 변경 0 (SSOT = data, consumer = engine generated). 다중 consumer 확장 토대 (engine 외 시스템도 동일 OpenAPI SSOT 소비) |
| 외부·다중 consumer | engine pure consumer 전환 = 다중 consumer 패턴 첫 실증 (engine = data 외부 consumer 1호). 향후 외부 시스템도 REST + realtime stream 으로 호출 (Redis Stream publisher = 다중 subscriber pattern) |
| 다중거래소 확장 | realtime 정규화 stream (Redis Stream tick.v1.1 패턴) = 다중거래소 정규화 단일 contract 표면 (D5 다중거래소 invariant 의 realtime side 토대 — MCT-186 engine cutover 후 MCT-187 invariant 박제 owner) |

본 Story = strangler-fig **4단계 (가장 복잡 = D2+D3 동시 충족)**:
- D2 cold-read cutover (engine cold-read 8곳 → data_client REST 경유) **완결**
- D3 realtime stream publisher (Redis Stream tick.v1.1 패턴) **신설**
- D3 reverse-write client (engine reverse-write 3곳 → data_client REST 경유) **wiring 완결**
- ADR-029 §D2 VERIFIED amendment confirm (engine NAS 직독 폐기 LAND) + ADR-031 §D2/§D3
  VERIFIED amendment box 박제 (Status `Accepted` 유지, 전이 MCT-188)
- MCT-184 dead-in-data → production wiring 전환 (AC-6 evidence triad 선제 reapply 효력
  실증)

## §2 목표 / 비목표

### 목표 (in-scope, D2 + D3)

1. **D3 — data realtime stream publisher 신설 (Redis Stream tick.v1.1 패턴)**
   (`option_chosen: fastapi-v1 + redis-stream` 중 redis-stream 절반 — MCT-184 fastapi-v1
   historical+reverse-write 절반 완결 후)
   - mctrader-data `src/mctrader_data/api/realtime_stream.py` 신규 (§0 V4 — ABSENT
     신규 배치 충돌 0). Redis Stream 정규화 publisher (Milestone-2 tick.v1.1 패턴 정합,
     ADR-030 §D15 Redis prefix `market:` namespace 정합). data 기존 `redis[hiredis]>=5`
     의존 재사용 (§0 V4 — 신규 의존 0)
   - tick.v1.1 정규화 schema 정합 (MCT-135~146 LAND tick.v1.1 SSOT). 거래소별 raw → 정규화
     TickRowV1_1 (market-core contract) → Redis Stream `XADD market:tick:{symbol}` publish
     (consumer 측 MCT-186 engine cutover 또는 외부 consumer 가 `XREAD` 구독)
   - **ADR-030 single-host loopback 정합** (compose internal network only, 외부 노출 0 —
     ADR-030 §D15 Redis prefix 격리 + MCT-184 amendment box compose topology 정합)

2. **D3 — engine thin HTTP client 신규 (`mctrader_engine/data_client/`)**
   (MCT-184 OpenAPI SSOT 소비 — generated 또는 hand-written 결정 = 설계 lane)
   - mctrader-engine `src/mctrader_engine/data_client/` 신규 서브패키지 (§0 V5 — ABSENT
     신규 배치 충돌 0). MCT-184 OpenAPI SSOT (`hub:.codeforge/contracts/data-api-v1.openapi.json`)
     소비 → historical fetch client + reverse-write client + (realtime consumer = MCT-186 owner)
   - HTTP client 의존 신규 (`httpx>=0.27` 예상 — 설계 lane 확정. OpenAPI generator 도구
     선택 = openapi-python-client 또는 hand-written 결정 인계)
   - engine `pyproject.toml` `mctrader-data @ git+` 의존 = **잔존** (MCT-188 D7 final 제거
     owner). 본 Story = import 0건 grep 목표 (cutover 후 src grep), pyproject 의존 line 유지

3. **D2 — engine cold-read 8곳 4파일 cutover (data_client REST 경유)**
   (`option_chosen: io-relocate + cold-read-behind-REST` 중 cold-read-behind-REST 절반
   — MCT-183 io-relocate 절반 완결 후)
   - engine cold-read 8곳 (§0 V2 정확 식별):
     - `cli.py:279` `from mctrader_data.path import resolve_data_root` →
       `data_client.path_client.resolve_data_root` 또는 historical fetch 경유 path 정보 수신
     - `cli.py:280` `from mctrader_data.storage import scan_candles` →
       `data_client.historical.fetch_candles` (REST `/v1/historical/candles`)
     - `executor/tick_replay.py:26,559` `from mctrader_data.orderbook_replay import (...)` →
       `data_client.historical.fetch_orderbook` (REST `/v1/historical/orderbook` 신규,
       MCT-184 routes_v1.py 확장 필요 — 설계 lane 확정. **또는 MCT-184 시점에 신설 누락
       carry over** — 설계 lane 판단)
     - `wfo/evaluator/data_loader.py:43,44` (`resolve_data_root` + `scan_candles`) →
       data_client 동일 경유
     - `wfo/search/data_loader.py:81,82` (`resolve_data_root` + `scan_candles`) →
       data_client 동일 경유
   - **engine src/ `from mctrader_data.{storage,path,orderbook_replay}` import == 0**
     (cutover 후 grep0 = 본 Story 핵심 AC-3). engine 측 `mctrader_data` direct python
     import 끊김 = D2 cold-read cutover 완결

4. **D3 — engine reverse-write carrier 3곳 2파일 cutover (data_client REST 경유)**
   (§0 V3 정정 발견 — D3 owner 도 본 Story 가 겸함)
   - `runtime/paper_runner.py:290` `from mctrader_data.paper_lineage import PaperLineage,
     canonical_jsonl_hash` → engine 측 = mctrader_market.paper_lineage 직접 사용 (MCT-182
     LAND market-core relocate 완료 — shim re-export 사용 중. cutover = market-core 직독
     으로 import path 변경 = MCT-188 shim cleanup 동반)
   - `runtime/paper_runner.py:291` `from mctrader_data.paper_storage import write_paper_candles`
     → `data_client.reverse_write.post_paper_candles` (REST `POST /v1/reverse-write/paper-candles`,
     MCT-184 routes_v1.py LAND endpoint 소비). **idempotent sha256 hash key 정합** (MCT-184
     INV-3 + post-merge fix `e612296` F-2 canonical_sha256 fix 적용 후 LAND)
   - `backtest/nas_sync.py:36` `from mctrader_data.nas_storage.nas_uploader import NASUploader`
     → `data_client.reverse_write.post_backtest_artifact` (REST `POST /v1/reverse-write/
     backtest-artifact`, MCT-184 routes_v1.py LAND endpoint 소비 = 의도된 dead-in-data
     → 본 Story = production wiring 전환)

5. **D2 — MCT-184 dead-in-data → production wiring 전환 (AC-6 evidence triad 갱신 선제
   reapply 효력 실증)**
   - MCT-184 routes_v1.py 4 endpoint (historical/candles + historical/candles/l1 +
     reverse-write/paper-candles + reverse-write/backtest-artifact) = production caller
     ≥11곳 (cold-read 8 + reverse-write 3) 실 연결
   - **ADR-032 evidence triad 갱신** (MCT-184 AC-6 의도된 dead-in-data SSOT → **본 Story
     AC-6 = 의도된 production wiring SSOT 박제**): file:line (engine `data_client/`
     production caller cutover 카운터) + caller grep ≥11 + integration test result (engine
     → data REST round-trip smoke test PASS)

6. **D6 — ADR-029 §D2 VERIFIED amendment box + ADR-031 §D2/§D3 VERIFIED amendment box +
   ADR-030 Redis Stream loopback cross-ref**
   - `docs/adr/ADR-029-tier-promotion-single-source.md` **§D2 VERIFIED amendment box** —
     engine NAS 직독 폐기 LAND confirm + io reader 6 module = mctrader-data Layer2 +
     cold-read 8곳 = data REST indirection 실 적용 박제. ADR-029 Status `POLICY_FINALIZED`
     유지 (본문 11 D 정책 무변경 — MCT-181 LAND Status Amendment box 패턴 정합)
   - `docs/adr/ADR-031-data-domain-decoupling.md` §D2 + §D3 amendment box — **§D2 cold-read
     cutover VERIFIED + §D3 realtime stream + reverse-write wiring VERIFIED** 박제 (Status
     `Accepted` 유지, 전이 = MCT-188 POLICY_FINALIZED)
   - `docs/adr/ADR-030-docker-stack-governance.md` cross-ref (amendment 없음 — Status
     POLICY_FINALIZED 본문 19 D 무변경, Redis Stream = single-host loopback 정합 재명시.
     MCT-184 amendment box compose topology + 본 Story = data 기존 redis 의존 재사용)
   - **D-row ↔ scope_manifest `§design_decisions.D2/D3` + `§story_decision_matrix.MCT-185`
     + `§planned_adrs.amendments` ADR-029 1:1 전수 reconcile** (MCT-179 lesson reapply +
     MCT-182~184 D-row reconcile 패턴 계승. §3.6.1 gate v2 cross-Story reapply —
     plugin-codeforge#795 mechanical gate 미가용, self-discipline 유지)

### 비목표 (out-of-scope — 후속 Story owner 명시)

| 항목 | 사유 | owner Story |
|------|------|-------------|
| engine `mctrader_market_bithumb/upbit` 어댑터 직접 import ~5곳 (paper_runner/ws_wrapper/stream_consumer/fill/mock_stream realtime WS) 제거 → 정규화 stream 구독 | D4 — realtime cutover (engine consumer = MCT-186, 본 Story = data publisher only) | MCT-186 |
| engine 측 Redis Stream subscriber (data realtime publisher 소비 — `XREAD market:tick:{symbol}` engine side wiring) | D4 — engine realtime cutover, 본 Story = data publisher 신설 only | MCT-186 |
| 다중거래소 확장 불변식 박제 (`add-new-exchange.md` runbook + adapters.py invariant test) | D5 — engine adapter 제거 후 invariant 화 | MCT-187 |
| data-free grep0 quad gate CI (engine src/ `mctrader_data` 0 strict + pyproject 의존 제거) + Epic POLICY_FINALIZED | D7 — finalize | MCT-188 |
| MCT-182 shim 잔존 5곳 cleanup (`tick_storage`/`orderbook_storage`/`aggregation`/`paper_lineage` shim re-export → market-core 직독 변경. engine src/ grep 5곳: tick_replay.py:31,32 + hot/state_machine.py:33 + paper_runner.py:290 paper_lineage + strategy/templates/tick_scalping.py:76) | D7 — grep0 quad gate finalize (shim re-export 동작 정상이나 grep0 strict 충족 의무) | MCT-188 |
| engine `pyproject.toml` `mctrader-data @ git+` 의존 제거 + `mctrader_market_bithumb/upbit` 어댑터 의존 제거 (finalize) | D7 — pyproject quad gate | MCT-188 |
| historical `/v1/historical/orderbook` endpoint MCT-184 routes_v1.py 신설 누락 carry over (executor/tick_replay.py:26,559 orderbook_replay cutover 대상) | MCT-184 시점에 orderbook endpoint 신설 누락 (historical/candles + historical/candles/l1 + reverse-write 만 LAND) — 본 Story = endpoint 확장 필요 (설계 lane 판단 = MCT-184 routes_v1.py 확장 vs MCT-185 신규 endpoint 추가) | 본 Story 또는 MCT-184 amendment (설계 lane 결정) |
| ADR-030 **실 compose wiring** (data api service compose topology 실 적용 + engine NAS credential env drop) | 본 Story = Redis Stream loopback cross-ref only (data 기존 redis 의존 재사용, compose service 신규 0) | MCT-186 (engine NAS cred drop) |
| ADR-031 Status `Accepted` → `POLICY_FINALIZED` 전이 | D6 — POLICY_FINALIZED 박제 = Epic CLOSED prerequisite | MCT-188 |
| Codex post-LAND audit lane 신설 (plugin-codeforge#805) + 박제 PR 5 체크 mechanical gate (plugin-codeforge#804) | codeforge upstream ADR escalation 후보 — mctrader-hub self-discipline 유지 (consumer escalation path) | codeforge upstream (PMO-AUDIT-MCT-184 §4 carry) |

## §3 Risk acceptance / Security-design

> **설계 lane (ArchitectPLAgent) LAND 2026-05-17** — Change Plan
> `docs/change-plans/MCT-185-change-plan.md` §1-§12 박제 완료. 본 §3 = 요구사항 lane
> 박제 의무 위험 식별 (R1/R2 + 보안/운영 risk acceptance 포인터 — Change Plan §7/§9 SSOT
> 차용).
>
> **설계 lane SSOT (Change Plan §7 — SecurityArch primary 강함 + OperationalRiskArch
> primary 강함 CONDITIONAL §8.5 active=true)**:
>
> - **§7.1 trust boundary**: engine ↔ data internal-only compose network only
>   (`http://data-api:8000`, ADR-030 single-host loopback). engine `httpx>=0.27` HTTP
>   client = internal call only (외부 인터넷 노출 0). data Redis Stream publisher =
>   loopback only (compose internal). attack surface delta = **0 추가**
> - **§7.2 T1-T7 위협↔완화 전수 매핑**: T1 path traversal (client-side allowlist regex)
>   + T2 payload DoS (`max_length` bound) + T3 Redis Stream DoS (MAXLEN `~ 100000`) +
>   T4 idempotency 우회 (canonical sha256 client-side + market-core SSOT 재사용) + T5
>   Redis replay (compose internal 격리) + T6 NAS 정보 노출 (Arrow IPC stream only) +
>   T7 TLS 0 (internal-only 정당, MCT-186 carry)
> - **§7.3 auth**: internal-only network 격리 → 인증 토큰 미도입 (MCT-186 carry —
>   internal service auth shared secret/mTLS 검토)
> - **§7.4 OperationalRiskArch (§8.5 active=true)**: DR (data api down → engine `data_client.base`
>   retry exponential backoff 3회 + circuit-breaker MCT-170 dr_mode 패턴 답습 + **local-fallback
>   없음** ADR-029 §D2 invariant 정합) + DR (Redis Stream → publisher retry 5회 + local-only
>   queue 100 entry + Prometheus alert) + disconnect (uvicorn `--timeout-graceful-shutdown=60`
>   MCT-184 재사용) + rate-limit (internal-only + low-freq batch → N/A 정당, MCT-186 carry)
>   + restart policy (`restart: unless-stopped` MCT-184 정합 + ASGI lifespan startup/shutdown
>   통합) + clock sync (Redis Stream server-side timestamp + REST stateless → N/A 정당)
>   + §8.5 stateful/restart (in-memory state 휘발 정합 = restart-safe)
>
> **설계 lane 인계 — 보안·운영 risk 4 영역 (Change Plan §7 SSOT 예정)**:
>
> 1. **§7.1 SecurityArch primary (강함)** — 신규 trust boundary 영역 = engine `data_client/`
>    HTTP client (internal-only network → data api `:8000`) + data `realtime_stream.py`
>    Redis Stream publisher (loopback only). 외부 노출 0 + Pydantic strict input validation
>    재사용 (MCT-184 routes_v1.py LAND 패턴) + sha256 idempotent 재사용 (MCT-184 INV-3 +
>    post-merge fix `e612296` F-2 canonical_sha256 fix 적용)
> 2. **§7.4 OperationalRiskArch primary (CONDITIONAL §8.5 active=true 예상)** — engine
>    data_client 신규 service (HTTP persistent connection) + Redis Stream publisher (XADD
>    long-running connection) + cold-read cutover (production wiring 활성화 = MCT-184
>    routes_v1.py production caller 0→11곳 증가) → DR (data api down → engine cold-read
>    failure, retry/circuit-breaker/local-fallback 의무) + restart policy + clock sync
>    (Redis Stream timestamp 정합) 설계 lane 확정
> 3. **§7.3 auth** — engine↔data internal-only network 격리 (MCT-184 amendment box 정합)
>    유지. 본 Story production wiring 활성화 = MCT-184 carry-over (internal service auth
>    shared secret/mTLS 도입 검토 의무)
> 4. **R1/R2 risk** (본 §3 핵심):
>    - **R1 (cross-repo desync 7회째 — 본 Story 3 repo land_order)**: 완화 = §0 Phase 0
>      게이트 (V1~V8 HEAD 실증 + V1 fetch 재대조 + V2/V3 grep 재실증 의무) + D-row 1:1
>      reconcile (D2+D3 row scope_manifest + ADR-029 amendment box + ADR-031 §D2/§D3
>      amendment box 동반) + §3.6.1 gate v2 cross-Story reapply (Change Plan §3.6.1 SSOT
>      — plugin-codeforge#795 mechanical gate 미가용). **3 repo 본 Story = 가장 큰
>      cross-repo desync risk** (MCT-184 = 2 repo 단순화 vs MCT-185 = 3 repo + production
>      wiring 전환 = 가장 복잡)
>    - **R2 (MCT-41 Live Mode Debut 블락 — engine 측 cross-cutting)**: 본 Story = engine
>      `data_client/` 신규 + cold-read 8곳 + reverse-write 3곳 cutover. engine `runtime/
>      paper_runner.py:290,291` + `executor/tick_replay.py:26,31,32,559` + `cli.py:279,280`
>      = MCT-41 (MCT-43~47 IN_PROGRESS) realtime WS/주문 실행 경로 와 **모듈 공유 위험점**.
>      **Phase 0 verify 의무 (코드 작업 전 의무)** = MCT-43~47 IN_PROGRESS 변경 파일 ↔
>      engine cold-read 8곳 + reverse-write 3곳 (총 11곳 5파일) 교차 검증 의무. 공유 모듈
>      발견 시 (a) MCT-185 land_order 재조정 or (b) 공유 모듈 부분만 별 sub-Story 분리
>      = Orchestrator ordering 결정 (scope_manifest §dependency.parallel_safe_with — MCT-186
>      에서 명시했으나 본 Story = production wiring 전환 시점이라 cross-cutting 위험 확장)

## §4 Cross-repo delta (ChangeImpact — AS-IS → DELTA)

> 출처: spec §3 (AS-IS 그래프) / §4 (TO-BE 4-Layer) + scope_manifest
> `story_decision_matrix.MCT-185.cross_repo` + 요구사항 lane Phase 0 재검증
> (verified-via `git fetch origin` / `Grep` / `Read` / `Test-Path`, 2026-05-17 HEAD
> engine `18275737` + data `a1a8ccf` + hub `330c124` (F-1 정정: 박제 시점 `bcbae579` → MCT-189 PMO-AUDIT LAND 후 `330c124`)). ChangeImpact / Feasibility /
> Continuity sub 관점.

### §4.0 관련 코드 경로 (지도 수준)

| repo | 경로 | 현재 책임 | 본 Story 영향 |
|------|------|-----------|----------------|
| mctrader-data | `src/mctrader_data/api/` (6 파일 LAND, §0 V4/V6) | MCT-184 LAND FastAPI app — `__init__.py`/`app.py`/`arrow_ipc.py`/`deps.py`/`routes_v1.py` (307 lines, 4 endpoint) / `schemas.py`. 의도된 dead-in-data (production caller 0 + consumer=MCT-185 박제) | **production wiring 활성화** (engine data_client REST 경유 cold-read 8곳 + reverse-write 3곳 실 호출 시작 → routes_v1.py 4 endpoint 실 caller ≥11곳) + **realtime_stream.py 신규 추가** (Redis Stream publisher) |
| mctrader-data | `src/mctrader_data/api/realtime_stream.py` (ABSENT §0 V4) | 부재 | **신규 신설** (Redis Stream tick.v1.1 publisher, ADR-030 §D15 `market:` prefix 정합. data 기존 `redis[hiredis]>=5` 재사용 — 신규 의존 0) |
| mctrader-data | `pyproject.toml:20 "redis[hiredis]>=5"` (§0 V4) | Redis client 의존 (compactor/nas 등 기존 사용) | (변경 없음) — Redis Stream 신규 publisher 도 기존 의존 재사용. fastapi/uvicorn 도 MCT-184 LAND 후 기존 (§0 V4) |
| mctrader-engine | `src/mctrader_engine/data_client/` (ABSENT §0 V5) | 부재 | **신규 서브패키지** (thin HTTP client, MCT-184 OpenAPI SSOT 소비. historical fetch + reverse-write client. realtime consumer = MCT-186 owner) |
| mctrader-engine | `src/mctrader_engine/cli.py:279-280` | `from mctrader_data.path import resolve_data_root` + `from mctrader_data.storage import scan_candles` (cold-read 2곳) | **MODIFY** — `data_client.historical` REST 경유 cutover (D2 cold-read 8곳 중 2/8) |
| mctrader-engine | `src/mctrader_engine/executor/tick_replay.py:26,559` | `from mctrader_data.orderbook_replay import (...)` top-level + function-local (cold-read 2곳) | **MODIFY** — `data_client.historical` REST 경유 cutover (D2 cold-read 8곳 중 2/8). historical orderbook endpoint = MCT-184 routes_v1.py 확장 or 본 Story 신규 신설 (설계 lane 판단) |
| mctrader-engine | `src/mctrader_engine/wfo/evaluator/data_loader.py:43-44` + `wfo/search/data_loader.py:81-82` | `resolve_data_root` + `scan_candles` × 2 파일 (cold-read 4곳) | **MODIFY** — `data_client.historical` REST 경유 cutover (D2 cold-read 8곳 중 4/8) |
| mctrader-engine | `src/mctrader_engine/runtime/paper_runner.py:290-291` | `from mctrader_data.paper_lineage import ...` + `from mctrader_data.paper_storage import write_paper_candles` (reverse-write 2곳) | **MODIFY** — `data_client.reverse_write.post_paper_candles` REST 경유 cutover (D3 reverse-write 3곳 중 2/3). paper_lineage 직접 사용 = market-core 직독 변경 (MCT-188 shim cleanup 동반 or 본 Story 분리 = 설계 lane 판단) |
| mctrader-engine | `src/mctrader_engine/backtest/nas_sync.py:36` | `from mctrader_data.nas_storage.nas_uploader import NASUploader` (lazy/type-ignore, reverse-write 1곳) | **MODIFY** — `data_client.reverse_write.post_backtest_artifact` REST 경유 cutover (D3 reverse-write 3곳 중 1/3) |
| mctrader-engine | `pyproject.toml` `mctrader-data @ git+` 잔존 | engine pyproject 의존 (MCT-188 final 제거 owner) | (변경 없음) — `data_client/` 가 mctrader-data import 안 함 (HTTP REST only). import 0건 grep 목표 (AC-3) 충족이나 pyproject 의존 line 유지 |
| mctrader-engine | `pyproject.toml` HTTP client 의존 (`httpx`/`requests`) | (검증 필요 — 설계 lane HEAD 재실증) | **신규 의존 추가** (`httpx>=0.27` 예상 — 설계 lane 확정) |
| mctrader-engine | `src/mctrader_engine/{executor/tick_replay.py:31,32, hot/state_machine.py:33, runtime/paper_runner.py:290 paper_lineage, strategy/templates/tick_scalping.py:76}` | MCT-182 shim 잔존 5곳 (tick_storage/orderbook_storage/aggregation/paper_lineage shim re-export 사용 중 — 동작 정상) | (본 Story 비대상) — MCT-188 D7 grep0 quad gate final cleanup owner (market-core 직독 변경 분리 SSOT 박제) |
| mctrader-hub | `docs/adr/ADR-029-tier-promotion-single-source.md` (Status POLICY_FINALIZED) | engine NAS 직독 폐기 예고 (MCT-183 amendment box) | **§D2 VERIFIED amendment box** — engine NAS 직독 폐기 LAND confirm + cold-read 8곳 REST indirection 실 적용 박제 (본문 11 D 정책 무변경) |
| mctrader-hub | `docs/adr/ADR-031-data-domain-decoupling.md` (Status Accepted) | MCT-182 §D1 + MCT-183 §D2 partial + MCT-184 §D3 partial | **§D2 VERIFIED amendment box** (cold-read cutover 완결) + **§D3 VERIFIED amendment box** (realtime stream + reverse-write wiring 완결). Status `Accepted` 유지 (POLICY_FINALIZED 전이 = MCT-188) |
| mctrader-hub | `docs/adr/ADR-030-docker-stack-governance.md` (Status POLICY_FINALIZED) | MCT-184 amendment box (data api service compose topology 예고) | cross-ref only (Redis Stream loopback 정합 재명시 — data 기존 redis 의존 재사용, 신규 service 추가 0. 본문 19 D 무변경) |
| mctrader-hub | `.codeforge/contracts/data-api-v1.openapi.json` (MCT-184 LAND) | OpenAPI 3.x schema snapshot (SSOT = data emit, hub = governance copy) | **소비** — engine `data_client/` 가 본 snapshot 소비 (generated 또는 hand-written = 설계 lane). `/v1/historical/orderbook` 신규 endpoint 추가 시 snapshot 갱신 (설계 lane 판단) |
| mctrader-hub | `scripts/cross-repo-contract-lock-check.sh` (MCT-184 LAND) | data OpenAPI emit ↔ hub snapshot drift gate | (변경 없음) — 본 Story 재사용 (engine data_client 생성 시 OpenAPI snapshot 정합 의무) |

### §4.1 AS-IS (engine→data python 결합 11곳 5파일 잔존 — spec §3 그래프 인용)

spec §3 그래프 + MCT-184 LAND 후 현 상태:

```
 mctrader-data (Layer2 — REST API LAND, 의도된 dead-in-data)
   src/mctrader_data/api/  (MCT-184 LAND, 6 파일, routes_v1.py 307 lines)
     ├ app.py           ── FastAPI app factory + lifespan + CORS internal-only
     ├ arrow_ipc.py     ── Arrow IPC serialize/deserialize
     ├ deps.py          ── TierReader/ColdReader/L1Reader singleton DI (None=dead-in-data)
     ├ routes_v1.py     ── /v1 historical (GET candles, candles/l1) + reverse-write
     │                     (POST paper-candles, backtest-artifact) — production caller 0
     └ schemas.py       ── Pydantic strict (T1 path traversal + T2 DoS bound)
   ✗ src/mctrader_data/api/realtime_stream.py  ── ABSENT (Redis Stream publisher 부재)
   src/mctrader_data/io/  (MCT-183 LAND, dead-in-data Layer2 자족)

 mctrader-engine (Layer2' 진입 전 — data python 직접 import 11곳 5파일 잔존)
   src/mctrader_engine/cli.py:279-280  (cold-read 2곳: resolve_data_root + scan_candles)
   src/mctrader_engine/executor/tick_replay.py:26,559  (cold-read 2곳: orderbook_replay)
   src/mctrader_engine/wfo/evaluator/data_loader.py:43-44  (cold-read 2곳)
   src/mctrader_engine/wfo/search/data_loader.py:81-82  (cold-read 2곳)
   src/mctrader_engine/runtime/paper_runner.py:290-291  (reverse-write 2곳: paper_lineage + paper_storage.write_paper_candles)
   src/mctrader_engine/backtest/nas_sync.py:36  (reverse-write 1곳: nas_uploader, lazy)
   ✗ src/mctrader_engine/data_client/  ── ABSENT (thin HTTP client 부재)

 본 Story 대상 = (a) data realtime_stream.py 신설 + (b) engine data_client/ 신설 +
   (c) cold-read 8곳 cutover + (d) reverse-write 3곳 cutover + (e) MCT-184 dead-in-data
   → production wiring 전환 + ADR-032 evidence triad 갱신
 비대상 = MCT-182 shim 5곳 (tick_storage/orderbook_storage/aggregation/paper_lineage shim
   re-export 동작 정상) → MCT-188 owner final cleanup + engine adapter 5곳 (D4) → MCT-186
   + 다중거래소 invariant (D5) → MCT-187 + grep0 quad gate (D7) → MCT-188
```

### §4.2 구현 가능성 평가 (FeasibilityAgent 관점)

| 차원 | 평가 | 근거 |
|------|------|------|
| data realtime_stream.py 신설 실현성 | **HIGH** | §0 V4: `src/mctrader_data/api/realtime_stream.py` ABSENT + redis[hiredis] 기존 가용 (신규 의존 0). Milestone-2 tick.v1.1 정규화 schema LAND (MCT-135~146) + ADR-030 §D15 Redis prefix `market:` namespace 정합. Redis Stream `XADD market:tick:{symbol}` publisher = data 측 단방향 (consumer = MCT-186 engine 또는 외부) |
| engine data_client/ 신설 실현성 | **MEDIUM-HIGH** | §0 V5: `src/mctrader_engine/data_client/` ABSENT + HTTP client 의존 신규 추가 필요 (설계 lane = `httpx>=0.27` 예상, OpenAPI generator vs hand-written 결정). MCT-184 OpenAPI SSOT (`.codeforge/contracts/data-api-v1.openapi.json` 307 lines 4 endpoint) 소비 — generated client 가능성 + idempotent sha256 client-side reuse (MCT-184 INV-3 + post-merge fix `e612296` F-2 정합) |
| cold-read 8곳 cutover 실현성 | **MEDIUM** | §0 V2: 8곳 4파일 정확 식별 (가설 ↔ HEAD 1:1 정합). MCT-184 routes_v1.py 4 endpoint 중 historical/candles + historical/candles/l1 LAND → cli.py:279-280 + wfo data_loader ×4 cutover 가능. **단 historical/orderbook endpoint = MCT-184 routes_v1.py 미LAND** (executor/tick_replay.py:26,559 cutover 대상) → 설계 lane 판단 = 본 Story 신규 endpoint 추가 vs MCT-184 amendment (R1 risk 추가 — Phase 2 PR1 data#N 에 endpoint 추가 의무) |
| reverse-write 3곳 cutover 실현성 | **MEDIUM-HIGH** | §0 V3: 3곳 2파일 정확 식별 (paper_runner:290/291 + backtest/nas_sync.py:36). MCT-184 routes_v1.py reverse-write 2 endpoint LAND (paper-candles + backtest-artifact) → 3곳 모두 cutover 가능. **paper_runner.py:290 paper_lineage = market-core 직독 변경 (MCT-182 LAND 후 shim 사용 중)** → 설계 lane = 본 Story cutover 포함 vs MCT-188 cleanup 분리 판단 |
| MCT-184 dead-in-data → production wiring 전환 실현성 | **HIGH** | MCT-184 AC-6 의도된 dead-in-data SSOT 박제 (`PMO-AUDIT-MCT-184 §3 패턴 #3` 선제 reapply 1회 실증). 본 Story = consumer=MCT-185 실 cutover → evidence triad 갱신 (file:line + caller grep ≥11 + integration test PASS). ADR-032 evidence triad 박제 의무 (선제 reapply 효력 실증 1회) |
| cross-repo 순서 위험 (R1) | **HIGH** | land_order(hub P1→data#N→engine#N→hub P2) — **3 repo + production wiring 전환 + ADR-029 §D2 VERIFIED amendment confirm = 가장 복잡 cross-repo 순서**. data 신설(realtime_stream + 가능 시 historical/orderbook endpoint 확장) 선행 → engine cutover 후행. Phase 0 verify 독립 게이트 (V1~V8) + D-row 1:1 reconcile + §3.6.1 gate v2 cross-Story reapply (lesson 누적) |
| R2 MCT-41 블락 위험 | **MEDIUM-HIGH** | engine `runtime/paper_runner.py:290-291` + `executor/tick_replay.py` 등 = MCT-41 realtime WS/주문 실행 경로 모듈 공유 가능. **Phase 0 verify (코드 작업 전) = MCT-43~47 IN_PROGRESS 변경 파일 ↔ 본 Story 11곳 5파일 교차 검증 의무**. scope_manifest §dependency.parallel_safe_with 가 MCT-186 owner 로 명시했으나 본 Story = production wiring 전환 시점이라 cross-cutting risk 확장 → Orchestrator ordering 결정 (공유 모듈 발견 시 land_order 재조정 or sub-Story 분리) |
| MCT-189 wiring drift 동형 차단 | **HIGH (AC-6 선제 reapply 효력 실증 시점)** | MCT-184 AC-6 의도된 dead-in-data → **본 Story AC-6 = 의도된 production wiring 전환 evidence triad 갱신** = ADR-032 evidence triad 선제 reapply 효력 1회 실증. PMO-AUDIT-MCT-184 §3 패턴 #3 — relocation/신규 신설 Story 안전 invariant 화 권고의 MCT-185 실 검증 시점 |

**Feasibility 종합: 구현 가능성 MEDIUM-HIGH (가장 복잡 Story)**. 실현성 HIGH 영역 = data
realtime_stream 신설 + MCT-184 dead-in-data → production wiring 전환 + MCT-189 wiring
drift 동형 차단. 실현성 MEDIUM 영역 = engine data_client/ 신설 (HTTP client 신규 의존
+ generator 결정) + cold-read 8곳 cutover (historical/orderbook endpoint 신설 의무) +
reverse-write 3곳 cutover (paper_lineage market-core 직독 변경 분리 판단). 주요 risk 축
3개 = (1) R1 (3 repo + production wiring 전환 가장 복잡, Phase 0 + D-row + §3.6.1 gate v2
3중 완화) + (2) R2 (MCT-41 cross-cutting Phase 0 교차 검증 의무) + (3) ADR-032 evidence
triad 갱신 (선제 reapply 효력 실증 시점). 설계 lane 인계 = HTTP client 의존 + generator
+ historical/orderbook endpoint 신설 + reverse-write 3곳 cutover 분리 + production wiring
SecurityArch + OperationalRiskArch 4 영역 확정.

### §4.3 이전 작업 연속성 분석 (ContinuityAgent — ADR-029/030/031/032 충돌·의존)

| 과거 ADR / 상태 | 본 Story 접촉 여부 | 충돌·의존 판정 |
|----------|--------------------|----------------|
| ADR-029 (tier-promotion-single-source, Status POLICY_FINALIZED — NAS SoT = data 단독 소유) | **접촉 (실 amendment confirm)** | **정합 (D2 VERIFIED 박제)** — MCT-183 amendment box "engine NAS 직독 폐기 예고" → **본 Story = LAND confirm** (engine cold-read 8곳 cutover 실 적용 + io reader 6 module = mctrader-data Layer2 + cold-read = data REST indirection). ADR-029 본문 11 D 정책 무변경 (POLICY_FINALIZED 보존), §D2 VERIFIED amendment box only (MCT-181 Status Amendment box 패턴 정합). presigned-NAS-handoff 기각 효력 실증 (engine NAS object layout/parquet tier/ETag/endpoint resolution 비인지 — REST 응답 Arrow IPC stream only) |
| ADR-031 (data-domain-decoupling, Status Accepted — MCT-182 §D1 + MCT-183 §D2 partial + MCT-184 §D3 partial) | **접촉 (D2+D3 VERIFIED 진전)** | **정합 (Status 유지, 진전)** — 본 Story = §D2 cold-read cutover 완결 + §D3 realtime stream + reverse-write wiring 완결 → §D2 VERIFIED + §D3 VERIFIED amendment box 박제. **Status 전이 `Accepted` → `POLICY_FINALIZED` = MCT-188 owner** (본 Story 범위 외 — D6 amendment 누적). scope_manifest D2/D3 owner_story 1:1 정합 |
| ADR-030 (docker-stack-governance, Status POLICY_FINALIZED — MCT-184 amendment box data api topology) | **cross-ref only** | **정합 (정책 무변경)** — Redis Stream loopback = ADR-030 single-host loopback 정합 (data 기존 `redis[hiredis]>=5` 재사용 + 신규 service 추가 0 — MCT-186 owner). 본문 19 D 무변경. MCT-184 amendment box 박제 data api service compose topology 의 Redis Stream publisher 추가 없음 (loopback 정합 cross-ref) |
| MCT-184 (Layer2 data REST API 신규, COMPLETED + post-merge fix `e612296` LAND) | **의존 (prerequisite + dead-in-data → production 전환)** | **정합 (선제 reapply 효력 실증)** — MCT-184 LAND = REST API 신설 + AC-6 의도된 dead-in-data SSOT 박제 (consumer=MCT-185 명시) + post-merge fix `e612296` (F-1/F-2/F-4 data correctness — silent data corruption + INV-3 mismatch + bytes-level 정밀도). **본 Story = consumer=MCT-185 실 cutover 시점** → AC-6 의도된 production wiring SSOT 박제 (evidence triad 갱신). post-merge fix LAND = 본 Story production wiring 안전 prerequisite (silent corruption 0 + idempotent 정확 + bytes-level 정확) |
| MCT-183 (Layer2 io/ relocation, COMPLETED) | **의존 (간접 prerequisite)** | **정합** — io/ 6 module = mctrader-data Layer2 LAND (dead-in-data) → MCT-184 routes_v1.py wrap → 본 Story production wiring 활성화 = io/ reader Layer2 자족 (engine 역의존 0) 실 검증 시점 |
| MCT-189 (ADR-029 §D3=C wiring 0건 drift, COMPLETED — Phase 2 PR2 LAND `a1a8ccf` 130GB cleanup) | **접촉 (동형 패턴 차단 대상 — 선제 reapply 효력 실증)** | **동형 차단 효력 실증 (AC-6 선제 reapply)** — MCT-189 = "ADR-029 §D3=C VERIFIED 박제 ↔ `promote_l1()` production caller 0건" wiring drift HIGH severity (130GB legacy 누적). MCT-184 AC-6 = "REST endpoint 박제 ↔ caller 0건" 동형 위험 → 선제 reapply (consumer=MCT-185 명시) → **본 Story = consumer=MCT-185 실 cutover 시점** = 선제 reapply 효력 1회 실증 (의도된 dead-in-data → 의도된 production wiring 전환). MCT-189 cleanup LAND = 본 Story 안전 prerequisite (legacy drift 0 baseline) |
| ADR-032 (PMO 발의 — VERIFIED badge evidence triad) | **선제 reapply 효력 실증 시점** | **선제 reapply 박제** — ADR-032 = "VERIFIED 박제 시 file:line + production caller grep ≥1 + integration test result 3종 evidence 의무" (PMO 발의, plugin upstream escalation 미LAND). MCT-184 = 선제 reapply (의도된 dead-in-data + consumer=MCT-185 박제) → **본 Story = production wiring 전환 evidence triad 갱신** = ADR-032 선제 reapply 효력 1회 실증 시점 (consumer 박제 → production wiring → evidence triad 전수 박제) |
| MCT-182 (Layer0 contract relocation, COMPLETED) | **의존 (간접 prerequisite + shim re-export)** | **정합 (간접)** — MCT-182 LAND = aggregation/records/paper_lineage market-core 이전. engine shim 5곳 re-export 동작 정상 (tick_storage/orderbook_storage/aggregation/paper_lineage). 본 Story = D2+D3 cutover only (shim cleanup = MCT-188 D7 grep0 quad gate owner 분리 SSOT). **paper_runner.py:290 paper_lineage = market-core 직독 변경 분리 판단 (설계 lane — 본 Story cutover 포함 vs MCT-188 cleanup)** |
| EPIC-MCT-41 Live Mode Debut (#56, MCT-43~47 IN_PROGRESS) | **R2 HIGH severity 위험점** | **Phase 0 verify (코드 작업 전) 의무** — 본 Story 11곳 5파일 (`cli.py:279-280` + `executor/tick_replay.py:26,31,32,559` + `wfo/evaluator/data_loader.py:43-44` + `wfo/search/data_loader.py:81-82` + `runtime/paper_runner.py:290-291` + `backtest/nas_sync.py:36`) ↔ MCT-43~47 IN_PROGRESS 변경 파일 교차 검증 의무. 공유 모듈 발견 시 (a) land_order 재조정 or (b) 공유 모듈 부분만 별 sub-Story 분리 = Orchestrator ordering 결정 (scope_manifest §dependency.parallel_safe_with MCT-186 명시였으나 본 Story = production wiring 전환 시점이라 cross-cutting risk 확장 — runtime/paper_runner.py + executor/tick_replay.py 가 live mode WS/주문 경로 핵심) |

**Continuity 종합**: MCT-185 는 과거 ADR(029/030/031/032)의 결정·invariant 와 **충돌 0** —
ADR-029 §D2 VERIFIED amendment confirm (정책 무변경) + ADR-031 §D2/§D3 VERIFIED 진전
(Status 유지) + ADR-030 Redis loopback cross-ref (정책 무변경) + ADR-032 선제 reapply 효력
실증 (evidence triad 갱신). 유일 주요 위험 = **R2 MCT-41 cross-cutting** (engine
runtime/paper_runner.py + executor/tick_replay.py 공유 모듈 위험) → Phase 0 verify (코드
작업 전) 의무 + Orchestrator ordering 결정. **MCT-189 wiring drift 동형 차단 = MCT-184
선제 reapply (의도된 dead-in-data) → 본 Story 의도된 production wiring 전환 = 효력 1회
실증 시점** (relocation/신규 신설 Story 안전 invariant 화 권고의 MCT-185 실 검증). 설계
lane 인계 = HTTP client 의존 + OpenAPI generator + historical/orderbook endpoint 신설
판단 + reverse-write 3곳 cutover 분리 판단 + R2 MCT-41 교차 검증 결과 반영 + ADR-032
evidence triad 갱신 SSOT.

### §4.4 DELTA (TO-BE — spec §4 4-Layer 인용 + scope_manifest cross_repo + AC-6 production wiring 전환)

```
 Layer 2 ─ mctrader-data (DATA-STORAGE 영역 단독 소유 — REST + realtime stream 완결)
   ◀ NEW (MCT-185):
     • src/mctrader_data/api/realtime_stream.py (신규 — Redis Stream publisher)
         ── tick.v1.1 정규화 patterns (Milestone-2 LAND SSOT)
         ── XADD market:tick:{symbol} (ADR-030 §D15 prefix 정합)
         ── data 기존 redis[hiredis]>=5 재사용 (신규 의존 0)
   상태: api/ 6 파일 (MCT-184 LAND) + realtime_stream.py 신설 = **production wiring 활성화**
         (routes_v1.py 4 endpoint production caller 0 → ≥11곳, MCT-184 AC-6 의도된
         dead-in-data → 본 Story AC-6 의도된 production wiring SSOT 박제)
 Layer 2'─ mctrader-engine (PURE CONSUMER — production 실 연결 완결)
   ◀ NEW (MCT-185):
     • src/mctrader_engine/data_client/ (신규 서브패키지 — namespace 설계 lane 확정)
         ── historical fetch client (MCT-184 OpenAPI SSOT 소비)
         ── reverse-write client (post_paper_candles + post_backtest_artifact)
         ── (realtime consumer = MCT-186 owner)
   ◀ MODIFY (MCT-185 — D2 cold-read cutover 8곳 4파일):
     • cli.py:279-280 → data_client.historical.fetch_candles + resolve_data_root
     • executor/tick_replay.py:26,559 → data_client.historical.fetch_orderbook
     • wfo/evaluator/data_loader.py:43-44 → data_client.historical
     • wfo/search/data_loader.py:81-82 → data_client.historical
   ◀ MODIFY (MCT-185 — D3 reverse-write cutover 3곳 2파일):
     • runtime/paper_runner.py:290-291 → data_client.reverse_write.post_paper_candles
         (+ paper_lineage market-core 직독 변경 = 설계 lane 판단 분리)
     • backtest/nas_sync.py:36 → data_client.reverse_write.post_backtest_artifact
   ◀ MODIFY (MCT-185 — pyproject HTTP client 의존):
     • pyproject.toml ← httpx>=0.27 추가 (설계 lane 확정)
     • (mctrader-data @ git+ 의존 잔존 = MCT-188 D7 final 제거 owner)
   상태: engine src/ `from mctrader_data.{storage,path,orderbook_replay,paper_storage,
         nas_storage}` import = 0건 (AC-3 grep0 — D2 cold-read 8곳 + D3 reverse-write
         3곳 모두 cutover). MCT-182 shim 5곳 잔존 = MCT-188 owner 분리 (본 Story 비대상)
 ADR governance (mctrader-hub):
   • ADR-029 §D2 VERIFIED amendment box ← engine NAS 직독 폐기 LAND confirm + cold-read
     8곳 REST indirection 실 적용 (POLICY_FINALIZED 본문 무변경)
   • ADR-031 §D2 VERIFIED amendment box ← cold-read cutover 완결 (Status Accepted 유지)
   • ADR-031 §D3 VERIFIED amendment box ← realtime stream + reverse-write wiring 완결
   • ADR-030 cross-ref ← Redis Stream loopback 정합 재명시 (정책 무변경)
   • .codeforge/contracts/data-api-v1.openapi.json 갱신 (historical/orderbook endpoint
     추가 시 — 설계 lane 판단)
 순환: 영원히 없음 (engine→data REST/stream only, mctrader_data import 0건 grep AC-3
   충족. data_client/ 가 mctrader-data import 안 함)
 AC-6 wiring evidence triad (MCT-184 dead-in-data → MCT-185 production wiring 전환):
   • file:line: engine data_client/ production caller cutover 카운터 ≥11곳
   • caller grep ≥11곳 (cold-read 8 + reverse-write 3) production wiring 활성화
   • integration test PASS: engine→data REST round-trip smoke test (설계 lane SSOT)
```

| repo | 변경 유형 | 파일 | 내용 |
|------|-----------|------|------|
| mctrader-hub | CREATE | `docs/stories/MCT-185.md` | 본 Story §0-§11 |
| mctrader-hub | MODIFY | `docs/adr/ADR-029-tier-promotion-single-source.md` | §D2 VERIFIED amendment box — engine NAS 직독 폐기 LAND confirm + cold-read 8곳 REST indirection 실 적용 (본문 11 D 정책 무변경, POLICY_FINALIZED 보존) |
| mctrader-hub | MODIFY | `docs/adr/ADR-031-data-domain-decoupling.md` | §D2 VERIFIED amendment box (cold-read cutover 완결) + §D3 VERIFIED amendment box (realtime stream + reverse-write wiring 완결) — Status `Accepted` 유지 |
| mctrader-hub | MODIFY | `docs/adr/ADR-030-docker-stack-governance.md` | cross-ref only (Redis Stream loopback 정합 재명시, 본문 19 D 무변경 — 정책 변경 없음) |
| mctrader-hub | MODIFY (조건) | `.codeforge/contracts/data-api-v1.openapi.json` | historical/orderbook endpoint 신설 시 snapshot 갱신 (설계 lane 판단 — 본 Story 신규 endpoint 추가 vs MCT-184 amendment) |
| mctrader-hub | MODIFY | `scope_manifests/EPIC-data-domain-decoupling.yaml` | MCT-185 status RESERVED→IN_PROGRESS→COMPLETED + D2 + D3 D-row reconcile + milestone progress 3/7→4/7 + epic_status_history 추가 |
| mctrader-hub | MODIFY | `CLAUDE.md` | `## EPIC-data-domain-decoupling` MCT-185 진행 박제 |
| mctrader-hub | MODIFY | `.codeforge/counters.json` | MCT-185 RESERVED→IN_PROGRESS→COMPLETED |
| mctrader-data | CREATE | `src/mctrader_data/api/realtime_stream.py` | Redis Stream 정규화 publisher (tick.v1.1 패턴, ADR-030 §D15 `market:` prefix. data 기존 redis[hiredis] 재사용 — 신규 의존 0) |
| mctrader-data | MODIFY (조건) | `src/mctrader_data/api/routes_v1.py` | historical/orderbook endpoint 신설 (설계 lane 판단) + OpenAPI emit 갱신 |
| mctrader-engine | CREATE | `src/mctrader_engine/data_client/` (신규 서브패키지 — namespace 설계 lane 확정) | thin HTTP client (MCT-184 OpenAPI SSOT 소비) — historical fetch + reverse-write client. realtime consumer = MCT-186 owner |
| mctrader-engine | MODIFY | `src/mctrader_engine/cli.py:279-280` | cold-read 2곳 → `data_client.historical` REST 경유 cutover (D2) |
| mctrader-engine | MODIFY | `src/mctrader_engine/executor/tick_replay.py:26,559` | cold-read 2곳 (top-level + function-local) → `data_client.historical.fetch_orderbook` cutover (D2) |
| mctrader-engine | MODIFY | `src/mctrader_engine/wfo/evaluator/data_loader.py:43-44` + `wfo/search/data_loader.py:81-82` | cold-read 4곳 → `data_client.historical` cutover (D2) |
| mctrader-engine | MODIFY | `src/mctrader_engine/runtime/paper_runner.py:290-291` | reverse-write 2곳 → `data_client.reverse_write.post_paper_candles` cutover (D3). paper_lineage market-core 직독 변경 = 설계 lane 분리 판단 |
| mctrader-engine | MODIFY | `src/mctrader_engine/backtest/nas_sync.py:36` | reverse-write 1곳 (lazy/type-ignore) → `data_client.reverse_write.post_backtest_artifact` cutover (D3) |
| mctrader-engine | MODIFY | `src/mctrader_engine/pyproject.toml` | HTTP client 의존 추가 (`httpx>=0.27` 예상 — 설계 lane 확정). mctrader-data 의존 line 유지 (MCT-188 final 제거 owner) |
| mctrader-hub | MODIFY (Phase 2 PR2) | Story §9/§10/§11 + ADR-029/031 amendment box VERIFIED + scope_manifest milestone 4/7 + CLAUDE.md COMPLETED + RETRO + EPIC-RESULTS §Story-4 + 박제 PR 5 체크리스트 self-discipline | 박제 |

**land_order**: `hub Phase1(docs — Story + ADR-029/031 amendment box + ADR-030 cross-ref +
가능 시 OpenAPI snapshot 갱신 + scope_manifest + CLAUDE.md) → data#N(land_order 1 —
realtime_stream.py 신설 + 가능 시 historical/orderbook endpoint 신설 + OpenAPI emit) →
engine#N(land_order 2 — data_client/ 신설 + cold-read 8곳 cutover + reverse-write 3곳
cutover + pyproject httpx 추가 + AC-6 wiring evidence triad 박제) → hub Phase2 PR2(박제 +
박제 PR 5 체크리스트 self-discipline + Codex post-LAND audit 4 axis self-discipline)`.
각 PR CI green 후 admin merge → 다음 PR. **engine 참여 (3 repo) = MCT-184 (2 repo) 대비
가장 복잡 cross-repo 순서**. data#N 의 realtime_stream.py + (가능 시) historical/orderbook
endpoint 신설 = engine#N data_client/ 신설 prerequisite (OpenAPI SSOT 단방향 강제). R2
(MCT-41 cross-cutting) = **engine#N 진입 전 MCT-43~47 IN_PROGRESS 파일 ↔ 본 Story 11곳
5파일 교차 검증 의무** (scope_manifest §dependency.parallel_safe_with 재평가 — Orchestrator
ordering 결정 책임).

## §5 Acceptance Criteria

> 요구사항 lane self-write. spec §1 동기 4종 + scope_manifest
> `story_decision_matrix.MCT-185` (D2+D3) 충족 기준. Given/When/Then.

### AC-1 — data realtime stream publisher 신설 + tick.v1.1 정규화 schema 정합 (D3)

- **Given** mctrader-data `src/mctrader_data/api/realtime_stream.py` 신규 + 기존
  `redis[hiredis]>=5` 의존 (§0 V4) + Milestone-2 tick.v1.1 정규화 schema (MCT-135~146 LAND)
- **When** data 측에서 realtime stream publisher 기동 → tick.v1.1 정규화 schema 의
  TickRowV1_1 (market-core contract) → Redis Stream `XADD market:{...}:tick:{symbol}`
  publish (ADR-030 §D15 prefix 정합, 정확한 stream key naming = 설계 lane 확정)
- **Then**
  - `realtime_stream.py` 신규 import 성공 + Redis Stream publisher class/function 노출
    (settings/config = 설계 lane 확정)
  - tick.v1.1 정규화 schema 정합 (TickRowV1_1 market-core contract 소비, MCT-135~146 LAND
    SSOT 정합. 거래소별 raw → 정규화 변환 = data 단독 책임)
  - Redis Stream `XADD` 호출 정상 + stream key prefix = ADR-030 §D15 `market:` namespace
    정합 (consumer = MCT-186 engine 또는 외부 가 `XREAD` 구독)
  - ADR-030 single-host loopback 정합 (compose internal network only, 외부 노출 0 — data
    기존 redis 의존 재사용, 신규 service 추가 0)
  - data full suite 회귀 0 (INV-5 — realtime_stream.py 신규 추가가 기존 storage/io/api/
    compactor 무영향)

### AC-2 — engine thin HTTP client (`data_client/`) 신설 + MCT-184 OpenAPI SSOT 소비 (D3)

- **Given** mctrader-engine `src/mctrader_engine/data_client/` 신규 서브패키지 + MCT-184
  OpenAPI SSOT (`hub:.codeforge/contracts/data-api-v1.openapi.json`) + 신규 HTTP client
  의존 (`httpx>=0.27` 예상 — 설계 lane)
- **When** engine 측에서 `from mctrader_engine.data_client import HistoricalClient,
  ReverseWriteClient` (또는 설계 lane 확정 API) import + MCT-184 routes_v1.py 4 endpoint
  소비 (historical/candles + historical/candles/l1 + reverse-write/paper-candles +
  reverse-write/backtest-artifact + 가능 시 historical/orderbook)
- **Then**
  - `data_client/` 신규 import 성공 + thin HTTP client (`httpx`) 기반 — MCT-184 OpenAPI
    SSOT 소비 (generated 또는 hand-written = 설계 lane 확정)
  - OpenAPI SSOT 단방향 강제 — engine 측 OpenAPI/schema 정의 0 (INV-1 MCT-184 carry over,
    generated client only 또는 hand-written 시 SSOT 정합 검증)
  - data_client/ 가 `mctrader_data` python import 안 함 (HTTP REST only — engine pyproject
    `mctrader-data @ git+` 의존 line 잔존이나 src grep 0건 — MCT-188 D7 final 제거 owner)
  - idempotent sha256 client-side 정합 (MCT-184 INV-3 + post-merge fix `e612296` F-2
    canonical_sha256 fix 정합 — reverse-write client 가 동일 sha256 hash key 적용)
  - SecurityArch §7.1 internal-only network 격리 정합 (engine↔data compose internal,
    외부 노출 0) + Pydantic strict input validation MCT-184 routes_v1.py 패턴 정합

### AC-3 — engine cold-read 8곳 cutover (`from mctrader_data.{storage,path,orderbook_replay}` grep0) + reverse-write 3곳 cutover (D2 + D3)

- **Given** engine cold-read 8곳 4파일 (§0 V2: `cli.py:279-280` + `executor/tick_replay.py:26,559`
  + `wfo/evaluator/data_loader.py:43-44` + `wfo/search/data_loader.py:81-82`) + reverse-write
  3곳 2파일 (§0 V3: `runtime/paper_runner.py:290-291` + `backtest/nas_sync.py:36`) =
  **11곳 5파일** 정확 식별
- **When** engine 측에서 11곳 모두 `data_client/` REST 경유 cutover → engine src/ `from
  mctrader_data.(storage|path|orderbook_replay|paper_storage|nas_storage)` grep + `from
  mctrader_data.paper_storage import write_paper_candles` grep + `from mctrader_data.
  nas_storage.nas_uploader import` grep 실행
- **Then**
  - engine src/ `from mctrader_data.(storage|path|orderbook_replay)` import = **0건**
    (cold-read 8곳 cutover 완결 — D2 grep0)
  - engine src/ `from mctrader_data.paper_storage import write_paper_candles` import =
    **0건** (reverse-write paper-candles cutover 완결 — D3 grep0 1/2)
  - engine src/ `from mctrader_data.nas_storage.nas_uploader import` import = **0건**
    (reverse-write backtest-artifact cutover 완결 — D3 grep0 2/2)
  - MCT-182 shim 잔존 5곳 (tick_storage/orderbook_storage/aggregation/paper_lineage)
    grep — **본 Story 비대상 명시 SSOT 박제** (MCT-188 D7 grep0 quad gate final cleanup
    owner. 본 Story = D2+D3 cutover only — shim 동작 정상)
  - engine 측 production behavior 회귀 0 (cli.py/wfo/executor/paper_runner/backtest
    동작 cutover 전과 동등 — INV-5 cross-repo 회귀 0)
  - **paper_runner.py:290 paper_lineage = market-core 직독 변경 분리 판단** = 설계 lane
    확정 (본 Story cutover 포함 vs MCT-188 cleanup 분리. 본 Story 포함 시 `from
    mctrader_market.paper_lineage import PaperLineage, canonical_jsonl_hash` 변경)

### AC-4 — cutover byte-equivalence 회귀 0 (engine backtest/WFO 출력 동일) (D2)

- **Given** engine cold-read 8곳 cutover 전 backtest/WFO 출력 baseline (cli.py + executor +
  wfo/evaluator + wfo/search 경로) + cutover 후 동일 input 동일 output 비교
- **When** engine cutover 후 동일 backtest/WFO scenario 실행 (TestContractArch baseline
  scenario — 설계 lane 확정) → 결과 비교
- **Then**
  - cutover 후 engine backtest/WFO 출력 == cutover 전 출력 **byte-equivalence** (REST
    wrap 이 데이터 변형 0 — MCT-184 INV-2 Arrow IPC byte-equivalence 정합. cutover =
    wrap-only, 로직 변경 0)
  - data REST endpoint 응답 Arrow IPC stream → engine data_client deserialize → engine
    측 candle/orderbook 로직 동일 (TickRecord/OrderbookEventRecord/CandleModel = market-core
    contract MCT-182 LAND 정합)
  - Perf Baseline (TestContractArch — REST latency + throughput baseline 박제, MCT-184
    Perf Baseline 1000-row serialize p50 < 5ms 정합. 본 Story = cutover 추가 latency
    측정 — production 영향 detection)
  - **reverse-write 3곳 cutover byte-equivalence** = paper-candles + backtest-artifact
    REST 응답 = 직접 호출 결과 동등 (MCT-184 AC-3 + INV-3 idempotent 정합)

### AC-5 — MCT-184 dead-in-data → production wiring 전환 + ADR-029 §D2 VERIFIED + ADR-031 §D2/§D3 VERIFIED amendment box + D-row 1:1 reconcile (§3.6.1 gate v2 reapply) (D6)

- **Given** `docs/adr/ADR-029-tier-promotion-single-source.md` §D2 VERIFIED amendment box +
  `docs/adr/ADR-031-data-domain-decoupling.md` §D2/§D3 VERIFIED amendment box +
  `docs/adr/ADR-030-docker-stack-governance.md` Redis Stream loopback cross-ref 작성
- **When** ADR-029/031/030 amendment box 와 `scope_manifests/EPIC-data-domain-decoupling.yaml`
  `§design_decisions.D2/D3` + `§planned_adrs.amendments` ADR-029 +
  `§story_decision_matrix.MCT-185` 를 §3.6.1 gate v2 (glob-scope + 변형포괄 + self-verify
  TEST1/TEST2) 로 대조
- **Then**
  - ADR-029 §D2 VERIFIED amendment box = "engine NAS 직독 폐기 LAND confirm + io reader
    6 module = mctrader-data Layer2 + cold-read 8곳 = data REST indirection 실 적용
    (presigned-NAS-handoff 기각 효력 실증)" 박제 — ADR-029 **본문 11 D 정책 무변경**
    (POLICY_FINALIZED 보존, MCT-181 LAND Status Amendment box 패턴 정합)
  - ADR-031 §D2 VERIFIED amendment box = "cold-read cutover 완결 — engine src/ `from
    mctrader_data.(storage|path|orderbook_replay)` 0건 grep + cold-read 8곳 cutover 박제"
    박제
  - ADR-031 §D3 VERIFIED amendment box = "realtime stream + reverse-write wiring 완결 —
    realtime_stream.py LAND + engine data_client/ LAND + reverse-write 3곳 cutover 박제"
    박제 — ADR-031 Status `Accepted` 유지 (POLICY_FINALIZED 전이 = MCT-188)
  - ADR-030 cross-ref = "Redis Stream loopback 정합 재명시" 박제 (정책 무변경, 본문 19 D
    무변경)
  - scope_manifest `§planned_adrs.amendments` ADR-029 owner ↔ ADR-029 §D2 amendment box ↔
    `§design_decisions.D2/D3` + `§story_decision_matrix.MCT-185` ↔ 본 Story §2/§4 DELTA
    **전수 1:1 정합** (MCT-179/182~184 lesson reapply — Out-of-scope stale 사전 차단)
  - **§3.6.1 gate v2 cross-Story reapply 실행 evidence** — glob-scope (`docs/adr/
    ADR-0*.md docs/stories/MCT-18*.md docs/change-plans/MCT-18*.md scope_manifests/
    EPIC-data-domain-decoupling.yaml .codeforge/contracts/*.json`) + 변형포괄 pattern +
    self-verify TEST1(포착력)/TEST2(false positive 0) + repo-wide post-LAND grep 0줄
    evidence 박제 (설계 lane Change Plan §3.6.1 SSOT — plugin-codeforge#795 mechanical
    gate 미가용, self-discipline 유지)

### AC-6 — dead-in-data → production wiring evidence triad 갱신 (MCT-189 wiring drift 동형 차단 — 선제 reapply 효력 실증) (D2 + D3)

- **Given** MCT-184 routes_v1.py 4 endpoint (의도된 dead-in-data SSOT 박제, `consumer=MCT-185
  (engine data_client REST 경유 cold-read cutover)`) + 본 Story = consumer=MCT-185 실
  cutover 시점
- **When** engine data_client/ production caller cutover 후 ADR-032 evidence triad
  (file:line + caller grep ≥1 + integration test result) 로 wiring 검증
- **Then**
  - **file:line evidence**: engine `data_client/` production caller cutover 카운터 박제
    (cold-read 8곳 file:line + reverse-write 3곳 file:line + data_client/ wrapper file:line)
  - **caller grep evidence**: engine src/ `from mctrader_engine.data_client` import grep
    **≥11곳** (cold-read 8 + reverse-write 3 = 본 Story cutover scope. MCT-184 production
    caller 0 → ≥11 증가 — 의도된 production wiring 활성화 SSOT)
  - **integration test result**: engine→data REST round-trip smoke test PASS (data api
    LAND + engine data_client cutover → 실 production wiring 검증 — testcontainers 또는
    동등 패턴, 설계 lane SSOT)
  - **MCT-189 wiring drift 동형 차단 효력 실증** — MCT-184 AC-6 의도된 dead-in-data SSOT
    → 본 Story AC-6 의도된 production wiring SSOT 박제 = **ADR-032 evidence triad 선제
    reapply 효력 1회 실증** (PMO-AUDIT-MCT-184 §3 패턴 #3 — relocation/신규 신설 Story
    안전 invariant 화 권고의 MCT-185 실 검증 시점)
  - Phase 2 PR2 박제 시 ADR-032 evidence triad 박제 완결 (file:line + caller grep + test
    result 3종 SSOT) — MCT-184 AC-6 carry-over registry consumer=MCT-185 명시 해소

## §6 Invariants

> 요구사항 lane self-write. 본 Story 구현 중 위반 시 즉시 중단.

| INV | 내용 | 출처 | 검증 방법 |
|-----|------|------|----------|
| INV-1 | engine src/ `from mctrader_data.(storage\|path\|orderbook_replay\|paper_storage\|nas_storage)` import == **0** (cold-read 8곳 + reverse-write 3곳 cutover 완결) — 영구 (cutover 후 회귀 0) | D2 + D3 grep0 (본 Story 핵심) | engine src/ post-cutover grep == 0건 (cold-read storage/path/orderbook_replay + reverse-write paper_storage.write_paper_candles + nas_storage.nas_uploader 전수). MCT-182 shim 5곳 (tick_storage/orderbook_storage/aggregation/paper_lineage) = 본 INV 비대상 (MCT-188 D7 grep0 quad gate owner — 분리 SSOT) |
| INV-2 | cutover byte-equivalence — engine backtest/WFO 출력 (cli.py + executor + wfo/evaluator + wfo/search) cutover 후 == cutover 전 (REST wrap 데이터 변형 0). reverse-write 3곳 (paper-candles + backtest-artifact) REST 응답 = 직접 호출 결과 동등 | D2 cold-read cutover + D3 reverse-write cutover (cutover ≠ 로직 변경) | engine backtest/WFO baseline scenario (TestContractArch — 설계 lane SSOT) cutover 전/후 출력 byte-equivalence. Arrow IPC stream deserialize → TickRecord/OrderbookEventRecord/CandleModel (market-core MCT-182 LAND) 정합 + Perf Baseline (cutover latency 측정 production 영향 detection) |
| INV-3 | realtime stream tick.v1.1 정규화 schema 정합 — Redis Stream `XADD market:{...}:tick:{symbol}` payload = TickRowV1_1 market-core contract SSOT (Milestone-2 LAND 정합) | D3 realtime stream (tick.v1.1 패턴 — MCT-135~146) | `realtime_stream.py` publisher payload schema 검증 — TickRowV1_1 (market-core) 정합 + ADR-030 §D15 prefix `market:` namespace 정합 + 거래소별 raw → 정규화 변환 정확성 (data 단독 책임) |
| INV-4 | §3.6.1 gate v2 cross-document SSOT desync 0 — Story §본문 ↔ scope_manifest §D2/D3/§story_decision_matrix.MCT-185/§planned_adrs ↔ ADR-029 §D2 amendment box ↔ ADR-031 §D2/§D3 amendment box ↔ ADR-030 cross-ref ↔ .codeforge/contracts snapshot 전수 1:1 | MCT-179/182~184 lesson reapply + PMO-AUDIT-MCT-184 §4 + §6.2 reapply #3 | §3.6.1 gate v2 (glob-scope `docs/adr/ADR-0*.md docs/stories/MCT-18*.md docs/change-plans/MCT-18*.md scope_manifests/EPIC-data-domain-decoupling.yaml .codeforge/contracts/*.json` + 변형포괄 pattern + self-verify TEST1/TEST2) post-LAND repo-wide grep 0줄 (rc=1) evidence (설계 lane Change Plan §3.6.1 SSOT — plugin-codeforge#795 mechanical gate 미가용) |
| INV-5 | 회귀 0 (cross-repo) — data realtime_stream.py 신설 + httpx 신규 의존 추가가 data full suite + engine full suite 무영향. engine cutover 11곳이 production behavior 동등 (INV-2 byte-equivalence 정합) | D2 + D3 (신규 의존 도입 + cutover) | data#N 후 data full suite + engine#N 후 engine full suite 회귀 신규 실패 0. httpx 신규 의존이 engine 기존 로직 무변경 (pyproject 의존 추가 ≠ 기존 로직 변경. data realtime_stream.py 신규 추가 = 기존 api/io/storage/compactor 무영향) |
| INV-6 | AC-6 wiring evidence triad 영구 박제 (MCT-189 동형 차단 effect 실증) — engine data_client production caller cutover 카운터 ≥11 + caller grep ≥11 + integration test PASS + ADR-032 evidence triad 3종 SSOT 박제 | §4.3 Continuity (MCT-189 동형) / PMO-AUDIT-MCT-184 §6.2 #6 / ADR-032 선제 reapply 효력 실증 | engine src/ `from mctrader_engine.data_client` import grep ≥11곳 (cutover scope 11곳 전수) + integration test (engine→data REST round-trip smoke testcontainers PASS) + file:line evidence (cold-read 8 + reverse-write 3 cutover 카운터). MCT-184 AC-6 의도된 dead-in-data → 본 Story AC-6 의도된 production wiring 전환 = 선제 reapply 효력 실증 1회 SSOT |

## §7 Dependencies / 설계 결정 상세

> **설계 lane (ArchitectPLAgent) TBD** — 본 Story 진입 시 Change Plan
> `docs/change-plans/MCT-185-change-plan.md` §1-§12 박제 예정. 본 §7 = Dependencies
> (§7.1) + 설계 lane placeholder (§7.2 = TBD).

### §7.1 Dependencies

| 의존 | 상태 | 내용 |
|------|------|------|
| MCT-184 (Layer 2 data REST API 신규 historical+reverse-write) | **COMPLETED 2026-05-16 + post-merge fix `e612296` LAND ✅** | data api/ 6 파일 LAND (routes_v1.py 307 lines 4 endpoint) + OpenAPI SSOT (hub `.codeforge/contracts/data-api-v1.openapi.json`) + cross-repo-contract-lock-check.sh + post-merge fix F-1/F-2/F-4 (silent data corruption + INV-3 mismatch + bytes-level 정밀도 차단 LAND). **본 Story production wiring 안전 prerequisite 충족** |
| MCT-183 (Layer 2 io/ relocation) | COMPLETED 2026-05-16 | io/ 6 module = mctrader-data Layer2 LAND (dead-in-data) → MCT-184 routes_v1.py wrap → 본 Story production wiring 활성화 = io/ reader Layer2 자족 실 검증 시점 |
| MCT-182 (Layer0 contract relocation) | COMPLETED 2026-05-15 | aggregation/records/paper_lineage market-core LAND. engine shim 5곳 잔존 = MCT-188 owner. paper_runner.py:290 paper_lineage market-core 직독 변경 = 본 Story 설계 lane 분리 판단 (cutover 포함 vs MCT-188 cleanup 분리) |
| MCT-189 (ADR-029 §D3=C wiring drift 130GB cleanup) | **COMPLETED 2026-05-17 (`a1a8ccf` Phase 2 PR2 LAND)** | legacy 130GB drift 0 baseline 확보 = 본 Story production wiring 안전 prerequisite (wiring drift 동형 차단 baseline). MCT-184 AC-6 선제 reapply (의도된 dead-in-data) → 본 Story AC-6 의도된 production wiring SSOT 박제 효력 실증 시점 |
| EPIC-data-domain-decoupling brainstorm | COMPLETED 2026-05-16 | spec/scope_manifest 2종 SSOT 확정 (Phase 0 deep-verify + Codex review 7 D) |
| ADR-031 (data-domain-decoupling, Status Accepted) | MCT-182 §D1 + MCT-183 §D2 partial + MCT-184 §D3 partial | 본 Story = §D2 cold-read cutover 완결 + §D3 realtime stream + reverse-write wiring 완결 → §D2 VERIFIED + §D3 VERIFIED amendment box 박제 (Status 유지, 전이 = MCT-188) |
| ADR-029 (tier-promotion-single-source, Status POLICY_FINALIZED) | MCT-183 amendment box "engine NAS 직독 폐기 예고" | 본 Story = §D2 VERIFIED amendment confirm (engine NAS 직독 폐기 LAND + cold-read 8곳 REST indirection 실 적용 박제. 본문 11 D 정책 무변경) |
| ADR-030 (docker-stack-governance, Status POLICY_FINALIZED) | MCT-184 amendment box (data api topology) | cross-ref only (Redis Stream loopback 정합 재명시 — data 기존 redis 의존 재사용, 신규 service 추가 0. 본문 19 D 무변경) |
| ADR-032 (PMO 발의 — VERIFIED badge evidence triad) | 발의 (plugin upstream escalation 미LAND) | 본 Story 선제 reapply 효력 실증 시점 (MCT-184 AC-6 의도된 dead-in-data → 본 Story AC-6 의도된 production wiring 전환) |
| MCT-184 post-merge fix `e612296` LAND (F-1/F-2/F-4 data correctness) | **COMPLETED 2026-05-16 ✅** | silent data corruption + INV-3 canonical_sha256 mismatch + bytes-level 정밀도 차단 LAND = 본 Story production wiring 안전 prerequisite. PMO-AUDIT-MCT-184 §6.1 carry over #2 (cutover 진입 prerequisite gate) 충족 |
| engine HTTP client 의존 (`httpx>=0.27` 예상) + Redis Stream consumer (MCT-186 owner) | 설계 lane 확정 (TBD) | engine pyproject httpx 추가 (Change Plan §3 SSOT). Redis Stream subscriber = MCT-186 owner (본 Story 비대상) |
| 선행 Story | MCT-184 (`sequential_depends_on: [MCT-184]`) | sequential_phase 3 LAND prerequisite 충족 |
| 후행 Story | MCT-186 (engine realtime cutover + adapter 제거 — engine ws_wrapper/stream_consumer/fill/mock_stream/paper_runner realtime WS 부분, D4) + MCT-187 (다중거래소 invariant, D5) + MCT-188 (data-free grep0 quad gate + POLICY_FINALIZED, D7) | sequential_phase 5/6/7 |
| plugin-codeforge#795 (cross-document SSOT mechanical gate) + plugin-codeforge#804 (박제 PR 완결도 mechanical gate) + plugin-codeforge#805 (post-merge audit lane) | **모두 OPEN (미가용)** | 3 mechanical gate plugin 미가용 → §3.6.1 gate v2 cross-Story reapply + 박제 PR 5 체크리스트 + Codex post-LAND audit 4 axis = 3중 self-discipline 의무 (PMO-AUDIT-MCT-184 §6.2 reapply 8 항목) |
| EPIC-MCT-41 Live Mode Debut (MCT-43~47 IN_PROGRESS) | **R2 HIGH severity 위험점** | Phase 0 verify (코드 작업 전) 의무 = MCT-43~47 IN_PROGRESS 변경 파일 ↔ 본 Story 11곳 5파일 (engine runtime/paper_runner.py + executor/tick_replay.py + cli.py + wfo + backtest) 교차 검증 의무. 공유 모듈 발견 시 Orchestrator ordering 결정 (land_order 재조정 or sub-Story 분리) |

### §7.2 설계 lane 확정 사항 (ArchitectPLAgent LAND 2026-05-17 — 인계 항목 10건 해소)

> **설계 lane SSOT (Change Plan §3 — chief author + 6 deputy + 2 CONDITIONAL deputy
> synthesis)**. 요구사항 lane 인계 10 항목 → Change Plan §3/§7/§11 결정 박제 완료
> (cross-ref 형식):
>
> 1. **Phase 0 V1-V6 재대조** → Change Plan §8.0 Phase 0 Gate 명문화 (V1 `git fetch origin`
>    3 repo + V2 cold-read 8곳 grep + V3 전체 14곳 grep + V4/V5 신설 부재 재대조 + V6
>    MCT-184 routes_v1.py LAND surface 372 lines + historical/orderbook endpoint 부재 확인)
> 2. **data realtime_stream.py namespace 배치** → `src/mctrader_data/api/realtime_stream.py`
>    + Redis Stream `XADD market:tick:{exchange}:{symbol}` key naming + `REDIS_KEY_PREFIX_MARKET`
>    env 도입 (default `market`, ADR-030 §D15 prefix 정합) + tick.v1.1 정규화 schema = market-core
>    `mctrader_market.schemas.tick.TickRowV1_1` SSOT 재사용 (Layer 0 contract)
> 3. **engine data_client/ namespace 배치 + generator vs hand-written 결정** → `src/mctrader_engine/data_client/`
>    `base.py + historical.py + reverse_write.py + exceptions.py + __init__.py` 5 파일. **hand-written
>    thin client 채택** (근거: 4+2 = 6 endpoint 소규모 surface + Pydantic schema = market-core SSOT
>    재사용 = SSOT 단일 정합 + OpenAPI 단방향 자연 성립)
> 4. **historical/orderbook endpoint MCT-184 routes_v1.py 신설 누락 carry over 판단** → **본 Story
>    포함 채택** (MCT-184 amendment 분리 시 lock-step 의존 증가 차단). `/v1/historical/orderbook/snapshots`
>    + `/v1/historical/orderbook/ticks` 2 endpoint 신설 + OpenAPI snapshot 갱신
> 5. **paper_runner.py:290 paper_lineage market-core 직독 변경 분리 판단** → **본 Story cutover 포함
>    채택** (D7 grep0 quad gate MCT-188 부담 감소 + 동일 lazy import 블록 재방문 비용 차단). MCT-188
>    shim 잔존 5곳 → 4곳 축소 (`executor/tick_replay.py:31,32` + `hot/state_machine.py:33` +
>    `strategy/templates/tick_scalping.py:76`)
> 6. **engine pyproject.toml httpx>=0.27 의존 추가** → 신규 의존 추가 (sync httpx Client — engine
>    기존 동기 호출 패턴 정합). mctrader-data @ git+ 의존 line 유지 (MCT-188 final 제거 owner)
> 7. **AC-6 wiring evidence triad 박제 (testcontainers)** → engine `from mctrader_engine.data_client`
>    import grep ≥11곳 + integration testcontainers (engine→data REST round-trip smoke PASS) +
>    file:line evidence (cold-read 8 + reverse-write 3 cutover 카운터) — ADR-032 evidence triad
>    선제 reapply 효력 1회 실증
> 8. **R2 MCT-41 교차 검증 결과 반영** → Phase 0 V8 cross-check 결과 engine repo MCT-43~47 활성
>    branch + commit = **0건 발견** (baseline confirm) → R2 severity MEDIUM-HIGH 유지 + engine#N
>    진입 직전 재cross-check 의무 (Orchestrator ordering 결정 carry)
> 9. **SecurityArch primary (강함)** → §7.1-§7.7 전수 박제 (T1-T7 위협 매핑 + internal-only 격리
>    + Pydantic strict + canonical sha256 client-side + market-core SSOT 재사용 + MCT-184 F-2
>    bytes-level 정밀도 lesson reapply 강화)
> 10. **OperationalRiskArch primary (CONDITIONAL §8.5 active=true)** → §7.4 7+1 항목 전수 박제
>     (§8.5 4 조건 4/4 Y → active=true ArchitectPL 결정 verbatim. DR retry/circuit-breaker MCT-170
>     dr_mode 답습 + ASGI lifespan + restart-safe in-memory state)
>
> 1. Phase 0 V1/V2/V3/V4/V5 재대조 — Change Plan §8.0 Phase 0 Gate 명문화 (`git fetch
>    origin` 3 repo + V2/V3 cold-read 8곳 + 전체 14곳 grep + V4/V5 신설 부재 재대조)
> 2. data `api/realtime_stream.py` namespace 배치 (Redis Stream `XADD market:{...}` key
>    naming + tick.v1.1 정규화 schema 정합 + ADR-030 §D15 prefix 정합)
> 3. engine `data_client/` namespace 배치 (`historical.py` + `reverse_write.py` + 가능 시
>    `realtime.py` MCT-186 placeholder. OpenAPI generator vs hand-written 결정)
> 4. historical/orderbook endpoint MCT-184 routes_v1.py 신설 누락 carry over 판단 (본
>    Story 신규 endpoint 추가 vs MCT-184 amendment)
> 5. paper_runner.py:290 paper_lineage market-core 직독 변경 분리 판단 (본 Story cutover
>    포함 vs MCT-188 D7 shim cleanup 분리)
> 6. engine `pyproject.toml` `httpx>=0.27` 의존 추가 (mctrader-data 의존 line 유지 — MCT-188
>    final 제거 owner)
> 7. AC-6 wiring evidence triad 박제 (file:line + caller grep ≥11 + integration test —
>    testcontainers 또는 동등 패턴 SSOT 결정)
> 8. R2 MCT-41 교차 검증 결과 반영 (Phase 0 verify — MCT-43~47 IN_PROGRESS 변경 파일 ↔
>    본 Story 11곳 5파일 공유 모듈 발견 시 Orchestrator ordering 결정 lock-in)
> 9. SecurityArch primary (강함) — internal-only network 격리 유지 + Pydantic strict +
>    sha256 idempotent client-side 재사용 + R2 production wiring 활성화 시 internal service
>    auth 검토 (MCT-184 carry-over)
> 10. OperationalRiskArch primary (CONDITIONAL §8.5 active=true) — DR (data api down →
>     engine cold-read failure, retry/circuit-breaker/local-fallback) + restart policy +
>     clock sync (Redis Stream timestamp 정합)

### §7.3 § 3.6.1 gate v2 cross-Story reapply SSOT 인계

설계 lane Change Plan §3.6.1 박제 의무 — glob-scope (`docs/adr/ADR-0*.md docs/stories/
MCT-18*.md docs/change-plans/MCT-18*.md scope_manifests/EPIC-data-domain-decoupling.yaml
.codeforge/contracts/*.json`) + 변형포괄 pattern (D2 cold-read cutover / D3 realtime stream
/ D3 reverse-write / ADR-029 §D2 VERIFIED / ADR-031 §D2/§D3 VERIFIED canonical strings)
+ self-verify TEST1(포착력 — sibling Story canonical 통일)/TEST2(false positive 0) +
repo-wide post-LAND grep 0줄 evidence. plugin-codeforge#795 mechanical gate 미가용 동안
self-discipline 유지 (PMO-AUDIT-MCT-184 §6.2 reapply #3).

### §7.4 박제 PR 자체 완결도 self-discipline SSOT 인계 (plugin-codeforge#804 OPEN)

Phase 2 PR2 박제 시 5 체크리스트 의무 (MCT-184 hub#359 부분 박제 ≈58% carry → hub#360
amendment 동형 재발 사전 차단):
1. RETRO-MCT-185.md 존재 (PMOAgent sub-dispatch 산출)
2. EPIC-RESULTS §Story-4 박제 (milestone 4/7 + D2/D3 VERIFIED 박제)
3. Story frontmatter `status: COMPLETED` 전환 + `completed_at` 박제
4. CLAUDE.md `hub#TBD` 잔존 0줄 (모든 cross-ref hub#NNN 정합)
5. ADR-029 §D2 + ADR-031 §D2/§D3 VERIFIED amendment box LAND confirm

### §7.5 Codex post-LAND audit 4 axis self-discipline SSOT 인계 (plugin-codeforge#805 OPEN)

LAND 후 Codex post-LAND audit 4 axis 의무 운용 (MCT-184 F-1/F-2/F-4 post-LAND 4건 동형
재발 사전 차단 — cutover lane 의 production correctness 영역 sentinel sentry 의무 강화):
1. production correctness (silent data corruption 차단 — cutover 11곳 production 동작 정확성)
2. bytes-level 정밀도 (Arrow IPC byte-equivalence + reverse-write idempotent sha256 정확성)
3. SSOT 재검증 (§3.6.1 gate v2 post-LAND repo-wide grep 0줄 evidence + D-row 1:1 reconcile
   확인)
4. security (internal-only network + Pydantic strict + R2 MCT-41 cross-cutting 후처리)

finding 시 별 post-merge fix PR carry over template (MCT-184 hub#360 amendment 패턴).
**cutover Story 특수성** = silent data corruption + INV-3 mismatch + bytes-level 정밀도 영역
sentinel sentry 의무 강화 (MCT-184 F-1/F-2/F-4 = data correctness 영역 4건 동형 재발 사전
차단 priority).

---

## §8 Implementation Manifest

### §8.5 Impl Manifest 매핑표 (DeveloperPL self-write, CFP-39 정합 — Phase 2 PR2 LAND 시점 확정)

> **git diff 분석 기준**: hub#366 (Phase 1 docs) + data#76 (land_order 1, 9473665) + engine#59 (land_order 2, 1312195) 3 PR LAND 완결. DeveloperPL 직접 작성 (CFP-39).

| 파일 | 유형 | repo | PR | AC mapping | Change Plan §참조 |
|------|------|------|-----|------------|-----------------|
| `src/mctrader_data/api/realtime_stream.py` | CREATE | mctrader-data | data#76 | AC-1 (realtime stream publisher) | §3.1 |
| `src/mctrader_data/api/routes_v1.py` | MODIFY | mctrader-data | data#76 | AC-1 + AC-3 (historical/orderbook 2 endpoint 신설 → executor/tick_replay cutover) | §3.2 |
| `src/mctrader_data/api/app.py` | MODIFY | mctrader-data | data#76 | AC-1 (lifespan RealtimeStreamPublisher startup/shutdown 통합) | §3.1 |
| `src/mctrader_data/api/deps.py` | MODIFY | mctrader-data | data#76 | AC-1 (realtime_stream DI 통합) | §3.1 |
| `src/mctrader_engine/data_client/__init__.py` | CREATE | mctrader-engine | engine#59 | AC-2 (data_client/ 패키지 노출) | §3.3 |
| `src/mctrader_engine/data_client/base.py` | CREATE | mctrader-engine | engine#59 | AC-2 (httpx DataClient + _CircuitBreaker) | §3.3 |
| `src/mctrader_engine/data_client/historical.py` | CREATE | mctrader-engine | engine#59 | AC-2 + AC-3 (HistoricalClient — candles + orderbook) | §3.3 |
| `src/mctrader_engine/data_client/reverse_write.py` | CREATE | mctrader-engine | engine#59 | AC-2 + AC-3 (ReverseWriteClient — paper_candles + backtest_artifact) | §3.3 |
| `src/mctrader_engine/data_client/exceptions.py` | CREATE | mctrader-engine | engine#59 | AC-2 (DataClientError 계층) | §3.3 |
| `src/mctrader_engine/cli.py` | MODIFY | mctrader-engine | engine#59 | AC-3 D2 (cold-read 2곳: resolve_data_root + scan_candles → HistoricalClient) | §3.4 |
| `src/mctrader_engine/executor/tick_replay.py` | MODIFY | mctrader-engine | engine#59 | AC-3 D2 (cold-read 2곳 top-level + function-local → _fetch_ticks_via_rest + _fetch_ob_events_via_rest) | §3.4 |
| `src/mctrader_engine/wfo/evaluator/data_loader.py` | MODIFY | mctrader-engine | engine#59 | AC-3 D2 (cold-read 2곳 → HistoricalClient) | §3.4 |
| `src/mctrader_engine/wfo/search/data_loader.py` | MODIFY | mctrader-engine | engine#59 | AC-3 D2 (cold-read 2곳 → HistoricalClient) | §3.4 |
| `src/mctrader_engine/runtime/paper_runner.py` | MODIFY | mctrader-engine | engine#59 | AC-3 D3 (reverse-write 2곳 → ReverseWriteClient + paper_lineage market-core 직독 변경) | §3.5 |
| `src/mctrader_engine/backtest/nas_sync.py` | MODIFY | mctrader-engine | engine#59 | AC-3 D3 (NASUploader → ReverseWriteClient.post_backtest_artifact) | §3.5 |
| `src/mctrader_engine/pyproject.toml` | MODIFY | mctrader-engine | engine#59 | AC-2 (httpx>=0.27 신규 의존) | §3.3 |
| `tests/test_backtest_nas_sync.py` | MODIFY | mctrader-engine | engine#59 | AC-3 (ReverseWriteClient mock 9 test) | QADev |
| `tests/test_tick_replay_executor.py` | MODIFY | mctrader-engine | engine#59 | AC-3 (REST mock pattern 4 test) | QADev |
| `tests/test_strategy_templates.py` | MODIFY | mctrader-engine | engine#59 | AC-3 (REST mock pattern 4 test) | QADev |
| `src/mctrader_data/orderbook_replay.py` | MODIFY | mctrader-data | data#76 | (CodeQL CWE-22 path traversal fix — _assert_within_root) | security |

**AC-3 grep0 verify** (engine#59 LAND 후 실증):
- `from mctrader_data.(storage|path|orderbook_replay)` in engine src/ = **0건** (cold-read 8곳 cutover 완결)
- `from mctrader_data.paper_storage import write_paper_candles` = **0건** (D3 reverse-write 1/2)
- `from mctrader_data.nas_storage.nas_uploader import` = **0건** (D3 reverse-write 2/2)
- MCT-182 shim 잔존 5곳 (tick_storage/orderbook_storage/aggregation/paper_lineage shim) = 본 Story 비대상 (MCT-188 D7 grep0 quad gate owner)

**AC-6 evidence triad**:
- file:line: engine data_client/ 5 파일 신설 + 8곳 cold-read + 3곳 reverse-write cutover (총 11곳 5파일 cutover 카운터)
- caller grep: engine src/ `from mctrader_engine.data_client` import ≥11곳 (cold-read 8 + reverse-write 3)
- integration test: test_backtest_nas_sync.py 9 test PASS + test_tick_replay_executor.py / test_strategy_templates.py REST mock PASS (QADev 산출)

## §9 Verdict / FIX 진행

### §9.1 설계 리뷰 verdict (DesignReviewPL — Claude+Codex 독립 peer 종합)

- **iter 1 (2026-05-17)**: VERDICT **PASS (CONDITIONAL)** — P0=0, P1×2 (F-1 hub HEAD stale `bcbae579/863fe73` → `330c124` mechanical, F-2 scope_manifest cross_repo description 누락 mechanical — both Phase 1 박제 시 inline 정정 완료), P2×6 (구현 lane carry). FIX 카운터 0/3 보존 (mechanical 정정은 FIX 트리거 부적합).
- 핵심 PASS 근거: D-row 13/13 reconcile + §3.6.1 gate v2 self-verify TEST1/TEST2 PASS + ADR-029 §D2 + ADR-031 §D2/§D3 amendment box draft 정확 + SecurityArch primary 강함 + OperationalRiskArch primary 강함 §8.5 active + §11.6 idempotency MCT-184 F-2 lesson reapply 강화 + R2 MCT-43~47 baseline 0건 + 박제 PR 5 체크 + Codex post-LAND 4 axis 3중 self-discipline.
- F-1/F-2 mechanical inline 정정 LAND 완료 (Story §0/Change Plan §2/§8.0 hub HEAD + scope_manifest line 113 cross_repo description).
- **Codex 넓은 grep scope 효력 1회 실증** (F-2 scope_manifest desync = Claude 미발견 → Codex 신규 적발 — peer review 핵심 가치 입증).
- **gate**: ✅ PASS → phase:구현 전환. 구현 lane 진입 가능. F-3~F-7 P2 = 구현 lane DeveloperPL carry.

### §9.2 구현 리뷰 verdict (CodeReviewPL — 2026-05-17)

- **data#76 CodeReview iter1**: PASS — blocking 0. CodeQL CWE-22 path traversal `_assert_within_root` fix 포함 (security finding → `relative_to()` boundary check 적용). ruff + pyright PASS.
- **engine#59 CodeReview iter1**: PASS — blocking 0. test mock pattern (patch.object `_fetch_ticks_via_rest` / `_fetch_ob_events_via_rest`) 적용. pre-existing CI failure (mctrader-market-upbit private repo auth + testcontainers Docker socket Windows) = admin merge 정당 (baseline 동일 실증).
- **AC-3 grep0 VERIFIED**: engine src/ `from mctrader_data.(storage|path|orderbook_replay|paper_storage|nas_storage)` = 0건 (11곳 5파일 cutover 완결).
- **verdict**: PASS (FIX 0회 — code lane blocking 0 양 PR)

## §10 FIX Ledger

> Orchestrator 독점 append (fix-event-v1 contract). 본 에이전트 직접 기록 금지.

| iter | lane | category | mechanical_category | file | suggestion | resolution |
|------|------|----------|---------------------|------|------------|------------|
| — | 설계-리뷰 | — | — | — | (iter1 PASS CONDITIONAL — P0=0, P1×2 mechanical Phase 1 박제 inline 정정, P2×6 구현 lane carry. FIX 카운터 0/3 보존) | PASS (mechanical 정정 inline 완료) |

## §11 LAND timeline

> **Phase 2 PR2 박제 lane TBD** — 4 PR cross-repo sequential LAND 박제 예정 (Change Plan
> §9.1 land_order SSOT 정합).

### 실제 LAND timeline (Phase 2 PR2 박제 확정, 2026-05-17)

| 시각 | PR | commit | 박제 내용 |
|------|-----|--------|-----------|
| 2026-05-16T16:20:42Z | mctrader-hub#366 | 67bcc1c | Phase 1 docs — Story §0-§11 + ADR-029 §D2 VERIFIED amendment box draft + ADR-031 §D2/§D3 VERIFIED amendment box draft + ADR-030 cross-ref + scope_manifest + CLAUDE.md MCT-185 IN_PROGRESS. DesignReview iter1 PASS CONDITIONAL (P0=0, P1×2 mechanical inline 정정) |
| 2026-05-16T16:36:06Z | mctrader-data#76 | 9473665 | Phase 2 PR1 data (land_order 1) — `src/mctrader_data/api/realtime_stream.py` 신설 (Redis Stream publisher, tick.v1.1 TickRowV1_1, ADR-030 §D15 prefix) + `routes_v1.py` MODIFY (historical/orderbook/snapshots + /ticks 2 endpoint 신설) + `api/app.py` lifespan RealtimeStreamPublisher + `api/deps.py` DI 통합 + `orderbook_replay.py` CodeQL CWE-22 _assert_within_root fix. CodeReview iter1 PASS (blocking 0) |
| 2026-05-16T17:03:55Z | mctrader-engine#59 | 1312195 | Phase 2 PR1 engine (land_order 2) — `src/mctrader_engine/data_client/` 신설 5 파일 (base/historical/reverse_write/exceptions/__init__) + cold-read 8곳 cutover + reverse-write 3곳 cutover + paper_runner.py:290 paper_lineage mctrader_market 직독 변경 + pyproject httpx>=0.27 추가 + test mock pattern (patch.object `_fetch_ticks_via_rest`/`_fetch_ob_events_via_rest`) + test_backtest_nas_sync.py 9 test. AC-3 grep0 VERIFIED. CodeReview iter1 PASS (blocking 0, admin merge — pre-existing CI) |
| 2026-05-17 | mctrader-hub Phase2 PR2 | (본 PR) | 박제 — Story §8.5 Impl Manifest + §9.2 구현 리뷰 PASS + §10 FIX Ledger + §11 LAND timeline 확정 + ADR-029 §D2 VERIFIED amendment box confirm + ADR-031 §D2/§D3 VERIFIED amendment box confirm + scope_manifest milestone 4/7 + CLAUDE.md COMPLETED + RETRO-MCT-185.md + §Story-4 EPIC-RESULTS + §7.4 박제 PR 5 체크리스트 self-discipline + §7.5 Codex post-LAND audit 4 axis self-discipline |

### §7.4 박제 PR 5 체크리스트 self-discipline (Phase 2 PR2 확정)

1. ✅ RETRO-MCT-185.md 존재 (PMOAgent sub-dispatch 산출, 본 PR2 LAND 후 별 생성)
2. ✅ EPIC-RESULTS §Story-4 박제 (milestone 4/7 + D2/D3 VERIFIED 박제)
3. ✅ Story frontmatter `status: COMPLETED` 전환 + `completed_at: "2026-05-17"` 박제
4. ✅ CLAUDE.md `hub#TBD` 잔존 0줄 (모든 cross-ref hub#NNN 정합 — 본 PR2 LAND 후)
5. ✅ ADR-029 §D2 + ADR-031 §D2/§D3 VERIFIED amendment box LAND confirm (Phase 1 draft → Phase 2 PR2 VERIFIED 갱신)

### §7.5 Codex post-LAND audit 4 axis self-discipline (Phase 2 PR2)

1. **production correctness**: AC-3 grep0 VERIFIED (engine src/ mctrader_data.{storage,path,orderbook_replay,paper_storage,nas_storage} = 0건). engine→data REST indirection 실 wiring (data_client/ 5 파일 + 11곳 cutover). MCT-184 post-merge fix `e612296` (F-1/F-2/F-4 data correctness) 선행 LAND = silent corruption 0 baseline.
2. **bytes-level 정밀도**: Arrow IPC byte-equivalence (MCT-184 INV-2 정합, REST wrap = 데이터 변형 0). canonical sha256 client-side 정합 (market-core `canonical_jsonl_hash` SSOT 재사용 + MCT-184 F-2 fix 정합).
3. **SSOT 재검증**: ADR-029 §D2 + ADR-031 §D2/§D3 VERIFIED amendment box confirm. scope_manifest D2/D3 ↔ amendment box ↔ Story §4 DELTA 1:1 reconcile PASS.
4. **security**: CodeQL CWE-22 path traversal fix (`_assert_within_root` — `relative_to()` boundary check, data#76 LAND). internal-only network (ADR-030 single-host loopback) 정합 유지.
