---
story_key: MCT-107
story_issues:
  - repo: mclayer/mctrader-hub
    number: 202
status: 구현
component: market
type: story
parent_epic: null
related_adrs: ADR-009, ADR-013
---

# MCT-107: code-review fix — mctrader-market

- **Status**: 구현 (fix 브랜치 생성 완료, 테스트·merge 필요)
- **Trigger**: Codex 심층 리뷰 2026-05-09 — CRITICAL 2건 / HIGH 6건 발견
- **Repo**: `mctrader-market` (`c:\workspace\mclayer\mctrader-market`)

## 1. 배경

리뷰 에이전트가 mctrader-market 전체 소스를 분석해 ADR-009/ADR-013 위반 및 도메인 모델
불변식 결함을 발견. 두 개의 fix 브랜치가 이미 생성·커밋됨.

## 2. Fix 브랜치 현황

### `fix/market-types-alembic`
| 항목 | 내용 |
|---|---|
| `types.py` | `Decimal38_18` validator: float 입력 거부 (`ValueError`), NaN/Inf 거부, 38-digit 초과 거부 |
| `types.py` | `Symbol.from_string()` maxsplit=1 → 다중 하이픈 거부 |
| `types.py` | `Symbol.__post_init__()` whitespace 검증 |
| `alembic/env.py` | `run_migrations_online()` 시작 시 `CREATE SCHEMA IF NOT EXISTS market` 실행 |

### `fix/market-domain-models`
| 항목 | 내용 |
|---|---|
| `candle.py` | ADR-009 16컬럼 완성: `trade_count`, `is_complete`, `source_ingested_at`, `data_snapshot_id`, `data_hash`, `schema_version` 추가 |
| `order.py` | `model_validator(mode="after")`: limit-without-price, overfill, quantity≤0, updated_at<created_at 거부 |
| `orderbook.py` | `bids`/`asks` `list→tuple` 변경 → frozen=True와 결합해 진정한 불변성 확보 |
| `providers.py` | `get_candles()` 파라미터 `datetime→UTCDateTime` |
| `models/symbol.py` | `CheckConstraint("symbol ~ '^[A-Z]+-[A-Z]+$'")` 추가 |

## 3. 수행 절차 (codeforge 세션)

1. `mctrader-market` 에서 두 브랜치 각각 `pytest` 실행 → 통과 확인
2. 누락 테스트 보완 (§4 참조)
3. 각 브랜치 PR 생성 → main merge
4. main 에서 최종 `pytest` 재확인

## 4. 누락 테스트 (이 Story 내 추가)

- `test_types.py`: float 입력 → ValueError, NaN → ValueError, >38 digits → ValueError
- `test_types.py`: `Symbol.from_string("KRW-BTC-OLD")` → ValueError
- `test_candle.py`: CandleModel 16컬럼 이름·타입 전체 assertion
- `test_protocols.py`: `OrderBookProvider` runtime protocol 준수 검증
- `test_protocols.py`: `OrderBookModel.bids.append(...)` → AttributeError (tuple)

## 5. AC

- [ ] `fix/market-types-alembic` pytest green
- [ ] `fix/market-domain-models` pytest green
- [ ] §4 누락 테스트 추가 및 통과
- [ ] 두 브랜치 PR merge → main green

## 6. 관련 ADR

- ADR-009: OHLCV v1 16-col Decimal(38,18) UTC
- ADR-013: Symbol frozen dataclass, Timeframe StrEnum, Order immutable Pydantic
