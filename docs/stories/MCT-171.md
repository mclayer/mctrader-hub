---
story_key: MCT-171
story_scope: cross-repo
story_issues:
  - repo: mclayer/mctrader-hub
    number: TBD  # Phase 1 PR (docs + DR runbook 본문)
  - repo: mclayer/mctrader-data
    number: 62  # Phase 2 PR1 (invariant 8종 + capacity_probe + ingest_blocker, MERGED 2026-05-14)
status: IN_PROGRESS
started_at: "2026-05-14"
epic_key: EPIC-tier-promotion-single-source
parent_dependency: MCT-170  # engine reader L1 + DR mode LAND 전제 (D7 + D8 owner)
depends_on:
  - MCT-167  # ADR-029 신규 + amendment 3건 + DR runbook stub LAND (2026-05-14)
  - MCT-168  # L1 NAS DualWriter wiring LAND (2026-05-14)
  - MCT-169  # local delete + ambiguity invariant LAND (2026-05-14)
  - MCT-170  # engine reader L1 확장 + DR mode + reader cache byte budget LAND (2026-05-14)
related_adrs:
  - "ADR-029 D4 + D5 + D6 + D11 owner Story"
  - "ADR-029 §D4 verify status entry: WAL local only, sealed segment NAS PUT 금지"
  - "ADR-029 §D5 verify status entry: A_modified graceful drain ingest block (D7-5=B Codex 채택)"
  - "ADR-029 §D11 verify status entry: 80%/95% hysteresis 4 layer (D7-8=C Codex 채택)"
created_at: 2026-05-14
phase_pair: phase1_phase2
delegates:
  - DeveloperAgent (mctrader-data: invariant_harness 8종 + capacity_probe + ingest_blocker)
  - DeveloperAgent (mctrader-data: prometheus_exporters 확장 — capacity Gauge + invariant violation Counter + latency Histogram)
  - QADeveloperAgent (mctrader-data: test_invariant_harness_8 + test_capacity_probe + test_ingest_blocker)
retro_file: docs/retros/RETRO-MCT-171.md  # Phase 2 PR2 PMOAgent 자동 dispatch
---

# MCT-171 — DR runbook 본문 + invariant 8종 확장 + 4 layer capacity 제한 정책 (EPIC-tier-promotion-single-source Story-5)

## §1 동기

**EPIC-tier-promotion-single-source Story-5** — ADR-029 D4 + D5 + D6 + D11 owner Story. MCT-167 governance singleton publish 시 DR runbook **stub 만** 박제 (5 fail mode placeholder + invariant 8종 placeholder + 4 layer capacity placeholder), 본 Story = stub → **운영 가능 runbook** 격상 + invariant 8종 enforcement impl + 4 layer capacity probe + collector ingest block.

### 1.1 직접 동기

prerequisite MCT-167~170 ALL LAND (2026-05-14) 후 Epic 의 **운영 안전성 박제 Story**:

1. **D4=B (WAL sealed local only)**: 사용자 directive — WAL NAS PUT 금지, RPO=0 보장은 D1 단독 의존. D5 capacity-bounded + D11 hard limit 으로 sealed→L1 derive fail risk mitigation.
2. **D5=A_modified (capacity-bounded ingest block)**: D7-5=B graceful drain 후 reject (in-flight WAL write 일관성 보존). 정상 운영 시 hot path 영향 0 (ADR-017 §D5 정합).
3. **D6=B (bucket versioning + cross-NAS replication)**: bucket versioning ✓ (MCT-161 LAND), cross-NAS replication = MCT-174 defer (mcnas02 물리 부재).
4. **D11 (4 layer capacity)**: WAL 30G / L1 20G / NAS 500G target / Host 200G hard limit + D7-8=C 80% warn aggressive rotate + 95% graceful block hysteresis.

### 1.2 Phase 0 verify 발견 (path discrepancy 박제)

session prompt (MCT-171-session-prompt.md) 가설 vs 실 코드 verify:

1. ✅ DR runbook stub 341 lines, line 275-328 = 본 Story author 영역 (3 section placeholder).
2. ⚠️ **Path discrepancy 1**: InvariantHarness 실 위치 = `mctrader-data/src/mctrader_data/nas_migration/invariant_harness.py` (prompt 의 `nas_storage/` 정정). 7종 SSOT = sha256/object_count/row_count/column_count/column_order/dtype/schema_version, `_INVARIANT_NAMES` tuple + 8 fail variant enum + `ADR009_CHANNEL_SCHEMA_MATRIX` (tick.v1/tick.v1.1/orderbook_snapshot.v1/ohlcv.v1).
3. ⚠️ **Path discrepancy 2**: collector = `mctrader-data/src/mctrader_data/collector.py` 단일 468 lines (prompt 의 `collector/dir` 정정). Codex D7-2=A 채택 = `capacity_probe.py` 단독 (collector.py sibling).
4. ✅ WAL module = `wal/` dir (ingester / ndjson_codec / replay / segment). D11 WAL 30G hard limit = segment.py rotate threshold 정합.
5. ⚠️ **Ambiguity invariant (8번째) 분산** — 현재 `compactor/promotion.py` 단독, InvariantHarness 외부. Codex D7-1=A 채택 = InvariantHarness 측 통합 (status enum 9 variant + `_INVARIANT_NAMES` 8개).
6. ❌ **Production data dir 부재** (host-local 측정 부재). WAL/L1 baseline = production runtime probe 의무 (dev 실측은 synthetic / mock).
7. ✅ Host disk 476G / 199G avail / 59% used. D11 host 200G hard = quota / mount 분리 의무.
8. ✅ MCT-174 reservation active (D6 cross-NAS replication owner, mcnas02 물리 부재 해소 후 진입).

### 1.3 Critical risk (Codex 합의)

**R-CRITICAL: WAL 30G 산정 근거 미검증** — peak market open burst (09:00 KST) 시 fill rate vs L1 promotion rate 검증 부재. Phase 2 진입 시 collector hot path runtime probe baseline 측정 의무 (50 sym × 3 channel × 12 seg/h 가정 vs 실측). 측정 결과 30G 초과 risk 검출 시 D11 WAL hard_limit 갱신 amendment 발의.

## §2 Phase 0 Context (brainstorm 답습)

본 세션 4 agent burst + Codex 9 결정점 review + PMO 2nd pass 박제분.

### 2.1 DomainAgent (verified-via 충실)

핵심 사실 5종 (`verified-via: Read docs/domain-knowledge/...`):

- **Hot path 무영향 invariant** (ADR-017 §D5): collector WAL/L1 ParquetWriter latency 0 의무 → D5 capacity-bounded ingest block = cold path 한정 정책 (`verified-via: Read docs/domain-knowledge/domain/parquet-streaming/cold-path-memory-invariant.md`)
- **NAS retry_queue fallback** (ADR-027 §D5): NAS unreachable 시 hot path 차단 0, retry_queue 경유 (DR runbook fail mode 2 직결)
- **Cold path memory invariant INV-4**: DualWriter ≤50MB, L2/L3 ≤256MB peak RSS+TM delta — 용량 4 layer (WAL 30G/L1 20G/NAS 500G/Host 200G) 와 별 layer, RSS 측정과 disk capacity 분리
- **Forward-only invariant detective only** (ADR-009 §D12): 1d 지연 = 1d 영구 손실, backfill 불가 → DR runbook 복구 window 정책 제약
- **sha256 SSOT caller-side** (INV-3, MCT-163 D2=A): multipart ETag ≠ sha256, NAS Metadata HEAD verify로 idempotency → replication failover 시 정합 판정 근거

### 2.2 ResearcherAgent

핵심 개념 3 + Unknown unknowns 2:

- **Backpressure propagation**: collector ingest block 시 upstream (ws_client / order_router) 큐 saturation 전파 — drop policy vs await vs circuit-break (Netflix concurrency-limits / Envoy adaptive concurrency 선행사례)
- **Invariant ordering & short-circuit**: 8종 enforcement 순서 (cheap → expensive: object_count → sha256) + first-fail abort vs collect-all (D7-3=B 1h periodic sweep 채택 = collect-all 패턴)
- **Capacity watermark hysteresis**: 4 layer threshold (high/low watermark) — single threshold thrashing 회피 (PostgreSQL effective_cache_size / Kafka log.retention.bytes 선행사례)
- **Unknown 1**: WAL 30G 산정 근거 부재 (R-CRITICAL 동치, §1.3 박제)
- **Unknown 2**: Ambiguity invariant InvariantHarness 통합 시 promotion.py 호출자 (cold_path) backward compat 위험 — D7-1=A InvariantHarness 측 통합으로 cold_path 호출 경로 보존 의무

### 2.3 RequirementsAnalystAgent

- **추정 동기 (why)**: production 운영 안전성 박제 의무 — 데이터 손실 / ambiguity / 용량 부족 대응 + invariant 강제 + 모니터링 체계 완성. MCT-167~170 LAND 후 운영 가능 수준 보장.
- **일치 여부**: 일치 — 사용자 directive D4/D5/D6/D11 = ADR-029 5 fail mode + invariant 8종 + 4 layer capacity 정확 정합.
- **WHY 기반 확장**: 운영 대응 가용성 (5 fail mode 검출+진단+복구) + Invariant visibility (violation 시그널 채널) + 용량 모니터링 (Grafana/alert chain).

### 2.4 PMOAgent (Phase 0 + Phase 2 박제)

- 단일 Story, vertical slice 강결합 (D5 + D10 + D11 cross-cut, 분리 시 partial-LAND 정합 fail risk)
- 의존 가능 epic: 없음 (EPIC-tier-promotion-single-source 내부, prerequisite ALL LAND)
- 주요 위험: R2 HIGH = D6 cross-NAS replication MCT-174 defer 의무 (mcnas02 물리 부재)

## §3 Codex review 9 결정점 (단일 패스 합성)

| D7-N | 채택 Option | 근거 |
|------|------------|------|
| **D7-1** invariant_harness 확장 vs 신규 module | **A — 기존 InvariantHarness 통합** | 7종 SSOT 유지 (MCT-152/153/155 caller 호환), status enum 9 variant + `_INVARIANT_NAMES` 8개 확장. ambiguity = write-time vs query-time 성격 차이는 timing 분리 (D7-3=B 1h periodic sweep) 로 해소. |
| **D7-2** capacity_probe module 위치 | **A — `capacity_probe.py` 단독** | collector.py sibling, hot path / Prometheus exporter 의존 0. threshold SSOT = 상수 파일 또는 env 의무. |
| **D7-3** invariant 8종 enforcement timing | **B — 1h periodic sweep** | D5 hot path 영향 0 정합 (ADR-017 §D5). detection lag 1h = ambiguity 발생 시 1h window 내 violation 검출. |
| **D7-4** 4 layer capacity probe 주기 | **C — hybrid (5min audit + threshold approach continuous)** | 정상 시 probe 비용 낮추고 capacity-bounded block 직전 빠른 감지. WAL 30G sizing 미검증 risk 계승. |
| **D7-5** D5 ingest block 정책 | **B — graceful drain 후 reject** | in-flight WAL write 일관성 보존. drain window 정의 = WAL atomic boundary (sealed segment) 의 drain 시점 정합. |
| **D7-6** Prometheus metric 설계 | **mandatory 3종 + violation Counter + latency Histogram + cardinality 제한** | label = `invariant_name` (8 enum) + `layer` (4 enum) only. free-form label 금지 (cardinality 폭발 방지). |
| **D7-7** DR runbook 5 fail mode 우선순위 | **(2) > (1) > (4) > (3) > (5) 유지** | + disk full → mode (2) 하위 명시 + mode (3) clock drift "ambiguity invariant 트리거 가능" 명시 (Codex 보완 권고). |
| **D7-8** WAL 30G hard limit fallback | **C — 80%/95% hysteresis** | 80% warn aggressive rotate + 95% graceful block. D4 WAL local only + D5 block 동시 만족. compactor lag 모니터링 의무. |
| **D7-9** Host 200G quota 강제 | **A + C bridge** | LVM/Docker volume infra-level quota (장기) + Prometheus `mctrader_capacity_usage_bytes{layer=host}` alert (단기 bridge). MCT-171 scope = bridge alert + alert chain, LVM 설정 = 별 infra task. |

## §4 Acceptance Criteria

### AC-1 — invariant 8종 enforcement (D7-1=A + D7-3=B)

**Given** InvariantHarness `_INVARIANT_NAMES` 7종 + ambiguity 8번째 통합 (status enum 9 variant + `compactor/promotion.py` 분산 로직 SSOT 흡수)
**When** 1h periodic sweep 실행
**Then** 8 invariant 모두 PASS, `mctrader_invariant_violation_total{invariant_name=<8 enum>}` Counter = 0, `mctrader_invariant_check_latency_ms` Histogram emit. `compactor/promotion.py` 분산 로직 0 (SSOT 단일). MCT-169 LAND 산출물 (D3 + D10 invariant test) 회귀 0.

### AC-2 — capacity_probe hybrid timing (D7-2=A + D7-4=C)

**Given** `mctrader_data/capacity_probe.py` 단독 module (collector.py sibling, hot path / exporter 의존 0)
**When** 5min audit sample 기본 + 4 layer 임의 layer usage >= 80% threshold 근접 시 continuous 전이
**Then** `mctrader_capacity_usage_bytes{layer=<WAL_local|L1_local|NAS_bucket|Host_disk>}` Gauge emit. collector.py hot path 영향 0 (sibling module, exporter 의존 0).

### AC-3 — ingest_blocker graceful drain (D7-5=B + D7-8=C)

**Given** `mctrader_data/ingest_blocker.py` 신규 module
**When** WAL local >= 95% 또는 L1 local >= 95% 도달
**Then** in-flight WAL write 완료 후 신규 ingest reject. WAL 30G hysteresis (D7-8=C): 80% warn + aggressive L1 rotate trigger, 95% graceful block (block ↔ unblock 임계 5% gap). `mctrader_ingest_blocked_total` Counter emit.

### AC-4 — DR runbook 본문 (D7-7)

**Given** DR runbook stub line 275-328 anchor
**When** 본 Story Phase 1 LAND
**Then** 5 fail mode step-by-step (검출 / 진단 / 복구 / verify / postmortem) + invariant 8종 본문 + 4 layer capacity step-by-step + Slack notification template 박제. fail mode priority: (2) NAS unreachable > (1) L1 NAS PUT fail > (4) Rate-limit > (3) Clock drift > (5) Replication failover. disk full → mode (2) 하위 명시 + mode (3) ambiguity invariant 트리거 가능 명시. quick triage flowchart (runbook 첫 페이지).

### AC-5 — Prometheus metric design (D7-6)

**Given** `nas_metrics/prometheus_exporters.py` 확장
**When** capacity probe + invariant harness emit
**Then** 의무 metric 4종:
- `mctrader_capacity_usage_bytes{layer=<4 enum>}` Gauge
- `mctrader_capacity_threshold_ratio{layer=<4 enum>}` Gauge
- `mctrader_invariant_violation_total{invariant_name=<8 enum>}` Counter
- `mctrader_invariant_check_latency_ms` Histogram
- `mctrader_ingest_blocked_total{reason=<wal_full|l1_full|nas_unreachable>}` Counter
label cardinality 제한 enforce (free-form label 검출 시 fail-fast).

## §5 Invariants

| Invariant | 설명 | 검증 방법 |
|-----------|------|-----------|
| **INV-1 (D4 WAL local only)** | WAL sealed segment NAS PUT 금지 (사용자 directive) | invariant test fail 시 violation emit + audit log |
| **INV-2 (D5 hot path 영향 0)** | 정상 운영 시 collector hot path 영향 0 (D11 임계 미도달 시 ingest 정상) | capacity_probe latency 측정 (collector latency 영향 0) |
| **INV-3 (D11 hard limit 정확성)** | 4 layer hard limit (WAL 30G + L1 20G + NAS 1TB + Host 200G) | runtime probe baseline 의무 + threshold alert verify |
| **INV-4 (8종 backward compat)** | MCT-151 7종 InvariantHarness API 변경 0 (8종 확장만, 기존 caller 회귀 0) | MCT-152/153/155 caller integration test ALL PASS |
| **INV-5 (DR runbook actionable)** | 5 fail mode 본문 step-by-step 운영 가능 수준 (placeholder/TBD 0건) | runbook 운영자 follow-along verify |
| **INV-6 (cardinality 제한)** | Prometheus label = invariant_name 8 enum + layer 4 enum hardcoded enforce | free-form label 검출 시 fail-fast |

## §6 산출물 (cross-repo: mctrader-hub + mctrader-data)

### 6.1 mctrader-hub (Phase 1 + Phase 2 PR2 박제)

- `docs/stories/MCT-171.md` (본 file)
- `docs/superpowers/specs/2026-05-14-MCT-171-dr-runbook-capacity-design.md` (신규)
- `docs/superpowers/plans/2026-05-14-mct-171-dr-runbook-capacity.md` (신규)
- `docs/runbooks/nas-bucket-disaster-recovery.md` (확장 — line 275-328 anchor 위에 본문 5 fail mode + invariant 8종 + 4 layer capacity)
- `docs/adr/ADR-029-tier-promotion-single-source.md` (Phase 2 PR2 — Status §D4 + §D5 + §D11 verify status entry)
- `scope_manifests/EPIC-tier-promotion-single-source.yaml` (Phase 1 IN_PROGRESS + Phase 2 PR2 milestone 5/6 COMPLETED)
- `CLAUDE.md` (Phase 2 PR2 — §MCT-171 COMPLETED + §EPIC milestone 5/6)
- `.codeforge/counters.json` (Phase 1 retitle_history append)
- `docs/retros/RETRO-MCT-171.md` (Phase 2 PR2 PMOAgent 자동 dispatch)

### 6.2 mctrader-data (Phase 2 PR1)

- `src/mctrader_data/nas_migration/invariant_harness.py` (8종 확장 — `_INVARIANT_NAMES` + status enum 9 variant + ambiguity check 통합)
- `src/mctrader_data/capacity_probe.py` (신규 — 4 layer hybrid probe, collector.py sibling, 의존 0)
- `src/mctrader_data/ingest_blocker.py` (신규 — graceful drain + 80%/95% hysteresis)
- `src/mctrader_data/nas_metrics/prometheus_exporters.py` (확장 — capacity Gauge + invariant violation Counter + latency Histogram + ingest blocked Counter)
- `tests/integration/test_invariant_harness_8.py` (신규)
- `tests/integration/test_capacity_probe.py` (신규)
- `tests/integration/test_ingest_blocker.py` (신규)

## §7 Risk

| R | severity | desc | mitigation |
|---|----------|------|-----------|
| R-CRITICAL | HIGH | WAL 30G 산정 근거 미검증 (peak market open 09:00 KST burst) | Phase 2 collector runtime probe baseline 측정 의무, 초과 시 D11 hard_limit amendment |
| R1 | HIGH | invariant_harness 8번째 통합 — compactor/promotion.py 분산 로직 흡수 시 회귀 | MCT-169 D3 + D10 invariant test 회귀 0 의무, MCT-152/153/155 caller backward compat |
| R2 | HIGH | capacity_probe hot path 영향 (D5 hot path 영향 0 정합) | collector.py sibling module 분리 + 5min idle baseline, exporter 의존 0, 1h sweep timing 정합 |
| R3 | MID | ingest_blocker graceful drain — in-flight WAL write 일관성 | WAL atomic boundary (sealed segment) 의 drain 시점 정합 + ADR-017 zero-loss invariant preserve |
| R4 | MID | Prometheus label cardinality drift | invariant_name 8 enum / layer 4 enum hardcoded enforce, free-form label 검출 시 fail-fast |
| R5 | LOW | DR runbook 본문 5 fail mode priority drift | Codex 합의 priority 박제 + disk full mode (2) 하위 명시, 본문 사용자 검토 1회 권고 |
| R6 | LOW | D7-9 Host 200G bridge alert only (LVM 부재) | bridge → 장기 LVM 의무 CLAUDE.md note 박제, 별 infra task 발의 |

## §8 PR 분리 (3 PR cross-repo sequential)

### Phase 1 PR (mctrader-hub, docs only)

- Story §1-§10 + spec + plan + DR runbook 본문 확장 + scope_manifest IN_PROGRESS retitle + counters retitle_history append
- Gate: Phase 0 4 agent brainstorm + Codex review 박제 verify

### Phase 2 PR1 (mctrader-data, code)

- invariant_harness 8종 + capacity_probe + ingest_blocker + Prometheus exporters 확장 + 3 integration test
- Gate: 3 test ALL PASS + AC-1/2/3/5 verify + Phase 2 WAL runtime probe baseline 측정 결과 박제

### Phase 2 PR2 (mctrader-hub, 박제)

- Story §11 retro_file + §12 verify result + ADR-029 Status verify entry + DR runbook verify LAND footer + scope_manifest milestone 5/6 COMPLETED + CLAUDE.md + RETRO-MCT-171.md
- Gate: PMOAgent 자동 dispatch (Phase 2 PR1 merge +5min grace, ADR-045)

**LAND 순서 강제**: Phase 1 PR → Phase 2 PR1 → Phase 2 PR2. PR1 fail 시 PR2 발의 0.

## §8.5 Impl Manifest (Phase 2 PR1 — mctrader-data#62, 3fb9d60)

### 변경 파일 매핑

| 파일 경로 | 변경 유형 | Change Plan 항목 | AC |
|-----------|-----------|-------------------|-----|
| `src/mctrader_data/capacity_probe.py` | 신규 | §5 D7-2=A + D7-4=C + D11 SSOT | AC-1 AC-2 |
| `src/mctrader_data/ingest_blocker.py` | 신규 | §5 D7-5=B + D7-8=C hysteresis | AC-3 |
| `src/mctrader_data/nas_metrics/prometheus_exporters.py` | 확장 (+5 metric) | §5 D7-6 Prometheus cardinality | AC-5 |
| `src/mctrader_data/nas_migration/invariant_harness.py` | 확장 (8th invariant) | §5 D7-1=A + D7-3=B | AC-4 |
| `src/mctrader_data/collector.py` | 확장 (IngestBlocker hook) | §5 D5=A_modified collector hook | AC-3 |
| `src/mctrader_data/compactor/promotion.py` | 수정 (DEPRECATED) | §5 D10 backward compat | INV-4 |
| `tests/integration/test_invariant_harness_8.py` | 신규 (8 tests) | QA Test Contract §4 AC-4 | AC-4 |
| `tests/integration/test_capacity_probe.py` | 신규 (15 tests) | QA Test Contract §4 AC-1/2 | AC-1 AC-2 |
| `tests/integration/test_ingest_blocker.py` | 신규 (15 tests) | QA Test Contract §4 AC-3/5 | AC-3 AC-5 |
| `tests/nas_migration/test_invariant_harness.py` | 수정 (== 7 → == 8) | Regression fix (INV-4) | INV-4 |

### 전체 테스트 결과

- 신규 통합 test: **38 passed** (test_invariant_harness_8: 8 / test_capacity_probe: 15 / test_ingest_blocker: 15)
- 회귀: **931 passed, 0 failed** (MCT-152/153/155/169 D10 회귀 0)
- CI: ubuntu-latest PASS / windows-latest PASS (PR #62, 2026-05-14T12:20Z)

### PR 정보

- PR: mclayer/mctrader-data#62
- merge commit: `3fb9d60`
- merged_at: 2026-05-14T12:20:08Z

## §9 Cross-ref

- ADR-029 (D4 + D5 + D11 owner): `docs/adr/ADR-029-tier-promotion-single-source.md`
- DR runbook stub (line 275-328 anchor): `docs/runbooks/nas-bucket-disaster-recovery.md`
- scope_manifest: `scope_manifests/EPIC-tier-promotion-single-source.yaml`
- spec: `docs/superpowers/specs/2026-05-14-MCT-171-dr-runbook-capacity-design.md`
- plan: `docs/superpowers/plans/2026-05-14-mct-171-dr-runbook-capacity.md`
- 세션 prompt: `docs/superpowers/prompts/MCT-171-session-prompt.md`
- MCT-167 governance singleton: `docs/stories/MCT-167.md`
- MCT-170 engine reader + DR mode: `docs/stories/MCT-170.md`
- MCT-172 (Epic CLOSED): counters.json reservation
- MCT-174 (NAS replication, D6 cross-NAS): counters.json reservation

## §10 Phase 2 Plan link

`docs/superpowers/plans/2026-05-14-mct-171-dr-runbook-capacity.md` 참조 — Phase 2 PR1 (data code) + Phase 2 PR2 (hub 박제) sequential LAND 의무.

## §11 retro_file

`docs/retros/RETRO-MCT-171.md` — Phase 2 PR2 PMOAgent 자동 dispatch (ADR-045 mandate, 5min grace + 4 attempts cumulative offset).

## §12 측정 결과

Phase 2 LAND 후 박제 — `docs/retros/RETRO-MCT-171.md` 참조.

기대 측정 항목:
- WAL/L1/NAS/Host 4 layer baseline (runtime probe 실측)
- invariant 8종 enforcement latency (Histogram p50/p99)
- 1h periodic sweep cost (collector hot path 영향 0 verify)
- graceful drain window (in-flight WAL write 완료 시간)
- 80% / 95% hysteresis 동작 verify (block ↔ unblock 5% gap)
