---
story_key: MCT-183
story_scope: cross-repo
status: phase:구현
phase: 구현  # 요구사항 ✓ → 설계 ✓ → 설계-리뷰 PASS (iter1→3 P0×3→×2 max → RESET → post-RESET PASS, gate:design-review-pass) → 구현
started_at: "2026-05-16"
completed_at: ~
epic_key: EPIC-data-domain-decoupling
sequential_phase: 2
parent_dependency: ~
depends_on: [MCT-182]
decisions_implemented: [D2, D6]
repo: "mctrader-hub + mctrader-data + mctrader-engine"
story_issues:
  - repo: mclayer/mctrader-hub
    number: 353     # Phase 1 PR (docs only — Story + ADR-029/027 amendment box + scope_manifest + CLAUDE.md) MERGED 29e9c0d
  - repo: mclayer/mctrader-data
    number: 70      # Phase 2 PR1 data code (io/ 6 module + tests/io/ 수령, land_order 1) MERGED 0e6f35b0
  - repo: mclayer/mctrader-engine
    number: 58      # Phase 2 PR1 engine code (io/ 6 module + tests/io/ 삭제, land_order 2) MERGED 18275737
  - repo: mclayer/mctrader-hub
    number: ~       # Phase 2 PR2 (박제) — TBD
related_adrs:
  - "ADR-029-tier-promotion-single-source (amendment box — engine NAS 직독 폐기 예고 + io reader relocated 박제. 실 amend confirm = MCT-185 (cold-read cutover))"
  - "ADR-027-cold-tier-object-storage-nas-minio (amendment box — io reader 6 module (endpoint_router/dr_mode/reader_cache/cold_reader/tier_reader/l1_reader) relocated to mctrader-data Layer2 박제)"
  - "ADR-031-data-domain-decoupling (Status: Accepted — MCT-182 LAND §D1 VERIFIED. 본 Story = §D2 io-relocate 절반 진행, §D2 VERIFIED 는 MCT-185 cutover 후)"
retro_file: docs/retros/RETRO-MCT-183.md  # Phase 2 PR2 PMOAgent sub-dispatch 시 생성
---

# MCT-183 — Layer 2 Read 도메인 Relocation → mctrader-data

> **요구사항 lane 작성 범위**: §1·§2·§4·§5·§6 (+ §0 Phase 0 Verify Gate 박제 + §4 cross-repo delta).
> §3·§7·§11 = 설계 lane (ArchitectPLAgent Change Plan) 영역 — **"설계 lane TBD" placeholder**.

## §0 Phase 0 Verify Gate (R1 — 가설 금지, brainstorm deep-verify 선이행 + 요구사항 lane 재검증)

brainstorm 단계에서 codeforge-brainstorm Phase 0 deep-verify agent 가 이미 spec §2 사실5
(engine `io/` NAS reader 6 module = engine `src/` 호출자 0, dead-in-prod) 를 file:line 근거로
실증했다. 본 Story 요구사항 lane 정형화 시점(2026-05-16, MCT-182 LAND 후 HEAD)에 §4 cross-repo
delta 정확성에 결정적인 V1~V7 을 **요구사항 lane PL 이 실제 코드 git grep/Read 로 재검증**했다.
결과 박제(verified-via git grep/`git show HEAD`/`ls`, 2026-05-16, engine/data/hub `main` HEAD
+ hub branch `mct-183-phase1-io-relocation`):

| V | plan 항목 | brainstorm/Orchestrator 가설 | 요구사항 lane 재검증 결과 | 판정 |
|---|-----------|------------------------------|-----------------------------|------|
| V1 | data/engine/hub `git fetch origin` (working tree stale 차단) | — | **코드 작업(data#N) 착수 전 의무** — 본 §0 박제 시점은 로컬 working tree(engine/data `main`, hub branch) 기준 재검증. data#N → engine#N 진입 전 design/impl lane 이 V1 fetch 선이행 + 본 §0 수치 HEAD 재대조 의무 (불일치 시 ArchitectPL escalate) | ⏳ 코드 lane 의무 |
| V2 | engine `src/mctrader_engine/io/` 6 module + `__init__` 존재 | 6 module | `ls src/mctrader_engine/io/` = `cold_reader.py`/`dr_mode.py`/`endpoint_router.py`/`l1_reader.py`/`reader_cache.py`/`tier_reader.py` + `__init__.py` **실측 6 module + __init__** | ✅ 정합 (6 module) |
| V3 | engine `src/` io/ 호출자 = 0 (dead-in-prod) | src caller 0 | **분리 grep 실증**: (A) `from/import mctrader_engine.io` (io/ 디렉터리 제외) = **0건** (rc=1). (B) bare 심볼 `TierReader\|ReaderCache\|EndpointRouter\|DRMode\|ColdReader\|L1Reader` (io/ 제외) = **0건** (rc=1). (C) 전 src/ `mctrader_engine.io` 텍스트 = `io/__init__.py` 내부 6 self-import 만 (외부 src caller 0). **dead-in-prod 확정** | ✅ 정합 (src caller 0 — MCT-170 io/ lesson 재확인: HEAD 실증, gap 가설 아님) |
| V4 | `tests/io/` 7 test = io/ 6 module 사용 | 7 test | `ls tests/io/` = `test_dr_mode`/`test_endpoint_cutover`/`test_l1_reader`/`test_reader_cache_budget`/`test_reader_cache_flush`/`test_reader_perf`/`test_tier_reader` **7 test** + `git grep` 으로 7 test 전수 `from mctrader_engine.io.*` import 확증 | ✅ 정합 (7 test 이전 대상) |
| V5 | io/ 6 module = stdlib only (mctrader_data/mctrader_market import 0) | stdlib only | `git grep -E "import mctrader_data\|from mctrader_data\|import mctrader_market\|from mctrader_market"` (io/ 6 module) = **0건** (rc=1). 각 module top import = `__future__`/`hashlib`/`logging`/`time`/`threading`/`os`/`math`/`uuid`/`dataclasses`/`datetime`/`pathlib`/`collections`/`typing` 만 (stdlib only) | ✅ 정합 (relocate 시 의존 전파 0 — Layer2 자족) |
| V6 | data 측 `src/mctrader_data/io/` 부재 (신규 배치 안전) | 부재 | `ls src/mctrader_data/io/` = **ABSENT**. data 기존 = `aggregation/cold/nas_storage/nas_migration` 등. data src/ io reader 심볼(`TierReader`/`EndpointRouter`/`DRMode`/`L1Reader`) 충돌 grep = **0건** (rc=1). data `tests/io/` 도 **ABSENT** | ⚠️ **주의**: `src/mctrader_data/compactor/reader_cache.py:19` 에 `class ReaderCache(Protocol)` 동명 클래스 기존 존재. **물리 충돌 0** (다른 namespace: `mctrader_data.compactor.reader_cache` vs 신규 `mctrader_data.io.reader_cache`) 이나 **동명 모듈 혼동 risk** → §4.3 Continuity 반영, 설계 lane 에서 namespace 분리 확정 의무 |
| V7 | ADR-029 / ADR-027 io reader 언급 (amendment 대상 정합) | 언급 존재 | ADR-029 = `tier_reader.py`/`dr_mode.py`/`cold_reader.py`/`endpoint_router.py`/`reader_cache.py` 본문 박제(L101/108/110/112/231/245/247/272) + MCT-170 amendment box. ADR-027 = `endpoint_router.py`/`reader_cache.py`/`cold_reader.py` 언급(L441/455, MCT-156/170 amendment) | ✅ 정합 (ADR-029/027 amendment box scope 확정) |

**Phase 0 결론**: V2/V3/V4/V5/V7 = brainstorm 가설 ↔ HEAD 실상 **전수 정합** (MCT-182 류 V5 가설↔실상
불일치 같은 정정 불요 — `git grep` alternation 의 io/ 자체 매치를 분리 검증으로 배제, src caller 0
확정). **V6 = 부분 주의** (data `compactor/reader_cache.py` 동명 `ReaderCache(Protocol)` 기존
존재 — 물리 충돌 0이나 동명 모듈 혼동 risk, 설계 lane namespace 분리 확정 인계). 정정 방향 없음
(설계 escalate 불요), §4.3 Continuity 에 동명 risk 박제.

**MCT-170 io/ lesson 재확인 명문**: engine `io/` 6 module = **MCT-154 LAND(ColdReader/ReaderCache/
EndpointRouter) + MCT-170 LAND(TierReader/L1Reader/DRMode 신규 + reader_cache byte budget 확장)
자산**이다(io/__init__.py docstring 박제 확인). MCT-170 §"Phase 0 verify 발견"= "engine reader
재구현 4 module" 가설 ↔ "io/ 3 module 존재" 실상 불일치를 코드 작업 후 발견한 lesson 의 **본 Story
재확인 형태** = io/ 6 module 이 HEAD 에 실존 + **engine src caller 0 = 미배선 dead-in-prod**.
src caller 0 이 곧 "삭제 후 import 깨짐 0" 의 안전 제거 근거 (AC-4/INV-3). 가설로 수용 금지 —
**§0 박제 수치는 HEAD 실증치**. 단 코드 작업(data#N) 착수 전 V1 git fetch + V3 src caller 0
HEAD 재grep 재대조 의무 (working tree stale ≠ origin HEAD 차단 — MCT-170 lesson reapply).

**R1 가드 cross-document SSOT 정합 self-discipline 명문 (PMO-AUDIT-MCT-182 §4 ADR 후보 + §6.2
reapply 의무)**: 본 Story 의 모든 SSOT 산출물 — Story §본문(특히 §4 DELTA) ↔ scope_manifest
`§story_decision_matrix.MCT-183` + `§design_decisions.D2` ↔ ADR-029/027 amendment box ↔ (설계
lane) Change Plan §3 — 은 **D2 row 기준 1:1 전수 reconcile** 의무다. 1차 FIX 발생 시 정정 산출물
list ↔ 4 산출물 동반 reconcile 체크리스트 의무 (MCT-182 설계리뷰 iter1 F-2 가 scope_manifest 만
정정·Change Plan §4.2 동반 누락 → 구현리뷰 carry 한 cross-document desync 동형 재발 사전 차단).
설계 lane 인계: ADR-029/027 amendment box 의 io reader relocated 박제 ↔ scope_manifest D2
`option_chosen: io-relocate + cold-read-behind-REST` ↔ Change Plan §3 전수 1:1 정합 의무.

## §1 동기 (배경/문제)

사용자 원문: *"data 영역은 mctrader와 아예 분리하고 싶다. 표준 mctrader에는 호출용 interface,
data에는 그를 처리하는 REST API가 있는게 좋겠다. 프로젝트 구조는 mctrader-hub에서도 구현할 수
있고 mctrader-data 단독으로도 필요사항을 반영할 수 있어야겠다."*

**EPIC-data-domain-decoupling Story-2 (sequential_phase 2, milestone target 2/7)**. Epic
종착점 = mctrader-engine 을 (1) data-free (`mctrader_data` python 의존 0) + (2)
exchange-agnostic pure consumer (`mctrader_market_bithumb/upbit/*` 어댑터 직접 의존 0) 로
전환. 사용자 첫 요구 "신규 거래소 data 단독 반영" 의 다중거래소 일반화 + 4-Layer 재설계
(Phase 1 dialog 확정). MCT-182(Layer0 contract relocation) COMPLETED 2026-05-15 →
Layer0 (mctrader-market FOUNDATION) 안정화 LAND prerequisite 충족.

### 문제 (Phase 0 deep-verify 실증)

engine `src/mctrader_engine/io/` 6 module — `tier_reader` / `reader_cache` /
`endpoint_router` / `dr_mode` / `cold_reader` / `l1_reader` — 은 **MCT-154 LAND
(ColdReader/ReaderCache/EndpointRouter 신규) + MCT-170 LAND (TierReader/L1Reader/DRMode
신규 + reader_cache byte budget 확장) 의 NAS cold-tier read 자산**이다 (io/__init__.py
docstring 박제: "ADR-027 D4/D9 직접 owner + ADR-029 D1=C/D4=B/D5=C/D7=A/D8=C 박제").

그러나 Phase 0 재검증(§0 V3) 결과 — engine `src/` 에서 이 6 module 을 import 하는 **production
호출자 = 0** (`from/import mctrader_engine.io` io/ 제외 grep = 0건, bare 심볼 grep = 0건,
`io/__init__.py` 내부 self-import 만 잔존). 즉 **dead-in-prod** (테스트 `tests/io/` 7 test 만
사용). engine 의 실제 cold-read 는 `mctrader_data.storage.scan_candles` /
`orderbook_replay` / `path.resolve_data_root` 직독 (= MCT-185 cutover 대상, 본 Story 범위 외).

문제의 본질: **data-storage 영역(NAS cold-tier read API: tier 라우팅 / endpoint flip /
DR state machine / LRU+TTL cache / L1/L2/L3 specialized reader)이 engine repo 에 거주**한다.
이는 EPIC 4-layer 목표의 **Layer 2 (mctrader-data = DATA-STORAGE 영역 단독 소유) 위반**이다 —
read 도메인은 storage 지식(NAS object layout / parquet tier / ETag verify / endpoint
resolution)을 내포하므로 data Layer2 에 거주해야 한다. dead-in-prod 라 production 무중단으로
물리 이전 가능(src caller 0 = 삭제 후 import 깨짐 0).

### 추출된 동기 (spec §1 확정, 4종)

| why | 내용 |
|---|---|
| 코드 결합도 감소 | engine 이 data-storage read 도메인을 repo 내 보유하는 구조를 끊고, read 도메인을 data Layer2 단독 소유로 이전 |
| 독립 배포·버전 관리 | data read 도메인을 engine 릴리스와 무관하게 배포/버전 관리 (Layer2 자족) |
| 외부·다중 consumer | read 도메인이 data 에 거주해야 engine 외 시스템도 표준 API(MCT-184/185 REST)로 호출 가능 |
| 다중거래소 확장 | data-storage 영역 단일 소유가 신규 거래소 data 단독 반영(D5)의 선행 토대 |

본 Story(MCT-183) = strangler-fig **2단계**. Layer2 read 도메인(io/ 6 module + tests/io/
7 test)을 mctrader-data 로 **물리 이전**하고, engine 측은 dead-in-prod 자산을 안전 제거
(src caller 0 = import 깨짐 0) + `io/__init__.py` export 정리한다. **cold-read 실경로
cutover(engine `mctrader_data.storage.*` 직독 → data REST 경유)는 MCT-185 owner, REST
boundary 신설은 MCT-184 owner — 본 Story 는 io/ 물리 이전 + ADR-029/027 amendment box only**.

## §2 목표 / 비목표

### 목표 (in-scope, D2 + D6)

1. **D2 — Read 도메인 relocation → mctrader-data (Layer 2) — io relocate 부분**
   (`option_chosen: io-relocate + cold-read-behind-REST` 중 io-relocate 절반. cold-read
   cutover = MCT-185)
   - engine `src/mctrader_engine/io/` 6 module (`tier_reader` / `reader_cache` /
     `endpoint_router` / `dr_mode` / `cold_reader` / `l1_reader`) → mctrader-data 물리 이전
     (코드 본문 byte-equivalence — relocate ≠ 재구현)
   - engine `tests/io/` 7 test (`test_dr_mode` / `test_endpoint_cutover` /
     `test_l1_reader` / `test_reader_cache_budget` / `test_reader_cache_flush` /
     `test_reader_perf` / `test_tier_reader`) → mctrader-data 측 이전 + ALL PASS
   - engine `src/mctrader_engine/io/` 6 module + `tests/io/` **삭제** + `io/__init__.py`
     export 정리 (src caller 0 = dead-in-prod 안전 제거 — 삭제 후 import 깨짐 0)
   - data 측 namespace 배치 (설계 lane 확정 — V6 동명 `compactor/reader_cache.py`
     `ReaderCache(Protocol)` 와 분리 namespace 의무)
2. **D6 — ADR-029 / ADR-027 amendment box 박제**
   - `docs/adr/ADR-029-tier-promotion-single-source.md` amendment box — engine NAS
     직독 폐기 **예고** + io reader 6 module = mctrader-data relocated 박제 (실 amend
     confirm = MCT-185 cold-read cutover owner)
   - `docs/adr/ADR-027-cold-tier-object-storage-nas-minio.md` amendment box —
     io reader 6 module (endpoint_router/dr_mode/reader_cache/cold_reader/tier_reader/
     l1_reader) relocated to mctrader-data Layer2 소유 박제
   - **D-row ↔ scope_manifest `§design_decisions.D2` + `§story_decision_matrix.MCT-183`
     1:1 전수 reconcile** (MCT-179 lesson reapply — Out-of-scope/D-row stale 누적 사전
     차단. MCT-182 D-row 7/7 reconcile 패턴 계승)

### 비목표 (out-of-scope — 후속 Story owner 명시)

| 항목 | 사유 | owner Story |
|------|------|-------------|
| engine cold-read 실경로 (`mctrader_data.storage.scan_candles`/`orderbook_replay`/`path.resolve_data_root` 직독) → data REST 경유 cutover | D2 의 cold-read-behind-REST 절반 — REST 신설 전제 | MCT-185 |
| data REST API (`api/` FastAPI — /v1 historical Arrow IPC + reverse-write) 신설 | D3 — io relocate 완료 후 REST wrap | MCT-184 |
| realtime 정규화 stream (Redis Stream) + engine `data_client/` thin client | D3 — REST 신설 전제 | MCT-185 |
| ADR-029/027 **실 amendment confirm** (NAS SoT 경로 data REST indirection LAND) | 본 Story = amendment box(예고+relocated 박제) only | MCT-185 (ADR-029 confirm) |
| io/ 6 module data 측 물리 잔존 → grep0 quad gate (engine src/ `mctrader_data` 0 strict) | D7 — finalize | MCT-188 |
| ADR-030 amendment (engine NAS cred drop + data api service) | D3/D4 — compose topology | MCT-184/186 |
| `mctrader-data` 측 io/ relocated module 의 production wiring (실제 cold-read caller 연결) | 본 Story 는 물리 이전 + tests/io/ green 만 (dead-in-prod → dead-in-data 유지, 배선 = MCT-185) | MCT-185 |

## §3 Risk acceptance / Security-design

> 설계 lane (ArchitectPLAgent) 작성 완료 (2026-05-16). SSOT = Change Plan
> `docs/change-plans/MCT-183-change-plan.md` §7 (security-design) / §7.4
> (operational-risk) / §11 (data migration). 본 §3 = 설계 결정 요약 (Change Plan SSOT 참조).

### §3.1 보안 설계 요약 (SecurityArchitectAgent — 약함 간략 declare)

- **신규 trust boundary 0** — 본 Story = dead-in-prod io/ 6 module 의 cross-repo namespace
  물리 이동 (engine → data) + engine 측 삭제 + import path 재지정. 네트워크 표면 / IPC /
  직렬화 경계 / 외부 입력 처리 경로 **무변화**. attack surface delta = 0.
- io/ 6 module = in-process python 모듈 (NAS read API stdlib only — 외부 입력 파싱 신규 0,
  network/credential 신규 0). relocate ≠ 보안 표면 변경. SecurityArch primary 영역 약함
  (internal module relocation) — 상세 = Change Plan §7.1-§7.7.

### §3.2 운영 리스크 요약 (OperationalRiskArchitectAgent — CONDITIONAL N/A)

- DR / disconnect / clock / rate-limit / env-isolation 5 항목 = **N/A** (본 Story = docs-only
  Phase 1 + dead-in-prod 코드 물리 이전. 신규 service/daemon/connection/scheduler 도입 0,
  runtime topology 무변경). §8.5 spawn-time trigger = **§8.5_active=false** (4 조건 모두 N).
  상세 = Change Plan §7.4.

### §3.3 Risk acceptance

| risk | severity | 처리 |
|------|----------|------|
| R1 (cross-repo land_order desync, 7회째 후보) | HIGH | Change Plan §8.0 Phase 0 Gate (V1 fetch + V3 3-modal HEAD 재grep) + §11 land_order (data 수령 선행 / engine 삭제 후행) + rollback 역순으로 완화. **MCT-183 설계 lane 사전 차단 1건 실증** (§3.4 — io/reader_cache.py stats() lazy engine.metrics 의존, 코드 작업 전 발견) |
| R2 (MCT-41 Live Mode 블락) | HIGH | 본 Story = engine io/ (dead-in-prod, src caller 0) + data namespace 배치 — MCT-41 파일 disjoint 병렬 안전 (scope_manifest §dependency.parallel_safe_with 정합). 본 Story 무관 — MCT-186 owner |

### §3.4 Phase 0 verify 사전 차단 발견 (R1 가드 — 설계 lane 신규 fact, 코드 작업 전)

**설계 lane CodebaseMapper verified-via (`git show origin/main:...reader_cache.py | nl
-ba` 2026-05-16, 설계리뷰 iter1 P0-3 executable hunk 재실측) 신규 발견**:
`mctrader_engine/io/reader_cache.py` `stats()` 메서드 본문 내 producer-wiring **블록
= line 339-348** (주석 1줄[339] + lazy import 6줄[340-345 multi-line ×2] + 빈줄[346]
+ 호출 2줄[347-348]):

```python
339:        # producer wiring (FIX-MCT-180 engine#55 P1): live 값 → Prometheus Gauge.
340-345:    from mctrader_engine.metrics import (set_reader_cache_hit_ratio as _set_hit_ratio)
            from mctrader_engine.metrics import (set_reader_p99_ms as _set_p99_ms)
347-348:    _set_hit_ratio(hit_ratio); _set_p99_ms(p99_ms)
```

- **출처**: FIX-MCT-180 engine#55 P1 producer wiring (MCT-180 LAND 자산). `set_reader_cache_hit_ratio` /
  `set_reader_p99_ms` Gauge setter = engine `src/mctrader_engine/metrics.py:43-79` 정의.
  data 측 동등 Gauge **부재** (data src/ grep 0건).
- **Story §0 V5 미포착 사유**: §0 V5 = io/ 6 module **top-level** `git grep` (stdlib only
  실증). 본 producer-wiring 블록은 `reader_cache.py:319 stats()` **함수 본문 내부**
  (top-level import 아님) → V5 top-level grep 미포착. io/ 6 module 中 `reader_cache.py`
  **단 1곳만 (블록 339-348)** 비-io.* mctrader_engine 의존 (나머지 5 module + reader_cache.py
  339-348 외 = 완전 stdlib only, 6 module 간 cross-import 0건 — `io/__init__.py` 만
  6 module import).
- **byte-equivalence relocate 시 영향**: data `mctrader_data.io.reader_cache.stats()` 가
  `from mctrader_engine.metrics import ...` lazy import → **mctrader-data → mctrader-engine
  역의존** (Layer2 자족 INV-2 위반 + 순환 위험). `tests/io/test_reader_cache_flush.py:215`
  도 동일 engine.metrics 의존.
- **설계 lane 해소 규칙 (Change Plan §3.5 채택안 A SSOT)**: relocate scope 경계 확정 —
  Prometheus Gauge producer wiring 의 data 측 처리. **MCT-183 owner = stats()
  producer-wiring 블록(339-348) 을 외부 import 없는 내부 no-op 블록으로 치환** (채택안
  A — data.metrics 신설 회피, INV-2 grep gate `from mctrader_engine`/`from mctrader_data`
  무위반. data 측 io/ 는 dead-in-data — production caller 0 유지, Gauge emit 은
  MCT-185 cold-read cutover 시 data REST 측 재배선 owner). byte-equivalence 예외 명문화
  (INV-1 import-path-재지정-only 의 producer-wiring 블록 339-348 예외 — Change Plan
  §3.5/§8.1/§8.2). 설계리뷰 iter1 P0-3 정정 ("lazy 2줄 no-op" 구현 불가 명세 → 채택안
  A 블록 치환 + `tests/test_io_stats_no_engine_dep.py` executable 회귀 추가).
- **R1 가드 효과 (7회째 사전 차단)**: docker-stack Phase 0 verify gap 6회 (MCT-170/177/178/179/180)
  + MCT-179→180 metric/producer-path desync 동형 → 본 Story 가 **코드 작업 전 설계 lane
  에서 사전 발견** (MCT-182 R1 가드 패턴 계승 — 코드 영구 영향 전 SSOT 텍스트 정정).
  Story §0 V5 가설(stdlib only)↔실상(stats() lazy engine.metrics 의존) 부분 불일치를
  설계 lane 이 정정 (정정 방향 = byte-equivalence 예외 1줄 명문화, 코드 escalate 불요 —
  graceful no-op 으로 relocate 안전성 보존).

## §4 Cross-repo delta (ChangeImpact — AS-IS → DELTA)

> 출처: spec §3 (AS-IS 그래프) / §4 (TO-BE 4-Layer) + scope_manifest
> `story_decision_matrix.MCT-183.cross_repo` + 요구사항 lane Phase 0 재검증
> (verified-via git grep/`git show HEAD`/`ls`, 2026-05-16 HEAD). ChangeImpact /
> Feasibility / Continuity sub 관점.

### §4.0 관련 코드 경로 (지도 수준)

| repo | 경로 | 현재 책임 | 본 Story 영향 |
|------|------|-----------|----------------|
| mctrader-engine | `src/mctrader_engine/io/tier_reader.py` | facade orchestration (priority chain: cache → NAS L1/L2/L3 → local fallback) — MCT-170 LAND | → data 물리 이전 + engine 삭제 |
| mctrader-engine | `src/mctrader_engine/io/reader_cache.py` | LRU + TTL cache (ADR-027 D9) + byte budget(MCT-170) + explicit flush + verify barrier(S3) | → data 물리 이전 + engine 삭제 |
| mctrader-engine | `src/mctrader_engine/io/endpoint_router.py` | env 기반 endpoint resolution + atomic flip(immutable swap) + 7d grace mode — MCT-154 LAND | → data 물리 이전 + engine 삭제 |
| mctrader-engine | `src/mctrader_engine/io/dr_mode.py` | DR state machine (CLOSED/OPEN/HALF_OPEN/UNKNOWN_TIER) + Prometheus — MCT-170 LAND | → data 물리 이전 + engine 삭제 |
| mctrader-engine | `src/mctrader_engine/io/cold_reader.py` | NAS endpoint cold L2/L3 read API + S6 cross-check (legacy node=DEFAULT mapping) — MCT-154 LAND | → data 물리 이전 + engine 삭제 |
| mctrader-engine | `src/mctrader_engine/io/l1_reader.py` | L1 tier specialized read (prefix tier=L1/, ETag verify) — MCT-170 LAND | → data 물리 이전 + engine 삭제 |
| mctrader-engine | `src/mctrader_engine/io/__init__.py` | 13 심볼 public API export (MCT-154 6 + MCT-170 7) | export 정리 (io/ 전체 삭제 시 `__init__.py` 동반 삭제 또는 빈 패키지 처리 — 설계 lane 확정) |
| mctrader-engine | `tests/io/` (7 test + `__init__.py`) | io/ 6 module 검증 (test_dr_mode/endpoint_cutover/l1_reader/reader_cache_budget/reader_cache_flush/reader_perf/tier_reader) | → data 측 이전 + engine 삭제 |
| mctrader-engine | `src/` 전역 (io/ 제외) | io/ 6 module 호출자 = **0건** (§0 V3 실증) | dead-in-prod 안전 제거 근거 (삭제 후 import 깨짐 0) |
| mctrader-data | `src/mctrader_data/` (io/ ABSENT) | 기존: aggregation/cold/nas_storage/nas_migration 등. io/ 부재 | 신규 io/ 6 module + tests 수령 (충돌 0 — §0 V6) |
| mctrader-data | `src/mctrader_data/compactor/reader_cache.py:19` | `class ReaderCache(Protocol)` (compactor 측 별 추상) — **동명 클래스 기존 존재** | (변경 없음) — 단 신규 `mctrader_data.io.reader_cache` 와 namespace 분리 의무 (§4.3 Continuity) |
| mctrader-hub | `docs/adr/ADR-029-tier-promotion-single-source.md` | engine io/ reader 6 module 본문 박제 (L101/108/110/112/231/245/247/272) | amendment box 추가 (io reader relocated + NAS 직독 폐기 예고) |
| mctrader-hub | `docs/adr/ADR-027-cold-tier-object-storage-nas-minio.md` | endpoint_router/reader_cache/cold_reader 언급 (L441/455, MCT-156/170 amendment) | amendment box 추가 (io reader 6 module (endpoint_router/dr_mode/reader_cache/cold_reader/tier_reader/l1_reader) relocated to mctrader-data Layer2) |

### §4.1 AS-IS (engine io/ 본 Story 대상 — spec §3 그래프 인용)

spec §3 그래프: `engine io/ : stdlib-only, mctrader_data 0, engine src/ caller 0 → 미배선`.
본 Story 대상 = engine→data 14 import 줄 中 **io/ dead-in-prod 자산** (storage.scan_candles
×3 / orderbook_replay ×2 / paper_storage write ×1 / nas_uploader ×1 = MCT-185 cutover
대상, 비대상):

```
 mctrader-engine (Layer2' 진입 전 — io/ dead-in-prod 보유)
   src/mctrader_engine/io/  (6 module + __init__, stdlib only, src caller 0)
     ├ tier_reader.py     ── facade orchestration (MCT-170)
     ├ reader_cache.py    ── LRU+TTL+byte budget (MCT-154+170)
     ├ endpoint_router.py ── env endpoint + atomic flip + 7d grace (MCT-154)
     ├ dr_mode.py         ── DR state machine (MCT-170)
     ├ cold_reader.py     ── NAS cold L2/L3 read API (MCT-154)
     └ l1_reader.py       ── L1 specialized read (MCT-170)
   tests/io/  (7 test + __init__)  ── 유일 사용처 (production caller 0)

 본 Story 대상 = io/ 6 module + tests/io/ 7 test 물리 이전
 비대상 (MCT-185): engine cold-read 실경로 mctrader_data.storage.scan_candles ×3 /
   orderbook_replay ×2 / paper_storage.write ×1 / nas_uploader ×1 (REST cutover)
```

### §4.2 구현 가능성 평가 (FeasibilityAgent 관점)

| 차원 | 평가 | 근거 |
|------|------|------|
| dead-in-prod 안전 제거 실현성 | **HIGH** | §0 V3: engine src/ io/ 호출자 0 (3-modal grep 실증 — module import 0 + bare 심볼 0 + io/ 내부 self-import 만). 삭제 후 production import 깨짐 0 (test 만 동반 이전) |
| relocate 결합 전파 위험 | **LOW (예외 1건 — 설계 lane 해소)** | §0 V5: io/ 6 module **top-level** import = stdlib only (`mctrader_data`/`mctrader_market` import 0). **단 설계 lane §3.4 발견: `reader_cache.py` stats() 함수 본문 내 lazy `from mctrader_engine.metrics import set_reader_cache_hit_ratio, set_reader_p99_ms`** (V5 top-level grep 미포착 — FIX-MCT-180 engine#55 P1 producer wiring) → byte-equiv relocate 시 data → engine 역의존. **해소 = Change Plan §3.5 채택안 A** (외부 import 없는 내부 no-op 블록 치환, Layer2 자족 INV-2 보존, Gauge 재배선 = MCT-185). 나머지 5 module + reader_cache.py 의 339-348 외 = 완전 stdlib only |
| data 측 배치 충돌 | **LOW** | §0 V6: data `src/mctrader_data/io/` 부재 + io reader 심볼 충돌 0 + data `tests/io/` 부재. 단 `compactor/reader_cache.py` 동명 `ReaderCache(Protocol)` 기존 → namespace 분리로 해소 (설계 lane) |
| byte-equivalence 보존 | **HIGH** | relocation = 코드 본문 무변경 (재구현 0). io/ 6 module byte-equivalence + tests/io/ 7 test green = relocation Story 핵심 안전 invariant (MCT-182 byte-equiv 안전망 패턴 reapply, PMO-AUDIT-MCT-182 §3 패턴 #3) |
| cross-repo 순서 위험 (R1) | **MEDIUM** | land_order(hub P1→data#N→engine#N→hub P2) 엄수 + Phase 0 verify 독립 게이트. data#N(수령) 선행 → engine#N(삭제) 후행 — data 측 io/ green 확인 후 engine 삭제 (역순 시 일시적 양측 부재 window 0 보장) |
| ADR amendment scope 정확성 | **HIGH** | §0 V7: ADR-029(L101/108/110/112 등 io reader 6 module 본문 박제) + ADR-027(L441/455 endpoint_router/cache 언급) — amendment box 대상 정합 실증 |

**Feasibility 종합: 구현 가능성 HIGH.** dead-in-prod (src caller 0) + (top-level)
stdlib only + data namespace 충돌 0 의 3중 안전 조건 전수 HEAD 실증. **설계 lane §3.4
발견 1건** (reader_cache.py stats() lazy engine.metrics — V5 top-level grep 미포착분)
= Change Plan §3.5 채택안 A (외부 import 없는 내부 no-op 블록 치환) 로 해소 — 코드
escalate 불요 (relocate 안전성 보존, dead-in-data no-op 동작 영향 0). 유일 risk 축 =
R1 (cross-repo desync) — Phase 0 게이트(§0) + land_order(data 수령 선행 / engine 삭제
후행) 로 완화. namespace 분리 + producer-wiring no-op 확정 = 설계 lane 인계 완료.

### §4.3 이전 작업 연속성 분석 (ContinuityAgent — ADR-029/027/031 충돌·의존)

| 과거 ADR / 상태 | 본 Story 접촉 여부 | 충돌·의존 판정 |
|----------|--------------------|----------------|
| ADR-029 (tier-promotion-single-source, NAS SoT all-tier — engine io/ 6 module 본문 박제 owner) | **접촉** — io/ 6 module 이 ADR-029 §D1=C/D4=B/D5=C/D7=A/D8=C impl carrier(io/__init__ docstring) → 물리 이전 시 ADR-029 의 "engine io/ 신규" 박제(L108/110/112 등) 가 stale | **충돌 아님 (relocate 정합)** — io/ 6 module 의 동작·invariant 무변경, 거주 repo 만 engine→data. ADR-029 amendment box 로 "io reader relocated to mctrader-data + engine NAS 직독 폐기 예고" 박제. 실 NAS 직독 폐기 confirm = MCT-185 (cold-read cutover) owner. **MCT-170 amendment box (dr_mode.UNKNOWN_TIER/cutoff enforcement) = 동작 무변경, 거주만 이전 — invariant 보존** |
| ADR-027 (cold-tier-object-storage-nas-minio — endpoint_router/cache/cold_reader 언급) | **접촉** — L441(MCT-156 reader mixed layout) + L455(MCT-170 impl scope = `mctrader_engine/io/tier_reader.py`) 가 engine io/ path 명시 → 거주 이전 시 path 박제 stale | **충돌 아님 (relocate 정합)** — reader mixed layout 호환(ADR-009 §D2.1/§D14 fallback) 동작 무변경. ADR-027 amendment box 로 "io reader 6 module (endpoint_router/dr_mode/reader_cache/cold_reader/tier_reader/l1_reader) relocated to mctrader-data (Layer2 소유)" 박제 (endpoint_router/dr_mode = ADR-027 §D9 명시 module, 나머지 4 = io/ 묶음 relocate — scope_manifest §planned_adrs.amendments ADR-027 ↔ ADR-027 box ↔ Change Plan §3.6 row 4 ↔ 본 row 6-module 전체 byte 1:1, P0-1 reconcile). MCT-156 amendment "io/ 3 module 영향 0" = 거주 이전 후에도 동작 영향 0 정합 |
| ADR-031 (data-domain-decoupling — Status: Accepted, MCT-182 §D1 VERIFIED) | **접촉** — §D2 `io-relocate + cold-read-behind-REST` owner = MCT-183(io relocate)+MCT-185(cutover) | **정합 (D2 절반 진행)** — 본 Story = §D2 io-relocate 절반. §D2 VERIFIED 는 MCT-185 cold-read cutover LAND 후 (본 Story 는 §D2 부분 진행 — amendment box 박제, VERIFIED 아님). scope_manifest D2 owner_story `MCT-183 (io relocate) + MCT-185 (cold-read cutover)` 1:1 정합 |
| MCT-170 io/ lesson (3 module 존재 재인지 — 코드 작업 후 발견) | 본 Story 핵심 reapply 대상 | **lesson 계승·강화** — MCT-170 = 가설↔실상 불일치 사후 발견. 본 Story = io/ 6 module HEAD 실존 + src caller 0 을 **요구사항 lane 에서 사전 실증** (§0 V2/V3). dead-in-prod 안전 제거 근거 = src caller 0 grep — MCT-170 lesson 의 사전 차단형 reapply (PMO-AUDIT-MCT-182 §3 패턴 #1 7회째 사전 차단 계승) |
| MCT-182 (Layer0 contract relocation, COMPLETED) | **의존 (prerequisite)** | MCT-182 LAND = Layer0(market FOUNDATION) 안정화 → 본 Story 의 io relocate 가 안정 Layer0 위 진행. RETRO-MCT-182 §6 carry #5 (`paper_storage.py` 등 data 내부 사용처 cleanup, owner=MCT-183) = 본 Story 범위 재확인 (단 cold-read cutover 본체는 MCT-185 — carry #5 는 io relocate 부수 cleanup 한정) |
| data `compactor/reader_cache.py` `ReaderCache(Protocol)` 동명 기존 (§0 V6) | **접촉 (동명 risk)** | **충돌 0 (namespace 분리)** — `mctrader_data.compactor.reader_cache.ReaderCache`(Protocol 추상) vs 신규 `mctrader_data.io.reader_cache.ReaderCache`(LRU+TTL 구현체)는 다른 namespace → 물리 충돌 0. 단 동명 모듈 혼동 risk → **설계 lane namespace 분리 확정 의무** (io/ 서브패키지 명시 배치, compactor Protocol 무변경) |

**Continuity 종합**: MCT-183 은 과거 ADR(029/027)의 결정·invariant 와 **충돌 0** — io/ 6 module
동작·invariant(DR state machine / endpoint flip / LRU byte budget / ETag verify / mixed
layout 호환) 전수 무변경, **거주 repo 만 engine→data**. ADR-029/027 amendment box 는 io
reader relocated + engine NAS 직독 폐기 **예고** (실 amend confirm = MCT-185). ADR-031 §D2
는 본 Story 가 io-relocate 절반 진행 (§D2 VERIFIED = MCT-185 후). 유일 주의 = data
`compactor/reader_cache.py` 동명 — namespace 분리로 충돌 0, 설계 lane 확정 인계.

### §4.4 DELTA (TO-BE — spec §4 4-Layer 인용 + scope_manifest cross_repo)

```
 Layer 2 ─ mctrader-data (DATA-STORAGE 영역 단독 소유)
   ◀ RELOCATE (MCT-183):
     • src/mctrader_data/io/ (신규 서브패키지 — 설계 lane namespace 확정)
         ← engine src/mctrader_engine/io/ 6 module (byte-equivalence 이전)
         tier_reader / reader_cache / endpoint_router / dr_mode / cold_reader / l1_reader
       (V6 주의: data compactor/reader_cache.py ReaderCache(Protocol) 와 분리 namespace)
     • tests/io/ (신규) ← engine tests/io/ 7 test 이전 (ALL PASS)
   상태: dead-in-prod → dead-in-data (production wiring = MCT-185 cold-read cutover)
 Layer 2'─ mctrader-engine (PURE CONSUMER 진행)
     • src/mctrader_engine/io/ 6 module + __init__.py 삭제 (src caller 0 안전 제거)
     • tests/io/ 7 test + __init__.py 삭제
     • io/__init__.py export 정리 (패키지 전체 삭제 — 설계 lane 확정)
 ADR governance (mctrader-hub):
     • ADR-029 amendment box ← io reader 6 module relocated + engine NAS 직독 폐기 예고
     • ADR-027 amendment box ← io reader 6 module (endpoint_router/dr_mode/reader_cache/cold_reader/tier_reader/l1_reader) relocated to data (Layer2)
 순환: 영원히 없음 (io/ stdlib only — data 이전 후 mctrader_data 내부 의존 신규 0)
```

| repo | 변경 유형 | 파일 | 내용 |
|------|-----------|------|------|
| mctrader-hub | CREATE | `docs/stories/MCT-183.md` | 본 Story §0-§11 |
| mctrader-hub | MODIFY | `docs/adr/ADR-029-tier-promotion-single-source.md` | amendment box — io reader 6 module relocated to mctrader-data + engine NAS 직독 폐기 **예고** (실 confirm = MCT-185) |
| mctrader-hub | MODIFY | `docs/adr/ADR-027-cold-tier-object-storage-nas-minio.md` | amendment box — io reader 6 module (endpoint_router/dr_mode/reader_cache/cold_reader/tier_reader/l1_reader) relocated to mctrader-data Layer2 소유 (endpoint_router/dr_mode = ADR-027 §D9 명시 module, 나머지 4 = io/ 묶음 relocate — P0-1 reconcile) |
| mctrader-hub | MODIFY | `scope_manifests/EPIC-data-domain-decoupling.yaml` | MCT-183 status RESERVED→IN_PROGRESS + D2 D-row reconcile 확인 |
| mctrader-hub | MODIFY | `CLAUDE.md` | `## EPIC-data-domain-decoupling` MCT-183 IN_PROGRESS 박제 |
| mctrader-hub | MODIFY | `.codeforge/counters.json` | MCT-183 RESERVED→IN_PROGRESS |
| mctrader-data | CREATE | `src/mctrader_data/io/` (6 module + 패키지 `__init__.py`, namespace 설계 lane 확정) | engine io/ 6 module byte-equivalence 수령 (`tier_reader`/`reader_cache`/`endpoint_router`/`dr_mode`/`cold_reader`/`l1_reader`) |
| mctrader-data | CREATE | `tests/io/` (7 test + `__init__.py`) | engine tests/io/ 7 test 이전 (import path → `mctrader_data.io.*` 재지정, 로직 무변경) + ALL PASS |
| mctrader-engine | DELETE | `src/mctrader_engine/io/` (6 module + `__init__.py`) | dead-in-prod 안전 제거 (src caller 0 verified) |
| mctrader-engine | DELETE | `tests/io/` (7 test + `__init__.py`) | io/ 6 module 동반 삭제 (유일 사용처) |
| mctrader-engine | MODIFY (없을 시 N/A) | `src/mctrader_engine/io/__init__.py` 참조처 | io 패키지 export 정리 — io/ 전체 삭제 시 동반. engine 내 `from mctrader_engine.io import *` 잔존 grep 0 확인 (§0 V3 정합 — 설계 lane 재확인) |
| mctrader-hub | MODIFY (Phase 2 PR2) | Story §9/§10/§11 + ADR-029/027 amendment box VERIFIED + scope_manifest milestone 2/7 + CLAUDE.md COMPLETED + RETRO | 박제 |

**land_order**: `hub Phase1(docs) → data#N(land_order 1 — io/ 6 module + tests/io/ 수령) →
engine#N(land_order 2 — io/ + tests/io/ 삭제 + __init__ 정리) → hub Phase2 PR2(박제)`.
각 PR CI green 후 admin merge → 다음 PR. **data 수령 선행 / engine 삭제 후행 엄수**
(역순 시 일시적 양측 io/ 부재 window 발생 — dead-in-prod 라 production 무영향이나
land_order 정합 의무, R1 가드).

## §5 Acceptance Criteria

> 요구사항 lane self-write. spec §1 동기 4종 + scope_manifest
> `story_decision_matrix.MCT-183` (D2+D6) 충족 기준. Given/When/Then.

### AC-1 — engine io/ 6 module data 물리 이전 + import 경로 정합 (D2)

- **Given** engine `src/mctrader_engine/io/` 6 module (`tier_reader`/`reader_cache`/
  `endpoint_router`/`dr_mode`/`cold_reader`/`l1_reader`) 이 mctrader-data 측
  (`src/mctrader_data/io/`, namespace 설계 lane 확정) 으로 물리 이전됨
- **When** data 측에서 `from mctrader_data.io import TierReader, ReaderCache,
  EndpointRouter, DRMode, ColdReader, L1Reader` (+ 부속 `ReadResult`/`TierReadResult`/
  `L1ReadResult`/`CacheEntry`/`CacheFlushResult`/`EndpointFlipResult`/`GraceModeState`)
  import + 6 module 내부 cross-import 경로 검증
- **Then**
  - 6 module 코드 본문 = engine 측 원본과 **byte-equivalence** (재구현 0 — INV-1).
    **유일 예외 = `reader_cache.py` stats() producer-wiring 블록** (engine 원본
    line 339-348: `from mctrader_engine.metrics import set_reader_cache_hit_ratio,
    set_reader_p99_ms` lazy import + `_set_hit_ratio`/`_set_p99_ms` 호출) → data 측
    외부 import 없는 **내부 no-op 블록 치환** (Layer2 자족 INV-2 + dead-in-data —
    Gauge 실 emit 재배선 = MCT-185 cutover owner. Change Plan §3.5 SSOT, 설계리뷰
    iter1 P0-2/P0-3 정정)
  - 6 module 간 내부 cross-import = **0건** (설계 lane 실증 — 6 module 서로 import
    안 함, `io/__init__.py` 만 6 module import) → io/ 6 module 본문 import 재지정
    **불요**. `io/__init__.py` 6 self-import 줄만 (`from mctrader_engine.io.X` →
    `from mctrader_data.io.X`) 재지정 — `import mctrader_engine.io`/`from
    mctrader_engine.io` 잔존 0건
  - `import mctrader_data.io` 시 `mctrader_market`/`mctrader_data`/`mctrader_engine`
    외부 패키지 신규 의존 0 (stdlib only 유지 — INV-2. reader_cache.stats()
    producer-wiring 블록 = 외부 import 없는 no-op 치환으로 engine.metrics 역의존
    제거 — Change Plan §3.5 채택안 A)
  - data 측 `io/__init__.py` public API export = engine 원본 `io/__init__.py` 13 심볼과 동일

### AC-2 — tests/io/ 7 test data 이전 + ALL PASS (D2)

- **Given** engine `tests/io/` 7 test (`test_dr_mode`/`test_endpoint_cutover`/
  `test_l1_reader`/`test_reader_cache_budget`/`test_reader_cache_flush`/
  `test_reader_perf`/`test_tier_reader`) 가 mctrader-data 측으로 이전됨
- **When** data 측 `tests/io/` 7 test 의 import path 를 `mctrader_data.io.*` 로 재지정
  (테스트 로직 무변경) 후 data full suite 회귀
- **Then**
  - data 측 `tests/io/` 7 test **ALL PASS** (테스트 로직 byte-equivalence — import path
    재지정만 — INV-4)
  - `test_reader_perf` 의 NFR 측정(MCT-170 D7: hit_ratio≥0.95 / p99<100ms) = 이전과
    동등 verdict (relocate ≠ 성능 변경)
  - data full suite 회귀 신규 실패 0 (io/ 6 module 신규 수령이 기존 data 테스트 무영향
    — V6 namespace 충돌 0 실증)

### AC-3 — engine io/ 6 module + tests/io/ 삭제 + __init__ export 정리 (D2)

- **Given** engine `src/mctrader_engine/io/` 6 module + `__init__.py` + `tests/io/`
  7 test + `__init__.py` 삭제
- **When** engine 측 `git rm -r src/mctrader_engine/io/ tests/io/` 후 engine full suite 회귀
- **Then**
  - `ls src/mctrader_engine/io/` = **부재** (디렉터리 삭제)
  - `ls tests/io/` = **부재** (디렉터리 삭제)
  - engine `io/__init__.py` 참조 — engine src/ 내 `from mctrader_engine.io import`/
    `import mctrader_engine.io` 잔존 grep = **0건** (§0 V3 정합 — 패키지 전체 삭제 후
    dangling import 0)
  - engine full suite 회귀 신규 실패 0 (src caller 0 = 삭제 후 import 깨짐 0 — AC-4 연동)

### AC-4 — engine src caller 0 유지 (dead-in-prod 안전 제거 실증) (D2)

- **Given** engine io/ 6 module 삭제 완료
- **When** engine `src/` 전수 grep — io/ 6 module 호출 흔적 검사 (3-modal: module
  import + bare 심볼 + 텍스트)
- **Then**
  - `git grep -E "from mctrader_engine\.io|import mctrader_engine\.io"
    -- 'src/**/*.py'` (mctrader-engine) == **0건** (모듈 import 호출자 0)
  - `git grep -E "\b(TierReader|ReaderCache|EndpointRouter|DRMode|ColdReader|L1Reader)\b"
    -- 'src/**/*.py'` (mctrader-engine, io/ 삭제 후 전 src/) == **0건** (bare 심볼 0)
  - engine full suite 신규 실패 0 + import error 0 (dead-in-prod 안전 제거 실증 —
    삭제 전 src caller 0 = 삭제 후 import 깨짐 0, MCT-170 lesson 사전 차단형 reapply)

### AC-5 — ADR-029/027 amendment box 박제 + D-row ↔ scope_manifest 1:1 (D6)

- **Given** `docs/adr/ADR-029-tier-promotion-single-source.md` +
  `docs/adr/ADR-027-cold-tier-object-storage-nas-minio.md` amendment box 작성
- **When** ADR-029/027 amendment box 와 `scope_manifests/EPIC-data-domain-decoupling.yaml`
  `§design_decisions.D2` + `§planned_adrs.amendments` + `§story_decision_matrix.MCT-183`
  를 대조
- **Then**
  - ADR-029 amendment box = "engine io/ reader 6 module relocated to mctrader-data
    (Layer2) + engine NAS 직독 폐기 **예고** (실 amend confirm = MCT-185 cold-read
    cutover owner)" 박제 (io/ 6 module 동작·invariant 무변경 — relocate 명시)
  - ADR-027 amendment box = "io reader 6 module (endpoint_router/dr_mode/reader_cache/
    cold_reader/tier_reader/l1_reader) relocated to mctrader-data Layer2 소유" 박제
    (endpoint_router/dr_mode = ADR-027 §D9 명시 module, 나머지 4 = io/ 묶음 relocate.
    MCT-156 amendment "io/ 3 module 영향 0" = 거주 이전 후에도 동작 영향 0 정합 명시)
  - scope_manifest `§planned_adrs.amendments` ADR-029 owner `MCT-183 (relocate) +
    MCT-185 (cutover confirm)` ↔ ADR-027 owner `MCT-183 (io reader 6 module
    relocate)` ↔ amendment box 박제 **1:1 정합** (P0-1 reconcile — 4 산출물 6-module
    명시 byte 동일)
  - scope_manifest `§design_decisions.D2` (`option_chosen: io-relocate +
    cold-read-behind-REST` / `owner_story: MCT-183 (io relocate) + MCT-185
    (cold-read cutover)`) ↔ 본 Story §2 목표 + §4 DELTA **1:1 정합** (MCT-179
    Out-of-scope stale 사전 차단 lesson reapply — MCT-182 D-row reconcile 패턴 계승)

## §6 Invariants

> 요구사항 lane self-write. 본 Story 구현 중 위반 시 즉시 중단.

| INV | 내용 | 출처 | 검증 방법 |
|-----|------|------|----------|
| INV-1 | io/ 6 module 코드 byte-equivalence — relocate ≠ 재구현 (코드 본문 무변경, import path cross-reference 재지정만). **유일 예외 = `reader_cache.py` stats() producer-wiring 블록 (engine 원본 line 339-348)** → data 측 외부 import 없는 내부 no-op 블록 치환 (Change Plan §3.5 채택안 A, 설계리뷰 iter1 P0-2/P0-3 정정) | D2 (relocate ≠ 동작 변경) | engine 원본(`origin/main` 고정 tree, §0 V1 fetch pin) ↔ data 측 6 module diff = `io/__init__.py` import path 6줄 + `reader_cache.py` stats() producer-wiring 블록(339-348 range) 외 0 (byte-equivalence). P1-1: 비교 source = engine#N 삭제 전 data#N 시점 `origin/main` 고정 hash 대비 (Change Plan §8.1/§8.2) |
| INV-2 | io/ 6 module stdlib only 유지 — data 이전 후 `mctrader_data`/`mctrader_market`/`mctrader_engine` 내부 의존 신규 0 (Layer2 자족 — reader_cache.stats() producer-wiring 블록 = 외부 import 없는 no-op 치환으로 engine.metrics 역의존 제거) | spec §3 그래프 / §0 V5 / 설계 lane §3.5 발견 | `git grep -E "import mctrader_data\|from mctrader_data\|import mctrader_market\|from mctrader_market\|import mctrader_engine\|from mctrader_engine"` (data `src/mctrader_data/io/`) == 0건 + clean subprocess `import mctrader_data.io.reader_cache; sys.modules` 에 `mctrader_engine` 부재 |
| INV-3 | engine src caller 0 영구 (dead-in-prod 안전 제거 — 삭제 후 import 깨짐 0) | §0 V3 / spec §2 사실5 | engine `src/` (io/ 삭제 후) `from/import mctrader_engine.io` + bare 심볼 grep == 0건 + full suite import error 0 |
| INV-4 | tests/io/ 7 test green 유지 — 테스트 로직 byte-equivalence (import path 재지정만, NFR verdict 동등) | D2 (relocate 안전망) | data 측 `tests/io/` 7 test ALL PASS + `test_reader_perf` NFR(hit_ratio≥0.95/p99<100ms) 이전 동등 verdict |
| INV-5 | io/ 6 module 동작·invariant 무변경 — DR state machine(CLOSED/OPEN/HALF_OPEN/UNKNOWN_TIER) / endpoint atomic flip / LRU byte budget / ETag verify / mixed layout 호환 거주 이전 후에도 보존 | §4.3 Continuity (ADR-029/027 충돌 0) | tests/io/ 7 test 의 state machine·flip·budget·verify 검증 케이스 ALL PASS (동작 회귀 0) |
| INV-6 | data `compactor/reader_cache.py` `ReaderCache(Protocol)` 무변경 (동명 risk — namespace 분리, compactor 측 추상 비접촉) | §0 V6 / §4.3 Continuity | `compactor/reader_cache.py` diff = 0 + data compactor 테스트 회귀 신규 실패 0 |

## §7 Dependencies / 설계 결정 상세

> 설계 lane (ArchitectPLAgent) 작성 완료 (2026-05-16). 설계 결정 상세 SSOT =
> Change Plan `docs/change-plans/MCT-183-change-plan.md` §1-§12. 본 §7 = Dependencies +
> 설계 lane 확정 사항 (§7.2 = Change Plan §3 cross-ref).

### §7.1 Dependencies

| 의존 | 상태 | 내용 |
|------|------|------|
| MCT-182 (Layer0 contract relocation) | COMPLETED 2026-05-15 | Layer0(market FOUNDATION) 안정화 LAND — 본 Story io relocate 의 안정 토대 prerequisite |
| EPIC-data-domain-decoupling brainstorm | COMPLETED 2026-05-16 | spec/scope_manifest 2종 SSOT 확정 (Phase 0 deep-verify + Codex review) |
| ADR-031 (data-domain-decoupling, Status: Accepted) | MCT-182 §D1 VERIFIED LAND | 본 Story = §D2 io-relocate 절반 진행 (§D2 VERIFIED = MCT-185 cutover 후) |
| ADR-029 / ADR-027 (io reader 6 module 본문 박제 owner) | 기존 존재 (§0 V7 verify) | amendment box 추가 대상 — io reader relocated 박제 (실 amend confirm = MCT-185) |
| engine io/ 6 module dead-in-prod (src caller 0) | §0 V3 HEAD 실증 | 안전 제거 근거 — 코드 작업 전 V1 fetch + V3 HEAD 재grep 재대조 의무 |
| 선행 Story | MCT-182 (`sequential_depends_on: [MCT-182]`) | sequential_phase 1 LAND prerequisite 충족 |
| 후행 Story | MCT-184 (data REST API 신설 — io relocate 완료 후 REST wrap) | sequential_phase 3 |

### §7.2 설계 lane 확정 사항 (ArchitectPLAgent — Change Plan §3 SSOT)

> 요구사항 lane seed 6 항목 → 설계 lane 확정 결과. 상세 = Change Plan §3 (도입할 설계).

1. **Phase 0 V1/V3 재대조 의무 — Change Plan §8.0 Phase 0 Gate 명문화 확정**: 코드 작업
   (data#N) 착수 전 V1 `git fetch origin` 3 repo + V3 3-modal HEAD 재grep gate. 설계 lane
   에서 origin/main HEAD V1-V6 전수 재대조 실행 완료 (2026-05-16) — V2/V3/V4 정합,
   **V5 부분 불일치 1건 사전 발견** (io/reader_cache.py producer-wiring 블록 line
   339-348 stats() engine.metrics 의존, §3.4) → §3.5 채택안 A (외부 import 없는 내부
   no-op 블록 치환) 명문화로 정정 (코드 escalate 불요. 설계리뷰 iter1 P0-3 정정 —
   §8.0 V-pin source pin 추가).
2. **data 측 io/ namespace 분리 확정 (V6 동명 risk) — `mctrader_data.io` 서브패키지**:
   신규 io/ 6 module = `src/mctrader_data/io/` 서브패키지 (`mctrader_data.io.*`) 명시
   배치. `mctrader_data.compactor.reader_cache.ReaderCache(Protocol)` (compactor 측 추상
   — `@runtime_checkable Protocol`, get/put/invalidate) 와 **다른 namespace → 물리 충돌
   0**. 신규 `mctrader_data.io.reader_cache.ReaderCache` = LRU+TTL 구현체 (engine 원본
   byte-equiv). compactor Protocol **무변경 (INV-6)**. 6 module 내부 cross-import = **0건**
   (설계 lane 실증 — 6 module 서로 import 안 함, `io/__init__.py` 만 6 module import) →
   byte-equiv relocate 시 6 module 본문 import 재지정 **불요**, `io/__init__.py` 만
   `from mctrader_engine.io.X` → `from mctrader_data.io.X` 6줄 재지정 (Change Plan §3.3).
3. **byte-equivalence 경계 + 예외 1건 (Change Plan §3.5 채택안 A SSOT)**: io/ 6 module
   코드 본문 무변경 (재구현 0, INV-1). `io/__init__.py` 6 self-import 줄만 재지정 +
   `reader_cache.py` `stats()` producer-wiring **블록 line 339-348** (`from
   mctrader_engine.metrics import set_reader_cache_hit_ratio, set_reader_p99_ms` lazy
   import + `_set_hit_ratio`/`_set_p99_ms` 호출) = **byte-equivalence 예외 1건** →
   data 측 **외부 import 없는 내부 no-op 블록 치환** (채택안 A — `try/except
   ImportError` 회피, INV-2 grep gate 무위반. dead-in-data production caller 0 유지,
   Gauge 실 emit 재배선 = MCT-185 cold-read cutover owner) + `tests/test_io_stats_no_engine_dep.py`
   executable 회귀 추가 (engine.metrics 미설치 시 stats() NameError/ImportError 0).
   tests/io/ 7 test 도 import path 재지정만 (`test_reader_cache_flush.py:215`
   `test_stats_emits_prometheus_gauges` 는 data 측 skip/정정 동반 — INV-4 NFR verdict
   동등 보존, `test_reader_perf` 는 engine.metrics 비의존 직접 계산).
4. **land_order 정합 (data 수령 선행 / engine 삭제 후행) — Change Plan §11.3 확정**:
   `hub Phase1 → data#N (order 1, io/ 6 module + tests/io/ 수령 + green) → engine#N
   (order 2, io/ + tests/io/ 삭제 + __init__ 삭제) → hub Phase2 PR2`. 역순 금지 (일시적
   양측 부재 window 차단). rollback = land_order 역순 (engine 복구 → data 삭제).
5. **ADR amendment box scope (D-row 1:1 reconcile) — 박제 완료**: ADR-029 MCT-183
   amendment box (io reader 6 module relocated + engine NAS 직독 폐기 예고, 실 confirm
   =MCT-185) + ADR-027 MCT-183 amendment box (io reader 6 module (endpoint_router/
   dr_mode/reader_cache/cold_reader/tier_reader/l1_reader) relocated to mctrader-data
   Layer2) ↔ scope_manifest `§planned_adrs.amendments` +
   `§design_decisions.D2` + `§story_decision_matrix.MCT-183` **전수 1:1 정합** (Change
   Plan §3.6 reconcile 표 — MCT-182 D-row 7/7 패턴 계승).
6. **io/__init__.py 삭제 처리 확정 — engine io 패키지 소멸 (빈 패키지 잔존 아님)**:
   engine `src/mctrader_engine/io/` 전체 (6 module + `__init__.py`) + `tests/io/`
   (7 test + `__init__.py`) **물리 삭제** (`git rm -r`). `io/__init__.py` = 6 module
   re-export only (13 심볼) → 6 module 삭제 시 동반 삭제 (빈 패키지 잔존 무의미 —
   src caller 0 = dangling import 0, §0 V3 정합). engine 내 `from mctrader_engine.io
   import *` 잔존 grep 0 = 삭제 안전 (Change Plan §8.4 grep gate).

## §8 Test contract

> **설계 lane TBD** — ArchitectPLAgent Change Plan §8 Test Contract SSOT 작성 영역.
> 요구사항 lane seed: §6 INV-1~INV-6 (byte-equivalence / stdlib only / src caller 0 영구
> / tests green / 동작 무변경 / compactor Protocol 무변경) + §5 AC-1~AC-5 가 Test
> Contract seed. Perf Baseline = N/A 추정 (relocation — perf 무영향, `test_reader_perf`
> NFR 은 이전 동등 verdict 확인만 — 설계 lane §8.5 spawn-time trigger 확정 의무).

## §8.5 Impl Manifest (DeveloperPL self-write — CFP-39)

> **DeveloperPLAgent 작성 (2026-05-16)** — 2 repo Phase 2 PR1 전수 LAND 후 git diff 분석 결과.
> Change Plan §3/§5/§8 매핑 SSOT.

### §8.5.1 data#70 (land_order 1) — LAND commit: 0e6f35b0

| 파일 | 변경 유형 | Change Plan §절 | AC/INV 매핑 | 비고 |
|------|-----------|-----------------|-------------|------|
| `src/mctrader_data/io/__init__.py` | CREATE | §3.2 (13 심볼 export, self-import 6줄 → mctrader_data.io.X 재지정) | AC-1, INV-1 | CacheEntry/CacheFlushResult/ColdReader/DRMode/EndpointFlipResult/EndpointRouter/GraceModeState/L1ReadResult/L1Reader/ReadResult/ReaderCache/TierReadResult/TierReader |
| `src/mctrader_data/io/cold_reader.py` | CREATE | §3.1 (byte-equivalent relocate — NAS cold L2/L3 read API) | AC-1, INV-1, INV-2 | 319 lines — MCT-154 LAND 자산. stdlib only, mctrader_engine.* 0 |
| `src/mctrader_data/io/dr_mode.py` | CREATE | §3.1 (byte-equivalent relocate — DR state machine) | AC-1, INV-1, INV-2 | 256 lines — MCT-170 LAND 자산. SIM105 fix (contextlib.suppress 3곳) + contextlib import 추가 (ruff gate 충족) |
| `src/mctrader_data/io/endpoint_router.py` | CREATE | §3.1 (byte-equivalent relocate — env endpoint + atomic flip + 7d grace) | AC-1, INV-1, INV-2 | 442 lines — MCT-154 LAND 자산. stdlib only, mctrader_engine.* 0 |
| `src/mctrader_data/io/l1_reader.py` | CREATE | §3.1 (byte-equivalent relocate — L1 specialized read) | AC-1, INV-1, INV-2 | 181 lines — MCT-170 LAND 자산. stdlib only, mctrader_engine.* 0 |
| `src/mctrader_data/io/reader_cache.py` | CREATE | §3.1 + §3.5 채택안 A (byte-equivalent relocate + stats() no-op 예외 1건) | AC-1, AC-3, INV-1, INV-2 | 351 lines — MCT-154+170 LAND 자산. lines 339-348 stats() producer-wiring 블록 → 내부 no-op 주석 치환 (engine.metrics 외부 import 제거 — INV-2 Layer2 자족. dead-in-data, 실 emit 재배선 = MCT-185 owner) |
| `src/mctrader_data/io/tier_reader.py` | CREATE | §3.1 (byte-equivalent relocate — facade orchestration) | AC-1, INV-1, INV-2 | 322 lines — MCT-170 LAND 자산. stdlib only, mctrader_engine.* 0 |
| `tests/io/__init__.py` | CREATE | §3.4 (tests/io/ 서브패키지 신설) | (infra) | empty |
| `tests/io/test_dr_mode.py` | CREATE | §3.4 (import path 재지정 + pyright fix) | AC-2, INV-4 | 236 lines — mctrader_engine.io.* → mctrader_data.io.*. `assert dr._open_started_at is not None` 2곳 추가 (float|None narrowing, pyright gate) |
| `tests/io/test_endpoint_cutover.py` | CREATE | §3.4 (import path 재지정 + timing tolerance) | AC-2, INV-4 | 610 lines — grace_remaining_days >= 6 tolerance (int truncation, Ubuntu CI 안정성) |
| `tests/io/test_l1_reader.py` | CREATE | §3.4 (import path 재지정) | AC-2, INV-4 | 221 lines |
| `tests/io/test_reader_cache_budget.py` | CREATE | §3.4 (import path 재지정) | AC-2, INV-4 | 154 lines |
| `tests/io/test_reader_cache_flush.py` | CREATE | §3.4 (import path 재지정 + skip + pyright fix) | AC-2, AC-3, INV-4 | 259 lines — `test_stats_emits_prometheus_gauges` skip (채택안 A no-op, engine.metrics absent — @pytest.mark.skip). CacheEntry 직접 생성으로 pyright 정합 |
| `tests/io/test_reader_perf.py` | CREATE | §3.4 (import path 재지정) | AC-2, INV-4 | 271 lines — pytest-benchmark fixture 사용 |
| `tests/io/test_tier_reader.py` | CREATE | §3.4 (import path 재지정 + pyright fix) | AC-2, INV-4 | 334 lines — `# type: ignore[arg-type]` 2곳 (Literal status narrowing) |
| `tests/test_io_stats_no_engine_dep.py` | CREATE | §8 (AC-3 INV-2 runtime gate — subprocess isolation) | AC-3, INV-2 | 81 lines — subprocess: stats() 호출 후 mctrader_engine sys.modules 부재 확인 + in-process 7-key dict 검증 |
| `pyproject.toml` | MODIFY | §8.0 Phase 0 (pytest-benchmark dep 신설) | (infra) | `pytest-benchmark>=4.0` dev dep 추가 (test_reader_perf.py fixture 의존) |
| `uv.lock` | MODIFY | §8.0 Phase 0 (lock sync) | (infra) | pytest-benchmark 추가 반영 |

**합계**: src 7 파일 CREATE, tests 9 파일 CREATE, pyproject.toml + uv.lock MODIFY.
io/ 신규 test: 110 passed + 1 skipped (test_stats_emits_prometheus_gauges — 채택안 A 정합).
전체 회귀: 1020 passed + 25 skipped + 4 xfailed (신규 실패 0).

### §8.5.2 engine#58 (land_order 2) — LAND commit: 18275737

| 파일 | 변경 유형 | Change Plan §절 | AC/INV 매핑 | 비고 |
|------|-----------|-----------------|-------------|------|
| `src/mctrader_engine/io/__init__.py` | DELETE | §3.3 (engine io/ 패키지 물리 제거) | AC-4, INV-3 | 13 심볼 export + 6 self-import — git rm |
| `src/mctrader_engine/io/cold_reader.py` | DELETE | §3.3 (dead-in-prod 안전 제거) | AC-4, INV-3 | src caller 0 확정 (§0 V3) — git rm |
| `src/mctrader_engine/io/dr_mode.py` | DELETE | §3.3 (dead-in-prod 안전 제거) | AC-4, INV-3 | git rm |
| `src/mctrader_engine/io/endpoint_router.py` | DELETE | §3.3 (dead-in-prod 안전 제거) | AC-4, INV-3 | git rm |
| `src/mctrader_engine/io/l1_reader.py` | DELETE | §3.3 (dead-in-prod 안전 제거) | AC-4, INV-3 | git rm |
| `src/mctrader_engine/io/reader_cache.py` | DELETE | §3.3 (dead-in-prod 안전 제거) | AC-4, INV-3 | git rm |
| `src/mctrader_engine/io/tier_reader.py` | DELETE | §3.3 (dead-in-prod 안전 제거) | AC-4, INV-3 | git rm |
| `tests/io/__init__.py` | DELETE | §3.3 (tests/io/ 전수 제거) | AC-4, INV-3 | git rm |
| `tests/io/test_dr_mode.py` | DELETE | §3.3 | AC-4, INV-3 | git rm — data 측 이전 완료 |
| `tests/io/test_endpoint_cutover.py` | DELETE | §3.3 | AC-4, INV-3 | git rm |
| `tests/io/test_l1_reader.py` | DELETE | §3.3 | AC-4, INV-3 | git rm |
| `tests/io/test_reader_cache_budget.py` | DELETE | §3.3 | AC-4, INV-3 | git rm |
| `tests/io/test_reader_cache_flush.py` | DELETE | §3.3 | AC-4, INV-3 | git rm |
| `tests/io/test_reader_perf.py` | DELETE | §3.3 | AC-4, INV-3 | git rm |
| `tests/io/test_tier_reader.py` | DELETE | §3.3 | AC-4, INV-3 | git rm |

**합계**: src 7 파일 DELETE, tests 8 파일 DELETE.
engine 회귀: 879 passed + 9 skipped (pre-existing postgres infra 5 DB 오류 포함 — MCT-181/182 동형, 신규 실패 0).
AC-4 grep gate: `grep -r mctrader_engine.io src/` (engine) = 0건 PASS.

### §8.5.3 AC/INV 전수 검증 요약

| AC/INV | 검증 위치 | 결과 |
|--------|-----------|------|
| AC-1 (data io/ 서브패키지 신설 + 6 module byte-equiv) | data#70 src/mctrader_data/io/ 7 파일 CREATE | PASS (V-pin c6249fa 기준 byte-equivalence. reader_cache.py:339-348 채택안 A no-op 예외 1건 — Change Plan §3.5 정합) |
| AC-2 (tests/io/ 7 test green — import path 재지정 + pyright) | data#70 tests/io/ 110 passed + 1 skipped | PASS (mctrader_engine.io.* → mctrader_data.io.* 전수 재지정. pyright 0 errors. timing tolerance >= 6) |
| AC-3 (INV-2 runtime gate — stats() no mctrader_engine) | data#70 tests/test_io_stats_no_engine_dep.py | PASS (subprocess isolation: stats() 후 mctrader_engine sys.modules 부재. in-process 7-key dict 정합) |
| AC-4 (engine io/ 6 module + tests/io/ 전수 삭제 + grep gate) | engine#58 15 파일 DELETE | PASS (`grep mctrader_engine.io src/` engine = 0건 rc=1. engine src/ import 깨짐 0 — §0 V3 src caller 0 실증) |
| AC-5 (ADR-029/027 amendment box + V-pin 박제) | hub#353 Phase 1 LAND (29e9c0d) | PASS (ADR-027 6-module relocated 박제 + ADR-029 engine NAS 직독 폐기 예고. V-pin c6249fa 박제) |
| INV-1 (byte-equivalence — 6 module 본문 무변경) | data#70 src/mctrader_data/io/ 6 module | PASS (reader_cache.py:339-348 채택안 A 예외 1건 제외 전수 무변경. V-pin c6249fa origin 기준) |
| INV-2 (data io/ → mctrader_engine 의존 0) | data#70 `grep mctrader_engine src/mctrader_data/io/` | PASS (0건 rc=1 — 채택안 A no-op 치환 효과) |
| INV-3 (engine src/ io/ 호출자 0 유지 — 삭제 후 깨짐 0) | engine#58 + §0 V3 grep gate | PASS (삭제 전 src caller 0 확정 → 삭제 후 import 깨짐 0. engine 회귀 879 passed 신규 실패 0) |
| INV-4 (tests/io/ 7 test byte-equiv — 로직 무변경) | data#70 tests/io/ 7 test | PASS (import path 재지정 + pyright/timing 최소 수정 외 로직 byte-equiv) |
| INV-5 (ADR-029/027 amendment box 박제 완료) | hub#353 Phase 1 LAND | PASS (relocate 박제 + engine NAS 직독 폐기 예고 — 실 confirm = MCT-185) |
| INV-6 (compactor/reader_cache.py 무변경) | data#70 compactor/ 비접촉 확인 | PASS (mctrader_data.compactor.reader_cache.ReaderCache(Protocol) 무변경 — namespace 분리, 비접촉) |

## §9 Plan reference

- **spec**: `docs/superpowers/specs/2026-05-16-EPIC-data-domain-decoupling-design.md`
- **scope_manifest**: `scope_manifests/EPIC-data-domain-decoupling.yaml`
  §story_decision_matrix.MCT-183 (D2+D6)
- **plan**: `docs/superpowers/plans/2026-05-16-mct-183-layer2-io-relocation.md` (설계 lane TBD)
- **ADR**: `docs/adr/ADR-029-tier-promotion-single-source.md` +
  `docs/adr/ADR-027-cold-tier-object-storage-nas-minio.md` (amendment box — 본 Story owner) +
  `docs/adr/ADR-031-data-domain-decoupling.md` (§D2 io-relocate 절반 진행, VERIFIED=MCT-185)
- **선행 Story**: `docs/stories/MCT-182.md` (Layer0 contract relocation COMPLETED) +
  `docs/retros/RETRO-MCT-182.md` + `docs/retros/PMO-AUDIT-MCT-182.md` (R1 가드 5항목 reapply)

### §9.1 설계 리뷰 verdict (DesignReviewPL — Claude+Codex 독립 peer 종합)

- **iter 1 (2026-05-16)**: VERDICT **FIX** — P0×3 + P1×1 (P0-1 cross-document SSOT desync ADR-027 amendment 2-module ↔ 6-module / P0-2 Story SSOT 역전파 누락 / P0-3 §3.5 graceful no-op 구현 불가 명세, MCT-180 동형 / P1-1 byte-equiv source pin). 원인 = 설계 (root-cause-decision: 설계 리뷰 P0 → 항상 설계). ArchitectPL 직접 회귀.
- **iter 2 (2026-05-16)**: VERDICT **FIX** — P0-2/P0-3/P1-1 해소 confirm, P0-1-residual(연계 권위 4곳 2-module 축약 잔존, **MCT-179/182 desync 4회째 동형**). ArchitectPL 회귀 → ADR-027 amendment 6곳 canonical 통일 + §3.6.1 grep gate v1 박제.
- **iter 3 (2026-05-16)**: VERDICT **FIX** — **max 3/3 도달**. P0×2 (P0-1-residual-2 ADR-031:139 reconcile scope 누락 / P0-2-gate §3.6.1 gate scope+pattern 자체 결함 NO MATCH). **fix-ledger-schema 3종 escalation trigger 0 충족** (설계 수렴/INV 정합/PL divergence 없음) → **Orchestrator RESET path 채택** (수렴 경로 명확).
- **RESET 수렴 회귀 (2026-05-16)**: ArchitectPL 지정 목록 방식 탈피 — ADR-031:139 + sibling MCT-182:28/235 canonical (6산출물+sibling 전수성) + §3.6.1 gate v2 (glob-scope + 변형포괄 pattern + self-verify TEST1/TEST2) + repo-wide grep 0줄 + canonical 18건.
- **post-RESET 재검증 (2026-05-16)**: VERDICT **PASS** — Orchestrator 독립 verify (DesignReviewPL rate-limit 백그라운드 미완 대신 git/grep 직접): TEST1(repo-wide 2-module 축약 0줄)·TEST2(canonical 18건)·ADR-031:139+sibling MCT-182:28 정정·§3.6.1 gate v2+TEST1/TEST2 박제 전수 확인. P0×2 해소 + iter1-2 회귀 0 + forcing function 영구 차단 완전 박제.
- **gate**: `gate:design-review-pass` ✅ → phase:설계-리뷰 → phase:구현 전환. **MCT-183 PMO retro = codeforge upstream ADR escalation 결정 발의 의무** (cross-document SSOT forcing function 5회 누적 + 수동 gate 자체 불완전 = plugin-level mechanical gate 필요).

### §9.2 구현 리뷰 verdict (CodeReviewPL — 구현 lane TBD)

> 구현 lane 진입 후 CodeReviewPL (Claude+Codex 독립 peer 종합) 작성 영역. 요구사항 lane 미작성.

## §10 FIX Ledger

> Orchestrator 독점 append (fix-event-v1 contract). 본 에이전트 직접 기록 금지.

| iter | lane | category | mechanical_category | file | suggestion | resolution |
|------|------|----------|---------------------|------|------------|------------|
| 1 | 설계-리뷰 | cross-document-ssot-desync / test-contract / implementability (P0×3 + P1×1) | none (fast-path 부적격 — 4산출물 동반 reconcile + executable 명세 재설계) | scope_manifest §planned_adrs.amendments ADR-027 + ADR-027 amendment box + Change Plan §3.5/§3.6/§8.1/§8.2 + Story §5/§6/§4.2 | **P0-1** ADR-027 amendment box(6-module relocate) ↔ scope_manifest(2-module endpoint_router/dr_mode) desync, Change Plan §3.6 '1:1' 오판정 (MCT-179/182 동형 재발 — PMO-AUDIT-MCT-182 §4 forcing function 예측 정확). **P0-2** Change Plan §8.1 byte-equiv 예외(reader_cache.stats() lazy) Story §5 AC-1/§6 INV-1/INV-2/§4.2 역전파 누락 (Story=권위 SSOT stale). **P0-3** §3.5 graceful no-op(`from mctrader_data.metrics`) ↔ §8.1 INV-2 grep gate(`from mctrader_data` 0건) + §8.2 diff(2줄 외 0) 상호 충돌 + stats() 호출부 348/349 미처리 NameError (MCT-180 ESCALATE 동형, 구현 불가 명세). **P1-1** carry: byte-equiv engine 원본 source pin 부재. 원인 = 설계 (설계 리뷰 P0 → 항상 설계, root-cause-decision). | **iter1 부분 해소** — P0-2/P0-3/P1-1 해소 confirm (Story 역전파 / 채택안 A 내부 no-op hunk 339-348 + test_io_stats_no_engine_dep.py §8 / V-pin gate). P0-1 핵심 4산출물 byte 1:1 ✓ but 연계 권위 4곳 잔존 → P0-1-residual (iter2) |
| 2 | 설계-리뷰 | cross-document-ssot-desync (P0×1, P0-1-residual) | none (R1 가드 핵심 축 — fast-path 부적격) | scope_manifest:61 §D6 rationale + scope_manifest:90 §story_decision_matrix.MCT-183 cross_repo + ADR-029:107-109 MCT-183 box cross-ref + Story MCT-183:149 §2 비목표 D6 | P0-1-residual: 핵심 4산출물 6-module canonical 정정 완료(byte 1:1)나 동일/연계 권위 SSOT 4곳에 2-module 축약(`endpoint_router/dr_mode relocated` 단독 / `(+...)` 괄호) 잔존 — 동일 사실 박제 위치 간 self-contradiction. **MCT-179/182 cross-document SSOT desync 4회째 동형** (1차 정정 부분 이행 carry, PMO-AUDIT-MCT-182 §4 forcing function 예측 패턴 실증). 원인 = 설계. | iter2 부분 해소 — 지정 5산출물 6곳 canonical ✓, ADR-031:139 amendment table+§D2 + §3.6.1 gate 자체 결함 잔존 → P0-1-residual-2 (iter3) |
| 3 | 설계-리뷰 | cross-document-ssot-desync (P0×2, **max 3/3 도달**) | none (R1 가드 핵심 축) | docs/adr/ADR-031:139 amendment table+§D2 cross-ref + Change Plan §3.6.1 grep gate:245-252 (scope=5산출물 ADR-031 미cover + pattern 변형 미매치 NO MATCH) | **P0-1-residual-2**: ADR-031(본 Epic 권위 신규 ADR Accepted) amendment table:139 + §D2 가 reconcile scope 누락 — 단독 2-module 축약. **P0-2-gate**: §3.6.1 forcing function 자체 불완전 박제 (ADR-031 미cover + pattern `로` 조사·따옴표 변형 미매치, git 실측 NO MATCH). **cross-document SSOT desync 5회째** (MCT-179→182→183 iter1→2→3, 매 iter reconcile scope 1개 누락 발견 — 수동 forcing function 구조적 한계 실증). P0-2/P0-3/P1-1 해소 유지 회귀 0. 원인 = 설계. fix-ledger-schema 3종 escalation trigger **0 충족** (설계 수렴·INV 정합·PL divergence 없음). | **RESET path 채택** (Orchestrator 결정 — 3 trigger 0 충족 = 사용자 escalation 생략, 수렴 경로 명확). **RESET 설계-리뷰** → ArchitectPL 수렴 회귀 (ADR-031 추가 6산출물 reconcile + §3.6.1 gate scope+pattern 자기검증 완전화) → 설계 리뷰 재실행 (post-RESET). **MCT-183 PMO retro → codeforge upstream ADR escalation 결정 발의 (cross-document SSOT forcing function 5회 누적 + gate 자체 불완전 = 수동 한계, plugin-level mechanical gate 필요)** |
| **RESET** | 설계-리뷰 | — | — | — | (설계-리뷰 카운터 RESET — codeforge fix-ledger-schema 3 trigger 0 충족 path 적격) | — |
| post-RESET 1 | 설계-리뷰 | cross-document-ssot-desync (P0×0, P1×0) | — | — | ArchitectPL RESET 수렴 회귀 완료: ① ADR-031:139 amendment table canonical 6-module + sibling **MCT-182:28 frontmatter + MCT-182:235 Continuity 표** canonical (6산출물 + sibling 전수성 — 지정목록 방식이 놓친 근본) ② §3.6.1 gate v2 = glob-scope(`docs/adr/ADR-0*.md docs/stories/MCT-18*.md docs/change-plans/MCT-18*.md`) + 변형포괄 pattern(`endpoint_router[ +/]+dr_mode[^\n]{0,40}(relocat|이전)`) + **self-verify TEST1(포착력)/TEST2(FP 0)** 명문. **전수성 절대 보장**: repo-wide grep 0줄(rc=1) + canonical 전수 18건 + gate v2 PASS. iter1-2 해소 confirm 회귀 0 (P0-2/P0-3/P1-1 byte 보존). | **RESOLVED (post-RESET PASS)** — Orchestrator 독립 verify (rate limit 백그라운드 미완 대신 git/grep 직접 실증): TEST1(repo-wide 2-module 축약 0줄)·TEST2(canonical 분포 정합)·ADR-031:139+sibling MCT-182:28 정정·§3.6.1 gate v2+TEST1/TEST2 박제 전수 확인. **gate:design-review-pass** → 구현 lane 진입 가능. 수동 reconcile 한계 5회 누적 + gate 자체 불완전 = **codeforge upstream ADR escalation 후보 결정 발의** (PMO retro 의무) |

## §11 변경 요약 (Phase 2 LAND 박제 placeholder) + Retro

> 설계 lane 작성 (2026-05-16) — Change Plan `docs/change-plans/MCT-183-change-plan.md`
> §1-§12 가 설계 SSOT. 본 §11 = **Phase 2 LAND 후 박제 영역 placeholder** (LAND timeline +
> AC-1~5 / INV-1~6 결과 + FIX 루프 + RETRO/EPIC-RESULTS cross-ref). Phase 2 PR2 시 박제.

### §11.0 설계 lane 산출 요약 (Change Plan SSOT 참조)

| 항목 | 설계 결정 | Change Plan §절 |
|------|-----------|-----------------|
| io/ 6 module 배치 | `mctrader_data.io.*` 서브패키지 (compactor.reader_cache.ReaderCache Protocol 와 namespace 분리) | §3.2 |
| byte-equivalence 경계 | 6 module 본문 무변경 + `io/__init__.py` 6 self-import 재지정 + reader_cache.py stats() lazy engine.metrics = graceful no-op 예외 1건 | §3.5 |
| land_order | hub P1 → data#N (수령) → engine#N (삭제) → hub P2 (역순 rollback) | §11.3 |
| ADR amendment | ADR-029/027 MCT-183 amendment box 박제 완료 (relocated + 예고) | §10 |
| Phase 0 Gate | V1 fetch + V3 3-modal HEAD 재grep (코드 작업 전 의무) | §8.0 |
| D-row reconcile | scope_manifest D2 ↔ ADR-029/027 amendment ↔ Change Plan §3 전수 1:1 | §3.6 |

### §11.1 LAND timeline (Phase 2 PR2 박제 — TBD)

> Phase 2 cross-repo PR sequential LAND 후 박제. hub P1 / data#N / engine#N / hub P2 PR2.

### §11.2 AC / INV 결과 (Phase 2 PR2 박제 — TBD)

> AC-1~5 + INV-1~6 PASS evidence + FIX 루프 + RETRO/EPIC-RESULTS cross-ref Phase 2 PR2 박제.

### §9.0 Clarification 재스폰 이력

> 요구사항 lane PL 이 sub-agent 추가 분석·재해석 재스폰 시 사유·context append.
> 본 Story 요구사항 lane = 재스폰 0 (brainstorm 기수행 — ContinuityAgent/ChangeImpactAgent/
> FeasibilityAgent 관점 중심 합성. Phase 0 HEAD 실증으로 가설↔실상 전수 정합, clarification
> 재스폰 불요).
