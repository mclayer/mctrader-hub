---
story_key: MCT-130
story_scope: mctrader-data
status: done
created_at: 2026-05-11
completed_at: 2026-05-11
---

# MCT-130: HeartbeatWriter TTL stale 정리

## §1 배경

MCT-129(Layer 1) 이전 누적된 파일 + 설정 오류 안전망 (Layer 2 of heartbeat stale 3-layer defense).
`HeartbeatWriter.run()` 시작 시 300s TTL 초과 파일 자동 삭제.

## §2 목표 및 범위

- `heartbeat.py`에 `import time` 추가
- `cleanup_stale_heartbeats(ttl_seconds=None)` 메서드 추가
- `run()` 첫 줄에 `self.cleanup_stale_heartbeats()` 추가
- 단위 테스트 6개 (`TestHeartbeatStaleCleanup` 클래스)

## §3 Acceptance Criteria

- [x] TTL(기본 300s) 초과 파일 삭제
- [x] 자기 파일(`heartbeat-{node_id}.json`) 보호
- [x] TTL 미만 활성 파일 보호
- [x] manifest_dir 부재 시 스킵
- [x] OSError 내성 (warn 로그, 수집 계속)
- [x] `run()` 시작 시 cleanup 1회 호출

## §4 설계 결정

spec: docs/superpowers/specs/2026-05-11-heartbeat-stale-cleanup-design.md §2 레이어 2

| 결정 | 선택 | 근거 |
|------|------|------|
| start() 신설 vs run() 호출 | run() 직접 호출 | start() 없음, run() 시작 시 1회 |
| TTL env var | MCTRADER_HEARTBEAT_STALE_CLEANUP_SECONDS=300 | 5s×60 = false-positive 위험 없음 |
| TOCTOU guard | mtime 재확인 후 삭제 | double-check 패턴 |

## §5 구현 노트

plan: docs/superpowers/plans/2026-05-11-heartbeat-stale-cleanup.md Task 2

## §6 테스트 전략

`TestHeartbeatStaleCleanup` 6 케이스: `os.utime` mtime 조작, monkeypatch stat, monkeypatch cleanup 호출 확인

## §7 Change Plan

spec §2 레이어 2 참조

## §8 Test Contract

6 단위 테스트 (stale 삭제 / 자기 보호 / 신선 보호 / 디렉터리 부재 / OSError / run 호출)

## §9 Review

plan Task 2 Step 5 전체 회귀 테스트 PASS

## §10 FIX Ledger

없음

## §11 PMO 회고

**Status**: done (2026-05-11 PMOAgent dispatch)

**RETRO pointer**: `docs/retros/RETRO-MCT-129-130-131-heartbeat-stale-defense.md` (3-Story 통합 회고)

핵심 항목:
- AC 6/6 (100%) — fix-clean (FIX 0 — heartbeat 본 작업 한정)
- mctrader-data commit `69d3c57` — `TestHeartbeatStaleCleanup` 6 단위 테스트 PASS 로컬
- CI: mctrader-data CI 실패 (pre-existing infra issue `mclayer/mctrader-data#30` — 본 Story 변경과 무관, follow-up Story 후보 §7 등록)
- 3-layer defense 의 중간 안전망 (Layer 2) — Layer 1 (MCT-129) 적용 이전 누적 파일 + 설정 오류 fallback. TOCTOU guard 적용, OSError 내성 검증.
