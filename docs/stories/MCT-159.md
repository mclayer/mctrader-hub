---
story_key: MCT-159
story_scope: cross-repo  # Phase 1 = mctrader-hub governance (본 PR), Phase 2 = mctrader-data primary impl + hub Story §11 SSOT
story_issues:
  - repo: mclayer/mctrader-hub
    number: TBD  # Phase 1 PR #281 생성 (issue 미생성 — PR only workflow)
status: COMPLETED  # Phase 2 MERGED 2026-05-13T11:04:04Z; Phase 3 (실 이관+GC) = 별 운영 cycle PENDING
epic_key: EPIC-cold-tier-stage-3-wiring
epic_milestone: Epic-cold-tier-stage-3-wiring
parent_dependency: EPIC-cold-tier-nas-minio  # Stage 2 CLOSED 2026-05-13 (#277)
related_adrs:
  - "ADR-027 (amend D4/D6/D9 — 본 Story Phase 1 PR)"
  - "ADR-009 (§D2.1 / §D14 reader fallback — amend 0)"
  - "ADR-017 (§D5 hot path 무영향 invariant — amend 0)"
  - "ADR-008 (secret management — credential masking 의무, NAS_MINIO_* env 재사용)"
created_at: 2026-05-13
delegates:
  - mctrader-data  # Phase 2 primary impl
---

# MCT-159 — L2/L3 cold tier backlog NAS migration (~8.85 GiB / 7118 files)

## §1 사용자 요구사항 (verbatim — 변경 차단)

### 사용자 원문

> "기존 수집 데이터가 가득 차 있는 것 같은데 S3 minio로 이관 가능하다면 개정된 데이터 경로에 따라 옮기도록 하자"

### 🔴 본 Story 한계 박제 (R3 HIGH severity surface, 사용자 directive)

본 Story 만으로 사용자 명시 동기 (disk 압박 해소) **미달성** — scope = 8.85 GiB / 전체 backlog ~183 GiB ≈ **4.8% only**. 진짜 disk 압박 원인 = L1 sealed segment 76,200 file (~115 GiB) + WAL 59 GiB = **MCT-160 책임 (sequential 의무)**.

**Sequential 3-step migration 박제**:

| Step | Story | Scope | Disk 해소 추정 |
|------|-------|-------|----------------|
| 1 | **MCT-159 (본 Story)** | L2/L3 cold tier 8.85 GiB / 7118 file | ~4.8% |
| 2 | MCT-160 (reserve, EPIC-compactor-operations) | compactor L1 backlog cleanup — orderbookdepth FIX + L2 offset overflow FIX + MCT-153 손실 retrofit | ~62% (L1 ~115 GiB) |
| 3 | MCT-161 (reserve, EPIC-compactor-operations) | NAS bucket versioning 활성화 + replication 정책 + 손실 재발 방지 | 0% (정책 박제) |

3-step sequence 완료 후 디스크 압박 완전 해소. 본 Story = Step 1 entrypoint vertical slice.

### Brainstorm trigger 사실 박제 (2026-05-13 사용자 실측 + 7 agent + Codex GPT-5.4)

- 로컬 disk 사용 = 212 GiB / 1007 GiB (23%), market/ = 124 GiB + WAL = 59 GiB = 183 GiB
- 로컬 cold tier backlog (이관 대상) = orderbooksnapshot L2 6.1 GiB / 2305 + L3 2.4 GiB / 429 + transaction L2 186 MiB / 3335 + L3 154 MiB / 1049 = **8.85 GiB / 7118 files**
- NAS bucket `mctrader-market` = 919 MiB / 114 obj (smoke 잔재 915 MiB + market 4.4 MiB hot pipeline 5분 분)
- MCT-156 Phase 2 LAND (`mctrader-data#47`, dff8aa5) 후 compactor 09:22 restart → 09:24 부터 NAS dual-write 정상 (`endpoint=mcnas01.internal.mclayer.it:9000`)
- 사용자 unstaged hot-fix 진행 중: `backfill_orchestrator.py` + `run_backfill.py` 의 `schema_version=*` glob layer 누락 정정 ("Pre-flight hot-fix 2026-05-13" 주석)
- MCT-153 backfill 산출물 4.2 GiB / 1370 obj **NAS 손실 확정** (bucket versioning 미활성 = 복구 불가, RETRO-MCT-156 §13.5.2 박제) — local source 보존으로 forward-only invariant 위반 0

### 8 D 결정 (사용자 final confirm 2026-05-13)

brainstorm Phase 0 (7 agent 병렬) + Phase 1 (Codex GPT-5.4 일괄 review + Sonnet decider 합성) → 사용자 3 gate confirm:

| # | 결정 | 옵션 | 사용자 confirm |
|---|------|------|---------------|
| **D1** | scope = (iii) MCT-159 이관 only + MCT-160/161 reserve | 4-issue triage | ✅ |
| **D2** | (b) MCT-153 BackfillOrchestrator 재호출 + 2 amendment (channel parametrize + hour key 처리) | path 선택 | ✅ |
| **D3** | scope = L2/L3 backlog 8.85 GiB only | 이관 대상 scope | ✅ |
| **D4** | ADR-027 D4 + D6 + D9 amendment 의무, D7/D11 변경 0 | ADR amendment | ✅ |
| **D5** | smoke/ 잔재 915 MiB 별 chore 분리 (MCT-158 연계) | smoke scope | ✅ |
| **D6** | local GC 7d grace 답습 (보수적) | GC 정책 | ✅ |
| **D7** | bucket versioning = MCT-161 별 Story + 손실 사실 = ADR-027 §D9 축약 박제 | versioning | ✅ |
| **D8** | orderbookdepth FIX = 이관 prerequisite 아님 (MCT-160 책임) | codec FIX 분리 | ✅ |

### Acceptance Criteria (5 AC + Edge Case 2)

**AC-1 (경로 규칙 준수)**: 대상 데이터 100% NAS 의 신규 경로 규칙 (`tier=L{2,3}/exchange=X/symbol=Y/date=D/hour=HH/node=MERGED/part-*.parquet`) 준수, legacy 경로 혼입 0건

**AC-2 (MCT-156 결정 준수)**: legacy hour-key 부재 layout 객체 = 대상 + 결과 목록 모두에서 제외 0건 (S1/S6 정합)

**AC-3 (7종 invariant ALL PASS)**: byte-level sha256 + set-level (object count + row count) + schema-level (column count + name order + dtype + schema_version pin) 7종 ALL PASS. 1종 FAIL 시 NAS PUT 차단 + quarantine 분리 + retry queue enqueue + SOP MANUAL_GATE escalation

**AC-4 (MCT-153 손실 박제)**: (a) ADR-027 §D9 축약 amendment LAND (b) MCT-161 reserve cross-link (c) 본 Story §11 데이터 마이그레이션 trace = "local source 보존 → forward-only invariant 위반 0"

**AC-5 (로컬 디스크 압박 해소 한계 surface)**: AC-3 검증 완료 + 7d grace 답습 후 local delete 실행 → 로컬 해제 용량 ≥ 8.85 GiB + WAL/hot data 무결성 보장. **§1 한계 박제 의무**

**Edge Case 1 (경로 매핑 실패)**: `date`/`hour`/`node` 누락 partition → 자동 이관 금지, quarantine 분리, 수동 검토

**Edge Case 2 (검증 부분 실패)**: NAS 적재 성공 + 검증 일부 실패 → 재시도 분리, 검증 완료 전 원본 삭제 차단

---

## §2 도메인 해석 (DomainAgent 통합)

### 본 Story 의 domain 위치

mctrader 6-repo 의 **데이터 layer (mctrader-data) — Stage 3 wiring follow-up backlog migration**. EPIC-cold-tier-nas-minio Stage 1+2 (MCT-147~155) + EPIC-cold-tier-stage-3-wiring Stage 3 wiring entrypoint (MCT-156) 의 누적 위에서 spawn된 **backlog migration sibling Story**. domain 책임 = wiring _이전_ 로컬 누적 L2/L3 backlog 의 NAS 강제 이관 (BackfillOrchestrator 재호출, channel/hour 2 amendment 정합).

### 4 핵심 도메인 사실 (DomainAgent 박제)

1. **신규 schema reader 호환**: ADR-009 §D2.1 + §D14 fallback (`node=` absent → `node=DEFAULT`, `tier=` absent → `tier=L1`) → mixed layout 자연 양립. **본 Story 산출물 = 신규 schema (`hour=HH` + `node=MERGED`)** → fallback 의존 0 (직접 partition pruning 자연 처리).
2. **forward-only invariant (ADR-009 §D12.2)**: local source 보존 + NAS append-only PUT → 정합. MCT-153 손실 사실에도 불구 local source 보존으로 invariant 위반 0.
3. **MCT-156 S1/S6/S7 결정의 본 Story 적용**: 신규 schema local backlog 는 **S1/S6 비적용 영역** (legacy hour-key 부재 객체에만 적용). S7 (L3 backfill 별 Story 불필요) 의 전제 = "hot pipeline wiring 후 forward-only 자연 누적" — 본 Story 의 backlog 는 wiring _이전_ 누적분 → S7 비적용. **RETRO-MCT-156 §13.5.2 박제**: "S1/S6/S7 결정 전제 깨짐 (legacy NAS 4.2 GiB 손실 확정 → rationale 무력화)" 박제.
4. **7종 invariant harness (MCT-151) inject 자동 의무** — MCT-153 BackfillOrchestrator 재사용 시 InvariantHarness 자동 적용 (D6 amendment 정합).

---

## §3 코드 경로 + ADR cross-ref

### 영향 코드 경로 (Phase 2 scope)

| 파일 | 변경 |
|------|------|
| `src/mctrader_data/nas_migration/backfill_orchestrator.py` | `__init__` `channel` 인자 추가 + `_discover_partitions` channel parametrize + `_build_chunk_spec` `hour` 축 추가 |
| `scripts/migration/run_backfill.py` | `--channel` flag + evidence pack MCT-159 + `_run_dry` channel 적용 |
| `tests/nas_migration/test_backfill_orchestrator.py` | fixture `schema_version=*` + `hour` 지원 + integration test 7종 (AC-1~AC-5 + Edge Case 2) |
| `tests/nas_migration/test_backfill_resumability_chaos.py` | 동일 fixture 갱신 |

### ADR cross-ref

- **ADR-027** D4 + D6 + D9 amendment (본 Phase 1 PR)
- **ADR-009** §D2.1 / §D14 reader fallback — amend 0 (정합 박제만)
- **ADR-017** §D5 hot path 무영향 invariant — amend 0 (L1 NAS upload 0 정합 유지)
- **ADR-008** secret management — NAS_MINIO_* env 재사용, 신규 credential 0

---

## §4 Change Impact Analysis (ChangeImpactAgent 통합)

### 확정 4 file + 조건부 0

ChangeImpactAgent 매핑 박제. 자세한 AS-IS / DELTA 표는 spec file `§3` 박제 참조.

### 🔴 CRITICAL — `make_partition_dir()` test fixture 미수정 silent 실패 위험

사용자 unstaged hot-fix 가 `_discover_partitions()` 만 수정 → `make_partition_dir()` test fixture 가 `schema_version=*` 누락 → **8+ integration test 가 silent 통과 (total_chunks=0 오통과) 위험**.

**Phase 2 Task 8 의무**: TDD red phase 강제 — fixture 갱신 first → 기존 test 들이 신규 schema path 사용 검증 → 그 후 channel parametrize + hour key 구현.

---

## §5 통합 요구사항 명세

### FR (Functional Requirements)

- FR-1: `BackfillOrchestrator` 의 channel parametrize — `Literal["orderbooksnapshot", "transaction"]` 양 channel 지원
- FR-2: `BackfillOrchestrator._build_chunk_spec` 의 hour 축 추가 — `nas_object_key` 에 `/hour=HH/` 박제 (있을 시)
- FR-3: `run_backfill.py` 의 `--channel` flag + dry-run 4 case 지원
- FR-4: integration test 7종 (AC-1~AC-5 + Edge Case 2) ALL PASS
- FR-5: 실 이관 execute 후 7d grace + gc_runner local delete

### NFR (Non-Functional Requirements)

- NFR-1 (Perf): 7118 file × per-chunk 3s @ 10-parallel ≈ **35 min wall-clock** (NFR budget 80 min 의 margin ≥ 45 min)
- NFR-2 (Correctness): 7종 invariant ALL PASS (MCT-151 InvariantHarness inject 자동)
- NFR-3 (Backward-compat): MCT-153 transaction-only path (default `channel="orderbooksnapshot"`) 회귀 0
- NFR-4 (Safety): 검증 부분 실패 시 local delete 차단 (CutoverVerifier gate)

### INV (Invariants)

- INV-1: forward-only invariant (ADR-009 §D12.2) — row 변경/삭제 0
- INV-2: L1 NAS upload 0 (ADR-017 §D5, ADR-027 §D5 — MCT-156 S3 결정 정합)
- INV-3: HTTP 유지 (TLS 0, MCT-155 ADR-027 D2 amend LAND)

---

## §6 Change Plan (D1~D11)

본 §6 = Phase 1 governance scope. Phase 2 impl scope 는 `docs/superpowers/plans/2026-05-13-mct-159-l2l3-backlog-nas-migration.md` 박제.

- D1: 영향 repo = mctrader-data (primary impl) + mctrader-hub (governance)
- D2: 영향 파일 = §3 확정 4 file + ADR-027 / Story file / scope_manifest / CLAUDE.md (양 repo)
- D3: 신규 메소드 / signature 변경 = `BackfillOrchestrator.__init__` `channel` 추가 + `_build_chunk_spec` `hour` 축
- D4: legacy code deprecation = 0건 (MCT-153 BackfillOrchestrator 재사용 only)
- D5: atomic in-place migration = N/A (HEAD-then-PUT idempotent, NASUploader 자동)
- D6: trace = scope_manifest design_decisions S8~S15 + History
- D7: discrepancy surface = 사용자 명시 vs 실제 동기 괴리 3건 (§1 R3 박제 + AC-5 한계)
- D8: Test Contract = §8
- D9: 운영 위험 = §7 OperationalRiskArch 박제
- D10: 보안 위험 = §7 SecurityArch 박제
- D11: 데이터 마이그레이션 = §11 (DataMigrationArch 변호)

---

## §7 보안 + 운영 위험 (Deputy 산출 박제)

### SecurityArch §7.1~§7.3 박제

- §7.1 (credential masking): NAS_MINIO_* env 재사용, 신규 credential 0. `.env` 0600 권한 + `.gitignore` 박제 (MCT-147 mitigation 정합)
- §7.2 (TLS 부재 영역): HTTP 운영 유지 (LAN 내부망 only, MCT-155 D2 amend 박제)
- §7.3 (NAS root credential 격리): MCT-159 backfill 실행 시 NAS_MINIO_ACCESS_KEY/SECRET_KEY = mctrader-admin root account. 별 IAM user 분리 = MCT-161 reserve 책임

### OperationalRiskArch §7.4 박제 (5 항목)

- §7.4.1 (DR/disconnect retry+hard floor): MCT-150 RetryQueue + hard floor 1000seg/10GB 자동 적용 (BackfillOrchestrator 재사용)
- §7.4.2 (Clock skew): CONDITIONAL N/A — closed-day filter (UTC midnight) 이 hour 단위 보다 거친 grain
- §7.4.3 (Rate-limit): LAN 내부망 = NAS NIC bandwidth 의존 (MCT-148 T2 latency baseline 사전 입증)
- §7.4.4 (Env isolation): mctrader-data compactor service block 의 NAS_MINIO_* env 만 영향 (ingester service env 변경 0)
- §7.4.5 (Health Prometheus): `mctrader_dual_write_result_total{status, tier}` Counter + `nas_uploader_*` metrics 재사용

### LiveOps + LiveOrdering: SKIP

live mode 무관, CONDITIONAL trigger 0.

---

## §8 Test Contract

`docs/superpowers/plans/2026-05-13-mct-159-l2l3-backlog-nas-migration.md` §Task 12 의 7 integration test 정합:

1. **AC-1 test**: 신규 schema path 100% 준수, legacy 경로 혼입 0
2. **AC-2 test**: legacy hour-key 부재 partition 제외 0건
3. **AC-3 test**: `InvariantHarness` inject 자동 verify (mock spec)
4. **AC-4 test** (parametrize matrix): orderbooksnapshot/transaction × L2/L3 4 case
5. **AC-5 EC-1 test**: 경로 매핑 실패 quarantine 분리
6. **AC-5 EC-2 test**: 검증 부분 실패 시 local delete 차단

### Perf Baseline

- per-chunk = 3s @ 10-parallel (MCT-153 NFR-3 baseline 재사용)
- 7118 file × 3s / 10 ≈ **35 min wall-clock** (NFR budget 80 min 의 margin ≥ 45 min)
- 측정 기록 위치 = `tests/perf/backfill_orchestrator_baseline.json`

### §8.5 Impl Manifest (Phase 2 — DeveloperPLAgent self-write, MCT-159 PR #49)

**작성 시점**: Phase 2 구현 완료 후 DeveloperPLAgent direct write (CFP-39 SSOT).
**PR**: `mclayer/mctrader-data#49` (merged 2026-05-13T11:04:04Z)
**merge commit**: `dff8aa5` → mctrader-data `main`
**변경 총계 (MCT-159 핵심 커밋)**: 11 commits, ~500 insertions(+), ~200 deletions(-)

| 파일 | 변경 유형 | Change Plan 항목 | diff 요약 |
|------|----------|-----------------|----------|
| `src/mctrader_data/nas_migration/backfill_orchestrator.py` | MODIFY | §3 D3 (channel parametrize + hour key) | `__init__` `channel: Literal[...]` 추가 + `_discover_partitions` channel_root parametrize + `_build_chunk_spec` hour 축 (`/hour=HH/`) 박제. (+40/-10) |
| `scripts/migration/run_backfill.py` | MODIFY | §3 D3 (run_backfill --channel flag) | `--channel` flag 추가 + evidence pack MCT-159 갱신 + `_run_dry` channel_root parametrize + `_build_orchestrator` channel= 전달. (+42/-15) |
| `tests/nas_migration/test_backfill_orchestrator.py` | MODIFY | §8 Test Contract (TDD + 7종 AC integration) | fixture `make_partition_dir` `schema_version=*` + `hour`/`node` keyword-only 파라미터 추가. channel parametrize 3 tests + hour key 2 tests + AC-1~AC-5+Edge Case 2 = 7종 integration tests. pyright Literal cast 추가. 총 ~450 insertions. |
| `tests/nas_migration/test_backfill_resumability_chaos.py` | MODIFY | §3 D3 (fixture 갱신 정합) | `make_10_partitions` 신규 schema 경로 (`schema_version=*/hour=*/node=MERGED/`) 갱신. unused import 제거. (+5/-8) |
| `tests/perf/__init__.py` | ADD | §8 Perf Baseline (NFR-1) | perf 패키지 init — 신규 파일. (+0) |
| `tests/perf/backfill_orchestrator_baseline.json` | ADD | §8 Perf Baseline (NFR-1) | 35.6 min @ 10-parallel (7118 chunks × 3s) baseline 박제. NFR budget 80 min, margin 44.4 min. (+27) |
| `.claude/_overlay/CLAUDE.md` | MODIFY | Phase 2 governance | BackfillOrchestrator 2 amendment (channel parametrize + hour key) + fixture 갱신 의무 + 회귀 보호 section 추가. (+26) |
| `src/mctrader_data/compactor/l1.py` | MODIFY | pre-existing CI gate | C408 dict literal 수정 (ruff CI gate). (+8/-10) |
| `src/mctrader_data/compactor/runner.py` | MODIFY | pre-existing CI gate | SIM108 ternary + pyright `# type: ignore[union-attr]` (ruff/pyright CI gate). (+3/-3) |
| `src/mctrader_data/cold/polars_fallback.py` | MODIFY | pre-existing CI gate | `# type: ignore[import-untyped]` Ubuntu CI polars 미설치 대응. (+2/-2) |
| `.github/workflows/ci.yml` | MODIFY | pre-existing CI gate | Test step `-m "not slow"` 추가 (10000-msg crash recovery 60s timeout 회피). (+1/-1) |

*(pre-existing CI gate 수정 파일 추가 22개 — ruff/pyright/test fix, MCT-159 논리 변경 0)*

### Change Plan §3/§5/§8 정합 검증

| Change Plan 항목 | 구현 상태 | 비고 |
|-----------------|----------|------|
| §3 `BackfillOrchestrator.__init__` `channel` 추가 | DONE | `Literal["orderbooksnapshot", "transaction"] = "orderbooksnapshot"`, backward-compat |
| §3 `_discover_partitions` channel_root parametrize | DONE | `self._channel` 사용, default orderbooksnapshot 회귀 0 |
| §3 `_build_chunk_spec` `hour` 축 추가 | DONE | `_extract_hive_value(parts, "hour")` + legacy backward-compat (hour 부재 = `/hour=HH/` skip) |
| §3 `run_backfill.py` `--channel` flag | DONE | 4 case dry-run (orderbooksnapshot/transaction × L2/L3) |
| §8 Test Contract — fixture 갱신 (TDD red phase) | DONE | `make_partition_dir` schema_version=*/hour/node keyword-only (Task 8 의무) |
| §8 Test Contract — 7종 AC integration test | DONE | AC-1~AC-5 + Edge Case 2, 86 tests ALL PASS |
| §8 Perf Baseline (NFR-1) | DONE | 35.6 min @ 10-parallel, margin 44.4 min (≥ 45 min 경계, Phase 3 실측 의무) |
| ADR-027 D4/D6 amendment (channel/hour SSOT) | DONE | Phase 1 PR #279 (mctrader-hub) 이미 LAND |

### Backward-compat invariant 검증

- MCT-153 transaction-only path default (`channel="orderbooksnapshot"`) = 회귀 0
- legacy hour-key 부재 partition path: `_build_chunk_spec` `hour_segment=""` fallback 정합
- `NFR-3` (default channel 회귀 0): `test_orchestrator_default_channel_orderbooksnapshot` PASS

### §8.5 Impl Manifest (FIX Iter 1 Phase 2 follow-up — DeveloperPLAgent self-write, MCT-159 PR #50)

**작성 시점**: FIX Iter 1 Phase 2 follow-up 구현 완료 후 DeveloperPLAgent direct write (CFP-39 SSOT).
**PR**: `mclayer/mctrader-data#50` (merged 2026-05-13T12:20:09Z)
**base**: PR #49 merge 이후 origin/main (commit `1bd5021`)
**변경 총계**: 2 commits, ~916 insertions(+), ~33 deletions(-)

| 파일 | 변경 유형 | Change Plan 항목 | diff 요약 |
|------|----------|-----------------|----------|
| `src/mctrader_data/nas_migration/invariant_harness.py` | MODIFY | FIX Iter 1 D1/D2 (channel-aware redesign) | `ADR009_CHANNEL_SCHEMA_MATRIX` module-level dict 추가 (obs.v1=11 / tick.v1=8 / tick.v1.1=11 / ohlcv.v1=16) + `__init__` `expected_column_count: int|None` amend (None=lookup mode) + `_resolve_expected_column_count` 신규 (D1 Hybrid) + `_check_schema_version` channel-aware (tuple/list valid set) + `_check_object_count` per-file basis (ADR-027 §D6.1). (+250/-15) |
| `src/mctrader_data/nas_metrics/prometheus_exporters.py` | MODIFY | FIX Iter 1 D6 (channel label) | `mctrader_invariant_verify_total{status,channel,tier}` Counter 신규 + `emit_invariant_verify` `channel`/`tier` param 추가 (backward-compat default='unknown') + `nas_invariant_status_count` label `channel`/`tier` 추가. (+35/-5) |
| `tests/nas_migration/test_invariant_harness.py` | MODIFY | FIX Iter 1 TestContract (T-new-1~4) | channel fixture 4종 추가 (`make_obs_v1_parquet`/`make_tick_v1_parquet`/`make_tick_v1_1_parquet`/`make_ohlcv_v1_parquet`) + `_make_channel_harness` (lookup mode) + `TestChannelAwareColumnCount` 클래스 (T-new-1~4 + matrix completeness guard). `_make_harness` = explicit 16-col injection (OHLCV 회귀 path). (+350/-8) |
| `tests/integration/test_real_nas_smoke.py` | ADD | FIX Iter 1 D4 (real-NAS smoke) | NEW — `@pytest.mark.real_nas` 4 case (orderbooksnapshot L2/L3 + transaction L2/L3). env `NAS_MINIO_ENDPOINT` 미설정 시 skip (CI green 보존). (+230) |
| `tests/nas_migration/test_backfill_orchestrator.py` | MODIFY | FIX Iter 1 TestContract (channel matrix fixture) | `make_channel_parquet` factory 추가 (ADR009_CHANNEL_SCHEMA_MATRIX 기반, channel별 schema 정합). docstring amendment. (+80/-5) |
| `tests/nas_metrics/test_prometheus_exporters.py` | MODIFY | FIX Iter 1 D6 (prefix freeze amend) | `mctrader_invariant_*` prefix 허용 추가 (NFR-4 prefix freeze test amend). (+3/-2) |
| `pyproject.toml` | MODIFY | FIX Iter 1 (real_nas marker) | `[tool.pytest.ini_options] markers` `real_nas` 추가. (+1) |

**test result**: 760 passed / 24 skipped (real_nas 4 포함) / 4 xfailed — CI green (ubuntu + windows).

### Change Plan FIX Iter 1 §D1~D4 정합 검증

| Change Plan 항목 | 구현 상태 | 비고 |
|-----------------|----------|------|
| D1 channel_count invariant Hybrid | DONE | `_resolve_expected_column_count`: schema_version prefix 추출 → matrix lookup primary / explicit fallback / miss=diagnostic |
| D2 per-file basis (`_check_object_count`) | DONE | per-partition glob → per-file comparison (ADR-027 §D6.1 박제) |
| D3 ADR-009 §D2.6 `ADR009_CHANNEL_SCHEMA_MATRIX` | DONE | Phase 1 PR #282 (mctrader-hub) LAND — 본 PR 소비 |
| D4 real-NAS smoke test | DONE | `tests/integration/test_real_nas_smoke.py` NEW, 4 case |
| D6 prometheus channel label | DONE | `mctrader_invariant_verify_total{status,channel,tier}` Counter + `emit_invariant_verify` channel/tier param |
| OHLCV cutover path 회귀 0 | DONE | `_make_harness` explicit 16-col injection 유지 (기존 18 test ALL PASS) |

---

## §9 단계 검증

### Phase 1 (mctrader-hub governance, 본 PR)

- AC-Phase1-A: ADR-027 D4 + D6 + D9 amendment 본문 LAND
- AC-Phase1-B: Story file `docs/stories/MCT-159.md` §1~§11 박제 LAND
- AC-Phase1-C: scope_manifest `EPIC-cold-tier-stage-3-wiring.yaml` MCT-159 추가 + S8~S15 + R3-R6 LAND
- AC-Phase1-D: counters.json LAND (MCT-159 retitle + MCT-160 + MCT-161 reserve, 이미 LAND)
- AC-Phase1-E: CLAUDE.md sections 갱신 (mctrader-hub + mctrader-data)

### Phase 2 (mctrader-data impl + test, 별 PR)

- AC-Phase2-D: backfill_orchestrator.py channel parametrize + hour key 박제 추가 LAND
- AC-Phase2-E: run_backfill.py --channel flag + dry-run 4 case PASS
- AC-Phase2-F: test fixture 갱신 + integration test 7종 ALL PASS
- AC-Phase2-G: Perf baseline 35 min @ 10-parallel target 측정 + margin ≥ 45 min 검증

### Phase 3 (실 이관 + GC, 별 운영 cycle)

- AC-Phase3-H: 4 case dry-run 후 사용자 review + 실 execute → NAS bucket 산출물 7118 obj + 8.85 GiB 박제
- AC-Phase3-I: 7d grace 경과 후 gc_runner dry-run + execute → local disk 해제 ≥ 8.85 GiB
- AC-Phase3-J: §11 self-write final trace 박제 (PMOAgent retro)

---

## §10 FIX Ledger

| 시점 | event_type | event_id | root_cause_decision | resolution | files | counter |
|------|------------|----------|---------------------|------------|-------|---------|
| 2026-05-13T11:40:44Z | integration_test_fail (production deploy surface, Phase 2 mock test PASS but real-NAS FAIL) | MCT-159-fix-01-invariant-contract-gap | **설계** (decision-table "Migration FAIL · data integrity 위반 → §11.X invariant 부재·모순" escalate 조건 충족) | ArchitectPLAgent 회부 — Change Plan §6/§7/§8/§11 amend + ADR-009 §D2 channel 별 col count amend + ADR-027 D6 channel-aware invariant contract amend + MCT-151 InvariantHarness redesign (channel introspection or per-channel dict) + chunk_spec ↔ partition verify 단위 contract clarification + TestContract real-NAS smoke 의무 | `invariant_harness.py:153,274` (16-col fixed enforce) / `backfill_orchestrator.py:_build_chunk_spec` (per-file vs per-partition contract) / Story §6 D3 / §7 / §8 / §11 / ADR-009 §D2 / ADR-027 D6 / MCT-151 contract | **1/3** (설계 lane 첫 FIX) |

### Iter 1 evidence pack (2026-05-13T11:40:44Z)

**Production failure surface** (사용자 직접 kill, 4 background task `b3uz5lc4o` / `b56o6i21z` / `b9x3pzaem` / `b0k187vi5`):

- **column_count_fail** 빈발: `InvariantHarness._expected_column_count=16` (ADR009_EXPECTED_COLUMN_COUNT default) vs 실 schema:
  - `orderbook_snapshot.v1`: **11 col** (ts_utc / received_at / exchange / symbol / baseline_seq / side / level / price / quantity / payload_hash / raw_json)
  - `tick.v1`: **8 col** (ts_utc / received_at / exchange / symbol / price / quantity / side / raw_json)
  - → channel-blind 16-col enforce = 양 channel 자연 FAIL (cardinal mismatch)
- **object_count_fail** 빈발: `_build_chunk_spec` 가 1 chunk = 1 file PUT 단위. `_check_object_count` 가 partition (hour=HH/node=MERGED/) 단위 local file count vs NAS object count → single chunk PUT 시 local 12 vs NAS 1 즉시 fail. **단위 mismatch contract gap**.

**NAS bucket partial state** (kill 직후 mc du):
- 919 MiB → 2.5 GiB (+1.6 GiB) / 114 obj → 2525 obj (+2411 obj)
- orderbooksnapshot L2: 999 MiB / 465 obj (dry-run 2201 partition 中 21%)
- transaction L2: 2.4 MiB / 1 obj (즉시 fail)
- forward-only invariant: 위반 0 (local source 보존, NAS replica only partial)

**reasoning_carryover (fix-event-v1 v1.2)**:
- `invariant_summary`: "forward-only 보존 + channel 별 schema 박제 의무 + per-file chunk vs per-partition verify 단위 일치 의무"
- `disputed_claims`: "ADR-009 §D2 16-col 가정의 보편성 + chunk_spec 의 partition aggregation 의무 여부"
- `transcript_ref`: `docs/findings/2026-05-13-parallel-execution-failure-MCT-159.md` (별 finding 박제 + 본 §10 Iter 1 cross-link)

**ArchitectPLAgent dispatch target** (Phase 1 follow-up):
- ADR-009 §D2 amendment (channel 별 col count 박제, schema introspection 권고)
- ADR-027 D6 amendment (channel-aware invariant contract)
- MCT-151 InvariantHarness redesign (`_expected_column_count` 박제 → channel-aware dict 또는 schema introspection)
- chunk_spec contract: per-file PUT 후 partition aggregation 시점 박제 또는 per-file invariant verify 로 단위 통일
- TestContract: real-NAS smoke 의무 (mock fixture 의 production schema 미커버 surface)

### Iter 1 ArchitectPLAgent verdict 박제 (2026-05-13, agent dispatch 후 resolution)

**verdict**: PASS — FIX Iter 1 설계 escalate 완료 (decision-table "Migration FAIL · data integrity 위반 → §11.X invariant 부재·모순 → 설계 escalate" 정합)

**6 deputy + chief author 합성 결과** (요약):

| Decision | 채택 | 출처 deputy |
|---------|------|-----------|
| **D1** column_count invariant | **Hybrid (schema_version prefix primary + explicit injection fallback)** | RefactorAgent 권고 (B parquet metadata 거부 — invariant self-binding 위반) |
| **D2** chunk_unit ↔ verify_unit contract | **per-file basis** (chunk_spec 변경 0) | DataMigrationArch primary (TestContractArch 2-stage dissent → optional Phase 3 후속 cycle 영역으로 박제 보존) |
| **D3** ADR amendment scope | ADR-009 §D2.6 신규 SSOT (`ADR009_CHANNEL_SCHEMA_MATRIX`) + ADR-027 D6 amendment + §D6.1 신규 | Chief author synthesis |
| **D4** Test Contract amend | channel fixture (orderbook_snapshot.v1 11/tick.v1 8/ohlcv.v1 16) + T-new-1~4 + T-real-NAS-smoke 의무 | TestContractArch |
| **D5/D6** Phase 1+2 PR scope | Phase 1 = mctrader-hub governance (ADR-009 §D2.6 + ADR-027 D6 + Story §10 amend + scope_manifest S16~/R7) / Phase 2 = mctrader-data impl (InvariantHarness channel-aware redesign + real-NAS smoke + prometheus channel label) | Chief author |
| **D7** Resume strategy | NAS bucket 2.5 GiB partial state 보존 + Phase 2 LAND 후 4 case `--resume-from <checkpoint>` 재시작 (verified=0 skip, quarantined chunk = 신규 contract 재verify, pending = 신규 진행) | OperationalRiskArch §11.6 primary |

**Phase 1 follow-up PR** (mctrader-hub, base=main, 본 amendment LAND scope):
- `docs/adr/ADR-009-ohlcv-schema.md` §D2.6 신규 추가 LAND
- `docs/adr/ADR-027-cold-tier-object-storage-nas-minio.md` D6 + §D6.1 amendment LAND
- `docs/stories/MCT-159.md` §10 Iter 1 verdict + §11.6 amendment LAND
- `scope_manifests/EPIC-cold-tier-stage-3-wiring.yaml` S16~S18 design_decisions + R7 risk LAND

---

## §11 데이터 마이그레이션 (DataMigrationArch 변호)

### 마이그레이션 사유

Stage 3 wiring (MCT-156 LAND) 이전 시점 로컬 누적 L2/L3 cold tier backlog (8.85 GiB / 7118 file, 신규 schema `hour=HH` + `node=MERGED`) 을 NAS MinIO 로 강제 이관.

### Forward-only invariant 정합 박제 (ADR-009 §D12.2)

- 로컬 source row 변경 / 삭제 = 0
- NAS PUT = append-only (`HEAD-then-PUT idempotent`, MCT-150 primitive)
- 7d grace 후 local delete = 검증 (7종 invariant ALL PASS) 후 only
- → forward-only invariant 위반 0

### MCT-153 손실 사실 + 본 Story 복구 정합 박제

- MCT-153 backfill 산출물 4.2 GiB / 1370 obj NAS 손실 확정 (RETRO-MCT-156 §13.5.2, bucket versioning 미활성 = 복구 불가)
- 본 Story 의 NAS replica 복구 backfill = 자연 정합 — local source 보존 → re-PUT 으로 NAS replica 재구성
- 단, 본 Story 산출물 = 신규 schema (`hour=HH` + `node=MERGED`) — MCT-153 산출물의 legacy layout (`date=D/[node=N/]file.parquet`, hour 부재) 과 다름. 즉 "재이관" 이 아니라 **신규 schema 로 첫 NAS 진입** 박제.

### MCT-160 / MCT-161 sequential 의무

- MCT-160 (reserve, EPIC-compactor-operations): L1 backlog cleanup → compactor 정상 cadence 회복 → 본 Story 의 backlog 가 forward-only 자연 누적 의 일부로 영구 합류
- MCT-161 (reserve, EPIC-compactor-operations): bucket versioning 활성화 → 본 Story 의 NAS replica 가 versioning protected 상태로 전환 → MCT-153 손실 재발 방지

### Trace (Phase 3 LAND 시점 박제 의무)

```
- migration_completed_at: <Phase 3 Task 15 완료 시점 ISO timestamp>
- gc_eligible_at: <migration_completed_at + 7 days>
- local_delete_executed_at: <Phase 3 Task 16 ISO timestamp>
- local_disk_freed: ~8.85 GiB (actual measured: <...>)
- WAL_preserved: true
- L1_sealed_preserved: true (MCT-160 책임 영역)
- forward_only_invariant_violation: 0
- 7종_invariant_pass_rate: <verified_chunks / total_chunks>
- quarantined_chunks: <count> (Edge Case 1 박제)
```

---

**박제 완료 — codeforge Story file §1~§11 schema 정합**.

---

## §12 Retrospective (PMOAgent, 2026-05-13)

본 §12 = **RETRO-MCT-159.md** (`docs/retros/RETRO-MCT-159.md`) summary. 상세 박제 = retro 별 file.

- 회고 (PMOAgent 작성, CFP-138 / ADR-045 mandate):
    retro_file: docs/retros/RETRO-MCT-159.md
    retro_summary: "MCT-159 Phase 2 LAND (2026-05-13). BackfillOrchestrator 2 amendment (channel parametrize + hour key) + 7종 AC integration test ALL PASS + perf baseline 35.6 min @ 10-parallel. MCT-159 논리 FIX 0. pre-existing CI 오류 (ruff 63 + pyright 28 + test 15) = main broken 노출 + bulk fix (MCT-156 §6 pattern 2차 재발 → ADR 발의 PROPOSED). Stage 3 milestone 2/3 (66.7%). Phase 3 (실 이관 + GC) = PENDING (별 운영 cycle)."
    learnings_count: 4
    feedback_back_to_codeforge: []

### §12.1 Phase 2 산출물 cross-link 박제

| repo | PR | 상태 | merge sha |
|------|----|-----|-----------|
| `mctrader-hub` | [#281](https://github.com/mclayer/mctrader-hub/pull/281) | MERGED 2026-05-13T10:19:13Z | `670d118` (Phase 1) |
| `mctrader-data` | [#49](https://github.com/mclayer/mctrader-data/pull/49) | MERGED 2026-05-13T11:04:04Z | `1bd50216` (squash) |

**mctrader-data#49 주요 커밋**:
- `7d7a5fa` — test: fixture `make_partition_dir` `schema_version=*` + hour/node keyword-only (TDD red phase)
- `3120494` — feat: BackfillOrchestrator channel parametrize (orderbooksnapshot + transaction)
- `b0bbe5e` — feat: BackfillOrchestrator hour key 박제 (`_build_chunk_spec`)
- `7f1f403` — feat: run_backfill.py `--channel` flag + evidence pack MCT-159
- `7ae0b88` — test: integration test 7종 (AC-1~AC-5 + Edge Case 2)
- `b0e2f0c` — test: perf baseline JSON (35.6 min @ 10-parallel)
- `cda008d` — docs: mctrader-data CLAUDE.md BackfillOrchestrator channel/hour amend section
- `1164b9f`, `63ad7a9`, `d605f2d`, `8baf5e5` — CI gate: ruff/pyright/test pre-existing bulk fix

### §12.2 FIX Cycle Summary

| Lane | FIX count | 메모 |
|---|---|---|
| design-review | 0 | Phase 1 first-try PASS |
| code-review | 0 (MCT-159 논리) | pre-existing CI 3 bulk commits = main broken 해소 (별 카운트) |

**Total FIX: 0** (MCT-159 논리) + pre-existing CI 106건 수정 (main broken — MCT-156 §6 pattern 2차 재발)

### §12.3 4 learnings 박제

1. **TDD fixture-first 의무** — ChangeImpactAgent §4 CRITICAL surface (fixture 미갱신 → silent 오통과 위험) 효과 입증. fixture-first 패턴 = 신규 partition dimension 도입 시 의무.
2. **ruff SIM117 + pytest.raises scope** — ruff auto-fix 가 nested `with` 를 합칠 때 `pytest.raises` scope 파괴. `# noqa: SIM117` 의무. test context 에서 SIM117 자동 수정 주의.
3. **pre-existing CI accumulate pattern** — MCT-156 §6 → MCT-159 §5 연속 2회 → ADR 발의 PROPOSED. mctrader-data main CI gate 엄격화 의무.
4. **Perf baseline 분석 추정 한계** — per_chunk_s = MCT-148 T2 재사용. Phase 3 실 이관 전 5-file sample dry-run + p99 실측 의무.

### §12.4 Phase 3 PENDING 박제

Phase 3 (실 이관 + GC) = 별 운영 cycle. PMOAgent re-dispatch = Phase 3 완료 시점.

| AC | 상태 |
|---|------|
| AC-Phase3-H (dry-run → 실 execute → NAS 7118 obj) | PENDING |
| AC-Phase3-I (7d grace → gc_runner → local disk 해제 ≥ 8.85 GiB) | PENDING |
| AC-Phase3-J (§11 self-write final trace) | PENDING |
