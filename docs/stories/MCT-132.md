---
story_key: MCT-132
story_scope: mctrader-data + mctrader-hub
status: done
created_at: 2026-05-11
completed_at: 2026-05-11
---

# MCT-132: Compactor stabilize Epic — mem_limit + observability + ParquetWriter close

## §1 배경

`mctrader-compactor` 컨테이너의 RAM 사용이 **48.5 GiB / 62.73 GiB (77.32%)**, CPU **122%** 까지 폭주 (단일 측정, 2026-05-11). 호스트 단일 Docker 인스턴스 (62.73 GiB 물리 메모리) 위에서 운영되어 호스트 OOM 직전 단계.

Trigger 가설: MCT-103 50 sym 확장 (forward-only history 누적 + L3 daily compaction accumulator) 이후 발현. 단 baseline (50 sym 확장 전) 기록 부재 + 추세 (증가/plateau) 미상 — 4 가설 (genuine leak / accumulator bloat / GC retention / legitimate scale ceiling) 중 어느 것도 사전 단정 불가.

상세: `docs/superpowers/specs/2026-05-11-compactor-memory-fix-design.md` §1.

## §2 목표 및 범위

Epic-A goal triad:
- **(a) Immediate OOM mitigation** — docker `mem_limit: 32g` + `memswap_limit: 32g` (swap disabled) 로 호스트 OOM 위험 즉시 차단
- **(b) Observability infra** — `/metrics` HTTP server (port 8080) + 5 Prometheus metrics + Grafana dashboard 로 Epic-B 진단 근거 확보
- **(c) Real leak candidate fix** — L1/L2/L3 ParquetWriter context manager wrap + tmp cleanup on exception + interval-driven `gc.collect()` (handle leak 가설 직접 fix)

Companion Epic: MCT-135 (planned, Epic-B v2 재설계 — streaming compaction + per-tier subprocess + ADR-017 amendment).

## §3 Acceptance Criteria

- [x] `compose.yml` 의 compactor 서비스에 `mem_limit: 32g` + `memswap_limit: 32g` 적용 (Task 4)
- [x] 5 Prometheus metric 노출: `compactor_process_rss_bytes` / `compactor_pyarrow_total_allocated_bytes` / `compactor_python_gc_gen_count` / `compactor_tier_pending_segments` / `compactor_writer_open_count` (Task 2 + Task 7)
- [x] L1/L2/L3 ParquetWriter close audit — `with` context manager + exception 시 tmp 정리 (Task 5 + Task 6)
- [x] Grafana dashboard "Compactor Memory & Throughput" 5 panel 등록 + provisioning 동작 (Task 8)
- [x] Phase 1 land report 박제: `docs/runbooks/compactor-mct132-phase1-land.md` (Task 9)
- [x] 21/21 compactor pytest PASS — 기존 회귀 없음 + 신규 8 test 추가

## §4 설계 결정

spec: `docs/superpowers/specs/2026-05-11-compactor-memory-fix-design.md` §2 (2-Epic 구조)

| 결정 | 선택 | 근거 |
|------|------|------|
| mem_limit value | 32 GiB (호스트 62.73 GiB 의 절반) | 호스트 OOM 차단 + 정상 working set 미만 시 재시작이 진단 신호로 수용 |
| Runtime tune | `MALLOC_TRIM_THRESHOLD_=131072` + `ARROW_DEFAULT_MEMORY_POOL=system` | glibc malloc free 시 OS 반환률 ↑, jemalloc → glibc 전환으로 RSS 추적성 ↑ |
| Close pattern | `with pq.ParquetWriter(...)` context manager + exception 시 tmp cleanup | atomic rename 전 실패 시 tmp 파일 누적 차단 + handle leak 가설 직접 fix |
| Observability scope (Phase 1) | 5 metric + Grafana dashboard (영구) — tracemalloc 은 별도 host-side script | tracemalloc 은 진단용 (12h × 10min snapshot), Prometheus 는 운영용 (5s sample) — 역할 분리 |
| GC interval | `MCTRADER_COMPACTOR_GC_INTERVAL_SECONDS=300` (interval-driven) | tick 마다 호출 시 CPU overhead, 5분 마다는 L3 cycle (1h+) 보다 짧아 retention 차단 충분 |

## §5 구현 노트

plan: `docs/superpowers/plans/2026-05-11-mct132-compactor-stabilize.md` — 9 task TDD 패턴.

### Phase 0 (관측 인프라 1차)
- Task 1: `tools/compactor-tracemalloc.py` collector (12h × 10min snapshot)
- Task 2: `metrics_server.py` bootstrap + RSS gauge (Linux procfs + macOS resource + Windows ctypes/psapi fallback)
- Task 3: compose `ports: ["8080:8080"]` expose + baseline runbook

### Phase 1 — 분기 α (MCT-133 A1)
- Task 4: `mem_limit: 32g` + 4 env vars
- Task 5: L1Compactor `with pq.ParquetWriter(...)` + tmp cleanup
- Task 6: L2/L3 동일 패턴 + runner `_tick` interval-driven `gc.collect()`

### Phase 1 — 분기 β (MCT-134 A2 part-2)
- Task 7: 5 metric full export + tier_pending epoch init bug fix
- Task 8: Grafana dashboard "Compactor Memory & Throughput" (5 panels) + dashboard.yml path bug fix
- Task 9: Phase 1 land report + CLAUDE.md sections

## §6 테스트 전략

- 21/21 compactor pytest PASS (`pytest tests/compactor/` in mctrader-data)
- TDD evidence: 각 신규 test FAIL → fix → PASS 박제
- Docker smoke: `docker compose up -d compactor` 후 `docker stats` 로 LIMIT=32GiB 확인 + `curl /metrics` 로 5 metric 응답 확인
- 24h effect verification: deferred (별도 절차 — `compactor-mct132-phase1-land.md` §24h effect verification)

## §7 Change Plan

spec §4 scope_manifest (Epic-A 부분):

```yaml
epic_a_scope_manifest:
  epic_key: MCT-132
  planned_adrs: 0
  planned_files:
    - mctrader-hub/monitoring/grafana/provisioning/dashboards/compactor.json  # 신규
    - mctrader-hub/tools/compactor-tracemalloc.py  # 신규
    - mctrader-hub/docs/runbooks/compactor-baseline.md  # 신규
    - mctrader-hub/docs/runbooks/compactor-mct132-phase1-land.md  # 신규
    - mctrader-data/compose.yml  # mem_limit + env
    - mctrader-data/src/mctrader_data/compactor/metrics_server.py  # 신규
    - mctrader-data/src/mctrader_data/compactor/{l1,l2,l3,runner}.py  # close audit + gc
    - mctrader-data/src/mctrader_data/metrics.py  # 5 metric
  planned_claude_md_sections:
    - "Compactor 운영 (mem_limit, 런타임 tune)"
    - "Compactor 관측 (Prometheus metric, tracemalloc)"
```

## §8 Test Contract

신규 8 test (mctrader-data `tests/compactor/`):
- `test_metrics_server.py` — `/metrics` HTTP 응답 + RSS gauge 양수
- `test_metrics_full.py` — 5 metric 모두 노출
- `test_l1_writer_close.py` — L1 writer exception 시 close 호출
- `test_l2_writer_close.py` — L2 동일
- `test_l3_writer_close.py` — L3 동일
- `test_runner_gc_interval.py` — interval-driven `gc.collect()` 호출
- `test_runner_pending_init.py` — tier_pending epoch=0 regression guard (Task 7-fix)

## §9 Review

Final review approved — 모든 finding minor. CI: 21/21 PASS. Docker smoke 확인.

Pre-existing bug 동반 fix:
1. `dashboard.yml` path bug (MCT-123 verification gap) — `/var/lib/grafana/dashboards` (broken bind mount) → `/etc/grafana/provisioning/dashboards`. 기존 모든 Grafana dashboard 가 load 안 되고 있던 상태.
2. `tracemalloc.py` filename stdlib shadowing.

## §10 FIX Ledger

| ID | Trigger | Root cause | Fix | Regression guard |
|----|---------|-----------|-----|------------------|
| 1 | Task 7 smoke: tier_pending Gauge 가 첫 tick 전 unset → scrape 시 metric missing | `runner.__init__` 에서 `_last_l2`/`_last_l3` epoch=0 인데 interval division 으로 huge stale value 표시 | `5adca62` — guard epoch init + prime tier labels with 0 | `test_runner_pending_init.py` 신규 |
| 2 | Task 8: Grafana dashboard 가 load 안 됨 | MCT-123 의 dashboard.yml `path: /var/lib/grafana/dashboards` 가 bind mount 없는 경로 | `60d95ff` — `/etc/grafana/provisioning/dashboards` 로 수정 | manual: Grafana UI 에서 dashboard 표시 확인 |
| 3 | Task 3: tracemalloc.py filename 이 stdlib 와 충돌 | host script 명이 `tracemalloc.py` → `import tracemalloc` 가 자기 자신 import | `dee59a5` — `compactor_capture.py` 로 docstring + runbook 갱신 | manual: dry-run 시 3 snapshots + traced bytes summary |

## §11 PMO 회고

(PMOAgent dispatch 시 채움)
