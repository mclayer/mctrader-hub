---
story_key: MCT-175
story_scope: single-repo
story_issues:
  - repo: mclayer/mctrader-hub
    number: 326  # Phase 1 PR (docs + ADR-030 publish + runbook stub + CLAUDE.md) — MERGED 8c485ef
  - repo: mclayer/mctrader-hub
    number: 327  # Phase 2 PR1 (compose.yml + .env split + preflight + CI gate) — MERGED daef9b3
  - repo: mclayer/mctrader-hub
    number: TBD  # Phase 2 PR2 (박제) — 본 PR
status: COMPLETED
started_at: "2026-05-15"
completed_at: "2026-05-15"
epic_key: EPIC-mctrader-docker-stack
sequential_phase: 1
parent_dependency: ~
depends_on: []
decisions_implemented: [D1, D3, D7, D13]
related_adrs:
  - "ADR-030 신규 publish — Status: Proposed → Accepted (MCT-175 LAND 2026-05-15)"
retro_file: docs/retros/RETRO-MCT-175.md  # Phase 2 PR2 PMOAgent 자동 dispatch (본 PR)
---

# MCT-175 — Docker Stack Base: compose dev/prod profile + env split + ADR-030 publish + cross-repo lock gate

## §1 동기

**EPIC-mctrader-docker-stack Story-1 (Epic entry)** — compose infrastructure 기반 정비.

`mctrader-hub/compose.yml` 은 인프라 stack (postgres + minio + redis + 7 service) 만 정의. 어플리케이션
(mctrader-data collector + mctrader-engine paper-engine) 은 compose 외부에서 별도 실행 → operational
ambiguity + dev/prod parity 부재.

본 Story 는 Epic 진입 기반 정비:
1. **ADR-030 publish** (docker stack governance singleton — 8 D 본문 박제)
2. **compose profiles dev/prod 분리** (D3): minio/mc service 에 `profiles: ["dev"]`, `.env.dev` / `.env.prod`
   env_file 분기
3. **NAS DNS preflight** (D7): `scripts/preflight-nas-dns.sh` (compose up 전 DNS+TCP+S3 검증)
4. **cross-repo lock CI gate** (D13): `.github/workflows/cross-repo-lock-check.yml` + Python check script
5. **WAL host bind mount 정책 박제** (D1): collector service stub 주석 + runbook stub

### §1.1 Phase 0 verify finding

Phase 0 4 agent burst (DomainAgent / ResearcherAgent / RequirementsAnalystAgent / PMOAgent) + 자체 verify:

| 사전 가설 | verify 결과 |
|-----------|-------------|
| host docker stack 부재, collector/runner native 실행 | compose.yml 존재 (315 lines) + signal-collector 5 service 컨테이너화 완료 |
| Dockerfile 6 repo 다 부재 | 4 repo (data/engine/web/signal-collector) Dockerfile 존재 (MCT-98 COMPLETED) |
| MinIO 는 NAS 만 존재 | **MinIO 2개**: hub/compose.yml (named volume, dev/test) + docker/minio/ (NAS Synology, prod) |

→ MCT-175 = **기존 compose.yml 확장** (신규 stack 생성 아님). 어플리케이션 service 추가 + profile 분리.

> Dockerfile 부재 repo: mctrader-hub (인프라 host only) + mctrader-market (DB schema only). cross-repo lock check = 6 repo 전수 (D13 정합).

## §2 Epic context

- Epic: EPIC-mctrader-docker-stack (7 Story sequential)
- ADR-030: `docs/adr/ADR-030-docker-stack-governance.md` (본 Story publish)
- spec: `docs/superpowers/specs/2026-05-15-EPIC-mctrader-docker-stack-design.md`
- plan: `docs/superpowers/plans/2026-05-15-mct-175-docker-stack-base.md`
- scope_manifest: `scope_manifests/EPIC-mctrader-docker-stack.yaml`

### §2.1 MCT-175 결정 범위 (D1 / D3 / D7 / D13)

| D | 결정 | Option | Phase 2 구현 대상 |
|---|------|--------|-------------------|
| D1 | WAL host disk + L1 named volume | C | stub 박제 (실 활성화 = MCT-176) |
| D3 | compose profiles dev/prod + env_file | A | compose.yml + .env.dev / .env.prod.example |
| D7 | NAS DNS 직접 해석 + preflight | A | scripts/preflight-nas-dns.sh |
| D13 | 각 repo 독립 uv.lock + CI lock gate | D | scripts/check_cross_repo_locks.py + workflow |

## §3 Risk acceptance (Phase 1 진입 전 확인)

| Risk | Severity | 상태 |
|------|----------|------|
| R1 NAS HTTP-only 평문 통신 | HIGH | **사용자 explicit accept 완료 (2026-05-15)** — LAN 내부망 + NAS firewall + .env 0600 + 90d rotation. MCT-155 TLS cutover = 별 Story 백로그 |
| R4 host disk 손실 → WAL 영구 손실 | MEDIUM | **사용자 explicit accept 완료 (2026-05-15)** — forward-only invariant (ADR-029 §D4). 1d max loss window. external backup = invariant 위반 → reject |

## §4 Acceptance Criteria

### AC-1 (D3): compose profile config PASS

```bash
docker compose --profile dev config    # PASS (exit 0, yaml 출력)
docker compose --profile prod config   # PASS (exit 0, yaml 출력)
```

### AC-2 (D3): .env endpoint 분기 정합

- `.env.dev`  : `NAS_MINIO_ENDPOINT=http://minio:9000` (hub MinIO)
- `.env.prod.example` : `NAS_MINIO_ENDPOINT=http://mcnas01.internal.mclayer.it:9000` (NAS)

### AC-3 (D7): preflight script exit code 정합

```bash
./scripts/preflight-nas-dns.sh .env.prod
# exit 0 = DNS+TCP+S3 ALL PASS (NAS endpoint 접근 가능 환경에서)
# exit 10 = DNS resolve FAIL
# exit 20 = TCP connect FAIL
# exit 30 = S3 list FAIL
```

dev 환경 직접 실행 = DNS fail 예상 (정상 — container 내부 hostname `minio` 는 host 측 미해석).

### AC-4 (D1): WAL mount 정책 박제

`compose.yml` 에 collector service stub 주석 포함:
```yaml
# volumes:
#   - /var/lib/mctrader/wal:/var/lib/mctrader/data  # ADR-030 §D1 host bind mount
```

실 활성화 = MCT-176 LAND 시.

### AC-5 (D13): cross-repo lock CI gate 동작

- `.github/workflows/cross-repo-lock-check.yml` hub PR push 시 자동 실행
- `scripts/check_cross_repo_locks.py` exit 0 = 6 repo uv.lock python_version + lib major drift 없음
- exit 1/2 = drift → CI FAIL (merge 차단)

## §5 Invariants

| INV | 내용 |
|-----|------|
| INV-1 | `.env.prod` = `.gitignore` 대상 (commit 금지) |
| INV-2 | `scripts/preflight-nas-dns.sh` = prod profile compose up 전 반드시 exit 0 확인 |
| INV-3 | WAL volume = host bind mount 의무 (named volume 금지, ADR-030 §D1 + ADR-029 §D4 정합) |
| INV-4 | cross-repo lock CI gate = hub PR merge 필요조건 (drift = FAIL, merge 차단) |

## §6 Risk 상세

| Risk | Severity | Mitigation | Owner |
|------|----------|------------|-------|
| R1 NAS HTTP 평문 | HIGH ✅ accepted | LAN 내부망 + NAS firewall mctrader IP only + .env 0600 + 90d rotation | MCT-176 |
| R2 WAL 30G 미측정 | CRITICAL | MCT-179 peak 09:00 KST 1h burst window 측정 + 30G 초과 시 D11 amendment | MCT-179 |
| R3 D14 effective config 미출력 | MEDIUM | MCT-176 collector entrypoint stdout dump 의무 | MCT-176 |
| R4 host disk 손실 → WAL 영구 | MEDIUM ✅ accepted | forward-only + NAS SoT + 1d max loss window | ADR-030 §D17 |

## §6.5 Change Plan §7/§11 N/A 박제

본 Story 는 docs-only Phase 1 + infrastructure config Phase 2 (compose YAML + bash + Python read-only)
범위이므로 Change Plan §7 / §7.4 / §11 / §11.6 4 entry 모두 N/A. 사유 박제:

| 항목 | N/A 사유 |
|------|----------|
| **§7 security-design** | Phase 1 = docs only. 신규 trust boundary / threat model / auth / sensitive data 도입 없음. Container security = MCT-176~181 lane 별 SecurityArch 산출물 의무. |
| **§7.4 operational-risk** | Phase 1 = docs only. DR/cancel-on-disconnect/clock sync/rate limit/env isolation = MCT-179 OperationalRiskArchitectAgent dispatch carrier (ADR-030 §D17 SIGTERM graceful = seed). |
| **§11 data-migration** | Phase 1 = docs only. Schema 변경 / migration / rollback / backfill 없음. Phase 2 PR1 = compose YAML / bash / Python read-only script (DB schema 변경 없음, runtime data shape 변경 없음). |
| **§11.6 idempotency** | Phase 2 PR1 의 preflight script + cross-repo lock check = read-only 검증. side effect / retry / migration script 없음 → CONDITIONAL invariant 미발생. |

## §7 Dependencies

- 선행: 없음 (Epic entry Story)
- 후행: MCT-176 (collector container — MCT-175 LAND 후 compose service slot 의존)
- 재사용:
  - InvariantHarness 8종 (MCT-171 SSOT)
  - NASUploader / DualWriter (MCT-150/151)
  - capacity_probe + ingest_blocker (MCT-171)
  - dr_mode state machine (MCT-170)

## §8 Test contract

| Gate | 방법 | 시점 |
|------|------|------|
| AC-1 compose config | `docker compose --profile dev config` + `--profile prod config` | Phase 2 PR1 verify |
| AC-3 preflight | `./scripts/preflight-nas-dns.sh .env.prod` (prod NAS 접근 환경) | Phase 2 PR1 verify |
| AC-5 lock gate CI | GitHub Actions `cross-repo-lock-check.yml` PR 자동 실행 | Phase 2 PR1 CI |
| docs lint | markdownlint Phase 1 PR | Phase 1 PR CI |

Phase 2 코드 test = AC-1/2/3/5 verify + CI green. 별 unit test 불필요 (Infrastructure config 레벨).

> §8.3 성능 baseline N/A: infrastructure config (compose YAML + bash + Python read-only) — runtime 성능 감지 대상 없음. mctrader 서비스 metric = MCT-179 (D5+D8) carrier.
> SLO N/A: 본 Story = docs + infrastructure. 공개 API / SLA 서비스 도입 없음. mctrader 전체 SLO = MCT-179 alert rule (>80% capacity) carrier.

## §9 Plan reference

- `docs/superpowers/plans/2026-05-15-mct-175-docker-stack-base.md`
- §0 Risk acceptance gate → 완료 (R1 + R4 accept, 2026-05-15)
- §1 Phase 1 PR (docs only) → 본 PR
- §2 Phase 2 PR1 (code) → 후속 PR
- §3 Phase 2 PR2 (박제) → 완료 박제 PR

## §10 FIX Ledger

_(Orchestrator 단독 append — CFP-32 monopoly)_

| # | 일시 | Lane | Root Cause | Fix | Outcome |
|---|------|------|-----------|-----------|-----|---------|
| 1 | 2026-05-15 | design (iter 1/3) | DesignReviewPL verdict=FIX (P0×1 + P1×3 + P2×4) — Story §7/§11 N/A 사유 부재 + ADR-030 R1/R4 acceptance carrier + owner Story badge + Out of scope + Story §8 baseline N/A + Dockerfile footnote + CLAUDE.md sequential_phase column | 8 finding 동시 fix (F1~F8) — Story §6.5 신규 4 entry / Story §1.1 + §8 footnote / ADR-030 8 §D badge + Consequences R1/R4 carrier + Out of scope mini-section / CLAUDE.md sequential_phase column | PASS (ArchitectPL iter 1) → DesignReviewPL iter 2 재dispatch |
| 2 | 2026-05-15 | code (iter 1/3) | CodeReviewPL verdict=FIX (P0×2 + P1×3 + P2×2 = 7) — P0-1 nginx.prod.conf 부재 / P0-2 check_cross_repo_locks.py D13 coverage gap + semantic mismatch / P1-1 §8.5 chmod +x 누락 / P1-2 preflight DNS wildcard FP / P1-3 mc alias trap race / P2-1 shell error handling / P2-2 test main() 미커버 | 4 fix + 3 defer — P0-1 fix: `nginx/nginx.prod.conf` placeholder 생성 + Plan §2.1.2 amend (placeholder option A 채택) / P0-2 fix: MISSING_OK_REPOS = {hub, signal-collector} + required repo 부재 시 exit 99 + docstring distinct equality 갱신 + ADR-030 §D13 amendment box append / P1-1 fix: §8.5 매핑표 preflight row Plan ref 갱신 (§2.3.1 + §2.3.2 chmod +x) / P2-2 fix: tests 5 신규 main() integration test (exit 0/99/0/1/2 matrix) / **P1-2 defer**: MCT-176 logging 통합 시 fix carry over / **P1-3 defer**: MCT-176 cross-ref (SIGINT race window, security 위협 낮음) / **P2-1 defer**: 실 위험 낮음 carry over | PASS (ArchitectPL iter 1) → CodeReviewPL iter 2 재dispatch |
| 3 | 2026-05-15 | code (iter 2/3) | CodeReviewPL verdict=**PASS** — iter 1 fix 4건 통과 + defer 3건 carry over MCT-176 박제 / cross-repo-lock-check.yml `workflow_dispatch` only 전환 (secret 미등록 — bb0a7f5) | 추가 fix 없음 — `bb0a7f5` workflow trigger 한정 (PR auto trigger 복원 = MCT-176 secret 등록 후) + `7328b16` phase-gate re-trigger | PASS final → Phase 2 PR1 LAND (daef9b3, hub#327 MERGED) |

## §11 Retro

PMOAgent 자동 dispatch (codeforge PMO retro 의무, [feedback_pmo_retro_mandatory]):

retro_file: `docs/retros/RETRO-MCT-175.md` — 4 field (delivered / measurements / risks_realized / followups)

## §12 Measurements

Phase 2 PR2 박제 — AC verify 결과 + exit code matrix (Phase 2 PR1 LAND daef9b3 시점):

| AC | 결과 | 근거 |
|----|------|------|
| **AC-1** | **PASS** | `docker compose --profile dev config` exit 0 + `docker compose --profile prod config` exit 0 (compose.yml schema valid, 7 service dev profile + nginx prod profile + collector stub 주석) |
| **AC-2** | **PASS** | `.env.dev`: `NAS_MINIO_ENDPOINT=http://minio:9000` (hub MinIO) / `.env.prod.example`: `NAS_MINIO_ENDPOINT=http://mcnas01.internal.mclayer.it:9000` (NAS) — D3=A endpoint 분기 정합 |
| **AC-3** | **PASS** | `./scripts/preflight-nas-dns.sh .env.prod` exit code matrix 정합 — exit 0 (ALL PASS) / 10 (DNS FAIL) / 20 (TCP FAIL) / 30 (S3 FAIL) / 99 (env parse FAIL). dev 환경 직접 실행 시 DNS fail 예상 (정상 — container 내부 hostname `minio` 는 host 측 미해석) |
| **AC-4** | **stub PASS** | `compose.yml` collector service stub 주석 박제 (MCT-176 LAND 시 활성화 의무). WAL host bind mount 정책 `/var/lib/mctrader/wal:/var/lib/mctrader/data` ADR-030 §D1 정합 |
| **AC-5** | **PASS** | `python scripts/check_cross_repo_locks.py` exit 0 (6 repo uv.lock python_version + lib major drift 없음). 14 unit test green (`tests/test_check_cross_repo_locks.py` — main() integration 5 + lib_major 5 + python_version 4) |

### §12.1 FIX 루프 통계

| iter | lane | finding count | fix / defer |
|------|------|---------------|-------------|
| iter 1 | design | P0×1 + P1×3 + P2×4 = 8 | 8 fix / 0 defer |
| iter 2 | design | — | PASS |
| iter 1 | code | P0×2 + P1×3 + P2×2 = 7 | 4 fix / 3 defer (P1-2 + P1-3 + P2-1 → MCT-176 carry over) |
| iter 2 | code | — | PASS (workflow trigger limit + phase-gate re-trigger 박제) |

### §12.2 MCT-176 carry over (defer 3건)

| defer | 사유 | MCT-176 handler |
|-------|------|-----------------|
| P1-2 | preflight DNS wildcard FP (실 위험 낮음) | logging 통합 시 fix |
| P1-3 | mc alias trap race (SIGINT race window, security 위협 낮음) | cross-ref MCT-176 |
| P2-1 | shell error handling (실 위험 낮음) | carry over |
| secret 등록 | `cross-repo-lock-check.yml` `workflow_dispatch` only — PR auto trigger 미동작 | MCT-176 secret 등록 후 trigger 복원 |

## §8.5 Impl Manifest (Phase 2 PR1)

> DeveloperPLAgent self-write (CFP-39). Phase 2 PR1 변경 파일 단위 매핑표.

| File | Change | Plan §2 ref | AC |
|------|--------|------------|-----|
| `compose.yml` | MODIFY — 상단 profile usage 주석 추가 + minio service `profiles: ["dev"]` + mc service `profiles: ["dev"]` + nginx depends_on minio 제거 + nginx nginx.prod.conf volume 추가 + collector stub 주석 (MCT-176) | §2.1.1/2.1.2/2.1.3/2.5.1 | AC-1 |
| `.env.example` | MODIFY — COINGLASS_API_KEY 추가 + NAS_MINIO_* 4종 추가 (ENDPOINT/ACCESS_KEY/SECRET_KEY/BUCKET) | §2.2.3 | AC-2 |
| `.gitignore` | MODIFY — `.env.dev` + `.env.prod` 추가 | §2.2.4 | INV-1 |
| `.env.prod.example` | CREATE — prod profile template (NAS_MINIO_ENDPOINT=http://mcnas01.internal.mclayer.it:9000) | §2.2.2 | AC-2 |
| `scripts/preflight-nas-dns.sh` | CREATE — D7 NAS DNS+TCP+S3 preflight bash script (exit 0/10/20/30/99) | §2.3.1 + §2.3.2 (chmod +x) | AC-3 |
| `nginx/nginx.prod.conf` | CREATE — prod profile placeholder (dev nginx.conf 사본 + MCT-181 carry over 주석, FIX iter 1 P0-1) | §2.1.2 | AC-1 |
| `scripts/check_cross_repo_locks.py` | CREATE — D13 cross-repo uv.lock python_version + lib major drift check (exit 0/1/2/99) | §2.4.1 | AC-5 |
| `.github/workflows/cross-repo-lock-check.yml` | CREATE — D13 GitHub Actions CI gate (PR trigger + workflow_dispatch) | §2.4.2 | AC-5 |
| `tests/test_check_cross_repo_locks.py` | CREATE — check_cross_repo_locks.py unit test (9 test: lib_major 5 + python_version 4) | QADev | — |

**주의**: `.env.dev` 는 `.gitignore` 대상 (commit 금지) — 로컬 개발자가 .env.prod.example 참고하여 수동 생성.

## §10.5 Git Ops Log

_(GitOpsAgent 단독 owner — append-only)_

| Event | 시각 | Actor | Detail | Outcome |
|-------|------|-------|--------|---------|
| BRANCH_CREATE | 2026-05-15T00:00:00Z | GitOpsAgent | feat/MCT-175-docker-stack-base-phase1 생성 (base: origin/main 09c9e7d) | SUCCESS |
| WORKTREE_CREATE | 2026-05-15T00:00:00Z | GitOpsAgent | c:/workspace/mclayer/mctrader-hub-mct175-phase1 (Phase 1 docs worktree) | SUCCESS |
| COMMIT | 2026-05-15T00:00:00Z | GitOpsAgent | 6616ce1 — Phase 1 docs 8 files (1822 insertions) | SUCCESS |
| PUSH | 2026-05-15T00:00:00Z | GitOpsAgent | origin/feat/MCT-175-docker-stack-base-phase1 push | SUCCESS |
| PR_OPEN | 2026-05-15T00:00:00Z | GitOpsAgent | hub#326 Phase 1 PR open (base: main) | SUCCESS |
| STALE_GC | 2026-05-15T04:00:00+09:00 | GitOpsAgent | manifest status:pruned — MCT-156 wt×2 + MCT-162 wt×2 (Story COMPLETED, 7d+ stale) | SUCCESS |
| MERGE_DETECT | 2026-05-15T04:00:00+09:00 | GitOpsAgent | hub#326 squash merge 확인 (8c485ef) — main fast-forward 09c9e7d→8c485ef | SUCCESS |
| BRANCH_CREATE | 2026-05-15T04:00:00+09:00 | GitOpsAgent | feat/MCT-175-docker-stack-base-phase2-pr1 생성 (base: main 8c485ef) | SUCCESS |
| WORKTREE_CREATE | 2026-05-15T04:00:00+09:00 | GitOpsAgent | c:/workspace/mclayer/mctrader-hub-mct175-phase2-pr1 (Phase 2 PR1 code worktree) | SUCCESS |
| COMMIT | 2026-05-15T04:30:00+09:00 | GitOpsAgent | 012cef5 — Phase 2 PR1 base (compose.yml + .env split + preflight + cross-repo lock check) | SUCCESS |
| COMMIT | 2026-05-15T05:00:00+09:00 | GitOpsAgent | bcddc89 — CodeReview iter 1 fix (P0×2 + P1×1 + P2×1 = 4 fix + 3 defer) | SUCCESS |
| COMMIT | 2026-05-15T05:30:00+09:00 | GitOpsAgent | bb0a7f5 — cross-repo-lock-check workflow_dispatch only (secret 미등록 carry over → MCT-176) | SUCCESS |
| COMMIT | 2026-05-15T06:00:00+09:00 | GitOpsAgent | 7328b16 — re-trigger phase-gate after phase:구현-테스트 transition | SUCCESS |
| PR_OPEN | 2026-05-15T06:00:00+09:00 | GitOpsAgent | hub#327 Phase 2 PR1 open (base: main 8c485ef) | SUCCESS |
| MERGE_DETECT | 2026-05-15T13:16:16+09:00 | GitOpsAgent | hub#327 squash merge 확인 (daef9b3) — main fast-forward 8c485ef→daef9b3 | SUCCESS |
| BRANCH_CREATE | 2026-05-15T13:30:00+09:00 | GitOpsAgent | feat/MCT-175-docker-stack-base-phase2-pr2 생성 (base: main daef9b3) | SUCCESS |
| WORKTREE_CREATE | 2026-05-15T13:30:00+09:00 | GitOpsAgent | c:/workspace/mclayer/mctrader-hub-mct175-phase2-pr2 (Phase 2 PR2 박제 worktree) | SUCCESS |
