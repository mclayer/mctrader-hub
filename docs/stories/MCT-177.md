---
story_key: MCT-177
story_scope: cross-repo
story_issues:
  - repo: mclayer/mctrader-hub
    number: 333  # Phase 1 PR (docs only) — dd59b65 MERGED 2026-05-15T08:56:31Z
  - repo: mclayer/mctrader-data
    number: 65   # Phase 2 PR1 (data code) — af6c812 MERGED 2026-05-15T09:30:00Z (land_order 1)
  - repo: mclayer/mctrader-engine
    number: 54   # Phase 2 PR1 (engine code) — 9cbe3b4 MERGED 2026-05-15T09:30:10Z (land_order 2)
  - repo: mclayer/mctrader-hub
    number: 334  # Phase 2 PR1 (hub code) — cc0c368 MERGED 2026-05-15T09:30:21Z (land_order 3)
  - repo: mclayer/mctrader-hub
    number: ~    # Phase 2 PR2 (박제) — 본 PR
status: COMPLETED
started_at: "2026-05-15"
completed_at: "2026-05-15"
epic_key: EPIC-mctrader-docker-stack
sequential_phase: 3
parent_dependency: ~
depends_on: [MCT-176]
decisions_implemented: [D2, D4, D10, D15]
carry_over_from: MCT-176
carry_over_items:
  - "YAML config loader (option A) — mctrader-data source_order 3-tier 복원"
  - "_register_signal_handlers + _SHUTDOWN_REQUESTED collect loop wiring (data)"
  - "cross-repo-lock-check secret 6 repo 측 secret read 검증"
related_adrs:
  - "ADR-030 §D2 amendment box (MCT-177 publish)"
  - "ADR-030 §D4 amendment box (MCT-177 publish)"
  - "ADR-030 §D10 amendment box (MCT-177 publish)"
  - "ADR-030 §D15 amendment box (MCT-177 publish)"
retro_file: docs/retros/RETRO-MCT-177.md  # PMOAgent sub-dispatch, Phase 2 PR2 LAND 시 생성
---

# MCT-177 — paper-engine daemon + SIGTERM graceful + universe override + Redis prefix

## §1 동기

**EPIC-mctrader-docker-stack Story-3 (sequential_phase 3)** — compose stack 에 두 번째 어플리케이션 service
추가. MCT-176 LAND (hub#330 + data#64 + hub#331 + Phase 2 PR2) 로 `mctrader-data collector` 컨테이너가
완성됨. 본 Story 는 그 위에 `mctrader-engine paper-engine` daemon 을 compose service 로 진입.

4 결정 구현:

1. **D2 paper daemon (restart unless-stopped)** — compose.yml 에 `paper-engine` service 신규
   (image: ghcr.io/mclayer/mctrader-engine, command: `paper --daemon`, restart: unless-stopped,
   healthcheck :8080, stop_grace_period: 60s). backtest-runner = MCT-178 (별도 profile, D2 oneshot 부분).

2. **D4 SIGTERM graceful + startup InvariantHarness scan** — mctrader-engine CLI 측
   `signal.SIGTERM` / `signal.SIGINT` handler 신규. 60s grace period 내 open position commit
   + WAL flush + exit 0. compose stop_grace_period 정합. startup 시 InvariantHarness 8종 scan
   (MCT-171 SSOT, 위반 시 warn + continue).

3. **D10 universe override** — env default `UNIVERSE_TOP_N=50` (.env.dev / .env.prod) 박제.
   compose command `--universe-id <id>` override (backtest case 포함). paper daemon 측
   50-sym universe default.

4. **D15 Redis key prefix** — Redis key namespace 분리:
   - `signal:*` (signal-collector 5종 — fear_greed / ecos / kimchi / announcement / coinglass)
   - `market:*` (mctrader-data tick + orderbook cache)
   - `engine:*` (paper-engine position + strategy state)
   migration: signal-collector 5종 기존 unprefixed key → `signal:*` rename (1주일 dual write 후 cutover).

### §1.1 MCT-176 carry over 통합

MCT-176 CodeReviewPL FIX iter 1 결과 이월된 3건을 본 Story 에서 통합 처리:

| # | 항목 | 이월 사유 | MCT-177 처리 |
|---|------|----------|-------------|
| CO-1 | YAML config loader (option A) | Phase 2 PR1 = env + built-in only (false claim 방지 위해 downgrade). YAML loader 미구현 | mctrader-data cli.py 에 `_load_yaml_config()` 신규 + `source_order` → `["env", "yaml_default", "built_in"]` 복원 + MCT-176 AC-2 + §8 test 3-tier chain 으로 amend |
| CO-2 | `_register_signal_handlers` + `_SHUTDOWN_REQUESTED` collect loop wiring | Phase 2 PR1 = stub (TODO 헤더 + docstring 확장 only) | non-asyncio entry point (`backfill` / `compact` one-shot) 측 `signal.signal()` 등록 + collect loop chunk boundary 측 `_SHUTDOWN_REQUESTED` polling 통합 |
| CO-3 | cross-repo-lock-check secret 6 repo 측 secret read 검증 | hub 측 단방향 secret 등록만 완료 | 6 repo (data/engine/web/market/signal-collector/hub) 측 `MCTRADER_CROSS_REPO_TOKEN` secret read 검증 script (`scripts/verify_cross_repo_secret.py`) 신규 + 결과 박제 |

## §2 Epic context

- **Epic**: EPIC-mctrader-docker-stack (7 Story sequential)
- **sequential_phase**: 3 (MCT-176 COMPLETED → MCT-177 IN_PROGRESS → MCT-178 PLANNED)
- **ADR**: ADR-030 §D2 + §D4 + §D10 + §D15 amendment box (본 Story Phase 1 박제)
- **spec**: `docs/superpowers/specs/2026-05-15-EPIC-mctrader-docker-stack-design.md`
- **plan**: `docs/superpowers/plans/2026-05-15-mct-177-paper-engine.md`
- **scope_manifest**: `scope_manifests/EPIC-mctrader-docker-stack.yaml`

## §3 Risk acceptance (MCT-175 R1+R4 carry over)

아래 risk 는 MCT-175 Phase 1 에서 사용자 explicit accept 완료. 본 Story 진입 시 재확인 불요.

| Risk | Severity | 상태 |
|------|----------|------|
| R1 NAS HTTP-only 평문 | HIGH | **user_acknowledged_at: 2026-05-15** by mclayer8865@gmail.com |
| R4 host disk 손실 → WAL 영구 손실 | MEDIUM | **user_acknowledged_at: 2026-05-15** by mclayer8865@gmail.com |

R2 (WAL 30G 미측정 CRITICAL) = MCT-179 owner. 본 Story 에서 직접 해소 불가, carry 유지.

ADR-030 §Consequences R1/R4 acceptance carrier 박제 참조:
`docs/adr/ADR-030-docker-stack-governance.md` §Consequences

## §4 Acceptance Criteria

### AC-1 paper-engine compose healthcheck PASS (D2)

```bash
docker compose --profile dev config | grep -A 20 "paper-engine:"
# 기대: paper-engine service 정의 (restart: unless-stopped + healthcheck :8080 + stop_grace_period: 60s)
docker compose --profile dev up -d paper-engine
docker inspect mctrader-paper-engine | grep '"Status"'
# 기대: "Status": "running"
curl -sf http://localhost:8080/health && echo "PASS"
# 기대: healthcheck endpoint 응답 200
```

### AC-2 SIGTERM graceful 60s grace (D4)

```bash
docker compose --profile dev up -d paper-engine
docker kill --signal=SIGTERM mctrader-paper-engine
# 60s 이내 exit 0 확인
docker wait mctrader-paper-engine
# 기대: exit code 0
docker logs mctrader-paper-engine | grep "graceful shutdown"
# 기대: "[paper-engine] SIGTERM received — graceful shutdown initiated" 로그 출력
```

### AC-3 UNIVERSE_TOP_N env + universe-id override (D10)

```bash
# env default 확인
grep UNIVERSE_TOP_N .env.dev
# 기대: UNIVERSE_TOP_N=50

# compose command override 확인
docker compose --profile dev run --rm paper-engine paper --universe-id alt-30 --daemon
docker logs mctrader-paper-engine | grep "universe"
# 기대: universe-id=alt-30 로그 출력
```

### AC-4 Redis key prefix 3 namespace (D15)

```bash
docker compose --profile dev up -d
# redis-cli SCAN 으로 prefix 확인
docker exec mctrader-redis redis-cli SCAN 0 MATCH "signal:*" COUNT 100
docker exec mctrader-redis redis-cli SCAN 0 MATCH "market:*" COUNT 100
docker exec mctrader-redis redis-cli SCAN 0 MATCH "engine:*" COUNT 100
# 기대: 각 namespace 에 key 존재, unprefixed key 는 1주일 dual write 기간 내 병존 허용
```

### AC-5 MCT-176 carry over 3건 (CO-1~CO-3)

```bash
# CO-1 YAML loader 3-tier
docker exec mctrader-collector mctrader-data effective-config --format json | python3 -c \
  "import json,sys; d=json.load(sys.stdin); assert d.get('_source_order') == ['env','yaml_default','built_in'], d"
# 기대: source_order 3-tier 확인

# CO-2 signal wiring
grep -n "_SHUTDOWN_REQUESTED" src/mctrader_data/cli.py
# 기대: collect loop chunk boundary 에 polling 존재

# CO-3 secret read 검증
python scripts/verify_cross_repo_secret.py
# 기대: 6 repo 모두 MCTRADER_CROSS_REPO_TOKEN secret READ 가능 또는 미등록 repo 목록 출력
```

## §5 Invariants

본 Story 구현 중 아래 invariant 위반 시 즉시 중단:

| Invariant | 출처 | 검증 방법 |
|-----------|------|----------|
| forward-only (WAL 객체 삭제 금지) | ADR-009 §D12 | paper-engine restart 후 WAL 기존 파일 유지 확인 |
| WAL fsync (sealed segment 원자적 기록) | ADR-029 §D4 | SIGTERM 60s grace 내 WAL flush 확인 (AC-2) |
| sha256 SSOT caller-side | MCT-163 INV-3 | `nas_uploader.put_streaming()` 호출 경로 sha256 전달 유지 |
| INV-4 engine RSS peak ≤ 256 MB delta | MCT-170 INV-4 | paper-engine steady-state RSS (50-sym universe, 1h window) |
| INV-5 Redis prefix migration 1주일 dual write | D15 policy | signal-collector 5종 legacy key + `signal:*` key 동시 존재 확인 (migration window) |

## §6 Risk 상세

### R-MCT-177-1 Redis prefix migration silent fail (MEDIUM)

- **위협**: signal-collector 5종 unprefixed key → `signal:*` rename 중 consumer (paper-engine) 가
  old key 로 read 시도 → 데이터 없음 (KeyError / null) 상황 무통보 발생
- **mitigation**:
  - 1주일 dual write (legacy unprefixed + `signal:*` 동시 write)
  - paper-engine 측 `signal:*` prefix 우선 read, fallback 없음 (dual write 기간 중 항상 `signal:*` 존재)
  - dual write 종료 시점: MCT-177 LAND + 7d 이후 별 PR 에서 legacy key cleanup
  - Prometheus `redis_key_migration_dual_write_active` flag Gauge (1=dual write 활성)
- **잔여 risk**: dual write 기간 내 compose 재시작 시 signal-collector 가 구버전 image 로 기동 =
  unprefixed key 만 write → paper-engine `signal:*` read miss. image tag pin (MCT-181) 전까지 주의.

### R-MCT-177-2 paper daemon position state loss on crash (MEDIUM)

- **위협**: paper-engine SIGTERM 수신 후 60s grace 내 open position commit 미완료 → restart 시
  position state 불일치 (`engine:position:*` Redis key stale)
- **mitigation**:
  - 60s grace = SIGTERM 수신 → open position 즉시 snapshot write → WAL flush → exit 0 순서 보장
  - restart unless-stopped 로 자동 재시작 후 Redis `engine:position:*` 읽어 state 복구
  - InvariantHarness startup scan: 8종 ambiguity invariant 검증 후 trading loop 진입
  - position snapshot = Redis atomic SET (SETNX 아님, restart 시 최신 snapshot 덮어쓰기 허용)
- **잔여 risk**: Redis container 자체 crash 시 position state 영구 손실. Redis AOF persistence = MCT-179
  에서 observability 설정 시 enable 여부 결정 예정.

### R-MCT-177-3 universe-id override misconfig (LOW)

- **위협**: compose command `--universe-id` override 값이 engine 내부 registry 에 없는 ID →
  paper daemon startup fail (KeyError) 또는 empty universe 로 trading loop 진입
- **mitigation**:
  - CLI 측 universe-id validate: registry 미존재 시 즉시 exit 1 + error 메시지
  - UNIVERSE_TOP_N=50 env default = 50-sym universe (MCT-103 LAND, 항상 유효)
  - compose command override 는 operator explicit action — runbook 박제 (AC-3 verify)

## §6.5 Change Plan §7/§11 N/A 박제

본 Story Phase 1 = docs only PR. 아래 항목 적용 여부:

### §7 Security (N/A — docs only, 참고 박제)

Phase 1 PR 은 실 코드 변경 없음. 아래는 Phase 2 PR1 구현 시 해당:

- paper daemon = trust boundary: NAS L1 read (DualWriter 경유) + Redis position state write
  (engine:position:* namespace). 외부 API 호출 없음.
- Redis key prefix = data consumer 분리 (engine: / market: / signal: 3 namespace).
  Redis auth = REDIS_PASSWORD env (ADR-030 §D3 .env.dev/.env.prod 분기 정합).
- SecurityArch carrier: MCT-176 §D9 (NAS credential rotation) + ADR-030 §D15 (Redis prefix).

### §7.4 Op-risk (N/A — docs only, 참고 박제)

- SIGTERM graceful 60s grace + restart unless-stopped: 자동 복구 policy 박제 (D4=C).
- paper daemon 5xx alert = MCT-179 (D8=C, Prometheus alert rule).
- ADR-030 §D17 정합 (host disk 손실 risk = R4 MEDIUM, 사용자 explicit accept).

### §11 Data-migration (N/A — docs only, 참고 박제)

- Redis prefix migration = signal-collector 5종 기존 unprefixed key → `signal:*` 일괄 rename.
  1주일 dual write 후 legacy key cleanup (cutover = LAND + 7d).
- rollback: dual write 기간 = old key 항상 존재 → consumer rollback 가능.
- cleanup PR: MCT-177 LAND + 7d 이후 별 PR (script: `scripts/redis-prefix-cleanup.sh`).

### §11.6 Idempotency (N/A — docs only, 참고 박제)

- paper daemon restart = Redis `engine:position:*` 읽어 state 복구 (idempotent).
- YAML loader = read-only (`/etc/mctrader/config.yaml` 읽기만, 부재 시 {} 반환, 멱등).
- `scripts/verify_cross_repo_secret.py` = read-only (gh secret list 조회만, 등록/수정 없음).

## §7 Dependencies

| 의존 | 상태 | 내용 |
|------|------|------|
| MCT-176 LAND | COMPLETED 2026-05-15 | collector container + NAS credential rotation + effective config dump. Phase 2 PR2 포함 전체 LAND |
| ADR-030 | Accepted (2026-05-15) | §D2 + §D4 + §D10 + §D15 amendment box = 본 Story Phase 1 박제 |
| ADR-029 §D4 | VERIFIED (MCT-171) | WAL local-only 정책 — paper daemon WAL fsync + 60s grace 정합 |
| InvariantHarness 8종 | MCT-171 SSOT + MCT-172 POLICY_FINALIZED | startup scan — paper daemon 기동 시 8 invariant 검증 |
| dr_mode state machine | MCT-170 LAND | NAS 5xx → DR mode (CLOSED/OPEN/HALF_OPEN) — paper engine 경유 |
| UNIVERSE_TOP_N=50 universe | MCT-103 LAND | 50-sym universe default (scorer expansion 완료) |

### MCT-176 carry over 통합 표 (CO-1~CO-3)

| # | 항목 | 이월 사유 | 처리 위치 | 완료 gate |
|---|------|----------|----------|----------|
| CO-1 | YAML config loader (option A) | source_order downgrade (false claim 차단) | mctrader-data Phase 2 PR1 | AC-5 CO-1 PASS |
| CO-2 | _register_signal_handlers + _SHUTDOWN_REQUESTED wiring | stub → non-asyncio 실 등록 | mctrader-data Phase 2 PR1 | AC-5 CO-2 PASS |
| CO-3 | 6 repo secret read 검증 | hub 단방향 등록만 확인 | scripts/verify_cross_repo_secret.py 신규 | AC-5 CO-3 PASS |

## §8 Test contract

### D4 SIGTERM unit test (Phase 2 PR1, mctrader-engine)

`tests/test_sigterm_paper_daemon.py` 신규:

- `test_sigterm_handler_sets_shutdown_flag`: SIGTERM 수신 시 `_SHUTDOWN_REQUESTED = True` 확인
- `test_sigint_handler_sets_shutdown_flag`: SIGINT 수신 시 동일 flag 확인
- `test_paper_daemon_exits_gracefully_on_shutdown`: `_SHUTDOWN_REQUESTED=True` 상태에서
  loop 가 chunk boundary 에서 break + exit 0 확인

### D15 Redis prefix unit test (Phase 2 PR1, mctrader-engine)

`tests/test_redis_prefix.py` 신규:

- `test_engine_key_prefix_applied`: `_engine_key("position:BTC-USD")` → `"engine:position:BTC-USD"` 확인
- `test_redis_key_prefix_env_override`: `REDIS_KEY_PREFIX_ENGINE=prod-engine` 환경변수 재정의 확인

### D10 universe-id override unit test (Phase 2 PR1, mctrader-engine)

- `test_universe_id_from_env`: `UNIVERSE_TOP_N=30` → `universe_id="top-30"` 확인
- `test_universe_id_explicit_override`: `--universe-id alt-30` CLI override 확인
- `test_universe_id_invalid_raises`: 미등록 universe-id → exit 1 확인

### CO-1 YAML loader unit test (Phase 2 PR1, mctrader-data)

`tests/test_yaml_config_loader.py` 신규:

- `test_yaml_loader_3tier_merge`: env > yaml > built-in 우선순위 확인
- `test_yaml_loader_file_absent_returns_empty`: yaml file 부재 시 {} 반환 (KeyError 없음)
- `test_yaml_loader_env_overrides_yaml`: env 값이 yaml 값을 덮어씀

`tests/test_effective_config.py` amend (MCT-176 §8 amendment 연속):

- `test_yaml_overrides_builtin` → 3-tier chain 으로 갱신 (YAML loader LAND 이후)
- `test_source_order_3tier`: source_order = `["env","yaml_default","built_in"]` 확인

### CO-2 signal wiring test (Phase 2 PR1, mctrader-data)

- `test_signal_handler_registered_in_collect`: collect command 실행 시 SIGTERM/SIGINT handler 등록 확인
- `test_collect_loop_exits_on_shutdown_flag`: `_SHUTDOWN_REQUESTED=True` → loop 즉시 break 확인

### CO-3 cross-repo secret verify (Phase 2 PR1, mctrader-hub)

`scripts/verify_cross_repo_secret.py` 신규 (gh CLI 기반, read-only):

- 6 repo (data/engine/web/market/signal-collector/hub) 순회
- 각 repo `MCTRADER_CROSS_REPO_TOKEN` secret 존재 여부 `gh secret list` 조회
- 미등록 repo 목록 출력 + exit 1 (모두 등록 시 exit 0)

## §8.5 Impl Manifest

> Phase 2 PR1 LAND 시 DeveloperPLAgent 가 git diff 분석 후 박제 (CFP-39).
> Phase 2 PR2 (본 PR) 시 ArchitectPLAgent confirm.

### mctrader-data#65 (af6c812, land_order 1)

| 파일 | 변경 유형 | owner | Change Plan §5 매핑 |
|------|-----------|-------|---------------------|
| `src/mctrader_data/cli.py` | MODIFY | DataEng | CO-1 `_load_yaml_config()` 신규 + `source_order` → `["env","yaml_default","built_in"]` 3-tier 복원 / CO-2 `_register_signal_handlers` non-asyncio entry (`backfill`/`compact`) 실 등록 + collect loop chunk boundary `_SHUTDOWN_REQUESTED` polling |
| `tests/test_yaml_config_loader.py` | CREATE | DataEng | CO-1 3-tier merge / file absent / env overrides yaml (3 test) |
| `tests/test_effective_config.py` | MODIFY | DataEng | CO-1 `test_yaml_overrides_builtin` 3-tier amend + `test_source_order_3tier` (MCT-176 §8 amendment 연속) |
| `tests/test_collect_shutdown.py` | CREATE | DataEng | CO-2 `test_signal_handler_registered_in_collect` + `test_collect_loop_exits_on_shutdown_flag` |

### mctrader-engine#54 (9cbe3b4, land_order 2)

| 파일 | 변경 유형 | owner | Change Plan §5 매핑 |
|------|-----------|-------|---------------------|
| `src/mctrader_engine/cli.py` | MODIFY | DevAgent | D4 paper daemon = 기존 `shutdown.py` asyncio SSOT + HealthServer (:8080) 재사용 (신규 daemon 코드 0 line — RefactorAgent (A) dead path 제거 판정) / D10 `--universe-id` CLI option + `UNIVERSE_TOP_N` env fallback + 미등록 시 exit 1 |
| `src/mctrader_engine/redis_keys.py` | MODIFY | DevAgent | D15 `REDIS_KEY_PREFIX_ENGINE` env (default `engine`) + `_engine_key()` helper. `engine:position:*` / `engine:strategy:*` namespace |
| `tests/test_sigterm_paper_daemon.py` | CREATE | DevAgent | D4 `test_sigterm_handler_sets_shutdown_flag` / `test_sigint_handler_sets_shutdown_flag` / `test_paper_daemon_exits_gracefully_on_shutdown` (기존 shutdown.py asyncio 경로 검증) |
| `tests/test_redis_prefix.py` | CREATE | DevAgent | D15 `test_engine_key_prefix_applied` / `test_redis_key_prefix_env_override` |
| `tests/test_universe_override.py` | CREATE | DevAgent | D10 `test_universe_id_from_env` / `test_universe_id_explicit_override` / `test_universe_id_invalid_raises` |

### mctrader-hub#334 (cc0c368, land_order 3)

| 파일 | 변경 유형 | owner | Change Plan §5 매핑 |
|------|-----------|-------|---------------------|
| `compose.yml` | MODIFY | InfraEng | D2 `paper-engine` service 신규 (image + `command: ["paper","--daemon"]` + `restart: unless-stopped` + healthcheck :8080 + `stop_grace_period: 60s` + depends_on redis/collector service_healthy) |
| `.env.dev` | MODIFY | InfraEng | D10 `UNIVERSE_TOP_N=50` + D15 `REDIS_KEY_PREFIX_ENGINE=engine` 박제 |
| `.env.prod.example` | MODIFY | InfraEng | D10 `UNIVERSE_TOP_N=50` + D15 `REDIS_KEY_PREFIX_ENGINE=engine` 박제 |
| `scripts/verify_cross_repo_secret.py` | CREATE | InfraEng | CO-3 6 repo (data/engine/web/market/signal-collector/hub) `MCTRADER_CROSS_REPO_TOKEN` secret read 검증 (gh CLI, read-only, 미등록 repo 목록 출력 + exit 1) |
| `tests/test_verify_cross_repo_secret.py` | CREATE | InfraEng | CO-3 unit test (mock gh secret list — 전체 등록 exit 0 / 일부 미등록 exit 1) |

## §9 Plan reference

- **plan**: `docs/superpowers/plans/2026-05-15-mct-177-paper-engine.md`
- **spec**: `docs/superpowers/specs/2026-05-15-EPIC-mctrader-docker-stack-design.md`
- **scope_manifest**: `scope_manifests/EPIC-mctrader-docker-stack.yaml` §MCT-177

## §10 FIX Ledger

> Orchestrator 독점 append (fix-event-v1 contract). 본 에이전트 직접 기록 금지.

| iter | lane | category | mechanical_category | file | suggestion | resolution |
|------|------|----------|---------------------|------|------------|------------|
| 1 | design (Phase 1 hub docs) | — | — | — | DesignReview iter 1 **PASS (no FIX)** — MCT-175/176 §6.5 lesson 누적 효과 (§6.5 §7/§11 N/A 4 entry 사전 박제) | PASS, Phase 2 PR1 진입 |
| 1 | code (data#65) | correctness | type_error | `src/mctrader_data/cli.py` | **P0** pyright — `_load_yaml_config()` 반환 `dict[str, Any]` 미선언 + `yaml.safe_load` `Any | None` narrowing 누락 (None 시 `{}` 반환 type guard 부재) | fix — return type annotation + `result = yaml.safe_load(f) or {}` narrowing + `tests/test_yaml_config_loader.py` file-absent 경로 추가 |
| 1 | code (engine#54) | correctness | logic_error | `src/mctrader_engine/cli.py` | **P0** paper daemon = data 동기 stub 패턴 carry over — 기존 `shutdown.py` asyncio SSOT + HealthServer(:8080) 미인지 후 신규 동기 SIGTERM loop 작성 (이중 graceful 경로 → :8080 healthcheck 미기동 risk). MCT-170 류 Phase 0 verify lesson 재현 | fix — **RefactorAgent 판정 (A) dead path 제거** + paper start core 위임 → 신규 daemon 코드 0 line (기존 shutdown.py asyncio + HealthServer 재사용). plan §2.2 amend (data 패턴 carry over 취소) |
| 1 | code (hub#334) | contract | — | `compose.yml` | **P0** paper-engine service `healthcheck.test` urllib 경로가 ADR-030 §D2 amendment box 박제 contract (`http://localhost:8080/health`) 와 불일치 (port 8080 hardcode 누락) + `depends_on` collector `service_healthy` condition 누락 | fix — healthcheck test ADR-030 §D2 박제 verbatim 정합 + `depends_on: {redis: service_healthy, collector: service_healthy}` 명시 |
| 1 | code (hub#334) | ownership | — | `scripts/verify_cross_repo_secret.py` | **P1** CO-3 script owner = hub (cross-repo secret 검증 = hub governance 영역). 초안이 data repo 측 배치 시도 → owner 경계 위반 | fix — `mctrader-hub/scripts/verify_cross_repo_secret.py` 로 배치 확정 + Story §8 §11.6 idempotency (read-only gh secret list) 박제 정합 |
| 2 | all lanes | — | — | — | data PASS / engine PASS / **hub CONDITIONAL_PASS** (sequential gate — data#65 + engine#54 LAND 후 hub#334 LAND order 강제) | PASS, Phase 2 PR1 3 PR sequential MERGED |

## §10.5 Git Ops Log

> GitOpsAgent 단독 append-only. 삭제/수정 금지.

| Event | 시각 | Actor | Detail | Outcome |
|-------|------|-------|--------|---------|
| WORKTREE_CREATE | 2026-05-15T14:00:00+09:00 | GitOpsAgent | Phase 1 worktree 제거 (squash merged hub#333 dd59b65) — feat/MCT-177-paper-engine-phase1 local branch deleted | SUCCESS |
| WORKTREE_CREATE | 2026-05-15T14:00:00+09:00 | GitOpsAgent | Phase 2 PR1 worktree 3개 동시 생성 (data e3141b6 / engine a00690b / hub dd59b65) — feat/MCT-177-paper-engine-phase2-pr1 | SUCCESS |
| PR_OPEN | 2026-05-15T14:00:00+09:00 | GitOpsAgent | data PR open — mctrader-data#65 (YAML loader + signal wiring, land_order=1) | SUCCESS |
| PR_OPEN | 2026-05-15T14:00:00+09:00 | GitOpsAgent | engine PR open — mctrader-engine#54 (SIGTERM + universe override + Redis prefix, land_order=2) | SUCCESS |
| PR_OPEN | 2026-05-15T14:00:00+09:00 | GitOpsAgent | hub PR open — mctrader-hub#334 (paper-engine service + Redis env + 6 repo secret, land_order=3) | SUCCESS |
| PR_MERGE | 2026-05-15T09:30:00Z | GitOpsAgent | data#65 squash MERGED — af6c812 (land_order 1) | SUCCESS |
| PR_MERGE | 2026-05-15T09:30:10Z | GitOpsAgent | engine#54 squash MERGED — 9cbe3b4 (land_order 2) | SUCCESS |
| PR_MERGE | 2026-05-15T09:30:21Z | GitOpsAgent | hub#334 squash MERGED — cc0c368 (land_order 3, sequential gate after data+engine) | SUCCESS |
| WORKTREE_REMOVE | 2026-05-15T18:30:00+09:00 | GitOpsAgent | Phase 2 PR1 worktree 제거 (data+engine+hub 3개 squash merged) — feat/MCT-177-paper-engine-phase2-pr1 local branch deleted (was f80c98b) | SUCCESS |
| WORKTREE_CREATE | 2026-05-15T18:30:00+09:00 | GitOpsAgent | Phase 2 PR2 worktree 생성 (main post-cc0c368) — feat/MCT-177-paper-engine-phase2-pr2 | SUCCESS |
| PR_OPEN | 2026-05-15T18:30:00+09:00 | GitOpsAgent | hub Phase 2 PR2 박제 PR open — 6 file (Story + ADR-030 + scope_manifest + CLAUDE.md + RETRO 신규 + EPIC-RESULTS §Story-3) | SUCCESS |

## §11 Retro

> Phase 2 PR2 LAND 시 PMOAgent 자동 dispatch 후 박제 완료.

- **retro_file**: `docs/retros/RETRO-MCT-177.md` (PMOAgent sub-dispatch, 본 PR 신규)
- **핵심 lesson**: engine#54 P0 = 기존 `shutdown.py` asyncio SSOT + HealthServer(:8080) 미인지 후
  data 동기 SIGTERM stub 패턴 carry over (MCT-170 류 Phase 0 verify lesson 재현). RefactorAgent
  판정 (A) dead path 제거 + paper start core 위임 → **신규 daemon 코드 0 line** (기존 검증 자산
  재사용). plan §2.2 amend (data 패턴 cross-repo 오적용 취소).
- **DesignReview iter 1 PASS** (no FIX) — MCT-175/176 §6.5 lesson 누적 효과.

## §12 측정

> Phase 2 PR1 LAND 후 측정 결과 박제 (Phase 2 PR2 confirm).

| 항목 | 측정값 | gate | verdict |
|------|--------|------|---------|
| paper-engine healthcheck :8080 응답 (AC-1) | 200 OK (기존 HealthServer 재사용 — daemon compose config exit 0 + HealthServer asyncio task) | 200 OK | **PASS** |
| SIGTERM → exit 0 elapsed (AC-2) | exit 0 via 기존 `shutdown.py` asyncio SSOT graceful (60s grace 정합, 신규 코드 0 line) | ≤ 60s | **PASS** |
| UNIVERSE_TOP_N=50 env + `--universe-id` override (AC-3) | `.env.dev`/`.env.prod.example` `UNIVERSE_TOP_N=50` + CLI `--universe-id` override + 미등록 exit 1 | env 50 + override | **PASS** |
| Redis prefix 3 namespace (AC-4) | `signal:*` / `market:*` / `engine:*` 분리 + `REDIS_KEY_PREFIX_ENGINE` env (default `engine`) | 3 namespace | **PASS** |
| CO-1 YAML loader 3-tier source_order (AC-5) | `["env","yaml_default","built_in"]` 복원 (MCT-176 downgrade 해소, false claim 차단) | ["env","yaml_default","built_in"] | **PASS** |
| CO-2 `_SHUTDOWN_REQUESTED` collect loop wiring (AC-5) | non-asyncio entry (`backfill`/`compact`) `signal.signal()` 등록 + collect loop chunk boundary polling | wiring 존재 | **PASS** |
| CO-3 6 repo secret verify (AC-5) | `scripts/verify_cross_repo_secret.py` read-only gh secret list — 6 repo 순회 + 미등록 목록 출력 | exit 0/1 정합 | **PASS** |
| cross-repo PR LAND | 4 PR sequential (hub#333 + data#65 + engine#54 + hub#334) | 4/4 MERGED | **PASS** |
| FIX 루프 | code iter 1 P0×3 + P1×1 (data pyright / engine daemon stub / hub compose contract / hub script owner) → iter 2 PASS | ≤ 3 iter | **PASS** (1 code iter) |
