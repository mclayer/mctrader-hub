---
story_key: MCT-173
story_scope: cross-repo  # Phase 1 = mctrader-hub governance, Phase 2 = mctrader-data (entry scan + runner extend + backfill + verify) + mctrader-hub 박제
story_issues:
  - repo: mclayer/mctrader-hub
    number: 298  # MCT-173 trigger (MCT-166 D3=B 별 Story)
status: IN_PROGRESS
epic_key: EPIC-data-accumulation-umbrella  # D7=C carry-over umbrella, MCT-164/165/166 sibling
epic_milestone: null
parent_dependency: null
depends_on:
  - MCT-164  # D4=C WAL 복구 verdict 부분가능 + wal_freeze.py 도구
  - MCT-165  # verify framework (V2=0 의무, AC-5)
  - MCT-166  # fix LAND prerequisite (path B = orderbooksnapshot WAL → L1, INV-3 schema 정합)
related_adrs:
  - "ADR-017 §D2 — `.compacted` rename idempotency (INV-2/AC-3 정합, Kafka KIP-280)"
  - "ADR-017 Amendment 2 (MCT-164) — compactor source 규약 + channel matrix dispatch"
  - "ADR-009 §D12 — forward-only invariant (historical materialization 정의)"
created_at: 2026-05-14
completed_at: null  # Phase 2 land 후
retro_file: docs/retros/RETRO-MCT-173.md
delegates:
  - mctrader-data  # Phase 2 primary impl (4 단계: entry scan + runner extend + backfill + verify)
---

# MCT-173 — upbit L1 backfill Story (frozen WAL → historical compaction)

## §1 동기 (Why)

### 사용자 명시 동기 (verbatim)
> "진행해"

MCT-166 ALL LAND (2026-05-14) 직후. MCT-164 D4=C WAL 복구 부분가능 verdict + MCT-166 D3=B 별 Story 분리 결정 인용.

### 근본 동기 (Phase 0 Analyst)

MCT-166 fix LAND 후 t≥LAND 신규 정상 누적. t<LAND 기간 frozen orderbooksnapshot WAL 데이터 살아있으나 L1 미생성 → backtest history 가용 범위 축소 손실. 본 Story = t<LAND historical materialization → backtest 커버리지 확장.

**Researcher 정합** (forward-only invariant temporal scope): historical materialization ≠ backfill (Kafka log compaction 선례, KIP-280 sentinel pattern). `.compacted` rename idempotency 보장 → at-least-once → effectively-once.

### 9 D 결정 (Codex 합성 + Sonnet 채택)

| # | 결정 | 옵션 | scope |
|---|---|---|---|
| **D1** | compactor.runner `--backfill` mode extend | **B** | 본 Story (D1=B 신규 도구 면적 최소) |
| **D2** | Phase 2 entry 실측 결정 | **C** | 본 Story (frozen WAL path / freeze flag) |
| **D3** | point-in-time snapshot (concurrent WAL 회피) | **A** | 본 Story (INV-1) |
| **D4** | 기존 `.compacted` rename | **A** | 본 Story (INV-2, ADR-017 §D2) |
| **D5** | frontmatter manifest | **B** | 본 Story (INV-4 partial boundary 박제) |
| **D6** | 일반 Story flow + 당일 처리 | **B** | 본 Story (MCT-164/166 답습) |
| **D7** | 1 Story + Phase 2 단계 분할 | **C** | 본 Story (entry scan → backfill → verify) |
| **D8** | MCT-165 V2=0 + 별 verify (defense in depth) | **C** | 본 Story (INV-5, AC-5/6) |
| **D9** | Phase 2 entry inventory 후 결정 | **C** | 본 Story (pre-existing L1 file 정책) |

## §2 Phase 0 Context

### DomainAgent
- forward-only detective only / MCT-164 D4=C 부분가능 / MCT-166 정상 전이 / ADR-017 변환 의무 / boundary 2026-05-09
- 지식 공백 3: historical-materialization / sealed segment idempotency / WAL freeze 도구 박제 부재

### ResearcherAgent
- 핵심 개념 3: WAL Replay Idempotency (Sentinel-based, Kafka KIP-280) / Active vs Sealed Segment Boundary / Partition Path Determinism
- Unknowns 2: Partial WAL segment in date boundary (incomplete day silent hole) / Pre-existing L1 file from unrelated cause

### RequirementsAnalystAgent
- WHY: t<LAND historical materialization → backtest 커버리지 확장
- 확장 3: frozen WAL scan / idempotency / health verify

### PMOAgent
- 1 Story (단일 vertical slice)
- carry-over umbrella label only
- risk: 신규 incoming + historical 충돌 boundary P1 (→ D7=C 단계 분할로 완화)

## §3 Scope

### Phase 1 (mctrader-hub PR — docs only)
- **Create**: `docs/superpowers/specs/2026-05-14-MCT-173-upbit-l1-backfill-design.md`
- **Create**: `docs/superpowers/plans/2026-05-14-mct-173-upbit-l1-backfill.md`
- **Create**: `docs/stories/MCT-173.md` (본 file)
- **Modify**: `.codeforge/counters.json` (MCT-173 title 확장)

### Phase 2 (mctrader-data PR + mctrader-hub PR)

#### mctrader-data
- **Create**: `scripts/backfill_entry_scan.py` (Phase 2.1 audit, D2=C/D9=C)
- **Create**: `docs/audit/MCT-173-entry-scan.md` (audit 결과 박제)
- **Modify**: `src/mctrader_data/compactor/runner.py` (`--backfill` mode, D1=B)
- **Create**: `src/mctrader_data/compactor/backfill.py` (frozen WAL iterator + manifest writer, D5=B)
- **Create**: `scripts/verify_backfill_partial_loss.py` (별 verify, D8=C)
- **Create**: `tests/unit/compactor/test_backfill.py`
- **Create**: `tests/integration/test_backfill_upbit_l1.py`
- **Modify**: `CLAUDE.md` (§backfill mode)

#### mctrader-hub (Phase 2 박제)
- **Modify**: `docs/stories/MCT-173.md` (§8-§12 추가)
- **Modify**: `docs/domain-knowledge/domain/data-health/exchange-channel-matrix.md` (MCT-166 result 아래 MCT-173 backfill result 추가)
- **Modify**: `CLAUDE.md` (§backfill mode + §verify gate)
- **Create**: `docs/retros/RETRO-MCT-173.md`

### scope_manifest 위치
D7=C carry-over umbrella label only (MCT-164/165/166 sibling) — 신규 Epic file 없음. 본 §3 자체가 Story-level SSOT.

## §4 Invariant (5 개)

- **INV-1**: Source WAL immutability — backfill 중 frozen WAL 무변경 (D3=A point-in-time snapshot)
- **INV-2**: Idempotency — 동일 backfill 반복 실행해도 final state 동일 (D4=A `.compacted` rename)
- **INV-3**: Schema 정합 — backfill L1 schema == MCT-166 path B L1 schema (`_ob_snapshot_dicts_to_arrow()` 재사용)
- **INV-4**: Partial boundary 박제 — frontmatter manifest 가 frozen 시작 ~ MCT-166 LAND 까지 date range 명시 (D5=B)
- **INV-5**: Verify gate — MCT-165 V2=0 AND 별 verify partial loss within threshold 양쪽 통과 후에만 §11 RETRO (D8=C)

## §5 Acceptance Criteria (7 개)

- **AC-1 (entry scan)**: Given MCT-166 fix LAND 직후, When Phase 2.1 entry scan 실행, Then frozen WAL path + freeze flag + pre-existing L1 inventory + partial WAL date boundary audit 박제
- **AC-2 (`--backfill` mode)**: Given frozen WAL segment list 입력, When `compactor.runner --backfill --exchange upbit --tier L1 --channel orderbooksnapshot` 실행, Then point-in-time snapshot 출력 + frontmatter manifest partial boundary 박제
- **AC-3 (idempotency)**: Given 동일 backfill job 재실행, When 기존 `.compacted` sentinel 존재, Then 해당 segment skip + source WAL 무변경
- **AC-4 (backfill 실행)**: Given Phase 2.3 trigger, When upbit orderbooksnapshot frozen WAL 전체 처리, Then L1 parquet row count > 0 + `_ob_snapshot_dicts_to_arrow()` schema 정합
- **AC-5 (MCT-165 verify)**: Given Phase 2.4 verify, When `data_health_check --tier L1 --exchange upbit --date <range>` 실행, Then V2 (forward-only loss) = 0
- **AC-6 (별 verify partial loss)**: Given Phase 2.4 별 verify, When frozen WAL row count vs L1 row count 비교, Then partial loss ratio 박제, threshold 초과 시 §10 FIX trigger
- **AC-7 (hub 박제)**: Given Phase 2 완료, When hub PR merge, Then Story §8-§12 + channel matrix MCT-166 result 아래 MCT-173 backfill result + RETRO

## §6 Risk (3 개)

- **R1 (High)**: Phase 2.1 entry scan 결과 frozen WAL path 가 예상과 다름 (MCT-164 wal_freeze.py 실제 동작 미확인) → runner extend 설계 재작업. **완화**: D2=C entry 실측 + D7=C Phase 2 단계 분할 (실측 결과에 따른 design amendment)
- **R2 (Mid)**: Partial WAL boundary 가 freeze 시각 메타데이터 부재로 추정 불가 → manifest 박제 불완전. **완화**: D5=B manifest best-effort 박제 + INV-4 명시
- **R3 (Mid)**: backfill L1 schema 가 MCT-166 path B 와 미세 차이 → downstream backtest reader 호환성 break. **완화**: INV-3 schema 정합 + 단위/통합 test 의무

## §7 Phase 분할

| Phase | 산출물 | PR |
|---|---|---|
| Phase 1 | spec + plan + Story §1-§7 + counters | mctrader-hub Phase 1 |
| Phase 2.1 (entry scan) | frozen WAL path + freeze flag + pre-existing L1 inventory + partial boundary audit | mctrader-data Phase 2 PR (audit commit) |
| Phase 2.2 (runner extend) | `--backfill` mode + manifest writer + 단위 test | mctrader-data Phase 2 PR (extend commit) |
| Phase 2.3 (backfill 실행) | L1 parquet 생성 + idempotency 검증 | mctrader-data Phase 2 PR (execute commit) |
| Phase 2.4 (verify) | MCT-165 V2=0 + 별 verify partial loss | mctrader-data Phase 2 PR (verify commit) |
| Phase 2 hub | 결과 박제 + channel matrix update + Story §8-§12 + CLAUDE.md + RETRO + Issue close | mctrader-hub Phase 2 PR |

## §8 ~ §12 (Phase 2 박제)

§8 Test Contract / §9 Operational Risk / §10 FIX Ledger + entry scan 결과 + manifest content + partial loss verdict / §11 Invariant cross-ref / §12 PMO retro = Phase 2 land 후 박제 의무.

§10 의무: MCT-164 §10 인용 (D4=C 부분가능 verdict) + MCT-166 §10 인용 (D2=B alternative path B 채택, INV-5 인과 chain) + Phase 2.1 entry scan 결과 (D2/D9 결정) + AC-2~7 verdict.

## §13 Cross-ref

- **MCT-164 §10** (D4=C WAL 복구 verdict, 본 Story trigger source)
- **MCT-165 V2** (verify framework, AC-5)
- **MCT-166** (D3=B 별 Story 결정 + D2=B alternative path B + LAND prerequisite)
- **ADR-017 §D2** (`.compacted` rename, INV-2 정합)
- **ADR-017 Amendment 2 (MCT-164)** (compactor source 규약)
- **ADR-009 §D12** (forward-only invariant)
- **Issue #298** (본 Story trigger)
- **EPIC-data-accumulation-umbrella** (MCT-164/165/166 sibling, 본 Story land 시 EPIC 종료 후보)
