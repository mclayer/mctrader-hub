---
story_key: MCT-168
story_scope: cross-repo
story_issues:
  - repo: mclayer/mctrader-hub
    number: TBD
  - repo: mclayer/mctrader-data
    number: TBD
status: IN_PROGRESS
epic_key: EPIC-tier-promotion-single-source
parent_dependency: MCT-167  # ADR-029 + ADR-017/027/009 amend LAND 전제
depends_on:
  - MCT-167  # ADR-029 신규 + amendment 3건 LAND (2026-05-14, PR #305)
  - MCT-161  # NAS bucket versioning + Object Lock (D6 prerequisite, LAND 2026-05-14)
  - MCT-163  # DualWriter put_streaming + L2/L3 iter_batches (R1 mitigation, LAND 2026-05-14)
related_adrs:
  - "ADR-029 D1=B + D2=B owner Story — Phase 2 LAND 시 verify status 박제 의무"
  - "ADR-017 §3 D3 amendment (L1 NAS PUT 의무, MCT-167 LAND)"
  - "ADR-027 §D5 폐기 + §D7 grace 0 + §D9 SoT 확장 (MCT-167 LAND)"
created_at: 2026-05-14
completed_at: null
phase_pair: phase1_phase2
delegates:
  - DeveloperAgent (mctrader-data: l1.py + runner.py + dual_writer.py)
  - QADeveloperAgent (mctrader-data: 3 test file)
retro_file: docs/retros/RETRO-MCT-168.md
---

# MCT-168 — L1 NAS DualWriter wiring (EPIC-tier-promotion-single-source Story-2)

## §1 동기

**EPIC-tier-promotion-single-source Story-2** — ADR-029 D1+D2 owner Story. L1 ParquetWriter atomic 직후 DualWriter.put_streaming() 호출 + retry_queue 재사용으로 L1 NAS dual-write hot path 확립.

### 1.1 직접 동기

MCT-167 (ADR-029 publish) 가 결정한 D1=B (L1 NAS PUT timing = L1 ParquetWriter atomic 직후, compactor 측) + D2=B (DualWriter retry_queue + local_only 재사용) 을 구현. ADR-027 §D5 "L1 NAS upload 금지" invariant 폐기 후 첫 번째 구현 Story.

NFR: L1 PUT p99 < 1500 ms (NAS LAN baseline + MCT-148 T2 ± 15%).

### 1.2 D1+D2 결정 (ADR-029 박제분)

- **D1=B**: L1 NAS PUT timing = L1 ParquetWriter atomic 직후 (compactor 측). 파일 단위 정합성 + hot path 지연 균형.
- **D2=B**: NAS PUT 동기/비동기 = 기존 DualWriter retry_queue + local_only 재사용. hot path block 최소 + MCT-150/151 primitive 재사용 (logical sync).
- **NFR**: L1 PUT p99 < 1500 ms (NAS LAN baseline + MCT-148 T2 ± 15%)

## §2 Phase 0 Context (brainstorm 답습)

별 세션 (2026-05-14) PMOAgent 2nd pass 박제분.

### 2.1 AC (6~8)

- **AC-6 (L1 NAS PUT trigger)**: compactor L1 segment atomic rename 직후 DualWriter.put_l1() 호출 + Prometheus `mctrader_dual_write_result_total{tier="L1",status}` emit
- **AC-7 (retry_queue + local_only)**: NAS PUT fail (max retry 소진) → retry_queue append + status=local_only 반환, compactor 정상 종료 (L1 local 보존)
- **AC-8 (L1 PUT p99 < 1500ms NFR)**: 100회 측정 p99 < 1500 ms, Prometheus histogram `mctrader_dual_write_l1_latency_seconds` 검증

### 2.2 INV (4~6)

- **INV-4**: L1 ParquetWriter atomic rename 성공 = L1 local SSOT (NAS PUT 실패해도 segment 보존)
- **INV-5**: DualWriter.put_l1() status enum 3종 (committed / local_only / hard_floor_blocked) 정확 1개 반환
- **INV-6**: retry_queue replay idempotent (NAS versioning + Object Lock 의존, MCT-161 정합)

### 2.3 Risk (3)

- **R-1 (HIGH)**: L1 PUT p99 NFR 미달 → 완화 = MCT-163 put_streaming 적용 + segment size budget + async PUT fallback escalate
- **R-2 (MID)**: retry_queue overflow → 완화 = max size cap + Prometheus alert + manual drain runbook
- **R-3 (MID)**: L1 mode + L2/L3 mode object key 충돌 → 완화 = tier prefix enforce (l1/ l2/ l3/) + path schema test

## §3 결정 (D1 + D2 — ADR-029 답습)

| D | 결정 | 옵션 | 본문 |
|---|---|---|---|
| **D1** | L1 NAS PUT timing — L1 ParquetWriter atomic 직후 (compactor 측) | **B** | ADR-029 §D1 |
| **D2** | NAS PUT 동기/비동기 — DualWriter retry_queue + local_only 재사용 | **B** | ADR-029 §D2 |

## §4 산출물

### Phase 1 (hub PR)

- docs/stories/MCT-168.md (본 파일, §1-§7)
- counters.json MCT-168 title 확장 + retitle_history
- scope_manifests/EPIC-tier-promotion-single-source.yaml MCT-168 IN_PROGRESS 갱신 (milestone 2/6)

### Phase 2 (data PR + hub PR)

- src/mctrader_data/compactor/l1.py — compact_segment() 에 DualWriter inject + L1 NAS PUT 호출
- src/mctrader_data/compactor/runner.py — L1Compactor inject DualWriter pass-through
- src/mctrader_data/nas_storage/dual_writer.py — put_l1() alias method (tier prefix = "l1/")
- tests/integration/compactor/test_l1_nas_put.py (신규)
- tests/integration/nas_storage/test_dual_writer_l1.py (신규)
- tests/unit/compactor/test_l1_hook.py (신규)

## §5 Change Plan

### §5.1 mctrader-data: L1 NAS PUT wiring

**파일 1: src/mctrader_data/nas_storage/dual_writer.py**

- `put_l1(path: Path) -> DualWriteResult` method 추가
  - nas_key = `"l1/" + str(path.relative_to(self._local_root)).replace("\\", "/")`
  - sha256 streaming 계산 (8MB chunk) 후 `write(local_path=path, nas_key=nas_key, data=path, sha256=sha256)` 위임
  - tier prefix = "l1/" (R-3 mitigation: L2="l2/", L3="l3/" 기존 분리 유지)
  - Prometheus `mctrader_dual_write_l1_latency_seconds` histogram observe (AC-8)
  - `mctrader_dual_write_result_total{tier="L1", status}` emit (AC-6)

**파일 2: src/mctrader_data/compactor/l1.py**

- `L1Compactor.__init__()` 에 `dual_writer: DualWriter | None = None` 주입
- `compact_segment()` 내 `_write_parquet_atomic()` 직후 (INV-4: atomic rename 완료 후) `put_l1()` 호출
- NAS PUT 실패 시 log warning + 정상 반환 (INV-4: local 보존, compactor 중단 0)
- Prometheus emit 은 `put_l1()` 내부 위임

**파일 3: src/mctrader_data/compactor/runner.py**

- `CompactorRunner.__init__()` 에서 `dual_writer` 를 `L1Compactor` 에 pass-through
  - `L1Compactor(root=root, dual_writer=dual_writer)` 로 생성 변경
- `_run_l2_for_parquet()` 주석 S3 invariant 제거 (ADR-027 §D5 폐기 정합)

### §5.2 mctrader-data: 3 test 파일

**파일 4: tests/unit/compactor/test_l1_hook.py**

- `L1Compactor` 에 mock DualWriter inject + `compact_segment()` 호출 시 `put_l1()` 1회 호출 확인
- NAS PUT fail (mock side_effect) → compactor 정상 종료 + L1 local file 보존 확인 (INV-4)
- `dual_writer=None` 시 NAS PUT 호출 0 (backward compat)

**파일 5: tests/integration/compactor/test_l1_nas_put.py**

- 실제 NAS 연결 (환경변수 의존) 또는 mock NASUploader 사용
- L1 compaction → NAS bucket `l1/` prefix 객체 존재 확인 (AC-6)
- retry_queue enqueue 확인 (NAS unreachable 시, AC-7)

**파일 6: tests/integration/nas_storage/test_dual_writer_l1.py**

- `DualWriter.put_l1()` 단독 테스트
- status 3종 반환 확인 (INV-5)
- tier prefix `l1/` key 확인 (R-3 mitigation)
- p99 latency histogram bucket 분포 확인 (AC-8 전제)

### §5.3 Prometheus 신규 metric

- `mctrader_dual_write_l1_latency_seconds` Histogram 신규 등록
  - labels: tier="L1"
  - buckets: (0.1, 0.25, 0.5, 1.0, 1.5, 3.0, 10.0) — NFR 1500ms 버킷 포함
  - prometheus_exporters.py 에 module-level 등록

## §6 AC 검증 계획

| AC | 검증 방법 | 판정 기준 |
|---|---|---|
| AC-6 | test_l1_hook.py + test_l1_nas_put.py | put_l1() 호출 확인 + NAS bucket l1/ prefix 객체 확인 |
| AC-7 | test_l1_hook.py NAS fail mock | retry_queue enqueue + compactor 정상 종료 확인 |
| AC-8 | test_dual_writer_l1.py latency mock + integration | p99 < 1500ms 확인 (histogram bucket) |

## §7 INV 검증 계획

| INV | 검증 방법 | 판정 기준 |
|---|---|---|
| INV-4 | test_l1_hook.py NAS fail mock | L1 local file 보존 확인 (NAS PUT fail 후) |
| INV-5 | test_dual_writer_l1.py | status enum 3종 중 정확 1개 반환 확인 |
| INV-6 | test_dual_writer_l1.py idempotency | 동일 key 재 PUT → skipped_idempotent 반환 확인 |

## §8 구현 상태

### §8.1 Phase 1 (hub PR)

- [ ] Story 파일 §1-§7
- [ ] counters.json MCT-168 title 확장
- [ ] scope_manifest MCT-168 IN_PROGRESS

### §8.2 Phase 2 (data PR)

- [ ] dual_writer.py put_l1() method
- [ ] l1.py DualWriter inject + compact_segment() hook
- [ ] runner.py dual_writer pass-through
- [ ] prometheus_exporters.py mctrader_dual_write_l1_latency_seconds
- [ ] test_l1_hook.py
- [ ] test_l1_nas_put.py
- [ ] test_dual_writer_l1.py

### §8.3 Phase 2 검증 결과 (후속 박제)

- L1 PUT p99 측정 결과: TBD
- AC-6 verdict: TBD
- AC-7 verdict: TBD
- AC-8 verdict: TBD
- INV-4 verdict: TBD
- INV-5 verdict: TBD
- INV-6 verdict: TBD

### §8.4 ADR-029 verify status (Phase 2 LAND 후 박제 의무)

- D1=B verify: TBD
- D2=B verify: TBD

### §8.5 Impl Manifest

| 파일 | 변경 유형 | Change Plan 매핑 | 상태 |
|---|---|---|---|
| src/mctrader_data/nas_storage/dual_writer.py | 기능 추가 | §5.1 put_l1() | Phase 2 |
| src/mctrader_data/compactor/l1.py | 기능 추가 | §5.1 DualWriter inject | Phase 2 |
| src/mctrader_data/compactor/runner.py | 연결 수정 | §5.1 pass-through | Phase 2 |
| src/mctrader_data/nas_metrics/prometheus_exporters.py | 신규 metric | §5.3 | Phase 2 |
| tests/unit/compactor/test_l1_hook.py | 신규 | §5.2 | Phase 2 |
| tests/integration/compactor/test_l1_nas_put.py | 신규 | §5.2 | Phase 2 |
| tests/integration/nas_storage/test_dual_writer_l1.py | 신규 | §5.2 | Phase 2 |

## §9 가드레일

1. **INV-4**: L1 local SSOT 보존 (NAS PUT fail 시 hard fail 금지 — compactor 정상 종료)
2. **INV-5**: DualWriter status enum 3종 정확 반환
3. **INV-6**: retry_queue replay idempotent (MCT-161 versioning 의존)
4. **R-1 mitigation**: MCT-163 put_streaming 활용 (data=Path)
5. **R-3 mitigation**: tier prefix enforce (l1/, l2/, l3/)
6. **Backward compat**: 기존 L2/L3 NAS PUT 회귀 0 (DualWriter 기존 L2/L3 mode 보존)
7. **Cross-repo branch race 회피** (memory feedback_parallel_session_branch_race)
8. **CI failure auto-recovery** (memory feedback_ci_failure_auto_recovery)
9. **Admin merge autonomy** (memory feedback_admin_merge_autonomy)
10. **dual_writer=None 시 NAS PUT 0** — backward compat (test/local dev 호환)

## §10 FIX Ledger

| iter | 실패 lane | 원인 유형 | 수정 요약 | 결과 |
|---|---|---|---|---|
| — | — | — | — | — |

## §11 PMO 회고

TBD (Phase 2 LAND 후 PMOAgent dispatch)

## §12 다음 Story

- **MCT-169** (L1 NAS verify + immediate local delete + tier promotion path) — MCT-168 LAND 후 진입
- 진입 gate: MCT-168 Phase 2 PR MERGED + L1 NAS dual-write production 검증 (NAS bucket l1/ prefix 출현)
