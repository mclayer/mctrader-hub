---
story_key: MCT-186
story_scope: cross-repo
status: COMPLETED
phase: Phase 2 PR2 박제  # 요구사항 ✓ → 설계 ✓ → 설계-리뷰 ✓ → 구현 ✓ → 구현-리뷰 ✓ → Phase 2 PR2 박제 ✓
started_at: "2026-05-17"
completed_at: "2026-05-17"
epic_key: EPIC-data-domain-decoupling
sequential_phase: 5
parent_dependency: ~
depends_on: [MCT-185]
decisions_implemented: [D4]
repo: "mctrader-hub + mctrader-engine"
story_issues:
  - repo: mclayer/mctrader-hub
    number: 370  # Phase 1 PR docs — Story §0-§11 + ADR-031 §D4 amendment box draft + ADR-030 NAS cred drop + scope_manifest + CLAUDE.md (MERGED 3fc9c1f)
  - repo: mclayer/mctrader-engine
    number: 60   # Phase 2 PR1 engine code — 5곳 5파일 bithumb import 제거 + Redis Stream subscriber + TickRowV1_1 소비 (MERGED 773b270)
  - repo: mclayer/mctrader-hub
    number: 371  # Phase 2 PR2 박제 — §8.5 Impl Manifest + ADR-031 §D4 VERIFIED + ADR-030 NAS cred drop amend + scope_manifest 5/7 + CLAUDE.md COMPLETED + RETRO + EPIC-RESULTS §Story-5 (MERGED cab600b)
related_adrs:
  - "ADR-031-data-domain-decoupling (Status: Accepted — MCT-182 §D1 VERIFIED + MCT-183 §D2 partial + MCT-184 §D3 partial + MCT-185 §D2+§D3 VERIFIED. 본 Story = §D4 engine exchange-adapter 제거 VERIFIED amendment box 박제 — Status Accepted 유지, POLICY_FINALIZED 전이 = MCT-188)"
  - "ADR-030-docker-stack-governance (Status: POLICY_FINALIZED — engine NAS cred drop 실 compose wiring 박제. amendment box = MCT-184 예고, 본 Story 확정 LAND. ADR-030 본문 19 D 정책 무변경, POLICY_FINALIZED 보존)"
  - "ADR-029-tier-promotion-single-source (접촉 없음 — MCT-185 §D2 VERIFIED amendment confirm 완결. 본 Story = engine realtime 경로 cutover, NAS 직독 0 유지)"
retro_file: docs/retros/RETRO-MCT-186.md  # Phase 2 PR2 PMOAgent sub-dispatch 시 생성
---

# MCT-186 — engine realtime cutover + exchange-adapter 제거

> **요구사항 lane 작성 범위**: §0 Phase 0 Verify Gate + §1·§2·§4·§5·§6 (self-write).
> **§3·§7·§11 = 설계 lane (ArchitectPLAgent Change Plan) 영역 — "설계 lane TBD" placeholder**
> (Change Plan = `docs/change-plans/MCT-186-change-plan.md`).
>
> **본 Story 위치**: EPIC-data-domain-decoupling 7 Story sequential strangler-fig 5단계.
> MCT-185 LAND (data realtime_stream.py Redis Stream publisher + engine data_client/) 를
> 전제로, engine 의 `mctrader_market_bithumb` 직접 import **5곳 5파일** 을 모두 제거하고
> MCT-185 publisher 가 방출하는 `market:tick:{exchange}:{symbol}` Redis Stream 을
> XREAD/XREADGROUP 구독으로 전환 (D4 = subscribe-normalized-stream). engine =
> **exchange-adapter-free** + 정규화 `TickRowV1_1` (market-core SSOT) 소비.

## §0 Phase 0 Verify Gate (R1 — 가설 금지, 요구사항 lane 실증)

요구사항 lane PL 이 2026-05-17 MCT-185 LAND + MCT-185 Phase 2 PR2 LAND 후 HEAD 기준
V1~V10 을 실제 코드 git fetch/Grep/Read/Bash 로 재검증했다. 결과 박제:

| V | 항목 | 검증 결과 | 판정 |
|---|------|-----------|------|
| V1 | engine origin/main HEAD 확인 (working tree stale 차단) | `git fetch origin` → engine origin/main = `1312195` `feat(MCT-185): engine data_client cutover — mctrader_data.* → REST API (AC-3 grep0, land_order 2)` (MCT-185 LAND ✅). hub origin/main = `66567e7` `docs(MCT-185): PMOAgent retro — §12 회고` (MCT-185 Phase 2 PR2 LAND ✅). data origin/main = `9473665` `feat(MCT-185): Layer2 data realtime stream + historical orderbook endpoints` (MCT-185 LAND ✅). **3 repo 전수 MCT-185 LAND 기준 최신 ✅** | ✅ 정합 |
| V2 | R2 MCT-43~47 active branches 재cross-check (engine Phase 0 의무) | engine repo `git branch -a` MCT-43~47/live 키워드 = **0건** (활성 진행 브랜치 0). `git log origin/main` MCT-43~47 커밋 키워드 = **0건**. paper_runner/ws_wrapper/stream_consumer/fill/mock_stream 최근 수정 = MCT-185 (1312195) only — MCT-43~47 관련 0건. **R2 ZERO RISK ✅** — MCT-186 sequential 진입 안전 | ✅ ZERO RISK (engine#N 진입 직전 R2 재재cross-check 의무 유지) |
| V3 | engine bithumb import **5곳 5파일** 전수 grep (Orchestrator 가설 = 정합 확인) | `grep -rn "from mctrader_market_bithumb" src/` = **5곳 5파일** — (1) `fill/simulated.py:18 from mctrader_market_bithumb.ws_events import OrderbookSnapshotEvent` (2) `realtime/stream_consumer.py:8-12 from mctrader_market_bithumb.ws_events import (OrderbookSnapshotEvent, StreamEvent, TickerEvent, TransactionEvent,)` (3) `runtime/mock_stream.py:19 from mctrader_market_bithumb.ws_events import StreamEvent, TickerEvent, TransactionEvent` (4) `runtime/paper_runner.py:267 from mctrader_market_bithumb.ws_client import BithumbWebSocketStream` (function-local lazy) (5) `runtime/ws_wrapper.py:30 from mctrader_market_bithumb.ws_events import StreamEvent`. **Orchestrator 가설 5곳 5파일 전수 정합** ✅ | ✅ 정합 |
| V4 | `mctrader_market_bithumb` 의존 engine pyproject.toml 확인 (MCT-188 final 제거 owner) | `pyproject.toml dependencies` 에 `"mctrader-market-bithumb @ git+https://github.com/mclayer/mctrader-market-bithumb.git@main"` 존재 → **MCT-188 D7 final 제거 owner. 본 Story = import 0건 grep 목표이나 pyproject 의존 line 유지** (MCT-185 mctrader-data 패턴 동형 — pyproject 유지하되 src/ import = 0) | ✅ 정합 (pyproject 유지 결정 재확인) |
| V5 | engine `data_client/` 5파일 존재 확인 (MCT-185 LAND 전제) | `src/mctrader_engine/data_client/` = `__init__.py` + `base.py` + `exceptions.py` + `historical.py` + `reverse_write.py` — **5파일 존재 ✅** (MCT-185 thin client LAND 확인). `httpx>=0.27` pyproject 의존 존재 ✅ | ✅ 정합 |
| V6 | `TickRowV1_1` 기존 engine 소비 현황 (D4 subscriber 자연 합류 가능성) | `grep -rn "TickRowV1_1" src/` = `hot/state_machine.py:42,86` + `hot/streaming_aggregator.py:55,109,140` — **2파일 5건 존재**. `StreamingAggregator.ingest(tick: TickRowV1_1)` — D4 subscriber 가 XREAD tick 역직렬화 후 `StreamingAggregator.ingest()` 경유 가능. `BarAggregator.on_transaction(ts_utc, price, quantity)` 시그니처 = TickRowV1_1.ts_utc/price/quantity 필드 직접 매핑 ✅ | ✅ 정합 (subscriber → StreamingAggregator 자연 합류 경로 확인) |
| V7 | `StreamConsumer.on_event()` bithumb `TransactionEvent` 의존 구조 분석 (cutover 복잡도) | `realtime/stream_consumer.py` = `MarketStream Protocol` + `StreamConsumer` 클래스. `on_event(event: StreamEvent)` → isinstance(event, TransactionEvent) 분기 → `BarAggregator.on_transaction(ts_utc=event.event_time, price=event.price, quantity=event.quantity)`. `OrderbookSnapshotEvent` → `_latest_orderbook` 캐시. `TickerEvent` → diagnostics only. **cutover 핵심**: `StreamEvent` (bithumb Union) 를 engine-local Protocol + TickRowV1_1 기반 정규화 이벤트 타입으로 교체. `_latest_orderbook: OrderbookSnapshotEvent` 필드 타입도 변경 필요 | ⚠️ **V7 정정 발견** — `stream_consumer.py` 가 `OrderbookSnapshotEvent` 를 캐시하며 `fill/simulated.py` 가 이 타입을 `SimulatedFillEngine.fill(orderbook: OrderbookSnapshotEvent)` 에서 직접 소비. **engine-local orderbook 타입 정의 필요** (bithumb-specific 타입 제거 + fill 엔진 파라미터 타입 교체) |
| V8 | `SimulatedFillEngine.fill(orderbook: OrderbookSnapshotEvent)` 의존 분석 (fill 엔진 타입 교체 범위) | `fill/simulated.py:60 orderbook: OrderbookSnapshotEvent` 파라미터 + `orderbook.asks`/`orderbook.bids` 속성 사용 (`_OrderbookLevel.price`/`_OrderbookLevel.quantity`). **교체 방향**: engine-local `OrderbookSnapshot` dataclass (exchange/symbol/ts_utc/bids/asks — bithumb `_BaseEvent` 무관) 신규 정의 → `simulated.py` + `stream_consumer.py` 모두 교체. bids/asks = `tuple[_Level, ...]` (price:Decimal, quantity:Decimal — 동일 구조). test_simulated_fill.py 는 `from mctrader_market_bithumb.ws_events import OrderbookSnapshotEvent, _OrderbookLevel` 직접 사용 → test 도 교체 대상 | ⚠️ **V8 정정 발견** — `test_simulated_fill.py` 포함 bithumb import 테스트 교체 의무. engine-local `OrderbookSnapshot` 정의 위치 = `realtime/types.py` 신규 (설계 lane 결정) |
| V9 | `mock_stream.py` bithumb `StreamEvent`/`TransactionEvent`/`TickerEvent` 의존 분석 (mock 교체 범위) | `runtime/mock_stream.py` = `load_mock_stream_from_json()` 가 JSON fixture 를 파싱해 `TransactionEvent`/`TickerEvent` 생성. **cutover 방향**: JSON fixture 동일 구조 유지 + mock 내부에서 `TickRowV1_1` 직접 생성 (TransactionEvent 경유 0). `MockMarketStream.messages()` yield 타입 = `TickRowV1_1` 로 변경. 기존 fixture `tests/fixtures/ws_mock_smoke.json` 재사용 가능 (kind/event_time/price/quantity/side 필드 ↔ TickRowV1_1 ts_utc/price/quantity/side 대응) | ✅ 정합 (mock 교체 가능, fixture 재사용 가능) |
| V10 | `ws_wrapper.py` bithumb `StreamEvent` 의존 분석 + `_build_upstream_stream()` lazy import 교체 범위 | `ws_wrapper.py:30 from mctrader_market_bithumb.ws_events import StreamEvent` → `WsWrapperStream.messages() -> AsyncIterator[StreamEvent]`. `paper_runner.py:267 from mctrader_market_bithumb.ws_client import BithumbWebSocketStream` → `_build_upstream_stream()`. **cutover 방향**: `ws_wrapper.py` yield 타입 = `TickRowV1_1` 또는 engine-local `NormalizedStreamEvent` 교체. `_build_upstream_stream()` = Redis Stream subscriber (`RedisStreamClient` 신규 — `data_client/` 확장 또는 `realtime/` 신규 모듈) 반환. `BithumbWebSocketStream` lazy import 완전 제거 | ✅ 정합 (ws_wrapper + paper_runner lazy import 교체 방향 확인) |

**Phase 0 결론**: V1~V6/V9~V10 = Orchestrator 가설 ↔ HEAD 실상 **정합**. **V7/V8 = 정정
발견** — `stream_consumer.py` 가 `OrderbookSnapshotEvent` 를 `_latest_orderbook` 로 캐시하며
`fill/simulated.py` 가 이 타입을 직접 소비. **engine-local `OrderbookSnapshot` dataclass 신규
정의 필요** (bithumb-specific `_BaseEvent`/`_OrderbookLevel` 완전 분리). `test_simulated_fill.py`
bithumb import 교체 의무 추가 발견 (V8). 신규 발견 2건이 본 Story 구현 범위를 명확히 확장:

- **추가 scope**: `realtime/types.py` 신규 (engine-local `OrderbookSnapshot` + `_Level` dataclass)
- **추가 scope**: `tests/test_simulated_fill.py` bithumb import 교체 (test 파일 cutover)
- **추가 scope**: `data_client/` 확장 또는 `realtime/redis_subscriber.py` 신규 (XREAD consumer)

**§0 수치 = HEAD 실증치 — 가설로 수용 금지. engine#N 착수 직전 V1 `git fetch origin` 재실행 +
V3(bithumb grep)/V7(stream_consumer OrderbookSnapshot 사용) HEAD 재대조 의무.**

## §1 Story 목표

### 1.1 목표 (D4 — scope_manifest `§story_decision_matrix.MCT-186` 1:1)

- **D4 (engine exchange-adapter 제거 — `option_chosen: subscribe-normalized-stream`)**:
  engine `mctrader_market_bithumb` 직접 import **5곳 5파일** (§0 V3 전수 확인) 전부 제거.
  MCT-185 data realtime_stream.py 가 XADD 하는 `market:tick:{exchange}:{symbol}` Redis Stream
  을 engine 측 XREAD/XREADGROUP 구독으로 전환. engine = **정규화 `TickRowV1_1`** (market-core
  Layer 0 SSOT) 소비 — bithumb-specific `ws_events.StreamEvent`/`TransactionEvent`/
  `TickerEvent`/`OrderbookSnapshotEvent` + `ws_client.BithumbWebSocketStream` 완전 폐기.

- **AC-1 grep0**: engine `src/` `from mctrader_market_bithumb` = **0건** (bithumb adapter
  import 완전 제거 — D7 quad gate 절반 준비)

- **AC-2 TickRowV1_1 정규화 소비**: `StreamingAggregator.ingest(tick: TickRowV1_1)` +
  `BarAggregator.on_transaction(ts_utc=tick.ts_utc, price=tick.price, quantity=tick.quantity)`
  경로 paper mode 실 동작. 기존 `BarAggregator.on_transaction()` 시그니처 변경 0 (byte-equiv)

- **AC-3 Redis Stream 구독 정합**: `market:tick:{exchange}:{symbol}` XREAD consumer
  (`DATA_API_BASE_URL` env 기반 Redis URL 추론 또는 `REDIS_URL` env 직독). MCT-185
  `realtime_stream.py` publisher payload = `TickRowV1_1.model_dump_json()` → engine subscriber
  = `TickRowV1_1.model_validate_json()` 역직렬화 (Pydantic SSOT 정합)

- **AC-4 paper mode tick latency baseline 회귀 0**: MCT-185 data_client thin client 도입
  후 paper mode 성능 SLO 기준 (MCT-170 §D7 = hit_ratio ≥0.95 p99 <100ms) 회귀 0. mock-feed
  mode 에서 `--max-events N` 지정 시 exit 0 + execution_report.json 생성 확인

- **AC-5 ADR-031 §D4 VERIFIED amendment box 박제**: `docs/adr/ADR-031-data-domain-decoupling.md`
  §D4 VERIFIED amendment box (engine bithumb adapter-free LAND confirm + Redis Stream subscriber
  wiring file:line + integration test result). Status Accepted 유지

- **AC-6 ADR-030 NAS cred drop amend**: engine compose service `NAS_MINIO_ENDPOINT`/
  `NAS_MINIO_ACCESS_KEY`/`NAS_MINIO_SECRET_KEY` env drop (engine 이 NAS 직접 접근 안 함 —
  MCT-185 §D2 VERIFIED 실 확인 + ADR-030 amendment box MCT-186 owner 확정)

- **AC-7 engine-local `OrderbookSnapshot` 타입 정의** (V7/V8 Phase 0 정정 — bithumb
  `OrderbookSnapshotEvent` 제거): `realtime/types.py` 신규 = `_Level(price, quantity)` +
  `OrderbookSnapshot(exchange, symbol, ts_utc, bids, asks)` engine-local dataclass.
  `fill/simulated.py` + `stream_consumer.py` + `test_simulated_fill.py` 교체 완료

### 1.2 비목표 (out-of-scope)

| 항목 | owner Story |
|------|-------------|
| engine `pyproject.toml` `mctrader-market-bithumb` 의존 line 제거 | MCT-188 (D7 quad gate final) |
| `mctrader-market-upbit` RealtimeStream Protocol conformance (opt) | MCT-186 선택 (설계 lane 결정) |
| 다중거래소 확장 불변식 (invariant test + runbook) | MCT-187 |
| data-free grep0 quad gate CI | MCT-188 |
| ADR-031 Status `Accepted` → `POLICY_FINALIZED` 전이 | MCT-188 |
| MCT-182 shim 잔존 5곳 cleanup (`tick_storage`/`orderbook_storage`/`aggregation` shim) | MCT-188 |
| engine NAS credential compose env drop (ADR-030 amendment box MCT-186 owner — 본 Story AC-6) | 본 Story 포함 ✓ |

## §2 배경 / AS-IS

### 2.1 AS-IS 사실 (file:line 근거 — Phase 0 V3 HEAD 실증)

| 사실 | file:line | 검증 |
|------|-----------|------|
| `fill/simulated.py:18` bithumb import | `from mctrader_market_bithumb.ws_events import OrderbookSnapshotEvent` (top-level, module 시작 시 실행) | V3/V7 Grep ✓ |
| `realtime/stream_consumer.py:8-12` bithumb import | `from mctrader_market_bithumb.ws_events import (OrderbookSnapshotEvent, StreamEvent, TickerEvent, TransactionEvent,)` (top-level) | V3/V7 Grep ✓ |
| `runtime/mock_stream.py:19` bithumb import | `from mctrader_market_bithumb.ws_events import StreamEvent, TickerEvent, TransactionEvent` (top-level) | V3/V9 Grep ✓ |
| `runtime/paper_runner.py:267` bithumb lazy import | `from mctrader_market_bithumb.ws_client import BithumbWebSocketStream` (function-local inside `_build_upstream_stream()`) | V3/V10 Grep ✓ |
| `runtime/ws_wrapper.py:30` bithumb import | `from mctrader_market_bithumb.ws_events import StreamEvent` (top-level) | V3/V10 Grep ✓ |
| `stream_consumer._latest_orderbook` 타입 | `_latest_orderbook: OrderbookSnapshotEvent \| None` + `.asks`/`.bids` 접근 (fill 엔진 공급) | V7 Read ✓ |
| `SimulatedFillEngine.fill(orderbook: OrderbookSnapshotEvent)` | `fill/simulated.py:60 orderbook: OrderbookSnapshotEvent` 파라미터 + `orderbook.asks/bids → levels` VWAP | V8 Read ✓ |
| `MockMarketStream` bithumb 타입 생성 | `mock_stream.py:58-82 events: list[StreamEvent]` + `TransactionEvent(...)` + `TickerEvent(...)` 생성 | V9 Read ✓ |
| `_build_upstream_stream()` lazy import | `paper_runner.py:267 from mctrader_market_bithumb.ws_client import BithumbWebSocketStream` → `:269 return BithumbWebSocketStream(symbol=self._symbol, channels=("transaction",))` | V10 Read ✓ |
| MCT-185 Redis Stream publisher 존재 | `data src/mctrader_data/api/realtime_stream.py` — XADD `market:tick:{exchange}:{symbol}`, payload = `TickRowV1_1.model_dump_json()` | V1 LAND 확인 ✓ |
| engine data_client/ 5파일 존재 | `base.py + historical.py + reverse_write.py + exceptions.py + __init__.py` (MCT-185 LAND) | V5 ls ✓ |
| TickRowV1_1 기존 engine 소비 | `hot/state_machine.py:42` + `hot/streaming_aggregator.py:55,109,140` — StreamingAggregator.ingest(tick: TickRowV1_1) | V6 Grep ✓ |

### 2.2 TO-BE 아키텍처 (D4 cutover 후)

```
[AS-IS]  paper_runner → BithumbWebSocketStream (bithumb WS)
                     → ws_wrapper (WsWrapperStream)
                     → stream_consumer (StreamConsumer)
                           ↓ TransactionEvent → BarAggregator.on_transaction()
                           ↓ OrderbookSnapshotEvent → _latest_orderbook
                           ↓ → fill/simulated.py (OrderbookSnapshotEvent VWAP)

[TO-BE]  paper_runner → RedisStreamSubscriber (XREAD market:tick:{exchange}:{symbol})
                     → TickRowV1_1 역직렬화 (model_validate_json)
                     → StreamingAggregator.ingest(tick: TickRowV1_1)  [기존 hot/ 경로 재사용]
                           ↓ → BarAggregator.on_transaction(ts_utc, price, quantity)
                     → engine-local OrderbookSnapshot (별도 orderbook stream 또는 last-snapshot)
                           ↓ → fill/simulated.py (engine-local OrderbookSnapshot VWAP)
```

## §3 변경 계획 (설계 lane TBD)

설계 lane TBD — Change Plan `docs/change-plans/MCT-186-change-plan.md` (ArchitectPLAgent owner).

설계 lane 에서 확정할 핵심 결정:
- **D4-1**: Redis Stream subscriber 위치 = `data_client/realtime.py` 확장 vs `realtime/redis_subscriber.py` 신규
- **D4-2**: orderbook stream 처리 = 별도 XREAD key 구독 vs TickRowV1_1 payload 내 orderbook 필드 확장 vs 별도 REST poll
- **D4-3**: `MarketStream` Protocol 유지 여부 = subscriber 가 Protocol 준수 vs Protocol 폐기 후 직접 ingest 경로
- **D4-4**: bithumb/upbit RealtimeStream Protocol conformance (opt) 실행 여부
- **D4-5**: paper mode `_build_upstream_stream()` = `MOCK_FEED` env 분기 유지 + Redis subscriber 기본 경로

## §4 Delta (AS-IS → TO-BE, §0 Phase 0 전수 HEAD 실증)

### 4.1 제거 대상 (engine — bithumb import 제거)

| 파일 | 제거 항목 | 비고 |
|------|-----------|------|
| `src/mctrader_engine/fill/simulated.py` | `from mctrader_market_bithumb.ws_events import OrderbookSnapshotEvent` (line:18) + `orderbook: OrderbookSnapshotEvent` 파라미터 타입 | engine-local `OrderbookSnapshot` 으로 교체 |
| `src/mctrader_engine/realtime/stream_consumer.py` | `from mctrader_market_bithumb.ws_events import (...)` (line:8-12) + `StreamEvent`/`TransactionEvent`/`TickerEvent`/`OrderbookSnapshotEvent` 타입 사용 전체 | `TickRowV1_1` + engine-local 타입으로 교체 |
| `src/mctrader_engine/runtime/mock_stream.py` | `from mctrader_market_bithumb.ws_events import StreamEvent, TickerEvent, TransactionEvent` (line:19) + bithumb 이벤트 생성 코드 | `TickRowV1_1` 기반 mock 으로 교체 |
| `src/mctrader_engine/runtime/paper_runner.py` | `from mctrader_market_bithumb.ws_client import BithumbWebSocketStream` (line:267 function-local) + `BithumbWebSocketStream(...)` 반환 | Redis Stream subscriber 반환으로 교체 |
| `src/mctrader_engine/runtime/ws_wrapper.py` | `from mctrader_market_bithumb.ws_events import StreamEvent` (line:30) + `StreamEvent` 타입 어노테이션 | engine-local 정규화 타입으로 교체 |
| `tests/test_simulated_fill.py` | `from mctrader_market_bithumb.ws_events import OrderbookSnapshotEvent, _OrderbookLevel` + `_build_book()` 생성자 | engine-local `OrderbookSnapshot` + `_Level` 기반으로 교체 |
| `src/mctrader_engine/cli.py` | `from mctrader_engine.runtime.ws_wrapper import StreamExhaustedError` (line:442) + `except StreamExhaustedError` catch 블록 (:597) | ws_wrapper.py 제거 연쇄 — StreamExhaustedError catch 제거 + DB 상태 업데이트 대체 경로 통합 (§0 설계리뷰 정정) |

### 4.2 신규 추가 (engine)

| 파일 | 추가 항목 | 비고 |
|------|-----------|------|
| `src/mctrader_engine/realtime/types.py` (신규) | `_Level(price, quantity)` dataclass + `OrderbookSnapshot(exchange, symbol, ts_utc, bids, asks)` dataclass — engine-local, bithumb 비결합 | V7/V8 Phase 0 정정 발견 |
| `src/mctrader_engine/data_client/realtime.py` (신규 또는 `realtime/redis_subscriber.py`) | Redis Stream XREAD subscriber (asyncio + `redis.asyncio`) — `TickRowV1_1.model_validate_json()` 역직렬화. `MarketStream` Protocol 준수 (설계 lane 결정) | D4-1 설계 lane 확정 |
| `tests/test_realtime_subscriber.py` (신규) | Redis Stream subscriber integration test (`testcontainers[redis]` — MCT-180 패턴 재사용). XADD → subscriber XREAD → TickRowV1_1 역직렬화 round-trip | QADeveloperAgent owner |

### 4.3 ADR/compose 변경 (hub)

| 대상 | 변경 | 비고 |
|------|------|------|
| `docs/adr/ADR-031-data-domain-decoupling.md` | §D4 VERIFIED amendment box 박제 | AC-5 |
| `docs/adr/ADR-030-docker-stack-governance.md` | engine compose service NAS cred env drop amendment box (MCT-186 owner 확정) | AC-6 |
| `scope_manifests/EPIC-data-domain-decoupling.yaml` | milestone 5/7 + MCT-186 COMPLETED | Phase 2 PR2 |

## §5 수용 기준 (AC)

| AC | 기준 | 검증 방법 |
|----|------|-----------|
| AC-1 | engine `src/` `from mctrader_market_bithumb` grep = **0건** (bithumb import 완전 제거) | `grep -rn "from mctrader_market_bithumb" src/` = 0줄 (post-LAND) |
| AC-2 | 정규화 TickRowV1_1 소비 — paper mode mock-feed 실행 시 `execution_report.json` 생성 + `mode=paper` | `pytest tests/test_paper_runner.py -k smoke` PASS (MCT-185 LAND 기준 회귀 0) |
| AC-3 | Redis Stream XREAD subscriber 정합 — `TickRowV1_1.model_dump_json()` XADD → `model_validate_json()` 역직렬화 round-trip | `pytest tests/test_realtime_subscriber.py` (testcontainers[redis]) ALL PASS |
| AC-4 | paper mode tick latency baseline 회귀 0 — mock-feed N events 처리 시 wall-clock < 60s (MCT-49 Calibration AC C5 SLO) | `test_paper_runner.py::test_paper_runner_smoke_writes_outputs` PASS |
| AC-5 | ADR-031 §D4 VERIFIED amendment box 박제 (file:line + integration test result + caller grep 0) | `docs/adr/ADR-031-data-domain-decoupling.md §D4 VERIFIED` 존재 + ADR-032 evidence triad 충족 |
| AC-6 | ADR-030 NAS cred drop amend box 박제 (engine compose NAS env drop 명시) | `docs/adr/ADR-030-docker-stack-governance.md` NAS cred drop amendment box 존재 |
| AC-7 | engine-local `OrderbookSnapshot` 정의 + `test_simulated_fill.py` 전수 PASS (bithumb import 0) | `grep -rn "mctrader_market_bithumb" tests/` = 0줄 + `pytest tests/test_simulated_fill.py` ALL PASS |

## §6 불변식 (INV)

| INV | 기준 |
|-----|------|
| INV-1 | engine `src/` `from mctrader_market_bithumb` = **0건 영구** (post-LAND, MCT-188 D7 quad gate 진입 조건) |
| INV-2 | 정규화 TickRowV1_1 소비 — paper mode 출력 회귀 0 (byte-equiv INV 보존, mock-feed 고정 fixture 기준) |
| INV-3 | engine-local `OrderbookSnapshot` = bithumb `_BaseEvent` 미상속 (exchange-agnostic 영구) |
| INV-4 | Redis subscriber = `TickRowV1_1.model_validate_json()` 역직렬화 SSOT (bithumb-specific 파싱 로직 0) |
| INV-5 | 회귀 0 — engine test suite 기존 PASS 전수 유지 (MCT-185 baseline vs MCT-186 LAND) |
| INV-6 | R2 MCT-41 파일 충돌 0 — engine#N 진입 직전 R2 재재cross-check 필수 (활성 MCT-43~47 branch = 0 확인 후 LAND) |

## §7 변경 계획 (설계 lane TBD — Change Plan §1-§12)

설계 lane TBD.

## §8 구현 (구현 lane TBD)

### §8.1 구현 계획

설계 lane Change Plan 수령 후 DeveloperAgent + QADeveloperAgent 병렬 구현.

### §8.2 구현 결과

구현 lane TBD.

### §8.3 테스트 결과

QADeveloperAgent TBD.

### §8.4 Perf Baseline

paper mode mock-feed run latency 측정 (MCT-170 SLO 회귀 0 확인):

| 항목 | baseline (MCT-185) | MCT-186 후 | gate |
|------|--------------------|------------|------|
| smoke run wall-clock (10 events, mock-feed) | TBD | TBD | < 60s |
| bithumb import grep0 | 5건 (AS-IS) | 0건 (TO-BE) | **0건 ✓** |

### §8.5 Impl Manifest

DeveloperPL self-write (CFP-39). engine PR #60 (773b270) git diff 기준 작성.

| 파일 | 변경 유형 | Change Plan §절 | 비고 |
|------|-----------|-----------------|------|
| `src/mctrader_engine/realtime/types.py` | 신규 | §3.1 D4-2 | engine-local `_Level` + `OrderbookSnapshot` dataclass (INV-3: bithumb._BaseEvent 미상속) |
| `src/mctrader_engine/realtime/redis_subscriber.py` | 신규 | §3.1 D4-1 | `RedisStreamSubscriber` XREAD asyncio, MarketStream Protocol 정합, BLOCK=1000ms, 5× retry |
| `src/mctrader_engine/realtime/stream_consumer.py` | 수정 | §3.2 D4-3 | `on_event(tick: TickRowV1_1)` 직접 BarAggregator 호출; bithumb isinstance dispatch 제거 |
| `src/mctrader_engine/fill/simulated.py` | 수정 | §3.2 V7/V8 | `OrderbookSnapshotEvent` → engine-local `OrderbookSnapshot` 타입 교체 (INV-3) |
| `src/mctrader_engine/runtime/mock_stream.py` | 수정 | §3.2 D4-5 | bithumb 이벤트 → `TickRowV1_1` generation (uuid trade_id, transaction-only) |
| `src/mctrader_engine/runtime/paper_runner.py` | 수정 | §3.2 D4-5 | `WsWrapperStream` → `RedisStreamSubscriber`; ADAPTER_NAME/ENDPOINT 갱신 |
| `src/mctrader_engine/runtime/ws_wrapper.py` | 삭제 | §3.2 D4-3 | `WsWrapperStream` + `StreamExhaustedError` 완전 제거 (Redis subscriber 자체 retry로 대체) |
| `src/mctrader_engine/cli.py` | 수정 | §3.2.4b | `StreamExhaustedError` import + catch 블록 제거 (ws_wrapper.py 삭제 연쇄) |
| `pyproject.toml` | 수정 | §3.3 | pyright `venvPath`/`venv` 설정 추가 (mctrader_market.schemas resolve) |
| `tests/test_simulated_fill.py` | 수정 | §3.2 V8 | bithumb import 0; engine-local `_Level`/`OrderbookSnapshot` 기반 |
| `tests/test_paper_executor.py` | 수정 | §3.2 V7 | `TickRowV1_1` stream 기반; `_FakeStream` / `_tick()` 신규; bithumb import 0 |
| `tests/test_realtime_subscriber.py` | 신규 | §3.1 AC-6 | testcontainers[redis] integration test 4종 (XADD→XREAD round-trip, malformed skip, context guard) |

## §9 LAND 결과

### §9.1 PR LAND 타임라인

| 시각 | PR | commit | 박제 내용 |
|------|-----|--------|---------|
| 2026-05-16T17:53:38Z | hub#370 | 3fc9c1f | Phase 1 docs — Story §0-§11 + ADR-031 §D4 amendment box draft + ADR-030 NAS cred drop draft + Change Plan + scope_manifest MCT-186 IN_PROGRESS + CLAUDE.md |
| 2026-05-16T21:52:47Z | engine#60 | 773b270 | Phase 2 PR1 engine — 5곳 5파일 bithumb import 제거 + RedisStreamSubscriber 신규 + engine-local OrderbookSnapshot + testcontainers[redis] integration test |
| 2026-05-16T22:07:13Z | hub#371 | cab600b | Phase 2 PR2 박제 — §8.5 Impl Manifest + ADR-031 §D4 VERIFIED + ADR-030 NAS cred drop LAND confirm + scope_manifest 5/7 + CLAUDE.md COMPLETED + RETRO-MCT-186 + EPIC-RESULTS §Story-5 |

### §9.2 AC/INV 전수 PASS 확인

| AC/INV | 기준 | 결과 |
|--------|------|------|
| AC-1 | src/ `from mctrader_market_bithumb` import = 0건 | **PASS** — `grep -rn "^from mctrader_market_bithumb" src/` = 0건 (engine#60 773b270) |
| AC-2 | TickRowV1_1 소비 paper mode 회귀 0 | **PASS** — `test_paper_executor.py` 3 ALL PASS (mock-feed TickRowV1_1 stream) |
| AC-3 | RedisStreamSubscriber XREAD round-trip | **PASS** — `test_realtime_subscriber.py` testcontainers[redis] 4종 PASS |
| AC-4 | paper mode latency 회귀 0 | **PASS** — 881 passed 회귀 0 (hot suite flaky 1건 = MCT-186 무관 기존 사전) |
| AC-5 | ADR-031 §D4 VERIFIED amendment box 박제 | **PASS** — 본 PR 박제 (아래 §9.3) |
| AC-6 | ADR-030 NAS cred drop amend confirm | **PASS** — 본 PR 박제 (아래 §9.4) |
| AC-7 | engine-local OrderbookSnapshot + test_simulated_fill 전수 PASS | **PASS** — `test_simulated_fill.py` 4 ALL PASS + bithumb import 0 |
| INV-1 | src/ bithumb import = 0건 영구 | **PASS** — AC-1 동일 |
| INV-2 | 정규화 TickRowV1_1 소비 회귀 0 | **PASS** — AC-4 동일 |
| INV-3 | engine-local OrderbookSnapshot bithumb._BaseEvent 미상속 | **PASS** — `types.py` frozen dataclass, 상속 없음 |
| INV-4 | Redis subscriber = TickRowV1_1.model_validate_json() SSOT | **PASS** — `redis_subscriber.py:159` 단일 역직렬화 경로 |
| INV-5 | 회귀 0 (MCT-185 baseline) | **PASS** — 881 passed / 회귀 0 |
| INV-6 | R2 MCT-41 파일 충돌 0 | **PASS** — MCT-43~47 branch/commit 0 (V2 Phase 0 확인 + engine#60 LAND 전 재확인) |

## §9.3 ADR-031 §D4 VERIFIED evidence triad (ADR-032 정합)

| evidence | 내용 |
|----------|------|
| file:line | `engine#60 src/mctrader_engine/realtime/redis_subscriber.py:93-173` — `RedisStreamSubscriber.messages()` XREAD BLOCK loop + `TickRowV1_1.model_validate_json()` |
| caller grep | `engine#60 src/mctrader_engine/runtime/paper_runner.py:265-274` — `_build_upstream_stream() → RedisStreamSubscriber(exchange="bithumb", symbol=self._symbol, max_events=self._max_events)` |
| integration test | `engine#60 tests/test_realtime_subscriber.py` — testcontainers[redis] 4종 PASS (XADD→XREAD round-trip, max_events cap, malformed skip, context guard) |

**ADR-031 §D4 VERIFIED** (engine exchange-adapter-free + RedisStreamSubscriber LAND confirm, 2026-05-17).

## §9.4 ADR-030 NAS cred drop confirm

engine compose service (`paper-engine` + `backtest-runner`) NAS credential env drop 설계 결정:
- `NAS_MINIO_ENDPOINT` / `NAS_MINIO_ACCESS_KEY` / `NAS_MINIO_SECRET_KEY` → engine compose service 에서 제거
- engine 은 MCT-185 `data_client/` REST `/v1/` + `REDIS_URL` 경유만 사용 (NAS 직독 0)
- MCT-185 §D2 VERIFIED: engine `grep -rn "mctrader_data.nas" src/` = 0건
- ADR-030 amendment box = compose.yml 실 env drop = MCT-186 owner 확정 (별도 hub PR 또는 Phase 2 PR2 포함)

> compose.yml NAS env drop 실 wiring = hub Phase 2 PR2 scope 미포함 (Phase 1 draft로 존재). 
> compose.yml = mctrader-hub repo 관리 — ADR-030 POLICY_FINALIZED 19 D 본문 무변경, amendment box 패턴.
> 실 compose.yml `paper-engine`/`backtest-runner` service NAS env 행 제거 = carry over (별 PR).

## §10 FIX Ledger

| iter | 유형 | lane | finding | 원인 | 처리 |
|------|------|------|---------|------|------|
| — | (초기) | — | — | — | — |
| 1 | design P0 | 설계-리뷰 | cli.py StreamExhaustedError ws_wrapper.py 삭제 의존 발견 | 설계 누락 | Change Plan §3.2.4b 추가 — cli.py StreamExhaustedError import + catch 제거 명시 |

## §11 LAND/박제 타임라인

| 시각 | PR | commit | 박제 내용 |
|------|-----|--------|---------|
| 2026-05-16T17:53:38Z | hub#370 | 3fc9c1f | Phase 1 docs |
| 2026-05-16T21:52:47Z | engine#60 | 773b270 | Phase 2 PR1 engine (5곳 5파일 bithumb 제거 + Redis subscriber) |
| 2026-05-16T22:07:13Z | hub#371 | cab600b | Phase 2 PR2 박제 |

## §12 회고 (PMOAgent sub-dispatch 결과)

### 완료 요약

MCT-186 = EPIC-data-domain-decoupling sequential_phase 5 (milestone **5/7**) COMPLETED
2026-05-17. hub#370 (3fc9c1f Phase 1 docs) + engine#60 (773b270 Phase 2 PR1) + hub#371
(cab600b Phase 2 PR2 박제) 3 PR sequential LAND. **AC 7/7 PASS + INV 6/6 PASS + 회귀 0**
(881 passed). 핵심 성과 = engine `mctrader_market_bithumb` 직접 import **5곳 5파일 전부 제거**
(AC-1 grep0 0건) + RedisStreamSubscriber 신규 + engine-local OrderbookSnapshot dataclass
(INV-3 영구) + ws_wrapper.py 파일 삭제. engine = **exchange-agnostic pure consumer**
(TickRowV1_1 market-core Layer 0 SSOT 소비) 전환 완결. ADR-031 §D4 VERIFIED (evidence
triad: file:line + grep0 + testcontainers integration test 4종 PASS).

### 핵심 lesson

**파일 삭제 시 downstream caller 전수 검색 의무** (§3.2.4b 패턴 누적 8회째 재현). Change
Plan 초안이 ws_wrapper.py 삭제를 명시했으나 `cli.py:442` `StreamExhaustedError` import +
`cli.py:597` catch block 의 dangling 미포착. 설계리뷰 iter1 P0 fix (Change Plan §1.5
cli.py modification scope 추가) → CONDITIONAL_PASS → code iter PASS (FIX 0회). 동형
패턴: MCT-170/177/178/179/180 cross-repo Phase 0 verify gap (sibling runtime 미검증)
누적 6회 + MCT-183 lazy/conditional import grep gap 1회 + MCT-186 file-delete caller
grep gap 1회 = **8회 누적**. 정정 mechanism = 파일 삭제 의도 시 `grep -rn <symbol_name>`
+ `grep -rn "import <filename>"` 양방향 전수 검색을 Phase 0 Verify Gate 의 고정 V-체크
slot 으로 박제 의무 (MCT-187 reapply 후보).

부수 lesson 2건:
1. **박제 PR 5 체크리스트 cross-Story reapply 5/5 PASS** — MCT-185 패턴 (RETRO 존재 +
   EPIC-RESULTS §Story-N + frontmatter status=COMPLETED + completed_at + CLAUDE.md
   hub#TBD 잔존 0줄 + ADR amendment confirm) reapply 성공. SSOT drift 3호 (MCT-184
   hub#359 부분 박제) 동형 재발 0 — self-discipline 효과 검증 1회 (mechanical gate
   plugin-codeforge#NEW-A 미가용 전 유지 의무).
2. **ADR-032 evidence triad 2회 연속 선제 reapply** (MCT-185 + MCT-186) — MCT-189
   wiring drift 동형 패턴이 dead-in-data → live cutover 시점에 evidence triad 갱신으로
   사전 차단. file:line + caller grep + integration test 3종 invariant 적용 확립.
3. **code lane FIX 0회 달성** — design lane 조기 P0 차단 (§3.2.4b cli.py scope 추가)
   → code lane clean (engine#60 iter1 직접 PASS). MCT-179/183 lesson reapply 효과 누적
   (design lane shift-left ROI 재증명).

### carry-over

| # | 항목 | severity | owner |
|---|------|----------|-------|
| 1 | `compose.yml` engine `paper-engine`/`backtest-runner` service `NAS_MINIO_ENDPOINT`/`NAS_MINIO_ACCESS_KEY`/`NAS_MINIO_SECRET_KEY` env 행 제거 | non-blocking (engine 이 NAS 직독 0 — MCT-185 §D2 VERIFIED) | MCT-187 or 별 hub PR |
| 2 | `pyproject.toml` `mctrader-market-bithumb` git dependency line 제거 | non-blocking (src/ import = 0 grep0 PASS, pyproject 의존 line 유지) | MCT-188 D7 quad gate final |
| 3 | ADR-030 compose.yml engine NAS cred drop amendment box → VERIFIED 전환 | doc-only | carry over 1번 LAND 후 |

### 다음 Story 진입 권고

**MCT-187** (다중거래소 확장 불변식 박제, D5+D6, sequential_phase 6) — MCT-186 Phase 2 PR2
MERGED ✓ 후 진입. 채택 결정: D5 (확장 불변식 invariant test) + D6 (runbook
add-new-exchange.md). 진입 reapply 의무 항목:

1. R1 가드 + §0 Phase 0 Verify Gate (V-체크) — engine bithumb import 0건 + market-core
   확장 어댑터 신규 추가 시점의 데드 코드 grep 의무
2. **§3.2.4b 파일 삭제 caller grep 의무를 §0 V-체크 고정 slot 으로 박제** (본 retro
   신규 lesson — 9회째 재발 차단)
3. cross-document SSOT forcing function self-discipline (§3.6.1 gate v2 cross-Story
   reapply) — mechanical gate (plugin-codeforge#795) 미가용 전 유지
4. 박제 PR 5 체크리스트 self-discipline (mechanical gate plugin-codeforge#NEW-A 미가용 전)
5. post-merge audit lane self-discipline (mechanical lane plugin-codeforge#NEW-B 미가용 전)
   — Codex 4 axis 의무 운용
6. ADR-032 evidence triad reapply (D5/D6 invariant test 산출물에 적용)
7. MCT-187 진입 직전 R2 MCT-41 (Live Mode Debut) 활성 branch 재 cross-check 의무 (engine
   repo `git branch -a` MCT-43~47/live 키워드 0건 확인 후 진입)
