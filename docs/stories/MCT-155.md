---
story_key: MCT-155
story_scope: cross-repo  # mctrader-data primary + mctrader-hub Story SSOT + 3 runbook + Stage 2 Epic-final retro
story_issues:
  - repo: mclayer/mctrader-hub
    number: 274
status: phase:진행중
epic_milestone: Epic-cold-tier-stage-2-migration
parent_epic: EPIC-cold-tier-nas-minio
created_at: 2026-05-13
delegates:
  - mctrader-data
---

# MCT-155 — Stage 2 마지막 Story (Phase E singleton — local GC + secret rotation 첫 cycle + TLS 재검토 user confirm + RPO=0 검증 회고 + Stage 2 EPIC CLOSED gate)

## §1 사용자 요구사항 (verbatim — 변경 차단)

### WHY (local GC + secret rotation 90d 첫 cycle + TLS 재검토 박제 + RPO=0 검증 회고 + Stage 2 EPIC CLOSED gate = Stage 2 종료의 정점)

EPIC-cold-tier-nas-minio Stage 2 (Cold Tier Migration L2/L3 → NAS MinIO 76GB) 의 **마지막 Story (Phase E singleton — Stage 2 EPIC CLOSED gate owner)**. MCT-150 (uploader hardening, ALL MERGED 2026-05-12 — `nas_uploader.py` HEAD-then-PUT idempotency + `retry_queue.py` sqlite-WAL persistent + `prometheus_exporters.py` 5종 metric + `nas_unreachable_sop.py` SOPRunner) + MCT-151 (dual-write atomic primitives + 7종 invariant harness, ALL MERGED 2026-05-13 — `dual_writer.py` 2-phase commit + `compaction_barrier.py` L1 drain barrier + `invariant_harness.py` 7종 sequential verify + `nas_invariant_*` 11종 metric + 4 alert rules) + MCT-152 (dual-write window 운영 + IOPS during 측정 + NAS unreachable SOP 실전, ALL MERGED 2026-05-13 — `dual_write_window_runner.py` cron-style 매일 verify + `docs/runbooks/nas-minio-unreachable-sop.md` operator runbook) + MCT-153 (backfill 76GB closed-day per-(symbol,day) chunking + 10-symbol 병렬 + node=DEFAULT, ALL MERGED 2026-05-13 — `nas_migration/backfill_orchestrator.py` BackfillOrchestrator Phase A→E + `scripts/migration/run_backfill.py` CLI + 26 PASS test) + MCT-154 (reader endpoint cutover + cache barrier + dual-write 7d 연장 + engine smoke, ALL MERGED 2026-05-13 — `endpoint_router.py` immutable swap + `reader_cache.py` flush+verify barrier + `cold_reader.py` S6 cross-check + 30 PASS test) 의 production-grade primitives + 운영 layer + historic 76GB orchestrator + read-side cutover + 7d grace 운영 위에서 spawn — **(a) Local GC** (cutover 후 7d grace 만료 + invariant ALL PASS 7일 누적 후 local L2 76GB 영구 삭제, ADR-027 D7 박제 — dry-run 선행 + 7일 grace + invariant gate 3중 lock) + **(b) Secret rotation 90d 첫 cycle** (NAS MinIO credential rotation, ADR-027 D2 4중 mitigation 의 90d cadence 박제 + MCT-147 runbook 활용) + **(c) TLS 재검토 user confirm 박제** (S12 박제 — 사용자 confirm 결정 = HTTP 유지, ADR-027 D2 amendment 의무) + **(d) RPO=0 검증 회고** (S8 박제 — cutover-1s/+1s diff 검증 `verify_rpo_zero.py` 스크립트 + 결과 evidence 박제) + **(e) Stage 2 EPIC CLOSED gate 6 AC 종합 verify** (Epic milestone #4 close trigger). 본 Story = **Stage 2 6 Story 중 마지막** (MCT-150~154 ALL MERGED → 본 Story Phase E singleton 진행). 본 Story 산출물이 (a) Stage 2 종료 gate **AC-3 (GC 후 free disk > 50%, 목표 ~70%)** 의 직접 owner (b) Stage 2 종료 gate **AC-6 (TLS 재검토 회고 박제)** 의 직접 owner (c) Stage 2 종료 gate **AC-1/AC-2/AC-4/AC-5 의 cross-verification owner** (Epic-final RpoVerifyResult + GcResult evidence + Stage 2 종합 retro) (d) ADR-027 amendment **2건 mandatory 박제** (D2 TLS 재검토 + D6 7종 invariant 명시화) (e) **Epic milestone #4 CLOSED trigger** (Stage 2 마지막 Story 의 EPIC-final gate) (f) 사용자 directive verbatim "적재한 데이터는 절대 유실하지 않도록 주의하라" 의 **최종 enforcement** (GC 진입 전 dry-run + 7d grace + invariant ALL PASS 7일 누적 = 3중 lock + RPO=0 검증 회고 = 4중 lock).

**Stage 2 마지막 Story — EPIC CLOSED gate atomicity 의 단독 owner**: MCT-150~154 5 Story 의 누적 evidence (uploader hardening + dual-write atomic + 7종 invariant + 운영 layer + historic 76GB 영구 이관 + read-side cutover + 7d grace 운영) 모두 **prerequisite**. 본 Story 가 처음으로 **destructive operation (local GC = local L2 76GB 영구 삭제)** 단독 owner. destructive operation 의 핵심 risk = (a) **GC 진입 시점 7d grace 미만료 또는 invariant 미PASS → 데이터 손실 표면화** (R5 박제 — critical impact) (b) **dry-run 결과 review 미반영 → 운영 실수** (사용자 review gate 의무) (c) **secret rotation 진행 중 인증 실패 → NAS endpoint 통신 단절 → 운영 trust 0** (4중 mitigation rotation runbook 의 핵심 enforcement) (d) **TLS 재검토 결정이 사용자 confirm (HTTP 유지) 와 모순** → ADR-027 amendment 충돌 (e) **Stage 2 EPIC CLOSED gate 6 AC 중 1건이라도 미달 → Epic 종료 차단**. 본 Story 무 박제 시 (a) Stage 2 종료 gate AC-3 0 진척 (cold L2 76GB local 잔존 → 디스크 압박 해소 0) (b) Stage 2 종료 gate AC-6 0 진척 (TLS 재검토 회고 박제 0) (c) ADR-027 D2/D6 amendment 의무 미이행 → 박제 trail 손상 (d) Stage 2 EPIC CLOSED gate 종합 verify 0 → Epic milestone #4 close 차단 (e) 사용자 directive "정공법 이관" 의 closure 박제 부재 → 추가 Story 필요 — 5개 모두 사용자 directive 와 직접 충돌.

### 사용자 directive (verbatim — Stage 2 entry directive 2026-05-12)

원문 (Stage 2 brainstorm spec §1 박제 — Stage 2 entry):

> 디스크압박 해소보다는 이전에 수행하던 NAS Minio로 데이터 저장소를 변경한 다음 이관을 수행하면 알아서 로컬 디스크 공간을 비울 수 있다. 적재한 데이터는 절대 유실하지 않도록 주의하라.

해석 (Stage 2 brainstorm spec §1 박제):

- **WHY** = cold tier 76GB NAS 영구 이관 + storage backend SoT 전환 + 디스크 회수 부수효과
- **명시-실제 일치** = 사용자가 임시 회수 path 명시적 거부 → 정공법 선호
- **데이터 무손실 = absolute hard constraint** = RPO=0 user confirmed (scope_manifest design_decisions S8 `user_confirmed: true`, 2026-05-12)

본 Story 의 사용자 directive 직접 대응 = **4중 lock for destructive transition** = (1) **cutover-1s/+1s diff RPO=0 검증** (S8 박제, GC 진입 전 fact 박제) + (2) **dry-run 선행** (D7 박제, 삭제 대상 list 산출 + user review gate) + (3) **7d grace 만료 + invariant ALL PASS 7일 누적** (D7 박제, time + invariant 2중 gate) + (4) **deletion log + 24h 지연 batch delete** (R5 박제, 실수 시 24h 윈도우 내 복구). 본 Story 무 박제 시 (a) GC 진입 시점 cutover 시점 RPO=0 위반 발견 시 → local L2 자료 삭제 후 NAS 측 누락 segment 복원 불가 → 영구 데이터 손실 (b) GC 시점 invariant FAIL silent → 손상된 NAS 자료에 의존하는 reader (engine) 가 stale data serve → 신뢰성 손실 (c) GC 진행 중 사용자 review 미가능 → 운영 실수 보호 부재 — 3개 모두 사용자 directive "절대 유실 금지" 와 직접 충돌. 본 Story = "정공법 이관" 의 **destructive 단독 owner** — 본 Story 가 Stage 2 의 마지막 정점, 모든 prerequisite 가 본 Story 안에 수렴.

### Stage 2 마지막 Story 위치 박제 (MCT-150/151/152/153/154 ALL MERGED → 본 Story Phase E singleton 진행)

scope_manifest `epic_milestones.stage_2_complete.progress` 박제 (2026-05-13 MCT-154 MERGED 후):

- **MCT-150 = Stage 2 첫 Story COMPLETE** (5 SP, Issue #253 closed + PR data#42 fc73d44 + hub#255 57940e1 ALL MERGED 2026-05-12 21:33Z, addresses S4/S10/S11/S12 4점, fix_cycle_total: 5 + escalate_count: 1 + codeforge_escalation #525)
- **MCT-151 = Stage 2 두 번째 Story COMPLETE** (8 SP, Issue #257 closed + PR data#43 65041bd2 MERGED 2026-05-13, 12 file + 59 PASS test GREEN on first pass, addresses S2/S5/S8 3점, fix_cycle_total: 1 + escalate_count: 0 + codeforge #525 lesson 4 invariants consumer-side 첫 입증 — FIX ~80% ↓)
- **MCT-152 = Stage 2 세 번째 Story COMPLETE** (5 SP, Issue #261 closed + PR data#44 + hub#264 MERGED 2026-05-13, addresses S2/S10/S11 3점, fix_cycle_total: 1 + escalate_count: 0 + codeforge #525 lesson #5 후보 발의 — scope_manifest planned_files invariant)
- **MCT-153 = Stage 2 네 번째 Story COMPLETE** (8 SP, Issue #265 closed + PR data#45 8cbbdd04 + hub#268 61cfc649 MERGED 2026-05-13, addresses S1/S5/S6/S7 4점, fix_cycle_total: 1 + escalate_count: 0 + codeforge #525 lesson #5 sub-invariant 후보 발의)
- **MCT-154 = Stage 2 다섯 번째 Story COMPLETE** (5 SP, Issue #270 closed + PR engine#52 + hub#272 MERGED 2026-05-13, addresses S3/S6/S9 3점, fix_cycle_total: 0 + escalate_count: 0 + codeforge #525 lesson #5 sub-invariant 첫 적용 — design-review FIX 0 + dimensional cascade 0 효과 입증)
- **누적 진행**: 31 SP / 36 SP total = **86.1%**
- **본 Story (MCT-155)** = `next: MCT-155  # 5 SP, depends_on MCT-154 ✅ + 7d grace 만료 — Stage 2 마지막 Story (local GC + secret rotation + RPO=0 검증 + Stage 2 종료 gate)`

scope_manifest `parallelism_decision.stage_2[4]` (Phase E) 박제: **"sequence: [MCT-155]" + "mode: serial" + "reason: GC = cutover + 7d grace 만료 의존. secret rotation + TLS 재검토 는 user-blocking confirm 필요"**. 사용자 directive 정합 — Phase E 는 5 prerequisite Story 의 누적 evidence + 시간 기반 (7d grace) gate + 사용자 confirm gate 의 누적 위에서 단일 closure 수행, 병렬화 시 EPIC closure atomicity 위반 risk → singleton enforcement.

### 산출물 (planned_files MCT-155 — scope_manifest 박제 + MCT-150~154 lesson 정합 path 사전 적용)

`scope_manifests/EPIC-cold-tier-nas-minio.yaml` `planned_files.MCT-155` 직접 인용 (lesson #5 file-level invariant 사전 적용 — `migration/` → `nas_migration/` rename, MCT-150~153 lesson 정합):

- **mctrader-data (primary, Phase 2)**:
  - `src/mctrader_data/nas_migration/cutover_verifier.py` — **신규** (CutoverVerifier API — `verify_rpo_zero(cutover_timestamp) -> RpoVerifyResult` cutover-1s/+1s diff 측정 + MCT-151 InvariantHarness 7종 invariant 정합 + FAIL 시 cutover rollback signal emit, S8 박제 직접 owner)
  - `src/mctrader_data/nas_migration/gc_runner.py` — **신규** (GcRunner API — `gc(tier="L2", dry_run=True) -> GcResult` D7 박제 정합 + 7일 grace + dry-run 선행 + invariant ALL PASS gate + 디스크 압박 시 tier/date 순차, R5 박제 3중 lock + deletion log + 24h 지연 batch delete)
  - `scripts/migration/verify_rpo_zero.py` — **신규 CLI** (cutover-1s/+1s diff verify CLI + RpoVerifyResult JSON output + Prometheus push + scripts/migration/ prefix 유지 MCT-153 동일)
  - `scripts/ops/rotate_minio_secret.py` — **신규 CLI** (90d secret rotation CLI + MinIO credential rotation + 4중 mitigation 정합 + emergency rotation path + scripts/ops/ prefix MCT-150 동일)
  - `tests/nas_migration/test_cutover_verifier.py` — **신규** (RpoVerifyResult enum 3종 + cutover-1s/+1s diff 검증 + InvariantHarness mock + FAIL signal emit 검증 + chaos test cutover rollback)
  - `tests/nas_migration/test_gc_runner.py` — **신규** (GcResult enum 4종 + 7d grace 미만료 시 blocked + dry-run output + invariant FAIL 시 blocked + 디스크 압박 시 tier 순차 + deletion log + 24h batch delete)
- **mctrader-hub (cross-repo Story SSOT + 3 runbook + Stage 2 Epic-final retro)**:
  - `docs/stories/MCT-155.md` — 본 file (RequirementsPL §1~§5 + ArchitectPL §6~§11 직접 author, hub SSOT)
  - `docs/runbooks/nas-minio-tls-review.md` — **신규** (S12 HTTP 유지 결정 박제 — 사용자 confirm 박제 + TLS 도입 시 cost / TLS handshake / cert rotation cadence 재평가 결과 박제 + ADR-027 D2 amendment 후보 박제, MCT-147 4중 mitigation 정합)
  - `docs/runbooks/nas-minio-stage2-runbook.md` — **신규** (Stage 2 종료 운영 runbook — Epic 6 AC 종합 verify checklist + GC 진입 절차 + secret rotation 절차 + TLS 재검토 절차 + EPIC CLOSED gate procedure, MCT-152 + MCT-154 runbook 패턴 정합)
  - `docs/retros/2026-05-stage2.md` — **신규** (Stage 2 Epic-final retro 별 file — 6 Story trail + lesson 4+1+sub invariants 누적 효과 측정 + codeforge #525 amendment 권고 + Epic CLOSED gate 6 AC 충족 evidence + Stage 1+2 종합 retro)

본 Story Phase 1 = hub Story file (본 file). Phase 2 = mctrader-data 의 6 file 신규 + mctrader-hub 3 runbook + 1 retro 신규 + Phase 2 PR (cross-repo, plan §Task scope).

**저자 권고 (path SSOT — MCT-150~154 lesson 5+1+sub invariants 누적 적용, scope_manifest path namespace 사전 검증)**:

scope_manifest `planned_files.MCT-155` 의 mctrader-data 6 file path = `src/mctrader_data/nas_migration/` (2 src) + `scripts/migration/` (1 CLI) + `scripts/ops/` (1 CLI) + `tests/nas_migration/` (2 test) — **본 path namespace 검증 결과**: scope_manifest 측 원래 `migration/` prefix 였으나 MCT-150~153 lesson #5 (file-level invariant) 정합 위해 **사전 갱신 (Phase 1 ArchitectPL author 시점 lesson 적용 — `migration/` → `nas_migration/`)**. 권고 = `nas_migration/` 채택 — 이유:

1. **MCT-151+153 land 동일 namespace 직접 활용** — `nas_migration/__init__.py` (MCT-151 P1 fix) + `nas_migration/invariant_harness.py` (MCT-151) + `nas_migration/backfill_orchestrator.py` (MCT-153) 이미 land. 본 Story 의 cutover_verifier + gc_runner = 동일 namespace 의 자연 module 그룹화 → import path 정합 (`from mctrader_data.nas_migration.invariant_harness import InvariantHarness` 의 caller 측 동일 namespace 내 module → import simple)
2. **`nas_*` prefix convention 정합** (MCT-150 P2-1 + MCT-151 ESCALATE #1 lesson) — mctrader-data 의 기존 `storage.py` (ADR-009 v1 OHLCV) module 과 namespace 충돌 회피. 본 Story = `nas_migration/cutover_verifier.py` + `nas_migration/gc_runner.py` 모두 `nas_*` prefix sub-domain
3. **lesson #5 sub-invariant cross-section quantitative consistency 적용** — 본 §1 산출물 6 file path ↔ §3 코드 경로 표 ↔ §6.3 디렉토리 트리 (ArchitectPL author scope) ↔ §9.2 Phase 2 산출물 표 (ArchitectPL author scope) ↔ §6.10 file-level scope_manifest sync ↔ §6.11 cross-section cascade table 의 quantitative consistency 의무 — RequirementsPL §1 author 시점 6 file 명시 + §3 표 6 row 정합 + §5.5 lesson invariants 박제. ArchitectPL §6 author 시 §6.3 + §9.2 + §6.10 + §6.11 quantitative cross-check 의무

**path 갱신 권고 = 1건 (사전 적용 완료)** — `migration/` → `nas_migration/` rename + scope_manifest 측 commit (Phase 1 ArchitectPL pre-emptive sync 2026-05-13). lesson #5 (file-level sync) **두 번째 case 효과 ✅ 유지** + lesson #5 sub-invariant (cross-section quantitative consistency) **두 번째 적용 case** = 본 Story (MCT-154 첫 적용 효과 → 본 Story = 누적 효과 검증).

### Acceptance Criteria (Issue #274 body 박제, 6건)

`mclayer/mctrader-hub#274` Issue body 박제 (6 AC, MCT-150/151/152/153/154 패턴 정합):

- **AC-1 (Local cold L2 76GB GC 완료 + free disk > 50% verify — Stage 2 종료 gate AC-3 직접 owner)**: `gc_runner.py` 의 `gc(tier="L2", dry_run=False) -> GcResult` 가 cutover 후 7d grace 만료 + invariant ALL PASS 7일 누적 + dry-run review pass 후 실 삭제 진입. local L2 sub-tree size = 0 verify. df -h before/after diff = free disk > 50% (목표 ~70%) verify. ADR-027 D7 박제 직접 enforcement (7일 grace + dry-run 선행 + 실 삭제 + 디스크 압박 시 tier/date 순차). **사용자 directive verbatim "절대 유실 금지" 의 GC 측 4중 lock**: (1) cutover RPO=0 verify 박제 (2) 7d grace 만료 + invariant ALL PASS 7일 누적 (3) dry-run 선행 + user review gate (4) deletion log + 24h 지연 batch delete (R5 mitigation). gc 진입 차단 condition: 7d grace 미만료 / invariant FAIL / dry-run review NEGATIVE / cutover RPO=0 unverified.

- **AC-2 (deletion log + 24h 지연 batch delete — R5 mitigation 직접 enforcement)**: `gc_runner.py` 가 실 삭제 진입 시 (a) deletion log file (`mctrader-data/.tmp/gc-deletion-log-MCT-155.json` gitignored) 신규 entry append (deleted_partition + deleted_at_iso + sha256 + size_bytes) (b) 24h batch delete window — dry-run + 실 삭제 사이 24h interval 의무 (operator manual gate, runbook 박제) — 24h 안에 실수 발견 시 deletion log 기반 NAS object 측 download + local 복원 가능 (recovery path 박제). 24h batch delete 진행 중 발견 시 (예: invariant 측 새 FAIL surface) 즉시 GC suspend + 사용자 escalation. R5 박제 직접 enforcement (`risk_register.R5`: "GC 가 검증 전 데이터 삭제 (data loss) — likelihood: low, impact: critical").

- **AC-3 (NAS MinIO credential 90d rotation 첫 cycle 완료 — D2 4중 mitigation 의 90d cadence 박제)**: `rotate_minio_secret.py` 의 90d rotation 첫 cycle 완료 — (a) 신규 access_key + secret_key 생성 (MinIO IAM API) (b) `.env` file (mctrader-hub + mctrader-data + mctrader-engine 양측) 갱신 + 0600 권한 verify (c) 양측 컨테이너 restart 또는 hot-reload (d) 신규 credential 동작 verify (sample PUT/GET 1회) (e) old credential revoke + audit log 박제 (rotation_log file). MCT-147 `docs/runbooks/nas-minio-secret-rotation.md` 패턴 정합 + emergency rotation path 박제 (credential leak 의심 시). ADR-027 D2 4중 mitigation 의 90d rotation cadence 의 첫 실증 박제. cycle 완료 evidence = `mctrader-data/.tmp/secret-rotation-MCT-155-cycle-1.md` (gitignored).

- **AC-4 (TLS 재검토 회고 박제 + ADR-027 D2 amendment — Stage 2 종료 gate AC-6 직접 owner)**: `docs/runbooks/nas-minio-tls-review.md` 신규 file 에 TLS 재검토 결과 박제 — (a) 사용자 confirm 결정 = **HTTP 유지** (Stage 1 정책 연장, 4중 mitigation 그대로 — scope_manifest design_decisions S12 user_confirmed: true 인용) (b) TLS 도입 시 cost 분석 (self-signed cert client trust 박제 + Let's Encrypt public DNS 의존성 + cert rotation cadence) (c) TLS 도입 시 운영 부담 분석 (handshake latency + cert expiry monitoring) (d) 외부 노출 검토 결과 (NAS 방화벽 port 9000 mctrader 호스트 IP only — 외부 노출 0 유지) (e) Stage 3 (또는 future epic) 진입 시 재평가 trigger (예: NAS 외부 노출 시점, 사용자 directive 변경 시점). **ADR-027 D2 amendment mandatory** — D2 wording 갱신 (Stage 2 cutover 후 재검토 결과 박제, scope_manifest `triggers_adr_amendment.adr=ADR-027.section=D2.mandatory=true.trigger_story=MCT-155` 정합) + amendment commit (별 PR 또는 본 Story Phase 2 PR 의 hub 측 산출물).

- **AC-5 (RPO=0 검증 회고 evidence 박제 + ADR-027 D6 amendment — S8 박제 직접 owner)**: `verify_rpo_zero.py` 의 cutover-1s/+1s diff verify 결과 박제 — (a) cutover-1s 시점 L2 segment list (mctrader-data 측 local + NAS 측) (b) cutover+1s 시점 L2 segment list (양쪽) (c) diff 측정 (cutover-1s 검증 segment 모두 cutover+1s NAS 존재 의무) (d) 7종 invariant verify ALL PASS 결과 cross-reference (MCT-151 InvariantHarness inject) (e) FAIL 시 cutover rollback signal emit (MCT-154 endpoint_router 정합). RpoVerifyResult enum 3종 박제 (`rpo_zero_verified` / `drift_detected` / `verify_inconclusive`). evidence pack `mctrader-data/.tmp/rpo-zero-verify-MCT-155.md` (gitignored). **ADR-027 D6 amendment mandatory** — D6 wording 갱신 (3종 → 7종 invariant 명시화, scope_manifest `triggers_adr_amendment.adr=ADR-027.section=D6.mandatory=true.trigger_story=MCT-151` deferred 후 본 Story 시점 land) + amendment commit. S8 박제 직접 enforcement (`scope_manifest design_decisions S8`: "RPO=0 at cutover" 사용자 confirmed).

- **AC-6 (Stage 2 EPIC CLOSED gate 6 AC 종합 verify + Epic milestone #4 close trigger)**: scope_manifest `epic_milestones.stage_2_complete.gate` 의 4 항목 + AC-1~AC-6 종합 verify — (a) MCT-150~155 ALL MERGED ✅ verify (b) 7종 invariant 7일 연속 PASS verify (MCT-152 dual_write_window_runner cron 결과 누적) (c) engine cold-read NAS hit dashboard PASS verify (Grafana — MCT-150 dashboard + MCT-154 evidence pack cross-reference) (d) local L2/L3 디스크 사용량 사전/사후 비교 — 압박 해소 확인 (df -h before/after diff, AC-1 evidence) (e) 6 Story trail RETRO 박제 verify (RETRO-MCT-150~155.md ALL ✅) (f) Stage 2 Epic-final retro 별 file 박제 (`docs/retros/2026-05-stage2.md`) — 6 Story trail + lesson 4+1+sub invariants 누적 효과 측정 + codeforge #525 amendment 권고. **Epic milestone #4 CLOSED trigger** — 6 AC ALL PASS gate 통과 시 PMOAgent 가 milestone #4 close + Epic 종료 박제 + EPIC-RESULTS-cold-tier-nas-minio land (또는 retro 별 file 로 대체).

### Addresses Stage 2 결정 3점 (scope_manifest design_decisions 직접 인용)

scope_manifest `design_decisions` 의 S1~S12 (Stage 2 12 결정) 중 본 Story addresses 3점:

| # | 결정 | 사용자 confirm | scope_manifest 인용 |
|---|---|---|---|
| **S8** ★ | RPO 강도 — RPO=0 at cutover (cutover-1s 검증 L2 segment 모두 cutover+1s NAS 존재 의무) | ✅ user_confirmed: true (2026-05-12) | `addressed_in: [MCT-151, MCT-155]` — 본 Story = MCT-155 retro 측 RPO=0 검증 회고 (AC-5) — `verify_rpo_zero.py` 박제 + cutover-1s/+1s diff 측정 + 결과 evidence 박제 + ADR-027 D6 amendment mandatory |
| **S9** | Cutover failure rollback — Cutover 후 dual-write 7d 연장 (grace overlap, belt-and-suspenders) | 없음 (Codex review + Sonnet decider) | `addressed_in: [MCT-154, MCT-155]` — 본 Story = 7d grace 만료 후 GC 진입 (AC-1) — gc_runner 의 7d grace + invariant ALL PASS 7일 누적 gate 직접 enforcement |
| **S12** ★ | Stage 2 TLS timing — HTTP 유지 (Stage 1 정책 연장) | ✅ user_confirmed: true (2026-05-12) | `addressed_in: [MCT-150, MCT-155]` — 본 Story = TLS 재검토 user confirm 박제 (AC-4) — `docs/runbooks/nas-minio-tls-review.md` 신규 + ADR-027 D2 amendment mandatory + 사용자 confirm = HTTP 유지 결정 박제 |

본 3 결정 = Stage 2 의 마지막 박제 (MCT-150 trail 의 S12 박제 + MCT-151 trail 의 S8 박제 + MCT-154 trail 의 S9 박제 → 본 Story = 모든 trail 의 closure).

### Stage 2 EPIC CLOSED gate 6 AC 종합 verify 박제 (본 Story = Epic milestone #4 close trigger)

scope_manifest `epic_milestones.stage_2_complete.gate` 4 항목 + Stage 2 brainstorm spec §4.4 의 6 AC = **6 AC ALL PASS gate**:

| AC ID | Acceptance Criterion | Owner Story | Evidence (본 Story 시점) | 본 Story 책임 |
|---|---|---|---|---|
| **AC-1** | 76GB cold L2 NAS 이관 + byte identity (7종 invariant ALL PASS) | MCT-153 | MCT-153 BackfillOrchestrator 실 운영 결과 evidence pack | cross-verify (verify_rpo_zero.py + InvariantHarness inject) |
| **AC-2** | 신규 L2 100% NAS write (post-cutover) — `nas_write_ratio == 1.0` for 7d grace | MCT-154 | MCT-152 dual_write_window_runner cron 결과 누적 (7d 동안 daily verify ALL PASS) + MCT-154 evidence pack | cross-verify (7d grace 만료 evidence 박제 — `engine_dual_write_grace_expired_at` Prometheus metric) |
| **AC-3** | GC 후 free disk > 50% (목표 ~70%) | **MCT-155** | gc_runner.py 실 운영 결과 + df -h before/after diff (본 Story AC-1) | **direct owner** |
| **AC-4** | 7종 invariant ALL PASS §10 박제 | MCT-151 + MCT-153 | invariant_harness output → §10 박제 (5 Story 누적) | cross-verify (RPO=0 검증 회고 시점 7종 invariant ALL PASS evidence cross-reference, 본 Story AC-5) |
| **AC-5** | backfill resumability (50% 중단 → 재개 → 100%) | MCT-153 | MCT-153 chaos test report (RETRO-MCT-153.md) | cross-verify (retro 별 file 측 cross-reference) |
| **AC-6** | TLS 재검토 회고 박제 | **MCT-155** | docs/runbooks/nas-minio-tls-review.md + ADR-027 D9 amendment | **direct owner** |

본 Story = **AC-3 + AC-6 직접 owner** + **AC-1/AC-2/AC-4/AC-5 cross-verify owner** (Stage 2 Epic-final retro 시점 6 AC 종합 verify). 6 AC ALL PASS gate 통과 시 Epic milestone #4 CLOSED trigger.

---

## §2 도메인 해석 (DomainAgent 통합)

### 본 Story 의 domain 위치

본 Story = **Stage 2 마지막 Story (Phase E singleton)** = Cold Tier Storage Backend SoT Transition (NAS MinIO) **Epic 의 종료 정점** — Stage 1 (MCT-147~149 ADR-027) + Stage 2 prerequisite (MCT-150~154) 6 Story trail 의 closure.

**핵심 도메인 관찰**:

1. **Destructive operation 단독 owner** — 본 Story 가 Stage 2 6 Story 중 유일하게 **destructive (local L2 76GB 영구 삭제)** + **secret rotation (인증 변경)** + **ADR amendment 2건 mandatory** 동시 owner. MCT-150~154 = 모두 non-destructive (PUT only, 새 module 추가, ADR amendment 0). 본 Story = irreversible.

2. **시간 기반 gate** — 본 Story 는 MCT-154 cutover 완료 후 **7d grace 만료** 의 시간적 prerequisite 위에서 spawn. 7d grace 시점 invariant verify 매일 ALL PASS 누적 결과 = 본 Story 진입 evidence (MCT-152 dual_write_window_runner cron 산출).

3. **사용자 confirm 의존** — TLS 재검토 (S12) + secret rotation (D2) = 사용자 explicit confirm + manual gate 의존. 자동화 0 (Phase 2 impl 측 user_blocking gate 의무).

4. **ADR-027 amendment 의무** — 본 Story 가 ADR-027 D2 (TLS 재검토) + D6 (7종 invariant) **2건 mandatory amendment** 의 직접 owner. amendment trail = scope_manifest `triggers_adr_amendment` 박제 정합. amendment 미이행 시 ADR-027 박제 trail 손상 + 후 Story (Stage 3 또는 future epic) 의 ADR 인용 신뢰성 0.

5. **Epic milestone #4 close trigger** — 본 Story = Stage 2 마지막 → Epic CLOSED gate 의 직접 trigger. 6 AC ALL PASS gate 통과 시 PMOAgent 가 milestone #4 close + Epic 종료 박제.

**도메인 상호작용**:

- **gc_runner ↔ MCT-153 BackfillOrchestrator** = backfill 완료 evidence 박제 의존 (76GB NAS 영구 이관 완료 → local L2 삭제 가능)
- **cutover_verifier ↔ MCT-151 InvariantHarness** = 7종 invariant 직접 inject (본 Story 신규 invariant 0)
- **cutover_verifier ↔ MCT-154 endpoint_router** = cutover rollback signal emit path (FAIL 시)
- **gc_runner ↔ MCT-152 dual_write_window_runner** = 7d grace 만료 + invariant ALL PASS 7일 누적 evidence 의존
- **rotate_minio_secret ↔ MCT-147 nas-minio-secret-rotation.md** = rotation runbook 패턴 정합
- **TLS review runbook ↔ ADR-027 D2** = amendment commit + 박제 wording 갱신

### Stage 2 12 결정 (S1~S12) 중 본 Story addresses 의 trail 종합

scope_manifest design_decisions S1~S12 중 본 Story addresses 3점 (S8/S9/S12):

| Decision | 박제 wording (scope_manifest 직접 인용) | 본 Story 직접 enforcement |
|---|---|---|
| **S8** ★ user_confirmed | "RPO=0 at cutover (cutover-1s 검증 L2 segment 모두 cutover+1s NAS 존재 의무)" | AC-5: `verify_rpo_zero.py` 의 cutover-1s/+1s diff 측정 + RpoVerifyResult enum 3종 + ADR-027 D6 amendment mandatory |
| **S9** | "Cutover 후 dual-write 7d 연장 (grace overlap, belt-and-suspenders)" | AC-1: `gc_runner.py` 의 7d grace + invariant ALL PASS 7일 누적 gate (GC 진입 차단 condition 의 첫 enforcement) |
| **S12** ★ user_confirmed | "HTTP 유지 (Stage 1 정책 연장) — Stage 2 cutover 후 MCT-155 재검토" | AC-4: `docs/runbooks/nas-minio-tls-review.md` 신규 + 사용자 confirm = HTTP 유지 결정 박제 + ADR-027 D2 amendment mandatory |

본 3 결정 의 closure 후 → Stage 2 12 결정 ALL addressed (MCT-150/151/152/153/154 trail 누적).

### MCT-150~154 lesson 4+1+sub invariants 누적 적용 (codeforge #525, **여섯 번째 case**)

본 Story = codeforge #525 lesson 4+1+sub invariants 의 **6번째 적용 case** + lesson #5 sub-invariant **두 번째 적용 case** (MCT-154 첫 적용 효과 → 본 Story = 누적 효과 검증):

1. **#1 API contract semantic completeness** (MCT-150 lesson) — 본 Story §6.2 모든 method docstring 의 enum/state semantics + caller 처리 의무 명시
2. **#2 Cross-module propagation completeness** (MCT-151 lesson) — 본 Story §6.7 cross-module contract 표 ~10 row (cutover_verifier ↔ InvariantHarness + endpoint_router + gc_runner ↔ BackfillOrchestrator + dual_write_window_runner + rotate_minio_secret)
3. **#3 Wording SSOT** (MCT-151 lesson) — 본 Story §6.8 RpoVerifyResult 3종 + GcResult 4종 + (rotate cycle 결과 enum) + 신규 metric prefix `nas_gc_*` + `nas_rpo_verify_*` + `nas_secret_rotation_*` SSOT 박제
4. **#4 Unconditional vs conditional placement** (MCT-152 lesson) — 본 Story §6.9 7d grace + invariant gate unconditional (GC 진입 직전 첫 단계, AC-1) + dry-run 선행 unconditional (실 삭제 전 의무, AC-1) vs deletion log conditional (실 삭제 진입 시점, AC-2) + 24h batch delete conditional (operator manual gate, AC-2) 분리
5. **#5 file-level scope_manifest sync** (MCT-153 lesson) — 본 Story §6.10 scope_manifest planned_files MCT-155 sync 검증 (path 갱신 권고 1건 — `migration/` → `nas_migration/` rename, lesson 사전 적용 완료)
6. **#5 sub-invariant cross-section quantitative consistency 두 번째 적용 case** (MCT-154 첫 적용) — 본 Story §6.11 핵심 설계 수치 (file count 6 / 7d grace 일수 7 / 24h batch delete window / 90d rotation cadence / RPO=0 invariant 7종 / 신규 metric 12종 / 신규 enum value 7종 등) cross-section cascade table

### Stage 1+2 종합 retro 박제 (Epic-final 시점)

본 Story = Stage 2 마지막 → Stage 1+2 종합 retro `docs/retros/2026-05-stage2.md` 신규 file 박제. 종합 retro 박제 항목:

- Stage 1 (MCT-147~149) trail 요약 (PoC 5종 + ADR-027 land)
- Stage 2 (MCT-150~155) trail 요약 (6 Story SP 누적 + FIX cycle 누적 + ESCALATE 누적)
- lesson 4+1+sub invariants 누적 효과 측정 (MCT-150 5 FIX → MCT-154 0 FIX → MCT-155 목표 0 FIX)
- codeforge #525 amendment 권고 (lesson #5 sub-invariant 공식 채택 권고 trail)
- Epic CLOSED gate 6 AC 충족 evidence summary
- Stage 3 (또는 future epic) 진입 권고 (TLS 재검토 trigger, Stage 2 정책 amendment 후보)

---

## §3 관련 ADR / 코드 경로

### §3.1 관련 ADR (2026-05-13 기준)

| ADR | 결정 | 본 Story 직접 인용 |
|---|---|---|
| **ADR-027** | Cold Tier Object Storage on NAS MinIO (Stage 1 + Stage 2 운영 정책) | D2 (TLS 재검토 amendment owner, AC-4) + D6 (3종 → 7종 invariant amendment owner, AC-5) + D7 (Local GC 7일 grace + dry-run + 디스크 압박 시 tier/date 순차, AC-1+AC-2 직접 enforcement) + D4 (cutover 4-step 의 step 4 local GC, AC-1) |
| **ADR-017** | Cold Tier Compaction Strategy + WAL/L1/L2/L3 tier scope | hot path (collector WAL + L1) 무영향 invariant 보존 — 본 Story = cold tier (L2) only, hot path 영향 0 |
| **ADR-009** | Standard Schema 16 columns + Hive partition layout | forward-only invariant (§D12.2) 보존 — 본 Story = read-side cutover 완료 + write-side dual-write 7d grace 운영 + GC = 양쪽 storage 변경 0 |
| **ADR-008** | Secret Management policy | 본 Story AC-3 (secret rotation 90d 첫 cycle) = ADR-008 secret management 준수 (`.env` 0600 + gitignored + audit log 박제) |

### §3.2 관련 코드 경로 (mctrader-data + mctrader-hub, MCT-150~154 land 산출물 cross-reference)

| 경로 | 책임 | 본 Story 산출물 dependency |
|---|---|---|
| **`mctrader-data/src/mctrader_data/nas_migration/__init__.py`** (MCT-151 P1 land) | nas_migration package marker | 본 Story `cutover_verifier.py` + `gc_runner.py` 동일 namespace |
| **`mctrader-data/src/mctrader_data/nas_migration/invariant_harness.py`** (MCT-151 land) | 7종 invariant verify harness | 본 Story `cutover_verifier.py` 가 직접 inject (RPO=0 검증 시 7종 invariant ALL PASS 의무) |
| **`mctrader-data/src/mctrader_data/nas_migration/backfill_orchestrator.py`** (MCT-153 land) | 76GB historic backfill orchestrator | 본 Story `gc_runner.py` 가 backfill 완료 evidence cross-reference (Prometheus metric `nas_backfill_*`) |
| **`mctrader-data/src/mctrader_data/ops/dual_write_window_runner.py`** (MCT-152 land) | dual-write window 매일 invariant verify cron | 본 Story `gc_runner.py` 가 7d grace + invariant ALL PASS 7일 누적 evidence cross-reference (Prometheus metric `nas_dual_write_window_*`) |
| **`mctrader-data/src/mctrader_data/nas_storage/dual_writer.py`** (MCT-151 land) | dual-write atomic primitive | 본 Story = read-only cross-reference (write-side 변경 0) |
| **`mctrader-data/src/mctrader_data/nas_storage/nas_uploader.py`** (MCT-150 land) | NAS PUT idempotent uploader | 본 Story = read-only cross-reference (rotate 후 재인증 verify 시 PUT/GET 1회) |
| **`mctrader-engine/src/mctrader_engine/io/endpoint_router.py`** (MCT-154 land) | reader endpoint atomic flip + 7d grace mode | 본 Story `cutover_verifier.py` 가 cutover rollback signal emit path (FAIL 시 endpoint_router.rollback() trigger) — but 본 Story 측 직접 호출 0 (signal emit only, operator manual gate) |
| **`mctrader-engine/src/mctrader_engine/io/cold_reader.py`** (MCT-154 land) | NAS cold L2/L3 reader | 본 Story = read-only cross-reference (rotate 후 첫 read verify 시 sample read 1회) |
| **`mctrader-hub/docs/runbooks/nas-minio-secret-rotation.md`** (MCT-147 land) | 90d rotation runbook | 본 Story `rotate_minio_secret.py` CLI 가 본 runbook 패턴 정합 + emergency rotation path 박제 |
| **`mctrader-hub/docs/runbooks/nas-minio-cutover-checklist.md`** (MCT-154 land) | cutover operator checklist | 본 Story = cutover 완료 후 evidence cross-reference (cutover timestamp + endpoint URL) |
| **`mctrader-hub/docs/runbooks/nas-minio-unreachable-sop.md`** (MCT-152 land) | NAS unreachable SOP | 본 Story = rotate 진행 중 인증 실패 시 SOP 정합 (rollback to old credential + alert) |
| **`mctrader-hub/configs/prometheus/nas_invariant_rules.yml`** (MCT-151 land) | nas_invariant_* alert rules | 본 Story 신규 metric (nas_gc_* + nas_rpo_verify_* + nas_secret_rotation_*) 측 alert 추가 가능성 (Phase 2 impl 측 결정) |

### §3.3 본 Story 신규 산출물 6 file (mctrader-data) + 4 file (mctrader-hub)

| 경로 | 책임 | scope_manifest planned_files MCT-155 정합 |
|---|---|---|
| **`mctrader-data/src/mctrader_data/nas_migration/cutover_verifier.py`** | CutoverVerifier API (RPO=0 검증, S8) | ✅ 정합 (lesson #5 사전 적용 — `migration/` → `nas_migration/`) |
| **`mctrader-data/src/mctrader_data/nas_migration/gc_runner.py`** | GcRunner API (Local GC, D7 + R5) | ✅ 정합 (lesson #5 사전 적용) |
| **`mctrader-data/scripts/migration/verify_rpo_zero.py`** | RPO=0 검증 CLI | ✅ 정합 (scripts/migration/ MCT-153 동일) |
| **`mctrader-data/scripts/ops/rotate_minio_secret.py`** | 90d secret rotation CLI | ✅ 정합 (scripts/ops/ MCT-150 동일) |
| **`mctrader-data/tests/nas_migration/test_cutover_verifier.py`** | RpoVerifyResult test | ✅ 정합 (lesson #5 사전 적용) |
| **`mctrader-data/tests/nas_migration/test_gc_runner.py`** | GcResult test | ✅ 정합 (lesson #5 사전 적용) |
| **`mctrader-hub/docs/stories/MCT-155.md`** | 본 file (Story SSOT) | ✅ 정합 |
| **`mctrader-hub/docs/runbooks/nas-minio-tls-review.md`** | TLS 재검토 결정 박제 + ADR-027 D2 amendment 후보 | ✅ 정합 |
| **`mctrader-hub/docs/runbooks/nas-minio-stage2-runbook.md`** | Stage 2 종료 운영 runbook | ✅ 정합 |
| **`mctrader-hub/docs/retros/2026-05-stage2.md`** | Stage 2 Epic-final retro 별 file | ✅ 정합 |

총 10 file 신규 (6 data + 4 hub) — scope_manifest sync 100% (lesson #5 file-level invariant pre-emptive 적용 완료).

---

## §4 Change Impact Analysis

### §4.1 Direct impact

**mctrader-data (primary)**:
- 신규 module 2종 (`nas_migration/cutover_verifier.py` + `nas_migration/gc_runner.py`) — 기존 module 변경 0
- 신규 CLI script 2종 (`scripts/migration/verify_rpo_zero.py` + `scripts/ops/rotate_minio_secret.py`) — 기존 CLI 변경 0
- 신규 test 2종 (`tests/nas_migration/test_cutover_verifier.py` + `tests/nas_migration/test_gc_runner.py`) — 기존 test 변경 0
- **destructive operation 의 첫 도입** — `gc_runner.gc(dry_run=False)` 가 local L2 76GB 영구 삭제 (MCT-150~154 모두 non-destructive)

**mctrader-hub**:
- 신규 runbook 2종 (`nas-minio-tls-review.md` + `nas-minio-stage2-runbook.md`)
- 신규 retro 1종 (`docs/retros/2026-05-stage2.md` Stage 2 Epic-final retro)
- 신규 Story SSOT 1종 (`docs/stories/MCT-155.md` 본 file)
- ADR-027 amendment **2건 mandatory** (D2 TLS 재검토 + D6 7종 invariant 명시화) — Phase 2 PR 측 hub 산출물

**mctrader-engine**:
- 본 Story 직접 변경 0 (read-only cross-reference)
- but Phase 2 운영 시점 cutover_verifier.py 의 rollback signal 측 endpoint_router.rollback() trigger 가능성 (operator manual gate)

### §4.2 Indirect impact

- **disk 사용량 변화**: GC 완료 시 local L2 76GB → 0 (free disk +76GB → 압박 해소 ~70%)
- **NAS 사용량 변화**: 0 (NAS 측 76GB 그대로 유지, 본 Story = local 측 GC only)
- **secret rotation 영향**: 양측 (mctrader-data + mctrader-engine) 컨테이너 신규 credential 진입 — restart 또는 hot-reload (Phase 2 결정)
- **ADR-027 박제 trail**: D2 + D6 amendment 후 wording 갱신 — 후 Story 의 ADR 인용 시 신규 wording 적용
- **Epic milestone #4 CLOSED**: PMOAgent 가 retro 후 milestone close trigger

### §4.3 Risk surface

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| **R5 (재인용)** GC 가 검증 전 데이터 삭제 (data loss) | low | critical | 4중 lock: cutover RPO=0 verify (S8) + 7d grace 만료 + invariant ALL PASS 7일 누적 + dry-run 선행 + 24h batch delete |
| **R-NEW-1** secret rotation 진행 중 양측 컨테이너 인증 실패 → NAS endpoint 통신 단절 | medium | high | (a) emergency rollback path (old credential 복원) (b) 양측 컨테이너 restart sequence (data 먼저 → engine 다음, runbook 박제) (c) sample PUT/GET verify 의무 (rotate 완료 전 |
| **R-NEW-2** TLS 재검토 결과가 사용자 confirm (HTTP 유지) 와 모순 시 ADR-027 amendment 충돌 | low | medium | 사용자 confirm = HTTP 유지 의 박제 trail 명확 (S12 user_confirmed: true) — 본 Story TLS 재검토 runbook 측 사용자 confirm 결정 verbatim 인용 + future re-evaluation trigger 명시 (예: NAS 외부 노출 시점) |
| **R-NEW-3** Stage 2 EPIC CLOSED gate 6 AC 중 1건이라도 미달 → Epic 종료 차단 | low | high | 본 Story Phase 2 진입 전 6 AC 충족 사전 verify (Phase 1 PR 단계 cross-check) + Phase 2 진행 중 AC-1~AC-3 실 운영 결과 박제 + retro 시점 종합 verify |

---

## §5 통합 요구사항 명세 (RequirementsPL author)

### §5.1 Functional Requirements (FR)

| ID | 요구사항 | AC mapping | Source |
|---|---|---|---|
| **FR-1** | `CutoverVerifier.verify_rpo_zero(cutover_timestamp) -> RpoVerifyResult` API — cutover-1s/+1s diff 측정 + 7종 invariant verify | AC-5 | S8 박제 |
| **FR-2** | `RpoVerifyResult` dataclass — status enum 3종 (`rpo_zero_verified` / `drift_detected` / `verify_inconclusive`) + diff metadata | AC-5 | S8 박제 |
| **FR-3** | `GcRunner.gc(tier, dry_run) -> GcResult` API — 7d grace + dry-run + invariant gate + 디스크 압박 시 tier/date 순차 | AC-1, AC-2 | D7 박제 + R5 mitigation |
| **FR-4** | `GcResult` dataclass — status enum 4종 (`dry_run_complete` / `executed` / `blocked_grace_period` / `blocked_invariant_fail`) + 삭제 metadata | AC-1, AC-2 | D7 박제 |
| **FR-5** | `verify_rpo_zero.py` CLI — RPO=0 verify CLI + JSON output + Prometheus push | AC-5 | S8 박제 |
| **FR-6** | `rotate_minio_secret.py` CLI — 90d rotation CLI + emergency rotation path + audit log | AC-3 | D2 4중 mitigation |
| **FR-7** | deletion log file (`mctrader-data/.tmp/gc-deletion-log-MCT-155.json` gitignored) — 실 삭제 entry append (deleted_partition + deleted_at_iso + sha256 + size_bytes) | AC-2 | R5 mitigation |
| **FR-8** | 24h batch delete window — dry-run + 실 삭제 사이 24h interval 의무 (operator manual gate) | AC-2 | R5 mitigation |
| **FR-9** | TLS 재검토 결정 박제 (`docs/runbooks/nas-minio-tls-review.md`) — 사용자 confirm = HTTP 유지 결정 verbatim + cost/handshake/cadence 재평가 결과 | AC-4 | S12 user_confirmed |
| **FR-10** | ADR-027 D2 amendment commit (Stage 2 cutover 후 재검토 결과 박제) | AC-4 | scope_manifest triggers_adr_amendment |
| **FR-11** | ADR-027 D6 amendment commit (3종 → 7종 invariant 명시화) | AC-5 | scope_manifest triggers_adr_amendment |
| **FR-12** | Stage 2 Epic-final retro 별 file (`docs/retros/2026-05-stage2.md`) — 6 Story trail + lesson 누적 효과 + codeforge #525 amendment 권고 + Epic CLOSED 6 AC evidence | AC-6 | Stage 2 종료 gate |
| **FR-13** | Stage 2 종료 운영 runbook (`docs/runbooks/nas-minio-stage2-runbook.md`) — Epic 6 AC 종합 verify checklist + GC/rotation/TLS 절차 | AC-1~AC-6 | 종합 |
| **FR-14** | Epic milestone #4 close trigger — 6 AC ALL PASS gate 통과 후 PMOAgent 가 milestone close + Epic 종료 박제 | AC-6 | Stage 2 종료 gate |

### §5.2 Non-Functional Requirements (NFR)

| ID | 요구사항 | 측정 mechanism |
|---|---|---|
| **NFR-1** | GC 실행 시간 < 10min (76GB local 삭제) | Prometheus metric `nas_gc_duration_seconds` |
| **NFR-2** | GC dry-run 실행 시간 < 5min (76GB scan) | Prometheus metric `nas_gc_dry_run_duration_seconds` |
| **NFR-3** | RPO=0 verify 실행 시간 < 3min (cutover-1s/+1s diff + 7종 invariant verify) | Prometheus metric `nas_rpo_verify_duration_seconds` |
| **NFR-4** | secret rotation 실행 시간 < 5min (양측 컨테이너 restart 포함) | Prometheus metric `nas_secret_rotation_duration_seconds` |
| **NFR-5** | deletion log file 무손실 (24h batch delete window 내 entry 손실 0) | file integrity check (sha256) |
| **NFR-6** | ADR amendment 박제 trail audit (D2 + D6 amendment commit hash + scope_manifest sync) | git log audit |

### §5.3 Out of Scope

- **NAS 측 GC** (NAS 측 76GB 영구 보존, 본 Story = local 측 GC only)
- **mctrader-engine 측 변경** (본 Story = read-only cross-reference, MCT-154 land 산출물 그대로 활용)
- **TLS 활성화** (사용자 confirm = HTTP 유지, 본 Story = 재검토 결정 박제 only — 활성화 0)
- **Stage 3 Epic 발의** (본 Story scope = Stage 2 closure only, Stage 3 진입 권고 retro 측 박제만)

### §5.4 Edge Cases (EC)

| EC ID | 시나리오 | 본 Story 대응 |
|---|---|---|
| **EC-1** | GC 진입 시점 7d grace 미만료 → blocked | GcResult.status = `blocked_grace_period` + alert + 재진입 거부 |
| **EC-2** | GC 진입 시점 invariant FAIL → blocked | GcResult.status = `blocked_invariant_fail` + alert + 재진입 거부 |
| **EC-3** | dry-run 결과 review NEGATIVE → 실 삭제 차단 | operator manual gate (24h batch delete window 내 review) |
| **EC-4** | 24h batch delete 진행 중 새 invariant FAIL surface → GC suspend | GcResult.status = `executed` (dry_run only) → 사용자 escalation |
| **EC-5** | secret rotation 진행 중 양측 컨테이너 인증 실패 → emergency rollback | rotate_minio_secret.py 가 emergency path 의 old credential 복원 + alert |
| **EC-6** | secret rotation 진행 중 한쪽만 갱신 후 다른 쪽 갱신 전 → 인증 mismatch | rotation runbook 박제 — sequential update (data 먼저 → engine 다음) + verify 의무 |
| **EC-7** | TLS 재검토 결과 = 사용자 confirm 변경 (예: TLS 활성화 결정) | scope_manifest design_decisions S12 user_confirmed update + ADR-027 D2 wording 갱신 (사용자 confirm 변경 박제) — 본 Story scope 외, future Story 에서 처리 |
| **EC-8** | RPO=0 verify FAIL (drift_detected) → cutover rollback signal emit | CutoverVerifier 가 cutover rollback signal emit (operator manual gate) — endpoint_router.rollback() trigger 가능성 |
| **EC-9** | Stage 2 EPIC CLOSED gate 6 AC 중 1건 미달 → Epic 종료 차단 | retro 시점 사전 verify + 미달 AC 측 별 Story 발의 (사용자 escalation) |
| **EC-10** | ADR-027 amendment 충돌 (D2 + D6 동시 amendment 시 conflict) | sequential amendment commit (D6 먼저 → D2 다음) + git log audit |
| **EC-11** | gc_runner 진입 직후 NAS unreachable → invariant verify 불가 | GcResult.status = `blocked_invariant_fail` + alert + 재진입 거부 (NAS 복구 후 재시도) |
| **EC-12** | gc_runner 24h batch delete 진행 중 컨테이너 restart → deletion log 무손실 보존 | deletion log file persistent (sqlite-WAL 또는 atomic write) + restart 후 resume from log |

### §5.5 lesson 4+1+sub invariants 사전 박제 의무 (RequirementsPL author scope, codeforge #525 6번째 case)

본 Story = codeforge #525 lesson 4+1+sub invariants 의 **6번째 적용 case**:

1. **#1 API contract semantic completeness** — §6.2 모든 method docstring 의 enum/state semantics + caller 처리 의무 명시 의무
2. **#2 Cross-module propagation completeness** — §6.7 cross-module contract 표 ~10 row 의무
3. **#3 Wording SSOT** — §6.8 enum value SSOT 박제 (RpoVerifyResult 3종 + GcResult 4종 + 신규 metric prefix `nas_gc_*` + `nas_rpo_verify_*` + `nas_secret_rotation_*` SSOT) 의무
4. **#4 Unconditional vs conditional invariant placement** — §6.9 placement 의도 명시 의무
5. **#5 file-level scope_manifest sync** — §6.10 scope_manifest planned_files MCT-155 sync 검증 (lesson 사전 적용 완료 — `migration/` → `nas_migration/` rename) 의무
6. **#5 sub-invariant cross-section quantitative consistency 두 번째 적용 case** — §6.11 cross-section cascade table 의무

ArchitectPL §6 author 시 위 6 invariants 사전 박제 (FIX cycle 0 또는 mechanical fast-path 목표).

---

## §6 Change Plan (ArchitectPL chief author + 6 deputy 통합)

### §6.0 Author trail + 6 deputy 통합 박제

**author**: ArchitectPLAgent (chief synthesizer + 6 deputy perspective 통합) — 단일 spawn, 본 §6~§11 통합 author. 신규 결정 0 (모두 RequirementsPL §1~§5 박제 wording transcribe + scope_manifest design_decisions S8/S9/S12 직접 인용 + ADR-027 D2/D6/D7 직접 인용 + MCT-150~154 land 산출물 직접 활용). lesson 4+1+sub invariants 사전 박제 의무 적용 (**6번째 case + sub-invariant 두 번째 적용 case**).

**6 deputy perspective 통합**:

1. **CodebaseMapper** — mctrader-data `nas_migration/` (MCT-151+153 land) + `nas_storage/` (MCT-150+151 land) + `ops/` (MCT-150+152 land) + `nas_metrics/` (MCT-150+152+153 land) 기존 sub-domain 분석. `nas_migration/cutover_verifier.py` + `nas_migration/gc_runner.py` = 자연 module 그룹화 (MCT-151 invariant_harness.py + MCT-153 backfill_orchestrator.py 와 동일 namespace). `scripts/migration/` (MCT-153 run_backfill.py 동일 prefix) + `scripts/ops/` (MCT-150 동일 prefix) 정합. **변경 영향**: 기존 file 변경 0, 신규 6 file (mctrader-data) + 4 file (mctrader-hub) 모두 namespace 충돌 0.
2. **Refactor** — cutover_verifier / gc_runner 2 module 결합도 분석. `cutover_verifier → InvariantHarness (MCT-151 inject) + endpoint_router (signal emit only) + boto3 S3 client → NAS endpoint`. `gc_runner → BackfillOrchestrator metric (MCT-153 read-only) + dual_write_window_runner metric (MCT-152 read-only) + InvariantHarness (gate verify) + os.unlink (local file system)`. 두 module = 단일 책임 (RPO=0 verify + GC) + dependency injection 패턴. **결합도 LOW**. **최소 변경 경로** = `nas_migration/` namespace 신규 2 file + scripts 2 file (mctrader-data 의 기존 module 변경 0).
3. **SecurityArch** — secret rotation 의 신규 trust boundary 분석. (a) NAS MinIO IAM (mctrader-data + mctrader-engine credential) 측 trust boundary 1 (b) `.env` file (mctrader-hub + mctrader-data + mctrader-engine 양측) trust boundary 2 (c) emergency rollback path (old credential file backup) trust boundary 3. credential = `${NAS_MINIO_ACCESS_KEY}` + `${NAS_MINIO_SECRET_KEY}` env 갱신 + `.env` 0600 + gitignored + audit log 박제. rotation 진행 중 양측 컨테이너 sequential update + verify 의무. **GC 측 trust boundary**: local file system (mctrader-data 컨테이너 측 `/data/cold/L2/`) — 본 Story 가 처음으로 destructive 진입.
4. **OperationalRiskArch** — §7.4 5 항목 + §8.5 spawn-time trigger 결정 input. **§8.5_active=true 결정 근거**: (a) Process restart-aware (gc_runner 24h batch delete 중 restart 시 deletion log 기반 resume 의무) Y (b) Background worker (24h batch delete window monitoring) Y (c) Stateful in-memory cache N (gc_runner = stateless, deletion log only persistent). 7d grace 만료 evidence + invariant ALL PASS 7일 누적 evidence cross-repo coordination = NFR-3 enforcement (mctrader-data gc_runner ↔ mctrader-data dual_write_window_runner ↔ mctrader-engine endpoint_router state sync).
5. **TestContractArch** — §8.5_active=true (chief 결정 verbatim 반영) + §8 Test Contract 26+ test 후보 (6 AC × per-AC 평균 4-5 test + §8.5 active 4건 + chaos 2건 = MCT-153 26 test 패턴 정합). 6 AC 모두 P0 + chaos test 2건 (cutover rollback 시나리오 EC-8 + GC suspend 시나리오 EC-4) + smoke test 1건 (rotation 후 sample PUT/GET verify).
6. **DataMigrationArch** — destructive operation (local L2 76GB GC) 의 data integrity invariant + RPO=0 검증 회고. ADR-009 forward-only invariant 직교 (NAS 측 변경 0, local 측만 삭제). §11.6 idempotency CONDITIONAL = 본 Story 적용 ✅ (gc_runner.gc(dry_run=True) 다중 호출 idempotent + cutover_verifier.verify_rpo_zero() 다중 호출 idempotent + rotate_minio_secret 다중 호출 시 emergency rollback safe).

**lesson 4+1+sub invariants 사전 박제 의무 적용** (RequirementsPL §5.5 박제 인용):

- **#1 API contract semantic completeness** = §6.2 모든 method docstring 의 enum/state semantics + caller 처리 의무 명시
- **#2 Cross-module propagation completeness** = §6.7 cross-module contract 표 ~10 row (cutover_verifier ↔ InvariantHarness + endpoint_router signal + gc_runner ↔ BackfillOrchestrator metric + dual_write_window_runner metric + InvariantHarness gate + 6 enum + 12 metric + 4 alert source)
- **#3 Wording SSOT** = §6.8 RpoVerifyResult 3종 + GcResult 4종 + (신규 metric prefix `nas_gc_*` 4종 + `nas_rpo_verify_*` 3종 + `nas_secret_rotation_*` 5종 = 12 신규 metric SSOT) + alert name 4종 SSOT 박제
- **#4 Unconditional vs conditional placement** = §6.9 cutover RPO=0 verify unconditional (GC 진입 직전 첫 단계, AC-5 → AC-1 prerequisite) + 7d grace + invariant ALL PASS 7일 누적 unconditional (GC 진입 gate, AC-1) + dry-run 선행 unconditional (실 삭제 전 의무, AC-1) vs deletion log conditional (실 삭제 진입 시점, AC-2) + 24h batch delete conditional (operator manual gate, AC-2) + secret rotation 90d cycle conditional (cron 또는 manual trigger, AC-3) + TLS 재검토 conditional (사용자 confirm gate, AC-4) 분리
- **#5 file-level scope_manifest sync** = §6.10 scope_manifest planned_files MCT-155 sync 검증 (path 갱신 권고 1건 — `migration/` → `nas_migration/` rename 사전 적용 완료, 두 번째 case)
- **#5 sub-invariant cross-section quantitative consistency 두 번째 적용 case** = §6.11 핵심 설계 수치 (file count 10 / 6 AC / 7d grace days 7 / 24h batch delete window / 90d rotation cadence / RPO=0 invariant 7종 / 신규 metric 12종 / 신규 enum value 7종) cross-section cascade table

### §6.1 Architecture (RefactorAgent perspective + chief synthesis)

본 Story 신규 2 module = `nas_migration/` 기존 sub-domain 의 자연 확장 (MCT-151 invariant_harness.py + MCT-153 backfill_orchestrator.py 와 동일 namespace). 결합도 LOW + 단일 책임 분리 + dependency injection 패턴.

**module 책임 분담**:

| module | 책임 | dependency | 채택 근거 |
|---|---|---|---|
| **`cutover_verifier.py`** (CutoverVerifier class) | RPO=0 검증 (cutover-1s/+1s diff 측정) + 7종 invariant verify + cutover rollback signal emit | InvariantHarness (MCT-151 inject) + boto3 S3 client + (signal only) endpoint_router | S8 박제 직접 owner (cutover-1s 검증 segment 모두 cutover+1s NAS 존재 의무) + ADR-027 D6 amendment trigger |
| **`gc_runner.py`** (GcRunner class) | Local L2 GC (7d grace + dry-run + invariant gate + 디스크 압박 시 tier/date 순차) + deletion log + 24h batch delete | InvariantHarness (gate verify) + Prometheus metric (BackfillOrchestrator + dual_write_window_runner read-only) + os.unlink (local file system) + sqlite (deletion log persistent) | D7 박제 직접 enforcement (3중 lock) + R5 mitigation (deletion log + 24h batch delete) |

**2 module 의 dependency 그래프**:

```
[caller (cutover operator CLI / cron)]
      v
[CutoverVerifier.verify_rpo_zero(cutover_timestamp) -> RpoVerifyResult]
      v (1) cutover-1s segment list (local + NAS)
[boto3 S3 client.list_objects_v2(Bucket, Prefix) + local glob]
      v (2) cutover+1s segment list (local + NAS)
[boto3 S3 client.list_objects_v2(Bucket, Prefix) + local glob]
      v (3) diff 측정 (cutover-1s in NAS @ cutover+1s)
[diff 결과 + 7종 invariant verify (InvariantHarness inject)]
      v (4) RPO=0 결정
[RpoVerifyResult.status = rpo_zero_verified | drift_detected | verify_inconclusive]
      v
[FAIL 시 → cutover rollback signal emit (operator manual gate, endpoint_router.rollback())]

[caller (GC operator CLI / cron)]
      v
[GcRunner.gc(tier="L2", dry_run=True) -> GcResult]
      v (1) cutover RPO=0 verify pre-check (CutoverVerifier 결과 cross-reference)
[CutoverVerifier output 박제 file load]
      v (2) 7d grace 만료 verify (dual_write_window_runner Prometheus metric)
[Prometheus metric `nas_dual_write_grace_remaining_days == 0`]
      v (3) invariant ALL PASS 7일 누적 verify
[Prometheus metric `nas_invariant_verify_status == all_pass` for 7d]
      v (4) dry-run 진입
[local glob /data/cold/L2/ + 삭제 대상 list 산출 + size sum + sha256 list]
      v
[GcResult.status = dry_run_complete + GcResult.deletion_targets[]]
      v (5) operator manual gate (24h batch delete window 내 review)

[caller (operator review pass + 24h interval 후 실 삭제 trigger)]
      v
[GcRunner.gc(tier="L2", dry_run=False) -> GcResult]
      v (1) dry-run 결과 cross-reference
[deletion log file (sqlite) + dry-run target list 비교]
      v (2) 실 삭제 진입 (per-target os.unlink + deletion log entry append)
[per-target: os.unlink + sqlite-WAL append (deleted_partition + deleted_at_iso + sha256 + size_bytes)]
      v (3) 디스크 압박 시 tier/date 순차 처리
[disk usage > 90% 시 L3 → L2 + oldest first]
      v (4) 24h batch delete 완료 verify
[GcResult.status = executed + GcResult.deleted_count + GcResult.freed_bytes]
```

**chief decision 6건**:

1. **CutoverVerifier 의 RPO=0 검증 mechanism** = **cutover-1s/+1s diff 측정 + 7종 invariant verify** 채택 (segment hash 비교 only 거부). 근거:
   - segment hash 비교 only 시 7종 invariant 의 schema-level invariant (column count + column order + dtype + schema_version) 누락
   - cutover-1s 시점 local L2 segment list (glob `/data/cold/L2/*/*/*.parquet`) ↔ cutover+1s NAS object list (boto3 list_objects_v2) diff 측정 → cutover-1s segment 모두 cutover+1s NAS 존재 의무 (S8 박제)
   - 7종 invariant verify = MCT-151 InvariantHarness inject (재정의 0) → ALL PASS gate
   - **RpoVerifyResult enum 3종** = `rpo_zero_verified` (diff 0 + 7종 invariant ALL PASS) / `drift_detected` (diff > 0 또는 7종 invariant 1종 이상 FAIL) / `verify_inconclusive` (NAS unreachable 또는 cutover timestamp 의 ±1s 측정 불가)

2. **GcRunner 의 GC 진입 gate mechanism** = **3중 lock unconditional gate** 채택 (single check 거부). 근거:
   - single check (예: 7d grace 만료만) 시 R5 critical risk → invariant FAIL 측 silent → 손상된 NAS 자료에 의존하는 reader 가 stale data serve
   - 3중 lock unconditional: (a) cutover RPO=0 verify pre-check (CutoverVerifier output 박제 file load + RpoVerifyResult.status == rpo_zero_verified) (b) 7d grace 만료 verify (Prometheus metric `nas_dual_write_grace_remaining_days == 0`) (c) invariant ALL PASS 7일 누적 verify (Prometheus metric `nas_invariant_verify_status == all_pass` for 7d 누적) — 1건이라도 FAIL 시 GcResult.status = `blocked_*` + alert + 재진입 거부
   - **GcResult enum 4종** = `dry_run_complete` (dry-run 정상 완료, deletion_targets[] 박제) / `executed` (실 삭제 완료, deleted_count + freed_bytes 박제) / `blocked_grace_period` (7d grace 미만료) / `blocked_invariant_fail` (invariant FAIL 또는 cutover RPO=0 unverified)

3. **deletion log + 24h batch delete mechanism** = **sqlite-WAL persistent + 24h interval operator manual gate** 채택 (in-memory log 거부). 근거:
   - in-memory log 시 컨테이너 restart 후 deletion log 손실 → 24h batch delete window 내 복구 불가
   - sqlite-WAL persistent (`mctrader-data/.tmp/gc-deletion-log-MCT-155.db` gitignored) — restart 후 resume from log 가능 (EC-12)
   - 24h interval operator manual gate = dry-run + 실 삭제 사이 24h interval 의무 (runbook 박제) — 운영 실수 보호 + 24h 안에 실수 발견 시 deletion log 기반 NAS object 측 download + local 복원 가능
   - **deletion log entry schema** = `(deleted_partition, deleted_at_iso, sha256, size_bytes, recovery_status)` — recovery_status 5 enum (`pending` / `recovered` / `confirmed_deleted` / `recovery_failed` / `expired`)

4. **secret rotation sequential update mechanism** = **data 먼저 → engine 다음 + verify 의무** 채택 (parallel update 거부). 근거:
   - parallel update 시 양측 컨테이너 동시 갱신 race → 한쪽만 갱신 후 다른 쪽 갱신 전 인증 mismatch (EC-5/EC-6)
   - sequential = data 먼저 (rotate_minio_secret CLI 가 mctrader-data 컨테이너 측 .env 갱신 + restart) → verify (sample PUT 1회) → engine 다음 (mctrader-engine 측 .env 갱신 + restart) → verify (sample GET 1회)
   - emergency rollback path = old credential file backup (`mctrader-data/.tmp/secret-backup-MCT-155-cycle-1/.env.old`) — rotation 진행 중 인증 실패 시 즉시 복원 + alert (`NasSecretRotationFailed`)
   - **rotation cycle 결과** = audit log file (`mctrader-data/.tmp/secret-rotation-MCT-155-cycle-1.md` gitignored) + Prometheus metric `nas_secret_rotation_cycle_count_total` + `nas_secret_rotation_last_success_iso`

5. **TLS 재검토 runbook 구조** = **사용자 confirm verbatim + 5 sub-section 결정 박제** 채택 (단순 wording 거부). 근거:
   - 단순 wording 시 후 Story 의 cross-reference 어려움 + 사용자 confirm trail audit 부실
   - 5 sub-section: §1 사용자 confirm 결정 verbatim (S12 user_confirmed: true 인용) / §2 TLS 도입 시 cost 분석 (cert 비용 + handshake latency + cadence) / §3 TLS 도입 시 운영 부담 분석 / §4 외부 노출 검토 결과 (NAS 방화벽 IP-allowlist 유지) / §5 future re-evaluation trigger (NAS 외부 노출 시점 / 사용자 directive 변경 시점 / Stage 3 발의 시점)
   - ADR-027 D2 amendment 측 본 runbook §1 verbatim 인용 + amendment commit (Stage 2 cutover 후 재검토 결과 박제 wording 갱신)

6. **Stage 2 Epic-final retro 별 file 구조** = **6 sub-section 종합 retro** 채택 (Story §12 만 거부). 근거:
   - Story §12 만 시 Epic-final retro 측 6 Story trail 누적 분석 + lesson 누적 효과 측정 + Stage 1+2 종합 retro 박제 부재
   - 6 sub-section: §1 Stage 1+2 trail 요약 (MCT-147~155 9 Story SP/FIX 누적) / §2 lesson 4+1+sub invariants 누적 효과 측정 (MCT-150 5 FIX → MCT-155 목표 0 FIX) / §3 codeforge #525 amendment 권고 (lesson #5 sub-invariant 공식 채택 권고) / §4 Epic CLOSED gate 6 AC 충족 evidence summary / §5 ADR-027 amendment 2건 trail (D2 + D6) / §6 Stage 3 (또는 future epic) 진입 권고 (TLS 재검토 trigger, Stage 2 정책 amendment 후보)
   - retro 별 file = `docs/retros/2026-05-stage2.md` (Stage 2 Epic-final retro pattern 박제)

### §6.2 신규 모듈 인터페이스 (Architect chief author — public API + lesson 4 invariants 의무)

#### §6.2.1 `CutoverVerifier` class (`src/mctrader_data/nas_migration/cutover_verifier.py`)

```python
"""RPO=0 검증 (cutover-1s/+1s diff 측정 + 7종 invariant verify) — S8 박제 직접 owner.

Responsibilities:
1. cutover-1s 시점 L2 segment list (local + NAS)
2. cutover+1s 시점 L2 segment list (local + NAS)
3. diff 측정 (cutover-1s in NAS @ cutover+1s)
4. 7종 invariant verify (MCT-151 InvariantHarness inject)
5. RPO=0 결정 + cutover rollback signal emit (FAIL 시)

ADR-027 D6 amendment trigger (3종 → 7종 invariant 명시화).
ADR-027 D4 cutover step 2 (검증) 의 RPO=0 측면 직접 owner.

§8.5 active (process restart-aware, CFP-378 AC-5):
- cutover timestamp = caller 인자 (process restart 후 동일 timestamp 재verify 정합)
- diff 결과 박제 = file persistent (`mctrader-data/.tmp/rpo-zero-verify-MCT-155.md` gitignored)
- restart 후 verify 재실행 시 동일 결과 (idempotent)

§6.7 Cross-module contract (lesson #2 invariant):
- RpoVerifyResult.status switch 의무 (caller GcRunner pre-check + retro 박제)
- InvariantHarness.verify() 결과 propagate (7종 invariant ALL PASS gate)
- endpoint_router.rollback() signal emit path (FAIL 시 operator manual gate)

§6.9 placement:
- diff 측정 = unconditional (verify 첫 단계)
- 7종 invariant verify = unconditional (verify 두 번째 단계)
- RPO=0 결정 = unconditional (verify 세 번째 단계)
- cutover rollback signal emit = conditional (FAIL 시점 only)
"""
from __future__ import annotations

from dataclasses import dataclass, field
from typing import Literal, Optional
import logging

import boto3
from botocore.client import BaseClient

from mctrader_data.nas_migration.invariant_harness import InvariantHarness, InvariantResult


@dataclass(frozen=True)
class RpoVerifyResult:
    """RPO=0 검증 결과 — caller switch 의무 (§6.7 cross-module contract).

    status enum (3종, §6.8.1 SSOT):
    - "rpo_zero_verified"     : diff 0 + 7종 invariant ALL PASS (RPO=0 보장)
    - "drift_detected"        : diff > 0 또는 7종 invariant 1종 이상 FAIL (cutover rollback 권고)
    - "verify_inconclusive"   : NAS unreachable 또는 cutover timestamp 의 ±1s 측정 불가 (재시도 권고)

    Caller 처리 의무 (§6.7 매핑):
    - "rpo_zero_verified"     -> GcRunner.gc() 진입 가능 (3중 lock 의 1번째 lock 통과)
    - "drift_detected"        -> cutover rollback signal emit (operator manual gate, endpoint_router.rollback())
    - "verify_inconclusive"   -> NAS 복구 후 재시도 (transient error)
    """
    status: Literal["rpo_zero_verified", "drift_detected", "verify_inconclusive"]
    cutover_timestamp_iso: str = ""
    cutover_minus_1s_segment_count: int = 0       # cutover-1s 시점 local L2 segment count
    cutover_plus_1s_nas_object_count: int = 0     # cutover+1s 시점 NAS object count
    diff_segments: list[str] = field(default_factory=list)  # cutover-1s in local but missing in NAS @ cutover+1s
    invariant_result: Optional[InvariantResult] = None       # MCT-151 InvariantHarness 결과
    verify_duration_ms: float = 0.0
    verify_error: str = ""                                    # verify_inconclusive 시 박제


class CutoverVerifier:
    """RPO=0 검증 (cutover-1s/+1s diff + 7종 invariant verify).

    Thread-safety: stateless (per-call instance 또는 thread-safe 사용 가능).

    Idempotency: 다중 호출 시 동일 결과 (cutover_timestamp 동일 시 동일 diff + 동일 invariant 결과).
    """

    def __init__(
        self,
        *,
        s3_client: BaseClient,
        invariant_harness: InvariantHarness,
        local_l2_root: str = "/data/cold/L2",
        nas_bucket: str = "mctrader-market",
        evidence_pack_path: str = "/tmp/rpo-zero-verify-MCT-155.md",
    ) -> None:
        self._s3_client = s3_client
        self._invariant_harness = invariant_harness
        self._local_l2_root = local_l2_root
        self._nas_bucket = nas_bucket
        self._evidence_pack_path = evidence_pack_path
        self._logger = logging.getLogger(__name__)

    def verify_rpo_zero(self, cutover_timestamp_iso: str) -> RpoVerifyResult:
        """cutover-1s/+1s diff 측정 + 7종 invariant verify + RPO=0 결정.

        Algorithm (Phase 1~5 sequential):
        Phase 1 (cutover-1s segment list, unconditional):
        1. local L2 segment list at cutover-1s (glob /data/cold/L2/*/*/*.parquet, mtime <= cutover-1s)
        2. NAS object list at cutover-1s (list_objects_v2 with LastModified <= cutover-1s)

        Phase 2 (cutover+1s segment list, unconditional):
        1. local L2 segment list at cutover+1s
        2. NAS object list at cutover+1s

        Phase 3 (diff 측정, unconditional):
        1. cutover-1s in local but missing in NAS @ cutover+1s = diff_segments[]
        2. diff 0 = RPO=0 candidate, diff > 0 = drift_detected

        Phase 4 (7종 invariant verify, unconditional):
        1. InvariantHarness.verify(local_partitions=[], nas_objects=[]) -> InvariantResult
        2. ALL PASS = RPO=0 가능, 1종 이상 FAIL = drift_detected

        Phase 5 (RPO=0 결정 + signal emit, conditional FAIL 시):
        1. ALL PASS + diff 0 -> RpoVerifyResult.status = rpo_zero_verified
        2. FAIL 또는 diff > 0 -> RpoVerifyResult.status = drift_detected + cutover rollback signal emit
        3. NAS unreachable 또는 timestamp ±1s 측정 불가 -> verify_inconclusive

        Returns:
            RpoVerifyResult — status enum 3종 + diff metadata + invariant_result + duration_ms

        §6.7 Cross-module contract:
        - GcRunner pre-check 직접 활용 (3중 lock 의 1번째 lock)
        - endpoint_router.rollback() signal emit path (FAIL 시 operator manual gate)
        - retro 박제 (`docs/retros/2026-05-stage2.md` §4 Epic CLOSED 6 AC evidence)
        """
        ...  # Phase 2 impl
```

#### §6.2.2 `GcRunner` class (`src/mctrader_data/nas_migration/gc_runner.py`)

```python
"""Local L2 GC (7d grace + dry-run + invariant gate + 디스크 압박 시 tier/date 순차) + deletion log + 24h batch delete.

Responsibilities:
1. cutover RPO=0 verify pre-check (CutoverVerifier output 박제 file load)
2. 7d grace 만료 verify (Prometheus metric `nas_dual_write_grace_remaining_days == 0`)
3. invariant ALL PASS 7일 누적 verify (Prometheus metric `nas_invariant_verify_status == all_pass` for 7d)
4. dry-run (삭제 대상 list 산출 + size sum + sha256 list)
5. 24h batch delete window (operator manual gate)
6. 실 삭제 진입 (per-target os.unlink + deletion log entry append)
7. 디스크 압박 시 tier/date 순차 (disk usage > 90% 시 L3 → L2 + oldest first)

ADR-027 D7 박제 직접 enforcement (7일 grace + dry-run 선행 + 실 삭제 + 디스크 압박 시 tier/date 순차).
R5 mitigation 직접 enforcement (deletion log + 24h batch delete + 3중 lock).

§8.5 active (process restart-aware + Background worker, CFP-378 AC-5):
- 24h batch delete 중 restart 시 deletion log 기반 resume (sqlite-WAL persistent)
- 7d grace 만료 evidence + invariant ALL PASS 7일 누적 evidence cross-repo coordination
- restart 후 deletion log file load → 미완료 deletion entry resume

§6.7 Cross-module contract (lesson #2 invariant):
- GcResult.status switch 의무 (caller operator runbook + retro 박제)
- CutoverVerifier 결과 cross-reference 의무 (3중 lock 의 1번째 lock)
- Prometheus metric (BackfillOrchestrator + dual_write_window_runner + InvariantHarness) read-only

§6.9 placement:
- 3중 lock pre-check = unconditional (GC 진입 직전 첫 단계)
- dry-run 진입 = unconditional (실 삭제 전 의무)
- deletion log entry append = conditional (실 삭제 진입 시점 only)
- 24h batch delete = conditional (operator manual gate)
- 디스크 압박 시 tier/date 순차 = conditional (disk usage > 90% 시점 only)
"""
from __future__ import annotations

from dataclasses import dataclass, field
from typing import Literal, Optional
import logging
import os

from mctrader_data.nas_migration.cutover_verifier import CutoverVerifier, RpoVerifyResult
from mctrader_data.nas_migration.invariant_harness import InvariantHarness


@dataclass(frozen=True)
class GcResult:
    """Local L2 GC 결과 — caller switch 의무 (§6.7 cross-module contract).

    status enum (4종, §6.8.2 SSOT):
    - "dry_run_complete"          : dry-run 정상 완료 (deletion_targets[] 박제, 실 삭제 0)
    - "executed"                  : 실 삭제 완료 (deleted_count + freed_bytes 박제)
    - "blocked_grace_period"      : 7d grace 미만료 (gc 진입 차단)
    - "blocked_invariant_fail"    : invariant FAIL 또는 cutover RPO=0 unverified (gc 진입 차단)

    Caller 처리 의무 (§6.7 매핑):
    - "dry_run_complete"          -> operator review (24h batch delete window 내) → 실 삭제 trigger
    - "executed"                  -> 정상 진행 (Stage 2 종료 gate AC-3 evidence)
    - "blocked_grace_period"      -> alert (`NasGcBlockedGracePeriod`) + 재진입 거부 (grace 만료 후 재시도)
    - "blocked_invariant_fail"    -> alert (`NasGcBlockedInvariantFail`) + 재진입 거부 (invariant 측 fix 후 재시도)
    """
    status: Literal["dry_run_complete", "executed", "blocked_grace_period", "blocked_invariant_fail"]
    tier: str = "L2"
    deletion_targets: list[str] = field(default_factory=list)  # dry_run_complete 시 박제 (target partition list)
    target_size_bytes: int = 0                                  # dry_run_complete 시 박제 (target total size)
    deleted_count: int = 0                                       # executed 시 박제
    freed_bytes: int = 0                                         # executed 시 박제
    free_disk_pct_after: float = 0.0                             # executed 시 박제 (df -h after)
    grace_remaining_days: int = 0                                # blocked_grace_period 시 박제
    invariant_fail_reason: str = ""                              # blocked_invariant_fail 시 박제
    duration_ms: float = 0.0


class GcRunner:
    """Local L2 GC (7d grace + dry-run + invariant gate + tier/date 순차).

    Thread-safety: stateless (per-call instance, deletion log persistent).

    Idempotency: dry_run 다중 호출 시 동일 결과 (deletion_targets list 동일).
                 실 삭제 다중 호출 시 idempotent (deletion log 기반 already-deleted skip).
    """

    def __init__(
        self,
        *,
        cutover_verifier: CutoverVerifier,
        invariant_harness: InvariantHarness,
        local_l2_root: str = "/data/cold/L2",
        deletion_log_path: str = "/tmp/gc-deletion-log-MCT-155.db",
        grace_evidence_path: str = "/tmp/grace-evidence-MCT-155.json",
        invariant_evidence_path: str = "/tmp/invariant-7d-evidence-MCT-155.json",
        cutover_verify_evidence_path: str = "/tmp/rpo-zero-verify-MCT-155.md",
    ) -> None:
        self._cutover_verifier = cutover_verifier
        self._invariant_harness = invariant_harness
        self._local_l2_root = local_l2_root
        self._deletion_log_path = deletion_log_path
        self._grace_evidence_path = grace_evidence_path
        self._invariant_evidence_path = invariant_evidence_path
        self._cutover_verify_evidence_path = cutover_verify_evidence_path
        self._logger = logging.getLogger(__name__)

    def gc(self, *, tier: str = "L2", dry_run: bool = True) -> GcResult:
        """Local L2 GC 진입 (7d grace + dry-run + invariant gate + tier/date 순차).

        Algorithm (Phase 1~5 sequential):
        Phase 1 (3중 lock pre-check, unconditional):
        1. cutover RPO=0 verify pre-check (CutoverVerifier output 박제 file load + RpoVerifyResult.status == rpo_zero_verified)
        2. 7d grace 만료 verify (Prometheus metric or evidence file `nas_dual_write_grace_remaining_days == 0`)
        3. invariant ALL PASS 7일 누적 verify (Prometheus metric or evidence file `nas_invariant_verify_status == all_pass` for 7d 누적)
        4. 1건이라도 FAIL -> GcResult.status = blocked_* + alert + return

        Phase 2 (dry-run 진입, unconditional):
        1. local glob /data/cold/L2/*/*/*.parquet (또는 디스크 압박 시 tier/date 순차)
        2. 삭제 대상 list 산출 + size sum + sha256 list (per-target)
        3. dry_run=True -> GcResult.status = dry_run_complete + deletion_targets[] + target_size_bytes + return

        Phase 3 (24h batch delete operator manual gate, conditional):
        1. dry_run + 실 삭제 사이 24h interval 의무 (runbook 박제, caller 책임)
        2. 24h interval 미경과 시 caller 측 manual gate (본 method 측 강제 0)

        Phase 4 (실 삭제 진입, conditional dry_run=False 시):
        1. dry-run 결과 cross-reference (deletion log file 측 dry-run target list 비교)
        2. per-target 처리 sequential:
           - sqlite-WAL append (deleted_partition + deleted_at_iso + sha256 + size_bytes + recovery_status="pending")
           - os.unlink (local file system)
           - sqlite-WAL update (recovery_status="confirmed_deleted")
        3. fail-safe: per-target 실패 시 deletion log entry retain (recovery_status="recovery_failed") + alert

        Phase 5 (디스크 압박 시 tier/date 순차, conditional disk usage > 90% 시):
        1. tier 순차: L3 → L2 (D7 박제)
        2. date 순차: oldest first (date 기반 partition sort)

        Returns:
            GcResult — status enum 4종 + deletion metadata + free_disk_pct_after

        §6.7 Cross-module contract:
        - operator runbook 직접 활용 (manual gate)
        - CutoverVerifier 결과 cross-reference (3중 lock)
        - Prometheus metric `nas_gc_*` emit (4종)
        - retro 박제 (`docs/retros/2026-05-stage2.md` §4 Epic CLOSED 6 AC evidence)
        """
        ...  # Phase 2 impl
```

#### §6.2.3 `verify_rpo_zero.py` CLI (`scripts/migration/verify_rpo_zero.py`)

```python
"""RPO=0 verify CLI — CutoverVerifier 호출 + JSON output + Prometheus push.

Usage:
    python scripts/migration/verify_rpo_zero.py \\
        --cutover-timestamp 2026-05-13T15:00:00Z \\
        --output /tmp/rpo-zero-verify-MCT-155.md \\
        --prometheus-push https://prometheus-pushgateway.example.com

Output:
    - JSON RpoVerifyResult (stdout)
    - Markdown evidence pack (--output)
    - Prometheus push (--prometheus-push)

Exit codes:
    0 = rpo_zero_verified
    1 = drift_detected
    2 = verify_inconclusive
"""
```

#### §6.2.4 `rotate_minio_secret.py` CLI (`scripts/ops/rotate_minio_secret.py`)

```python
"""90d MinIO credential rotation CLI — sequential update (data 먼저 → engine 다음) + verify.

Usage:
    python scripts/ops/rotate_minio_secret.py \\
        --emergency \\
        --output /tmp/secret-rotation-MCT-155-cycle-1.md \\
        --backup /tmp/secret-backup-MCT-155-cycle-1/

Sequential update:
    1. MinIO IAM API: 신규 access_key + secret_key 생성
    2. .env backup (mctrader-data + mctrader-engine, 0600)
    3. .env 갱신 (data 먼저)
    4. mctrader-data 컨테이너 restart 또는 hot-reload
    5. sample PUT 1회 verify
    6. .env 갱신 (engine 다음)
    7. mctrader-engine 컨테이너 restart 또는 hot-reload
    8. sample GET 1회 verify
    9. old credential revoke (MinIO IAM API)
    10. audit log 박제 (--output)

Emergency rollback path:
    --emergency 시 인증 실패 발견 즉시 old credential 복원 + alert

Exit codes:
    0 = rotation 정상 완료
    1 = rotation 실패 (emergency rollback 진행)
    2 = MinIO IAM API 접속 실패
"""
```

### §6.3 디렉토리 트리 (lesson #5 file-level invariant — 사전 갱신 완료)

```
mctrader-data/
├── src/mctrader_data/
│   └── nas_migration/                    [MCT-151+153 land + 본 Story 신규 2 file]
│       ├── __init__.py                   (MCT-151 P1 land)
│       ├── invariant_harness.py          (MCT-151 land — 본 Story inject)
│       ├── backfill_orchestrator.py      (MCT-153 land — 본 Story metric read-only)
│       ├── cutover_verifier.py           [신규 본 Story]
│       └── gc_runner.py                  [신규 본 Story]
├── scripts/
│   ├── migration/
│   │   ├── run_backfill.py               (MCT-153 land)
│   │   └── verify_rpo_zero.py            [신규 본 Story]
│   └── ops/
│       └── rotate_minio_secret.py        [신규 본 Story]
└── tests/nas_migration/
    ├── __init__.py                       (MCT-151 P1 land)
    ├── test_invariant_harness.py         (MCT-151 land)
    ├── test_backfill_orchestrator.py     (MCT-153 land)
    ├── test_backfill_resumability_chaos.py (MCT-153 land)
    ├── test_cutover_verifier.py          [신규 본 Story]
    └── test_gc_runner.py                 [신규 본 Story]

mctrader-hub/
├── docs/
│   ├── stories/
│   │   └── MCT-155.md                    [신규 본 Story — 본 file]
│   ├── runbooks/
│   │   ├── nas-minio-deploy.md           (MCT-147 land)
│   │   ├── nas-minio-secret-rotation.md  (MCT-147 land — 본 Story rotate_minio_secret CLI 패턴 정합)
│   │   ├── nas-minio-cutover-checklist.md (MCT-154 land — 본 Story cross-reference)
│   │   ├── nas-minio-unreachable-sop.md  (MCT-152 land — 본 Story emergency rollback 정합)
│   │   ├── nas-minio-tls-review.md       [신규 본 Story]
│   │   └── nas-minio-stage2-runbook.md   [신규 본 Story]
│   ├── retros/
│   │   └── 2026-05-stage2.md             [신규 본 Story — Stage 2 Epic-final retro 별 file]
│   └── adr/
│       └── ADR-027-cold-tier-object-storage-nas-minio.md  (amendment 2건 — D2 + D6 mandatory, 본 Story Phase 2 수정)
└── scope_manifests/
    └── EPIC-cold-tier-nas-minio.yaml     (planned_files MCT-155 path 사전 갱신 완료 — `migration/` → `nas_migration/`)
```

### §6.4 신규 Prometheus metric (12종 SSOT)

본 Story 신규 metric 12종 (3 prefix × 평균 4종):

| Metric | Type | Labels | 설명 | SSOT |
|---|---|---|---|---|
| **`nas_gc_run_count_total`** | Counter | `tier`, `result` | gc 실행 횟수 (per-tier × result enum) | §6.8.3 |
| **`nas_gc_dry_run_duration_seconds`** | Histogram | `tier` | dry-run 실행 시간 | §6.8.3 |
| **`nas_gc_executed_duration_seconds`** | Histogram | `tier` | 실 삭제 실행 시간 | §6.8.3 |
| **`nas_gc_freed_bytes_total`** | Counter | `tier` | 누적 freed bytes | §6.8.3 |
| **`nas_rpo_verify_run_count_total`** | Counter | `result` | RPO=0 verify 실행 횟수 (per result enum) | §6.8.4 |
| **`nas_rpo_verify_duration_seconds`** | Histogram | (none) | RPO=0 verify 실행 시간 | §6.8.4 |
| **`nas_rpo_verify_diff_segments_count`** | Gauge | (none) | 마지막 verify 의 diff segment 수 | §6.8.4 |
| **`nas_secret_rotation_cycle_count_total`** | Counter | `result` | rotation cycle 실행 횟수 (per result enum) | §6.8.5 |
| **`nas_secret_rotation_duration_seconds`** | Histogram | (none) | rotation cycle 실행 시간 | §6.8.5 |
| **`nas_secret_rotation_last_success_iso`** | Gauge | (none) | 마지막 success rotation timestamp | §6.8.5 |
| **`nas_secret_rotation_emergency_count_total`** | Counter | (none) | emergency rollback 횟수 | §6.8.5 |
| **`nas_secret_rotation_age_days`** | Gauge | (none) | 마지막 rotation 으로부터 경과 일수 (90d cadence alert) | §6.8.5 |

### §6.5 신규 Alert (4종 SSOT)

| Alert Name | Severity | Trigger | Source |
|---|---|---|---|
| **`NasGcBlockedGracePeriod`** | warning | `nas_gc_run_count_total{result="blocked_grace_period"}` increase > 0 | gc 진입 차단 (7d grace 미만료) |
| **`NasGcBlockedInvariantFail`** | critical | `nas_gc_run_count_total{result="blocked_invariant_fail"}` increase > 0 | gc 진입 차단 (invariant FAIL) — 사용자 즉시 escalation |
| **`NasRpoVerifyDriftDetected`** | critical | `nas_rpo_verify_run_count_total{result="drift_detected"}` increase > 0 | RPO=0 위반 검출 — cutover rollback 권고 |
| **`NasSecretRotationFailed`** | critical | `nas_secret_rotation_cycle_count_total{result="failed"}` increase > 0 OR `nas_secret_rotation_age_days > 100` | rotation 실패 또는 100d 초과 — 사용자 즉시 escalation |

### §6.6 evidence pack 박제 정책

scope_manifest gate 박제 의무 정합:

- **AC-5 RPO=0 검증 evidence**: `mctrader-data/.tmp/rpo-zero-verify-MCT-155.md` (gitignored) — verify_rpo_zero.py CLI output + RpoVerifyResult JSON + 7종 invariant verify 결과
- **AC-1 GC 완료 evidence**: `mctrader-data/.tmp/gc-evidence-MCT-155.md` (gitignored) — gc_runner GcResult + df -h before/after diff + deletion log summary
- **AC-3 secret rotation evidence**: `mctrader-data/.tmp/secret-rotation-MCT-155-cycle-1.md` (gitignored) — rotate_minio_secret CLI output + rotation cycle audit log
- **AC-6 Stage 2 EPIC CLOSED gate evidence**: `mctrader-hub/docs/retros/2026-05-stage2.md` (Stage 2 Epic-final retro 별 file) — 6 AC 종합 verify summary + cross-reference

### §6.7 Cross-module contract (lesson #2 invariant — 10 row)

| ID | Source module | Target module | Contract | 박제 |
|---|---|---|---|---|
| **C-1** | CutoverVerifier | InvariantHarness (MCT-151) | InvariantHarness.verify(local_partitions=[], nas_objects=[]) -> InvariantResult inject (재정의 0, 7종 invariant ALL PASS gate) | §6.2.1 |
| **C-2** | CutoverVerifier | GcRunner | RpoVerifyResult.status == rpo_zero_verified -> GcRunner 3중 lock 1번째 lock 통과 (file 박제 cross-reference) | §6.2.1 + §6.2.2 |
| **C-3** | CutoverVerifier | endpoint_router (MCT-154) | RpoVerifyResult.status == drift_detected -> cutover rollback signal emit path (operator manual gate, endpoint_router.rollback() trigger) | §6.2.1 |
| **C-4** | GcRunner | dual_write_window_runner (MCT-152) | Prometheus metric `nas_dual_write_grace_remaining_days` read-only (3중 lock 2번째 lock 통과) | §6.2.2 |
| **C-5** | GcRunner | InvariantHarness (MCT-151) | Prometheus metric `nas_invariant_verify_status` read-only for 7d 누적 (3중 lock 3번째 lock 통과) | §6.2.2 |
| **C-6** | GcRunner | BackfillOrchestrator (MCT-153) | Prometheus metric `nas_backfill_*` read-only (76GB historic backfill 완료 evidence cross-reference) | §6.2.2 |
| **C-7** | rotate_minio_secret | nas_uploader (MCT-150) | rotate 후 sample PUT 1회 verify (재인증 verify) | §6.2.4 |
| **C-8** | rotate_minio_secret | cold_reader (MCT-154) | rotate 후 sample GET 1회 verify (재인증 verify) | §6.2.4 |
| **C-9** | gc_runner / cutover_verifier | runbook nas-minio-stage2-runbook.md | operator manual gate trigger 의무 (24h batch delete + dry-run review + cutover rollback) | §6.6 |
| **C-10** | TLS review runbook | ADR-027 D2 amendment | runbook §1 verbatim 인용 + amendment commit (Stage 2 cutover 후 재검토 결과 wording 갱신) | §6.2 |

### §6.8 Wording SSOT (lesson #3 invariant — 7 신규 enum value + 12 신규 metric + 4 신규 alert)

#### §6.8.1 RpoVerifyResult.status enum (3종 SSOT)

| Value | 의미 | Caller 처리 |
|---|---|---|
| `rpo_zero_verified` | diff 0 + 7종 invariant ALL PASS | GcRunner.gc() 진입 가능 (3중 lock 1번째 통과) |
| `drift_detected` | diff > 0 또는 7종 invariant 1종 이상 FAIL | cutover rollback signal emit (operator manual gate) |
| `verify_inconclusive` | NAS unreachable 또는 timestamp ±1s 측정 불가 | 재시도 (transient error) |

#### §6.8.2 GcResult.status enum (4종 SSOT)

| Value | 의미 | Caller 처리 |
|---|---|---|
| `dry_run_complete` | dry-run 정상 완료 (실 삭제 0, deletion_targets[] 박제) | operator review (24h batch delete window 내) |
| `executed` | 실 삭제 완료 (deleted_count + freed_bytes 박제) | 정상 진행 (Stage 2 종료 gate AC-3 evidence) |
| `blocked_grace_period` | 7d grace 미만료 | alert + 재진입 거부 (grace 만료 후 재시도) |
| `blocked_invariant_fail` | invariant FAIL 또는 cutover RPO=0 unverified | alert + 재진입 거부 (invariant 측 fix 후 재시도) |

#### §6.8.3 nas_gc_* metric (4종 SSOT)

prefix = `nas_gc_*` (MCT-150 nas_uploader_* + MCT-151 nas_invariant_* + MCT-152 nas_dual_write_* + MCT-153 nas_backfill_* convention 정합)

| Metric | Type | Labels |
|---|---|---|
| `nas_gc_run_count_total` | Counter | `tier`, `result` (4 enum from §6.8.2) |
| `nas_gc_dry_run_duration_seconds` | Histogram | `tier` |
| `nas_gc_executed_duration_seconds` | Histogram | `tier` |
| `nas_gc_freed_bytes_total` | Counter | `tier` |

#### §6.8.4 nas_rpo_verify_* metric (3종 SSOT)

| Metric | Type | Labels |
|---|---|---|
| `nas_rpo_verify_run_count_total` | Counter | `result` (3 enum from §6.8.1) |
| `nas_rpo_verify_duration_seconds` | Histogram | (none) |
| `nas_rpo_verify_diff_segments_count` | Gauge | (none) |

#### §6.8.5 nas_secret_rotation_* metric (5종 SSOT)

| Metric | Type | Labels |
|---|---|---|
| `nas_secret_rotation_cycle_count_total` | Counter | `result` (3 enum: `success` / `failed` / `emergency_rollback`) |
| `nas_secret_rotation_duration_seconds` | Histogram | (none) |
| `nas_secret_rotation_last_success_iso` | Gauge | (none) |
| `nas_secret_rotation_emergency_count_total` | Counter | (none) |
| `nas_secret_rotation_age_days` | Gauge | (none) |

#### §6.8.6 alert SSOT (4종)

위 §6.5 alert 4종 SSOT — 본 절 reference only.

### §6.9 Unconditional vs conditional invariant placement (lesson #4 invariant)

| 항목 | placement | 의도 | 박제 |
|---|---|---|---|
| **cutover RPO=0 verify pre-check** (CutoverVerifier output 박제 file load) | unconditional | GC 진입 직전 첫 단계, AC-1 prerequisite (3중 lock 1번째) | §6.2.2 Phase 1.1 |
| **7d grace 만료 verify** (Prometheus metric `nas_dual_write_grace_remaining_days == 0`) | unconditional | GC 진입 gate, AC-1 (3중 lock 2번째) | §6.2.2 Phase 1.2 |
| **invariant ALL PASS 7일 누적 verify** (Prometheus metric `nas_invariant_verify_status == all_pass` for 7d) | unconditional | GC 진입 gate, AC-1 (3중 lock 3번째) | §6.2.2 Phase 1.3 |
| **dry-run 진입** (삭제 대상 list 산출) | unconditional | 실 삭제 전 의무 (R5 mitigation) | §6.2.2 Phase 2 |
| **diff 측정** (cutover-1s/+1s segment list) | unconditional | RPO=0 verify 첫 단계 (S8) | §6.2.1 Phase 1+2+3 |
| **7종 invariant verify** (InvariantHarness inject) | unconditional | RPO=0 verify 두 번째 단계 (S8 + D6 amendment) | §6.2.1 Phase 4 |
| **deletion log entry append** (sqlite-WAL persistent) | conditional | 실 삭제 진입 시점 only (dry_run=False), R5 mitigation | §6.2.2 Phase 4.2 |
| **24h batch delete operator manual gate** | conditional | 실 삭제 trigger 전 24h interval 의무 (operator 책임), R5 mitigation | §6.2.2 Phase 3 |
| **디스크 압박 시 tier/date 순차** | conditional | disk usage > 90% 시점 only (D7) | §6.2.2 Phase 5 |
| **secret rotation 90d cycle** | conditional | cron 또는 manual trigger (AC-3) | §6.2.4 |
| **emergency rotation rollback** | conditional | 인증 실패 발견 시점 only (EC-5/EC-6) | §6.2.4 |
| **cutover rollback signal emit** | conditional | RPO=0 verify FAIL 시점 only (EC-8) | §6.2.1 Phase 5 |
| **TLS 재검토 결정 박제** | conditional | 사용자 confirm gate (AC-4) | §6.6 + S12 user_confirmed |
| **ADR-027 D2 amendment commit** | conditional | TLS 재검토 결과 박제 후 commit (AC-4) | §6.7 C-10 |
| **ADR-027 D6 amendment commit** | conditional | RPO=0 검증 회고 결과 박제 후 commit (AC-5) | §6.7 + scope_manifest triggers_adr_amendment |

### §6.10 file-level scope_manifest sync (lesson #5 invariant — 두 번째 case 사전 적용 완료)

scope_manifest `planned_files.MCT-155` (2026-05-13 사전 갱신 완료) ↔ §6.3 디렉토리 트리 sync 검증:

| scope_manifest 측 path | §6.3 디렉토리 트리 측 path | sync |
|---|---|---|
| `src/mctrader_data/nas_migration/cutover_verifier.py` | `mctrader-data/src/mctrader_data/nas_migration/cutover_verifier.py` | ✅ |
| `src/mctrader_data/nas_migration/gc_runner.py` | `mctrader-data/src/mctrader_data/nas_migration/gc_runner.py` | ✅ |
| `scripts/migration/verify_rpo_zero.py` | `mctrader-data/scripts/migration/verify_rpo_zero.py` | ✅ |
| `scripts/ops/rotate_minio_secret.py` | `mctrader-data/scripts/ops/rotate_minio_secret.py` | ✅ |
| `tests/nas_migration/test_cutover_verifier.py` | `mctrader-data/tests/nas_migration/test_cutover_verifier.py` | ✅ |
| `tests/nas_migration/test_gc_runner.py` | `mctrader-data/tests/nas_migration/test_gc_runner.py` | ✅ |
| `docs/stories/MCT-155.md` | `mctrader-hub/docs/stories/MCT-155.md` | ✅ |
| `docs/runbooks/nas-minio-tls-review.md` | `mctrader-hub/docs/runbooks/nas-minio-tls-review.md` | ✅ |
| `docs/runbooks/nas-minio-stage2-runbook.md` | `mctrader-hub/docs/runbooks/nas-minio-stage2-runbook.md` | ✅ |
| `docs/retros/2026-05-stage2.md` | `mctrader-hub/docs/retros/2026-05-stage2.md` | ✅ |

총 10 file path sync ✅ (scope_manifest 사전 갱신 1건 — `migration/` → `nas_migration/` rename 완료, lesson #5 두 번째 case 사전 적용).

### §6.11 cross-section quantitative consistency cascade table (lesson #5 sub-invariant — 두 번째 적용 case)

본 Story 핵심 설계 수치 cross-section sync 검증 (MCT-154 첫 적용 case 후 본 Story = 누적 효과 검증):

| ID | 수치 | §1 산출물 | §3 코드 경로 | §6.3 디렉토리 트리 | §6.4 metric | §6.5 alert | §6.7 cross-module | §6.8 SSOT | §9.2 Phase 2 산출물 |
|---|---|---|---|---|---|---|---|---|---|
| **N-1** | file count = 10 (6 data + 4 hub) | ✅ 10 file 명시 | ✅ 10 row | ✅ 10 file 표시 | (n/a) | (n/a) | (n/a) | (n/a) | ✅ 10 file 인용 |
| **N-2** | 신규 module = 2 (cutover_verifier + gc_runner) | ✅ 2 module 명시 | ✅ 2 row | ✅ 2 신규 표시 | (n/a) | (n/a) | (n/a) | (n/a) | ✅ 2 module 인용 |
| **N-3** | 신규 CLI = 2 (verify_rpo_zero + rotate_minio_secret) | ✅ 2 CLI 명시 | ✅ 2 row | ✅ 2 신규 표시 | (n/a) | (n/a) | (n/a) | (n/a) | ✅ 2 CLI 인용 |
| **N-4** | 신규 test = 2 (test_cutover_verifier + test_gc_runner) | ✅ 2 test 명시 | ✅ 2 row | ✅ 2 신규 표시 | (n/a) | (n/a) | (n/a) | (n/a) | ✅ 2 test 인용 |
| **N-5** | 신규 runbook = 2 (tls-review + stage2-runbook) | ✅ 2 runbook 명시 | ✅ 2 row | ✅ 2 신규 표시 | (n/a) | (n/a) | (n/a) | (n/a) | ✅ 2 runbook 인용 |
| **N-6** | 신규 retro = 1 (2026-05-stage2.md) | ✅ 1 retro 명시 | ✅ 1 row | ✅ 1 신규 표시 | (n/a) | (n/a) | (n/a) | (n/a) | ✅ 1 retro 인용 |
| **N-7** | RpoVerifyResult enum = 3종 | (n/a) | (n/a) | (n/a) | (n/a) | (n/a) | ✅ C-1+C-2+C-3 | ✅ §6.8.1 | (n/a) |
| **N-8** | GcResult enum = 4종 | (n/a) | (n/a) | (n/a) | (n/a) | (n/a) | ✅ C-2+C-9 | ✅ §6.8.2 | (n/a) |
| **N-9** | 신규 metric = 12종 (nas_gc_* 4 + nas_rpo_verify_* 3 + nas_secret_rotation_* 5) | (n/a) | (n/a) | (n/a) | ✅ 12 row | (n/a) | ✅ C-4+C-5+C-6 | ✅ §6.8.3+§6.8.4+§6.8.5 | (n/a) |
| **N-10** | 신규 alert = 4종 | (n/a) | (n/a) | (n/a) | (n/a) | ✅ 4 row | (n/a) | ✅ §6.8.6 | (n/a) |
| **N-11** | 7d grace days = 7 | ✅ AC-1+AC-3 명시 | (n/a) | (n/a) | ✅ `nas_dual_write_grace_remaining_days` | (n/a) | ✅ C-4 | (n/a) | ✅ AC-1 인용 |
| **N-12** | 24h batch delete window | ✅ AC-2 명시 | (n/a) | (n/a) | (n/a) | (n/a) | ✅ C-9 | (n/a) | ✅ AC-2 인용 |
| **N-13** | 90d rotation cadence | ✅ AC-3 명시 | (n/a) | (n/a) | ✅ `nas_secret_rotation_age_days` | ✅ NasSecretRotationFailed (>100d) | ✅ C-7+C-8 | ✅ §6.8.5 | ✅ AC-3 인용 |
| **N-14** | 7종 invariant (MCT-151 inject) | ✅ AC-5 명시 + S8 | ✅ InvariantHarness 인용 | (n/a) | (n/a) | (n/a) | ✅ C-1 | ✅ §6.8.1 invariant 정합 | ✅ AC-5 인용 |
| **N-15** | 6 AC (Stage 2 종료 gate) | ✅ 6 AC 명시 | (n/a) | (n/a) | (n/a) | (n/a) | (n/a) | (n/a) | ✅ 6 AC 인용 |
| **N-16** | 3중 lock (cutover RPO=0 + 7d grace + invariant ALL PASS) | ✅ AC-1 + 사용자 directive 4중 lock | (n/a) | (n/a) | (n/a) | (n/a) | ✅ C-2+C-4+C-5 | (n/a) | ✅ AC-1 인용 |
| **N-17** | ADR-027 amendment 2건 mandatory (D2 + D6) | ✅ AC-4+AC-5 명시 | ✅ ADR-027 D2+D6 인용 | (n/a) | (n/a) | (n/a) | ✅ C-10 + scope_manifest triggers | (n/a) | ✅ AC-4+AC-5 인용 |

총 17 row cross-section quantitative consistency sync ✅ (MCT-154 13 row 대비 4 row 증가 — destructive operation + amendment 2건 추가 박제). lesson #5 sub-invariant 두 번째 적용 case 효과 검증 의무 = DesignReviewPL FIX 0 또는 dimensional cascade 0 목표.

---

## §7 보안 / 운영 위험 (SecurityArch + OperationalRiskArch 통합)

### §7.1 Trust boundary (SecurityArch)

| Boundary | Source | Target | Trust level |
|---|---|---|---|
| **B1** | mctrader-data 컨테이너 | NAS MinIO IAM API | LAN 내부망 (방화벽 IP-allowlist) |
| **B2** | mctrader-engine 컨테이너 | NAS MinIO IAM API | LAN 내부망 (방화벽 IP-allowlist) |
| **B3** | 양측 컨테이너 | `.env` file (0600 + gitignored) | container filesystem (root only read) |
| **B4** | 양측 컨테이너 | local L2/L3 file system (`/data/cold/L2/`) | container filesystem (mctrader user write) |
| **B5** | rotate_minio_secret CLI | `.env.old` backup file (emergency rollback) | container filesystem (root only read, 24h TTL) |

본 Story 신규 trust boundary = B5 (emergency rollback backup) — 보안 영향 LOW (24h TTL + root only + gitignored).

### §7.2 위협 모델 (Threat Model)

| Threat ID | 시나리오 | Likelihood | Impact | Mitigation |
|---|---|---|---|---|
| **T1** | secret rotation 진행 중 양측 컨테이너 인증 실패 → NAS endpoint 통신 단절 | medium | high | (a) emergency rollback path (B5) (b) sequential update (data 먼저 → engine 다음) (c) sample PUT/GET verify 의무 (rotate 완료 전) |
| **T2** | rotate_minio_secret CLI 의 신규 credential MinIO IAM API 생성 실패 (예: 권한 부족) | low | medium | (a) MinIO IAM API 권한 사전 verify (b) failure 시 즉시 rollback (old credential 유지) |
| **T3** | `.env.old` backup file 의 24h TTL 만료 후 emergency rollback 불가 | low | medium | (a) backup file 0600 (b) 24h TTL 박제 (operator 의무) (c) 만료 후 backup 자동 삭제 |
| **T4** | gc_runner 가 cutover RPO=0 verify pre-check 우회 (예: file 박제 위변조) | very low | critical | (a) sha256 verification (cutover_verify_evidence_path file content) (b) git audit log (c) 사용자 review 의무 (24h batch delete operator manual gate) |
| **T5** | TLS 재검토 결정 박제 위변조 (예: 사용자 confirm 위조) | very low | medium | (a) git audit log (commit signature) (b) 사용자 explicit verbatim 인용 (S12 user_confirmed_at: 2026-05-12) (c) ADR-027 D2 amendment commit signature audit |

### §7.3 Auth / Data 설계 (SecurityArch)

- **NAS MinIO credential** = `${NAS_MINIO_ACCESS_KEY}` + `${NAS_MINIO_SECRET_KEY}` env (`.env` 0600 + gitignored, MCT-147 4중 mitigation 정합)
- **rotation cadence** = 90d (D2 4중 mitigation 박제) — 본 Story = 첫 cycle 완료 + 다음 cycle 90d 후 (Stage 3 또는 Stage 2 정책 amendment 시점)
- **emergency rotation path** = credential leak 의심 시 즉시 (90d cycle 무관)
- **endpoint URL** = `${MINIO_ENDPOINT}` env (HTTP, S12 user_confirmed = HTTP 유지)
- **TLS 재검토 결과** = HTTP 유지 (사용자 confirm 박제 trail 정합 — `nas-minio-tls-review.md` §1 verbatim)

### §7.4 운영 리스크 (OperationalRiskArch — 5 항목)

| Risk ID | 시나리오 | Likelihood | Impact | Mitigation |
|---|---|---|---|---|
| **OP-1 DR (Disaster Recovery)** | NAS DSM 측 hardware failure 또는 disk corruption | low | critical | (a) NAS 측 76GB 영구 보존 (본 Story = local GC only, NAS 측 영향 0) (b) MCT-147 NAS deploy runbook 의 backup 정책 (c) emergency restore path (NAS 측 backup → mctrader-data 측 restore) — but 본 Story scope 외 (별 Story 발의 가능) |
| **OP-2 Disconnect** | rotate 진행 중 양측 컨테이너 NAS endpoint 통신 단절 | medium | high | (a) emergency rollback path (B5) (b) sample PUT/GET verify 의무 (c) sequential update + verify gate |
| **OP-3 Clock skew** | rotate cycle 시점 의 양측 컨테이너 system clock skew → audit log timestamp inconsistency | low | low | (a) NTP sync 의무 (양측 컨테이너) (b) audit log timestamp = ISO8601 UTC (c) skew > 1s 시 alert |
| **OP-4 Rate-limit** | MinIO IAM API rate-limit (rotate 다중 호출 시) | very low | low | (a) MinIO IAM API rate-limit 사전 측정 (b) sequential rotation (parallel 0) |
| **OP-5 Env-isolation** | 신규 env (`MINIO_ACCESS_KEY` 갱신) 가 mctrader-engine + mctrader-data 양측 모두 동일 valid credential 보유 의무 | medium | high | (a) sequential update (data 먼저 → engine 다음) (b) verify gate (sample PUT/GET 의무) (c) emergency rollback path |

### §7.5 §11.6 Idempotency consult (OperationalRiskArch consult)

§11.6 idempotency primary owner = DataMigrationArch. OperationalRiskArch consult 박제:

- **rotate_minio_secret 다중 호출 시 idempotent**: 동일 cycle_id 인자 시 (a) 신규 access_key 측 IAM API duplicate 거부 → emergency rollback 진입 → 동일 결과 (b) backup file 동일 path 재 write → idempotent. but **중복 cycle 진입 시 audit log entry 2건 박제** = 의도적 (per-cycle audit)
- **24h batch delete window 의 cron 다중 trigger 시 idempotent**: deletion log 기반 already-deleted skip → 동일 결과
- **process restart 시 deletion log 기반 resume**: §8.5 active 직접 enforcement (CFP-378 AC-5)

### §7.6 Rollback (운영 layer 측면)

본 Story rollback 절차:

| 레벨 | rollback 동작 | side effect | 사용자 directive 영향 |
|---|---|---|---|
| **L1 (gc 진입 차단 — blocked_*)** | gc_runner pre-check FAIL 시 자동 차단 + alert | local L2 변경 0 | 0 (RPO=0 보존, R5 critical 차단) |
| **L2 (rotate emergency rollback)** | rotate_minio_secret --emergency 옵션 + B5 backup 복원 | 양측 컨테이너 신규 credential 복원 + rotate audit log entry append (`emergency_rollback`) | 0 (인증 정합 즉시 복원) |
| **L3 (cutover rollback signal emit — FAIL 시)** | CutoverVerifier emit + operator manual gate (endpoint_router.rollback() trigger 가능성) | endpoint 원복 (NAS → local volume), 7d grace mode reset (active=False), cache cold start | 0 (RPO=0 보존, S9 belt-and-suspenders 활용) |
| **L4 (Phase 2 PR revert during dev)** | mctrader-data 6 file + mctrader-hub 4 file git revert | MCT-150~154 land 산출물 전체 보존, 본 Story 산출물 제거 | 0 (Stage 2 종료 gate 미진척 시점 — 운영 영향 0) |

본 Story rollback path = **L1 + L2 + L3 만 사용** (대부분 case). L4 = Phase 2 PR open 후 critical bug 발견 시 escalation path (사용자 confirm 필요 + DataMigrationArch 사후 audit).

### §7.7 사용자 directive enforcement (RPO=0 + 무손실 — 4중 lock)

본 Story 의 사용자 directive verbatim "절대 유실 금지" enforcement = **4중 lock for destructive transition**:

1. **(1차) cutover RPO=0 verify** — `verify_rpo_zero.py` CLI 의 cutover-1s/+1s diff 측정 + 7종 invariant verify (S8 박제, AC-5)
2. **(2차) 7d grace 만료 + invariant ALL PASS 7일 누적** — gc_runner pre-check 의 3중 lock 의 2번째+3번째 (S9 박제, AC-1)
3. **(3차) dry-run 선행 + operator review** — gc_runner.gc(dry_run=True) → 24h batch delete window operator manual gate (D7 박제, AC-1)
4. **(4차) deletion log + 24h 지연 batch delete** — sqlite-WAL persistent deletion log + 24h interval (R5 mitigation, AC-2)

본 4중 lock 무 박제 시 사용자 directive "절대 유실 금지" 위반 risk → 본 Story 의 critical enforcement.

---

## §8 Test Contract (TestContractArchitect 통합)

### §8.1 Test Categorization (5 AC × per-AC 평균 4-5 test + chaos 2건 + smoke 1건 = 26+ test 후보)

| Test ID | AC mapping | priority | 시나리오 |
|---|---|---|---|
| **T-1** | AC-5 | P0 | RpoVerifyResult.status enum 3종 transition (rpo_zero_verified) — diff 0 + 7종 invariant ALL PASS |
| **T-2** | AC-5 | P0 | RpoVerifyResult.status = drift_detected — diff > 0 (cutover-1s segment missing in NAS @ cutover+1s) |
| **T-3** | AC-5 | P0 | RpoVerifyResult.status = drift_detected — 7종 invariant 1종 이상 FAIL |
| **T-4** | AC-5 | P0 | RpoVerifyResult.status = verify_inconclusive — NAS unreachable transient |
| **T-5** | AC-5 | P0 | InvariantHarness inject mock — 7종 invariant ALL PASS pathway |
| **T-6** | AC-5 | P1 | cutover rollback signal emit path (FAIL 시 — operator manual gate verify) |
| **T-7** | AC-1 | P0 | GcResult.status = dry_run_complete — deletion_targets[] + target_size_bytes 박제 |
| **T-8** | AC-1 | P0 | GcResult.status = blocked_grace_period — 7d grace 미만료 (mock metric) |
| **T-9** | AC-1 | P0 | GcResult.status = blocked_invariant_fail — invariant FAIL (mock metric) |
| **T-10** | AC-1 | P0 | GcResult.status = blocked_invariant_fail — cutover RPO=0 unverified (file 부재) |
| **T-11** | AC-1 | P0 | GcResult.status = executed — 실 삭제 + free_disk_pct_after > 50% verify |
| **T-12** | AC-1 | P1 | 디스크 압박 시 tier/date 순차 (disk usage > 90% mock) |
| **T-13** | AC-2 | P0 | deletion log entry append (sqlite-WAL persistent) |
| **T-14** | AC-2 | P0 | deletion log file 무손실 (24h batch delete window 내 entry 손실 0) |
| **T-15** | AC-2 | P0 | 24h batch delete window operator manual gate verify (mock 24h interval) |
| **T-16** | AC-2 | P1 | recovery_status enum 5종 transition (pending → confirmed_deleted) |
| **T-17** | AC-3 | P0 | rotate_minio_secret CLI sequential update — data 먼저 → engine 다음 |
| **T-18** | AC-3 | P0 | rotate_minio_secret 후 sample PUT/GET verify (재인증 verify) |
| **T-19** | AC-3 | P0 | rotate_minio_secret emergency rollback (인증 실패 시 B5 backup 복원) |
| **T-20** | AC-3 | P1 | rotation cycle audit log file 박제 verify |
| **T-21** | AC-4 | P0 | TLS 재검토 runbook 박제 verify (사용자 confirm 결정 verbatim S12 인용) |
| **T-22** | AC-4 | P0 | ADR-027 D2 amendment commit verify (Stage 2 cutover 후 재검토 결과 wording 갱신) |
| **T-23** | AC-5 | P0 | ADR-027 D6 amendment commit verify (3종 → 7종 invariant 명시화) |
| **T-24** | AC-6 | P0 | Stage 2 EPIC CLOSED gate 6 AC 종합 verify (retro 별 file 박제) |
| **T-25** | (Chaos) | P1 | EC-8: RPO=0 verify FAIL → cutover rollback signal emit (operator manual gate trigger) |
| **T-26** | (Chaos) | P1 | EC-12: gc_runner 24h batch delete 진행 중 컨테이너 restart → deletion log 무손실 보존 + resume |

### §8.2 Test Coverage 매핑 (6 AC × per-AC test count)

| AC | test count | priority distribution |
|---|---|---|
| AC-1 (Local GC) | 6 (T-7~T-12) | 5 P0 + 1 P1 |
| AC-2 (deletion log + 24h batch delete) | 4 (T-13~T-16) | 3 P0 + 1 P1 |
| AC-3 (secret rotation 90d 첫 cycle) | 4 (T-17~T-20) | 3 P0 + 1 P1 |
| AC-4 (TLS 재검토 + ADR-027 D2 amendment) | 2 (T-21~T-22) | 2 P0 |
| AC-5 (RPO=0 검증 + ADR-027 D6 amendment) | 7 (T-1~T-6 + T-23) | 6 P0 + 1 P1 |
| AC-6 (Stage 2 EPIC CLOSED gate 6 AC 종합 verify) | 1 (T-24) | 1 P0 |
| Chaos | 2 (T-25 + T-26) | 2 P1 |

총 26 test (20 P0 + 6 P1) — MCT-154 30 test 대비 4 test 감소 (6 AC vs 5 AC + chaos 2건 vs 1건). 대신 ADR amendment verify 2건 + EPIC CLOSED gate verify 1건 추가.

### §8.3 §8.5 spawn-time trigger 결정 (CFP-378 AC-5 — ArchitectPL 결정 verbatim 반영)

§8.5_active = **true** (ArchitectPL 결정, OperationalRiskArch §6.0 deputy 4 perspective input)

**4 조건 평가**:
- Long-running connection (WebSocket / SSE / long-poll / persistent TCP / gRPC stream): **N**
- Stateful in-memory cache (>1 update/sec sustained, >5 min retention 또는 derived state): **N** (gc_runner = stateless, deletion log only persistent)
- Background worker / queue consumer (async job runner / scheduler / data stream consumer): **Y** (gc_runner 24h batch delete window monitoring)
- Process restart-aware system (in-flight 작업 보유 / persistent state / graceful shutdown 요구): **Y** (gc_runner 24h batch delete 중 restart 시 deletion log 기반 resume + cutover_verifier evidence file persist)

2 Y → §8.5_active=true.

### §8.4 §8.5 active test (3 건 의무)

| Test ID | §8.5 항목 | 시나리오 |
|---|---|---|
| **T-26** | Process restart-aware (위 §8.1 chaos) | gc_runner 24h batch delete 중 restart → deletion log 기반 resume |
| **T-27** | Background worker | gc_runner 24h batch delete window monitoring cron-style trigger |
| **T-28** | Cross-repo state coordination | cutover_verifier evidence file ↔ gc_runner pre-check file load (cross-process state sync) |

### §8.5 Edge Cases (12종 EC § 5.4 인용)

위 §5.4 EC-1~EC-12 → 본 §8 test mapping:
- EC-1 → T-8
- EC-2 → T-9
- EC-3 → T-15 (24h batch delete operator manual gate)
- EC-4 → T-26 (chaos)
- EC-5 → T-19 (emergency rollback)
- EC-6 → T-17 (sequential update)
- EC-7 → 별 Story scope (TLS 활성화 결정 변경)
- EC-8 → T-25 (chaos)
- EC-9 → T-24 (EPIC CLOSED gate)
- EC-10 → T-22 + T-23 (sequential amendment commit)
- EC-11 → T-9 (NAS unreachable invariant verify 불가)
- EC-12 → T-26 (chaos restart resume)

---

## §9 단계 검증

### §9.1 Phase 1 산출물 (RequirementsPL §1~§5 + ArchitectPL §6~§11 통합 author)

본 file (`docs/stories/MCT-155.md`) — 단일 file Phase 1 산출물.

### §9.2 Phase 2 산출물 (Implementation)

| 경로 | 책임 | scope_manifest sync |
|---|---|---|
| **`mctrader-data/src/mctrader_data/nas_migration/cutover_verifier.py`** | CutoverVerifier class + RpoVerifyResult dataclass | ✅ §6.10 N-1+N-2 |
| **`mctrader-data/src/mctrader_data/nas_migration/gc_runner.py`** | GcRunner class + GcResult dataclass | ✅ §6.10 N-1+N-2 |
| **`mctrader-data/scripts/migration/verify_rpo_zero.py`** | RPO=0 verify CLI | ✅ §6.10 N-3 |
| **`mctrader-data/scripts/ops/rotate_minio_secret.py`** | 90d rotation CLI | ✅ §6.10 N-3 |
| **`mctrader-data/tests/nas_migration/test_cutover_verifier.py`** | RpoVerifyResult test (7 test 후보) | ✅ §6.10 N-4 |
| **`mctrader-data/tests/nas_migration/test_gc_runner.py`** | GcResult test (10 test 후보 + chaos 1) | ✅ §6.10 N-4 |
| **`mctrader-hub/docs/runbooks/nas-minio-tls-review.md`** | TLS 재검토 결정 박제 (사용자 confirm + 5 sub-section) | ✅ §6.10 N-5 |
| **`mctrader-hub/docs/runbooks/nas-minio-stage2-runbook.md`** | Stage 2 종료 운영 runbook (6 AC 종합 verify checklist) | ✅ §6.10 N-5 |
| **`mctrader-hub/docs/retros/2026-05-stage2.md`** | Stage 2 Epic-final retro (6 sub-section) | ✅ §6.10 N-6 |
| **`mctrader-hub/docs/adr/ADR-027-cold-tier-object-storage-nas-minio.md`** | D2 + D6 amendment commit (2건 mandatory) | ✅ scope_manifest triggers_adr_amendment |

총 10 file (6 data + 4 hub) + ADR amendment (1 file 수정).

### §9.3 Phase 2 검증 gate

| gate | 측정 | pass condition |
|---|---|---|
| **pytest** | `pytest tests/nas_migration/test_cutover_verifier.py tests/nas_migration/test_gc_runner.py` | 26+ tests PASS, 0 FAILED |
| **pyright** | `pyright src/mctrader_data/nas_migration/ scripts/migration/ scripts/ops/` | 0 errors / 0 warnings |
| **ruff** | `ruff check src/mctrader_data/nas_migration/ scripts/migration/ scripts/ops/ tests/nas_migration/` | All checks passed (noqa 만 허용) |
| **§8.5 active test** | T-26 + T-27 + T-28 | 3 건 ALL PASS (cross-platform) |
| **chaos test** | T-25 + T-26 | 2 건 ALL PASS |
| **scope_manifest sync** | §6.10 file-level invariant + §6.11 cross-section quantitative consistency | 10 file path sync ✅ + 17 cross-section row sync ✅ |

### §9.4 Phase 2 실 운영 evidence (operator manual gate)

| Evidence | 박제 location | gate |
|---|---|---|
| **AC-5 RPO=0 검증 evidence** | `mctrader-data/.tmp/rpo-zero-verify-MCT-155.md` (gitignored) | RpoVerifyResult.status == rpo_zero_verified |
| **AC-1 GC 완료 evidence** | `mctrader-data/.tmp/gc-evidence-MCT-155.md` (gitignored) | GcResult.status == executed + free_disk_pct_after > 50% |
| **AC-3 secret rotation evidence** | `mctrader-data/.tmp/secret-rotation-MCT-155-cycle-1.md` (gitignored) | rotate cycle audit log + sample PUT/GET verify |
| **AC-6 EPIC CLOSED gate evidence** | `docs/retros/2026-05-stage2.md` (Stage 2 Epic-final retro) | 6 AC 종합 verify summary + cross-reference |

---

## §10 FIX Ledger

### §10.0 RESET (Phase 1 fresh, 카운터 0/3)

본 Story spawn 시점 fresh start (MCT-150 5 FIX cycle / MCT-151 1 FIX cycle / MCT-152 1 FIX cycle / MCT-153 1 FIX cycle / MCT-154 0 FIX cycle 패턴 trail 와 독립 카운터). codeforge #525 lesson 4 invariants 적용 **6번째 case** + lesson #5 file-level 두 번째 case (사전 적용) + lesson #5 sub-invariant **두 번째 적용 case** (MCT-154 첫 적용 효과 → 본 Story 누적 효과 검증).

**Author trail FIX#0** (Phase 1 fresh, 2026-05-13):
- ArchitectPL author = §6~§11 통합 (6 deputy + chief synthesis, 단일 spawn — env=0 fallback model)
- lesson 4+1+sub invariants 사전 박제 적용:
  - **#1 API contract semantic completeness** = §6.2.1 RpoVerifyResult 3 enum + §6.2.2 GcResult 4 enum 의 caller contract docstring 완전 박제
  - **#2 Cross-module propagation completeness** = §6.7 cross-module contract 표 10 row (MCT-154 12 row 대비 본 Story = destructive 단순)
  - **#3 Wording SSOT** = §6.8 enum value SSOT 박제 (RpoVerifyResult 3종 + GcResult 4종 = 7 신규 value) + MCT-150~154 53+ enum 인용 only + 12 신규 metric SSOT + 4 신규 alert SSOT
  - **#4 Unconditional vs conditional invariant placement** = §6.9 placement 의도 명시 15 row (cutover RPO=0 verify unconditional / 7d grace + invariant gate unconditional / dry-run 진입 unconditional / deletion log conditional / 24h batch delete conditional / secret rotation cycle conditional / TLS 재검토 conditional 등)
  - **#5 file-level scope_manifest sync 두 번째 case (사전 적용)** = §6.10 scope_manifest sync 검증 + path 갱신 1건 사전 적용 (`migration/` → `nas_migration/` rename) + 디렉토리 트리 10 file sync ✅
  - **#5 sub-invariant cross-section quantitative consistency 두 번째 적용 case** = §6.11 cross-section cascade table 17 row (MCT-154 13 row 대비 4 row 증가 — destructive operation + amendment 2건 추가 박제) sync ✅
- 신규 결정 6건:
  - **CutoverVerifier 의 RPO=0 검증 mechanism (§6.1)** — cutover-1s/+1s diff 측정 + 7종 invariant verify 채택 (segment hash 비교 only 거부)
  - **GcRunner 의 GC 진입 gate mechanism (§6.1)** — 3중 lock unconditional gate 채택 (single check 거부)
  - **deletion log + 24h batch delete mechanism (§6.1)** — sqlite-WAL persistent + 24h interval operator manual gate 채택 (in-memory log 거부)
  - **secret rotation sequential update mechanism (§6.1)** — data 먼저 → engine 다음 + verify 의무 채택 (parallel update 거부)
  - **TLS 재검토 runbook 구조 (§6.1)** — 사용자 confirm verbatim + 5 sub-section 결정 박제 채택 (단순 wording 거부)
  - **Stage 2 Epic-final retro 별 file 구조 (§6.1)** — 6 sub-section 종합 retro 채택 (Story §12 만 거부)
- ESCALATE 0 (모든 결정 박제 trail 정합 + lesson #5 file-level pre-check `nas_migration/` namespace 정합 ✅)

### §10.1 FIX events (fix-event-v1 contract, Orchestrator 단독 append)

(Phase 1 PR open 후 DesignReviewPL FIX cycle 시 append 예정. 본 Story Phase 1 fresh = 0 FIX 시작.)

FIX 카운터: design-review = 0/3 fresh / code-review = 0/3 fresh

### §10.N FIX cycle (CodeReviewPL 진행 시 append)

CodeReviewPL Phase 2 진입 시 FIX cycle append. **MCT-155 FIX 카운터 SSOT** = 본 §10.

---

## §11 데이터 마이그레이션 (DataMigrationArch + chief synthesis)

### §11.1 본 Story scope = local L2 destructive GC + RPO=0 검증 회고 + secret rotation (one-shot transition)

본 Story = ADR-027 D7 박제 직접 enforcement (local L2 GC) + S8 박제 직접 owner (RPO=0 검증 회고) + D2 4중 mitigation 의 90d cadence 첫 실증 (secret rotation). one-shot cutover-final transition. **schema migration 0** (ADR-009 16-col schema 그대로 유지). **NAS 측 data migration 0** (NAS 76GB 영구 보존, MCT-153 BackfillOrchestrator 결과 그대로 유지). **local L2 측 destructive GC** (76GB 영구 삭제). **본 Story = local 측 destructive operation + RPO=0 verify + rotation = 3 mechanism 박제**.

### §11.2 Forward-only invariant (ADR-009 §D12.2) 보존

본 Story = NAS 측 변경 0 (read-only):
- CutoverVerifier.verify_rpo_zero() = NAS list_objects_v2 read-only (NAS object 변경 0)
- GcRunner.gc() = local L2 file system unlink (NAS object 변경 0)
- rotate_minio_secret = MinIO IAM credential 갱신 (NAS data 변경 0)

ADR-009 §D12.2 forward-only invariant 정합:
- 기존 cold tier asset row 변경 / 삭제 0 (NAS 측)
- local 측 L2 destructive GC = forward-only invariant 와 직교 (local cache 측 정리, NAS = SoT 정합)

### §11.3 Drift detection 7종 schema invariant (MCT-151 InvariantHarness 인용 — full 활용)

본 Story 의 AC-5 RPO=0 verify = MCT-151 InvariantHarness.verify() 의 7종 invariant 모두 직접 활용 (재정의 0). 7종 invariant:

- **byte-level (1종)**: sha256
- **set-level (2종)**: object_count, row_count
- **schema-level (4종)**: column_count, column_order, dtype, schema_version

본 Story 신규 invariant 0 — MCT-151 land 7종 그대로 사용 (full 활용 — MCT-154 schema-only 4종 활용과 차별화). AC-5 직접 enforcement (cutover-1s/+1s diff + 7종 invariant ALL PASS gate). MCT-151 InvariantResult.status 8 enum 의 fail enum (sha256_fail / object_count_fail / row_count_fail / column_count_fail / column_order_fail / dtype_fail / schema_version_fail) cross-reference. ADR-027 D6 amendment 박제 (3종 → 7종 invariant 명시화) 의 Story-level evidence.

### §11.4 Evidence pack 박제 의무 (1회)

scope_manifest `epic_milestones.stage_2_complete.gate` 박제 의무 정합:

- **AC-3 GC 후 free disk > 50% — Stage 2 종료 gate AC-3 의 single source (본 Story 직접 owner)**: gc_runner.gc() 실 운영 결과 + df -h before/after diff (`mctrader-data/.tmp/gc-evidence-MCT-155.md`)
- **AC-6 TLS 재검토 회고 박제 — Stage 2 종료 gate AC-6 의 single source (본 Story 직접 owner)**: docs/runbooks/nas-minio-tls-review.md + ADR-027 D2 amendment commit
- **AC-5 RPO=0 검증** — `verify_rpo_zero.py` CLI output (`mctrader-data/.tmp/rpo-zero-verify-MCT-155.md`)
- **AC-3 secret rotation** — rotate audit log (`mctrader-data/.tmp/secret-rotation-MCT-155-cycle-1.md`)
- **AC-1/AC-2/AC-4/AC-5 종합** — Stage 2 Epic-final retro (`docs/retros/2026-05-stage2.md`)

**evidence pack location 결정** (§6.6 chief decision):
- 별 file: `mctrader-data/.tmp/*-MCT-155.md` (gitignored, 4종)
- Stage 2 Epic-final retro: `mctrader-hub/docs/retros/2026-05-stage2.md` (6 sub-section 종합)
- Story summary: 본 file `docs/stories/MCT-155.md §10` (PMOAgent retro 시점 aggregated 박제)

### §11.5 Rollback (운영 layer 측면)

본 Story rollback 절차 (§7.6 운영 rollback 박제 정합):

| 레벨 | rollback 동작 | side effect | 사용자 directive 영향 |
|---|---|---|---|
| **L1 (gc 진입 차단 — blocked_*)** | gc_runner pre-check FAIL 시 자동 차단 + alert | local L2 변경 0 | 0 (RPO=0 보존, R5 critical 차단) |
| **L2 (rotate emergency rollback)** | rotate_minio_secret --emergency 옵션 + B5 backup 복원 | 양측 컨테이너 신규 credential 복원 + audit log entry append (`emergency_rollback`) | 0 (인증 정합 즉시 복원) |
| **L3 (cutover rollback signal emit — FAIL 시)** | CutoverVerifier emit + operator manual gate (endpoint_router.rollback() trigger 가능성, MCT-154 land) | endpoint 원복 (NAS → local volume), 7d grace mode reset (active=False), cache cold start | 0 (RPO=0 보존, S9 belt-and-suspenders 활용) |
| **L4 (24h batch delete window 내 deletion log 기반 NAS object download + local 복원)** | deletion log entry 기반 NAS object download + local file system 복원 (recovery_status: pending → recovered) | NAS object 변경 0 (read-only download), local 측 복원 | 0 (24h window 내 실수 발견 시 복구 가능, R5 mitigation) |
| **L5 (Phase 2 PR revert during dev)** | mctrader-data 6 file + mctrader-hub 4 file git revert | MCT-150~154 land 산출물 전체 보존, 본 Story 산출물 제거 | 0 (Stage 2 종료 gate 미진척 시점 — 운영 영향 0) |

본 Story rollback path = **L1 + L2 + L3 + L4 만 사용** (대부분 case). L5 = Phase 2 PR open 후 critical bug 발견 시 escalation path (사용자 confirm 필요 + DataMigrationArch 사후 audit).

### §11.6 Idempotency (DataMigrationArch primary + OperationalRiskArch consult)

**CONDITIONAL idempotency 의무 박제** — 본 Story 적용 ✅ (process restart-aware + multi-call safe + destructive operation idempotent):

- **CutoverVerifier.verify_rpo_zero()** = idempotent (다중 호출 시 동일 결과):
  - cutover_timestamp 동일 시 동일 diff (NAS object list LastModified ≤ timestamp)
  - 7종 invariant verify 동일 결과 (InvariantHarness deterministic)
  - file 박제 = strict idempotent 0 (per-run unique timestamp) — but **의도적**: per-verify run 박제
- **GcRunner.gc(dry_run=True)** = idempotent (다중 호출 시 동일 결과 — deletion_targets list 동일)
- **GcRunner.gc(dry_run=False)** = idempotent at the operation level (deletion log 기반 already-deleted skip — 다중 호출 시 추가 삭제 0)
- **GcRunner._3_lock_pre_check()** = idempotent (read-only verify, side effect 별)
- **rotate_minio_secret CLI** = idempotent (동일 cycle_id 재진입 시 IAM API duplicate 거부 → emergency rollback path → 동일 결과)
- **rotate_minio_secret --emergency** = idempotent (B5 backup 복원 동일 결과)

**chunk-level idempotency 의무 (destructive operation)**:
- gc 진입 gate (3중 lock) = idempotent (read-only verify)
- dry-run = idempotent (read-only glob + size sum + sha256 list)
- 실 삭제 = idempotent (deletion log 기반 already-deleted skip)
- 24h batch delete = idempotent (operator manual gate trigger 동일 결과)

**idempotency 위반 surface 후보 (chief synthesis)**:
- evidence pack append = strict idempotent 0 (per-run unique timestamp) — but **의도적**: per-cycle 운영 시점 박제 의무 (RETRO 시점 multi-cycle 분석 input — 단, 본 Story = 단일 GC + 단일 rotate cycle 운영 가정)
- gc 다중 호출 시 deletion log entry 1번만 append (idempotent enforce) — but log file 측 read 비용 증가

### §11.7 Integrity invariant (DataMigrationArch)

본 Story 의 data integrity invariant:

| Invariant | 검증 mechanism | 박제 |
|---|---|---|
| **forward-only (ADR-009 §D12.2)** | 본 Story = NAS 측 read-only, NAS object 변경 0 — invariant 자동 보존 | §11.2 |
| **7종 invariant ALL PASS (MCT-151 InvariantHarness, AC-5 RPO=0 verify)** | CutoverVerifier.verify_rpo_zero() Phase 4 + InvariantHarness.verify() | §11.3 + AC-5 |
| **3중 lock GC 진입 gate (AC-1)** | GcRunner._3_lock_pre_check() Phase 1 (cutover RPO=0 + 7d grace + invariant ALL PASS 7일 누적) | §6.2.2 + AC-1 + I-1 |
| **dry-run 선행 의무 (D7, AC-1)** | GcRunner.gc(dry_run=True) Phase 2 + 24h batch delete operator manual gate | §6.2.2 + AC-1 + I-2 |
| **deletion log + 24h batch delete (R5, AC-2)** | GcRunner.gc(dry_run=False) Phase 4 + sqlite-WAL persistent | §6.2.2 + AC-2 + I-3 |
| **secret rotation sequential update + verify (AC-3)** | rotate_minio_secret CLI sequential (data 먼저 → engine 다음) + sample PUT/GET verify | §6.2.4 + AC-3 + I-4 |
| **emergency rollback path (B5 backup + 24h TTL)** | rotate_minio_secret --emergency + B5 backup file 복원 | §6.2.4 + EC-5 + I-5 |
| **TLS 재검토 결정 박제 trail (S12 user_confirmed)** | docs/runbooks/nas-minio-tls-review.md §1 verbatim 인용 + ADR-027 D2 amendment commit signature | §6.7 C-10 + AC-4 + I-6 |
| **ADR-027 D2 + D6 amendment commit (2건 mandatory)** | git commit + scope_manifest triggers_adr_amendment status: pending → committed | §6.7 + AC-4+AC-5 + I-7 |
| **Stage 2 EPIC CLOSED gate 6 AC 종합 verify** | Stage 2 Epic-final retro (`docs/retros/2026-05-stage2.md`) §4 6 AC 충족 evidence summary | §6.6 + AC-6 + I-8 |
| **NAS unreachable hot path 무영향 (ADR-027 D5 + ADR-017)** | 본 Story = local L2 destructive only — hot tier (L1) reader 는 scope 외 (local volume only 유지) | §2 관찰 1 |
| **RPO=0 (사용자 directive verbatim "절대 유실 금지") — 4중 lock enforce** | cutover RPO=0 verify (S8) + 7d grace + invariant ALL PASS (S9) + dry-run + operator review (D7) + deletion log + 24h batch delete (R5) = 4중 lock | §7.7 + I-9 |
| **MCT-153 backfill metric ↔ engine reader metric ↔ gc_runner metric 1:1 정합** | nas_backfill_* + engine_cold_reader_* + nas_gc_* cross-reference | §6.4 + I-10 |

**integrity invariant 위반 surface 후보** (chief synthesis):
- 24h batch delete window 내 deletion log file 손상 시 → 24h 안에 복구 불가 → but mitigation: sqlite-WAL persistent + sha256 verify + git audit log
- ADR-027 amendment 2건 동시 commit 시 conflict → sequential commit (D6 먼저 → D2 다음, EC-10) + git log audit
- Stage 2 EPIC CLOSED gate 6 AC 중 1건 미달 → Epic 종료 차단 → but mitigation: retro 시점 사전 verify + 미달 AC 측 별 Story 발의 (사용자 escalation, EC-9)

---

**§6~§11 Author trail 종료 (FIX#0, ArchitectPLAgent chief author + 6 deputy 통합 synthesis, 2026-05-13)**

**Phase 1 ArchitectPL 산출물 완료** — DesignReviewPL 진입 준비. lesson 4+1+sub invariants 사전 박제 효과 검증 시점 = DesignReviewPL FIX cycle 결과 (FIX 0 또는 mechanical fast-path 목표, MCT-151 + MCT-152 + MCT-153 + MCT-154 패턴 정합 — lesson #5 sub-invariant 두 번째 적용 case 누적 효과 입증).

**§6.10 + §6.11 lesson #5 + sub-invariant 적용 결과**: scope_manifest planned_files MCT-155 = path 갱신 권고 1건 사전 적용 완료 (file-level sync 두 번째 case 효과 ✅ pre-emptive land — `migration/` → `nas_migration/` rename) + cross-section quantitative consistency cascade table 17 row 박제 (sub-invariant 두 번째 적용 case 효과 검증 의무 — DesignReviewPL FIX 0 또는 dimensional cascade 0 목표).

---

## §12 Retrospective (PMOAgent, 2026-05-13)

(Phase 2 완료 후 PMOAgent dispatch 시점 작성 — RETRO-MCT-155.md 별 file 의 summary 및 EPIC CLOSED gate 6 AC 종합 verify 결과 박제)

### §12.1 Story Final Audit

(Phase 2 완료 후 PMOAgent 작성)

### §12.2 FIX Cycle Summary

(Phase 2 완료 후 PMOAgent 작성)

### §12.3 Stage 2 EPIC CLOSED gate 6 AC 종합 verify

(Phase 2 완료 후 PMOAgent 작성 — Stage 2 Epic-final retro `docs/retros/2026-05-stage2.md` cross-reference)

### §12.4 codeforge #525 Lesson 4+1+sub invariants 6번째 case 효과 측정

(Phase 2 완료 후 PMOAgent 작성)

### §12.5 ADR-027 amendment 2건 commit 박제

(Phase 2 완료 후 PMOAgent 작성 — D2 + D6 amendment commit hash + scope_manifest triggers_adr_amendment status update)

### §12.6 Stage 2 Epic CLOSED — Epic milestone #4 close trigger

(Phase 2 완료 후 PMOAgent 작성 — Epic milestone #4 close + Stage 1+2 종합 retro 별 file 박제)

---
