---
story_key: MCT-144
story_scope: hub+repo
story_issues:
  - repo: mclayer/mctrader-hub
    number: 230
status: phase:설계
epic_milestone: Epic-MCT-112-transaction-ssot-information-bar
parent_epic: MCT-112
created_at: 2026-05-12
delegates:
  - mctrader-web
---

# MCT-144: Transaction SSOT — Streamlit UI DuckDB 전환 (mctrader-web)

- **Issue**: #230
- **Epic**: MCT-112
- **Status**: phase:설계
- **Delegates**: mctrader-web
- **Phase**: 4 (병렬 with Story-8)

## §1 사용자 요구사항 (verbatim)

Epic MCT-112 Story-10 — Streamlit UI candle 조회를 DuckDB query 로 전환.

**범위**: `mctrader-web` repo.

### 변경 내용

**Candle Query 전환** (`mctrader_web/data/candle_query.py`):
- 기존 candle Parquet 직접 read → Cold path DuckDB query (Story-5 `duckdb_resample.py`)
- 임의 timeframe 조회 UI (1m/5m/15m + 사용자 입력 임의 timeframe + volume/tick/dollar bar)
- contract_metadata_version 표시 (재현성)

**페이지 caller 수정** (`mctrader_web/pages/*.py`):
- 차트 조회 페이지 candle_query 호출부 갱신
- provenance column 시각화 (legacy vs transaction_derived 색상/마커 구분)

### UX 목표

- typical query (50 sym × 1 day) < 1 sec
- cache hit/miss 시 UI 명시
- information bar contract_metadata UI 표시

### 의존

- **upstream**: Story-5 (Cold path DuckDB, MCT-139 MERGED)

## §7 설계 서사

(DeveloperPLAgent dispatch 산출물)

## §8 Test Contract

- DuckDB query latency < 1 sec
- 임의 timeframe + information bar UX
- provenance 시각화

## §11 PMO 회고

(Story 단위 inline retro — 2026-05-12)

### Story 결과

- **mctrader-web PR**: https://github.com/mclayer/web/pull/36 (MERGED 2026-05-11T21:01:06Z)
- **산출물**: 14 file. `data/candle_query.py` + `data/provenance.py` + `dashboard/pages/30_candle_query.py` + 4 test files + uv.lock + ci.yml. 26/26 local tests PASS.
- **CI**: check-gate+CodeQL Analyze PASS / ci fail (pre-existing CODEFORGE_CROSS_REPO_PAT — org-wide, mctrader-data 0.9.0 transitive private deps). Story-7/8/9 선례 정합 admin squash merge.
- **UX 달성**: typical query < 1 sec, cache hit/miss UI, provenance 시각화 (legacy gray dashed vs transaction_derived blue solid), contract_metadata_version 표시.

### Lesson learned

- **Cross-repo dependency upgrade 영향** — Story-7 (MCT-141, mctrader-data 0.9.0) 의 `mctrader-market-upbit` private deps 가 모든 mctrader-data consumer (engine/web) CI 에 영향. 별도 infra fix Story 권장.
- **DuckDBResampler + PolarsResampler wrapper pattern** — `QueryResult` 가 `source` / `contract_metadata_version` / `backend` / `cache_hit` / `elapsed_seconds` 노출. UX latency 측정 baseline 확보.

### Open question / Follow-up

1. **org-level `CODEFORGE_CROSS_REPO_PAT` 갱신** — admin:org scope 필요. 별도 infra Story.
2. **mctrader-data `mctrader-market-upbit` 의존 optional 분리** — codeforge hygiene Story.

### Phase 4 종료

Story-8 + Story-9 + Story-10 모두 MERGED. Phase 5 (Story-11 reconciliation → Story-12 retire) 진입 가능.
