# ADR-027 reservation marker

- **Reserved for**: MCT-149 (Epic A — Cold Tier on NAS MinIO Feasibility Spike)
- **Reserved at**: 2026-05-12
- **Authored by**: ArchitectAgent (chief author) — `codeforge-design` lane
- **Final path**: `docs/adr/ADR-027-cold-tier-object-storage-nas-minio.md`
- **Removal**: MCT-149 land 시 본 marker 삭제 + 본문 land

## Title (예약)

`ADR-027: Cold Tier Object Storage on NAS MinIO`

## Relationships (예약)

- `successor_of`: [ADR-017] (Zero-loss ingestion via WAL + tiered compaction)
- `complements`: [ADR-016] (Audit log append-only with hash chain)
- `references`: [ADR-008] (Secret management), [ADR-009] (OHLCV 16-col schema + forward-only invariant)

## Brainstorm 출처

- spec: [.tmp/brainstorm-cold-tier-nas-minio.md](../../.tmp/brainstorm-cold-tier-nas-minio.md)
- plan: [.tmp/plan-cold-tier-nas-minio.md](../../.tmp/plan-cold-tier-nas-minio.md)
- scope_manifest: [scope_manifests/EPIC-cold-tier-nas-minio.yaml](../../scope_manifests/EPIC-cold-tier-nas-minio.yaml)

## 11 결정점 (D1~D11, MCT-149 본문 author 시 박제 의무)

| ID | 결정 | 합의 |
|---|---|---|
| D1 | Bucket layout | 단일 `mctrader-market` + 엄격한 Hive prefix (schema/exchange/node/tier/date) |
| D2 | TLS / auth | TLS 강제 + Container Manager secret + `.env` fallback (0600) + 90일 rotation |
| D3 | ADR 형태 | 신규 ADR-027 (ADR-017 amendment 아님) |
| D4 | Cutover 전략 | dual-write window → 검증 통과 → reader endpoint 전환 → local GC |
| D5 | Failure mode | compactor 만 retry queue + backlog alert. WAL/L1/hot path 무영향 |
| D6 | 이관 검증 invariant | sha256 + object count + parquet row count 3종 ALL PASS |
| D7 | Local GC | 7일 grace + dry-run 선행 + 디스크 압박 시 tier/date 순차 GC |
| D8 | Spike scope | TLS handshake / latency baseline / large PUT 50MB / restart idempotency / partial object visibility |
| D9 | Reader | engine read-through cache (NAS = SoT, local = LRU/TTL cache) |
| D10 | 영향 repo | data + engine + hub. market/market-bithumb/web 제외 |
| D11 | admin_audit.sqlite | 본 ADR scope 외, 별도 epic |
